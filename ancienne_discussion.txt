## User

tu peux alysder le projet pour comprendre dabord avant que nous continuons a traivailler dessus?

## Assistant

<thinking>
**Analyzing project files**

I need to respond in French while analyzing the project. First, I should find and list the root path for the tools. The project root path appears to be E:\laragon\www\news, but since examples say the name is likely "news," I’ll use that. I should list the directory using "news." Then, I might want to inspect the files in parallel. It's important to establish paths before reading them, so let's go ahead and list those.
</thinking>

**Tool Call: List the `news` directory's contents**
Status: Completed

# Folders:
news\.agents
news\.aider-desk
news\.codestudio
news\.commandcode
news\.continue
news\.vscode
news\.zed
news\node_modules
news\supabase

# Files:
news\.gitignore
news\README.md
news\package-lock.json
news\package.json
news\projet.md
news\skills-lock.json


## Assistant

<thinking>
**Analyzing the project**

I need to analyze the project by reading through the package and the README first. It would be helpful to make a list of directories and any other relevant paths I’ve found so far. I’m considering doing parallel reads to gather information on Supabase as well. There’s a lot to dig into here, so I want to make sure I've got a clear understanding of everything! Let’s get started!
</thinking>

**Tool Call: Read file `package.json`**
Status: Completed

```news/package.json
{
  "devDependencies": {
    "supabase": "^2.98.0"
  }
}

```


**Tool Call: Read file `README.md`**
Status: Completed

```news/README.md
# bot

```


**Tool Call: Read file `projet.md`**
Status: Completed

```news/projet.md
Plan de Développement Progressif – Plateforme Média Mondiale Intelligente
Ce document complète le cahier des charges initial avec les éléments manquants, puis subdivise intégralement le projet en modules successifs, chacun réalisable, testable et autonome dès sa livraison. Ce cheminement direct garantit qu’aucune phase ne nécessite de revenir en arrière ou de “sauter” des fonctionnalités.

1. Éléments Complémentaires au Cahier des Charges Initial
1.1 Conformité légale et droits
Licence des contenus importés : Seules les sources autorisées (API officielles, RSS publics, partenariats) seront exploitées. Chaque article importé conserve un lien vers la source originale et un extrait limité (fair use). Le système de validation permettra de bloquer toute source non conforme.

Protection des données : Politique de confidentialité, consentement cookies, droit à l’effacement, export des données personnelles (conformité RGPD et lois locales).

Modération obligatoire : Signalement de contenus illicites sous 24h, filtres anti-spam obligatoires dès le module social.

1.2 Sécurité transversale
Chiffrement des données sensibles au repos (AES-256).

Hachage des mots de passe (bcrypt + sel).

Rate limiting sur toutes les API.

Validation stricte des uploads (taille, type, scan antivirus).

Authentification sécurisée (JWT avec rotation de refresh tokens).

Backup automatique quotidien de la base de données et des médias.

Logs d’audit pour les actions sensibles.

1.3 Performance & Infrastructure
CDN obligatoire pour les médias et les assets statiques.

Redis pour le cache de requêtes et sessions.

Queue asynchrone (BullMQ) pour l’import, les notifications, le traitement IA.

Architecture API REST versionnée (/api/v1/...), avec documentation Swagger.

Monitoring santé (uptime, latence, erreurs) via Sentry / UptimeRobot.

1.4 Évolutivité de la base de données
Ajout des tables transversales indispensables :

reading_history (traque du temps passé, scroll, appareil).

article_tags (tags multi-articles).

article_countries, article_languages (pivot multi-pays / multi-langue).

user_follows (abonnements entre utilisateurs – futur).

notification_settings (préférences canal par utilisateur).

ad_campaigns (gestion fine des campagnes pub).

2. Découpage en Modules Séquentiels
Chaque module hérite du précédent et apporte une brique complète, testable indépendamment.
L’ordre est conçu pour :

valider les fondamentaux techniques dès le début,

livrer une valeur utilisateur rapidement,

ne jamais casser ce qui fonctionne déjà.

text
Module 0   -> Fondation technique (CI/CD, base, API squelette, déploiement)
Module 1   -> Gestion de contenu (Articles, Catégories)
Module 2   -> Utilisateurs et Profils
Module 3   -> Interactions sociales (Likes, Commentaires, Partage)
Module 4   -> Collecte Automatique de News
Module 5   -> Personnalisation et Recommandation (basique)
Module 6   -> Monétisation Publicitaire
Module 7   -> Application Mobile (React Native)
Module 8   -> Panel d’administration avancé
Module 9   -> Intelligence Artificielle et Traduction
Module 10  -> Marketplace et Annonces Utilisateurs
Chaque module ci-dessous décrit : composants (API, jobs, UI), dépendances, livrables et tests de validation.

Module 0 : Fondation Technique & Infrastructure
Objectif : Mettre en place l’environnement commun à tous les modules suivants, sans logique métier.

Composants
Projet monorepo structuré (frontend/, backend/, mobile/, packages/).

Backend : Node.js + Express/Fastify, structure MVC, connexion PostgreSQL et Redis, gestion d’erreurs, logs, rate limiter.

Base de données : Schéma minimal (users, articles, categories et tables de liaison), migrations automatiques avec Knex/Prisma.

Authentification : JWT + refresh tokens, endpoints register/login/logout/refresh. Pas encore de rôles complexes (admin unique hardcodé).

CI/CD : GitHub Actions pour lint, tests, déploiement automatique sur Vercel (frontend) et Render/Railway (backend).

Documentation API : Swagger UI accessible sur /api-docs.

Stockage médias : Configuration Cloudinary (upload sécurisé par signature).

Cache : Redis fonctionnel, connecté.

Tests de validation
L’API est déployée et répond à GET /api/v1/health.

Un utilisateur admin peut se connecter via l’endpoint /auth/login.

Les migrations créent les tables en base.

Un fichier uploadé atterrit bien sur Cloudinary.

Le pipeline CI/CD s’exécute sans erreur.

Module 1 : Gestion de Contenu
Objectif : Permettre la création et l’affichage d’articles avec catégories.

Composants
Admin CRUD : Créer/modifier/supprimer un article (titre, contenu, image, statut brouillon/publié/archivé, auteur, source manuelle, catégories principales).

Gestion des catégories : Hiérarchiques (parent/enfant), ordre, icône. CRUD via admin.

Frontend public : Page d’accueil listant les derniers articles par catégorie, page article détaillé (SEO title/meta générés manuellement), pagination.

API publique : Endpoints pour récupérer articles (/articles, /articles/:slug, /categories).

Seed initial : 20 articles et 5 catégories pour démo.

Tests de validation
Un rédacteur peut créer un article, l’enregistrer en brouillon, le prévisualiser, puis le publier.

Le site web affiche la liste paginée des articles.

Les catégories fonctionnent (filtre par catégorie).

Le champ “source” et “auteur” apparaît sur la vue détaillée.

Aucune interaction utilisateur n’est encore requise (pas de login visiteur).

Module 2 : Utilisateurs et Profils
Objectif : Permettre aux visiteurs de créer un compte, de personnaliser leur expérience de base.

Composants
Inscription/Connexion : Formulaire email/mot de passe, OAuth Google/Apple/Facebook.

Profil public : Photo avatar, pays, langue, bio (optionnel).

Préférences : Choix des catégories favorites, langue d’affichage, mode sombre.

Historique de lecture : Sauvegarde automatique des articles consultés (avec timestamp). Affichage dans le profil.

Favoris : Bouton sauvegarder, liste accessible depuis le menu utilisateur.

Sécurité : Vérification d’email (optionnelle au MVP), réinitialisation de mot de passe, jetons refresh.

Tests de validation
Un visiteur peut s’inscrire, recevoir un email de vérification et activer son compte.

Après connexion, il peut modifier son avatar, son pays, sa langue, et sélectionner des catégories préférées.

L’historique se remplit automatiquement à chaque clic sur un article connecté.

Les favoris peuvent être ajoutés/retirés et persistent entre sessions.

Module 3 : Interactions Sociales
Objectif : Ajouter l’engagement communautaire de base.

Composants
Likes : Bouton like/unlike sur les articles, compteur. API avec suppression de doublon.

Commentaires : Rédaction, affichage en liste, pagination, auteur + date.

Réponses : Imbrication 1 niveau (commentaire -> réponse), pas de threads infinis.

Signalement : Bouton “signaler” sur commentaire, enregistrement en base avec motif. Pas encore de queue de modération (manuel admin direct).

Partage : Génération lien natif WhatsApp/Facebook/Telegram via Web Share API ou boutons dédiés.

Pagination et cache : Comments avec cursor, cache Redis des likes.

Tests de validation
Un utilisateur connecté peut liker un article, le compteur s’incrémente. Un second like retire le sien.

Il peut poster un commentaire, qui apparaît instantanément. Un autre utilisateur peut répondre.

Un visiteur non connecté voit les commentaires mais ne peut pas interagir.

Un signalement remonte bien en base avec l’ID du commentaire.

Module 4 : Collecte Automatique de News
Objectif : Alimenter automatiquement la plateforme en articles depuis des sources externes.

Composants
Gestion des sources : Admin peut ajouter une source (URL flux RSS, clé API, paramètres de fréquence).

Pipeline d’import (jobs BullMQ) :

Récupération des données (RSS, API News).
Nettoyage HTML, extraction texte et image principale.
Détection de doublons (hash du titre + date).
Catégorisation automatique basique (règles par mots-clés depuis les catégories existantes).
Queue de validation : Article en statut “pending_review”. Les imports auto peuvent être publiés automatiquement si la source est “de confiance”.
Interface admin : Liste des articles importés en attente, possibilité de valider, éditer, rejeter.

Résumé automatique : Extrait des 200 premiers caractères pour la preview.

Tests de validation
Ajouter un flux RSS valide, lancer l’import manuellement ou via cron simulé.

Voir les articles en attente dans l’admin, les valider, et les voir apparaître sur le site.

Un flux avec des articles déjà importés ne crée pas de doublons.

Un article rejeté disparaît de la queue et n’est pas publié.

Module 5 : Personnalisation et Recommandation (Basique)
Objectif : Offrir un fil d’actualité personnalisé selon le profil et l’activité.

Composants
Fil personnalisé (endpoint /feed) tenant compte :

Catégories préférées de l’utilisateur

Pays choisi

Historique de lecture des 30 derniers jours

Articles les plus likés/commentés (pondération simple)

Algorithme hybride sans IA (score = catégorie préférée + pays + récence + popularité).

Cache : feed pré-calculé par utilisateur régulièrement ou à la demande (Redis).

Notifications intelligentes (push) basées sur une règle simple : breaking news dans les catégories favorites, envoyées via Firebase Cloud Messaging (module mobile plus tard, ici on prépare les tokens).

Paramètres utilisateur : Choix de recevoir les notifications par catégorie.

Tests de validation
Un utilisateur sans historique voit les articles les plus récents populaires de son pays.

Après avoir lu 5 articles “Sport”, son feed remonte davantage d’articles “Sport”.

Les notifications sont envoyées uniquement pour les catégories autorisées par l’utilisateur.

Le temps de réponse du feed est < 200ms.

Module 6 : Monétisation Publicitaire
Objectif : Générer des revenus tout en préservant l’expérience utilisateur.

Composants
Gestion des campagnes : Admin crée une bannière (image, lien, position : header, sidebar, in-feed), dates de début/fin, nombre max d’affichages, ciblage par pays/catégorie.

Affichage côté site : Composant AdSlot, rotation des bannières respectant la fréquence.

Suivi : Clics et impressions enregistrés en base, dashboard simple pour l’annonceur (futur).

Articles sponsorisés : Un champ “sponsorisé” pour un article, avec logo “sponsorisé” et mise en avant dans le fil.

Intégration Google Ads : Emplacement dédié, gestion via script, mais pour l’instant on privilégie les campagnes internes.

Protection anti-fraude : Rate limiting sur les clics.

Tests de validation
Un administrateur crée une bannière pour la page d’accueil, elle s’affiche à la position prévue.

Un clic est comptabilisé et enregistré.

La bannière disparaît automatiquement après sa date de fin.

Un article marqué “sponsorisé” apparaît en haut du feed et porte le badge.

Module 7 : Application Mobile (React Native + Expo)
Objectif : Offrir l’expérience complète sur iOS et Android en réutilisant l’API.

Composants
Écrans principaux :

Accueil (feed personnalisé),

Détail article (avec partage, like, commentaires),

Catégories (navigation),

Profil (historique, favoris, paramètres),

Authentification.

Réutilisation de l’API existante (mêmes endpoints, tokens JWT).

Push notifications : Réception via Firebase, redirection vers l’article.

Mode hors-ligne : Cache local des derniers articles et du feed (AsyncStorage).

Interface adaptative : Dark mode automatique, gestes de navigation, performance optimisée (FlatList, lazy loading images).

Tests de validation
Se connecter avec le même compte que sur le web, retrouver son historique et ses favoris.

Liker, commenter un article depuis le mobile, les modifications sont visibles sur le web.

Recevoir une notification breaking news et atterrir sur l’article correspondant.

L’application fonctionne en mode avion avec cache.

Module 8 : Panel d’Administration Avancé
Objectif : Pilotage complet de la plateforme avec analytics et outils de modération.

Composants
Tableau de bord : Statistiques (articles publiés/jour, nouveaux utilisateurs, likes, commentaires, clics pub), graphiques simples.

Gestion fine des utilisateurs : Liste, recherche, suspension, attribution de rôles (admin, éditeur, modérateur).

Queue de modération : Commentaires et articles signalés, avec action (approuver, supprimer, bannir l’utilisateur).

Planification des publications : Articles programmés avec date/heure de mise en ligne.

Revenus publicitaires : Synthèse des impressions/clics par campagne.

Logs de sécurité : Connexions, actions admin, erreurs, exports CSV.

Tests de validation
L’admin voit un pic d’inscriptions après une campagne, confirmé par le graphique.

Un modérateur peut traiter un flux de signalements et supprimer un commentaire abusif.

Planification : un article programmé se publie automatiquement à l’heure prévue.

Un rôle “éditeur” peut créer des articles mais pas gérer les utilisateurs.

Module 9 : Intelligence Artificielle et Traduction
Objectif : Augmenter la qualité et la portée des contenus via l’IA.

Composants (basés sur OpenRouter / modèles légers)
Résumé automatique : Résumé de l’article en 3 lignes (bouton admin et automatique à l’import).

Génération de tags : Suggestions de tags depuis le contenu (modèle LLM).

SEO title/meta : Génération automatique avec validation humaine.

Détection de spam/fake : Score de confiance sur les commentaires et articles (analyse sémantique).

Traduction automatique : Traduction des articles dans les langues supportées (via API DeepL ou modèle open source). Conservation de l’article original et versions traduites liées.

Assistant de rédaction : reformulation, correction de fautes côté admin (appel LLM).

Tests de validation
Un article importé obtient automatiquement un résumé et des tags pertinents visibles dans l’admin.

Un commentaire à caractère injurieux est automatiquement marqué “à modérer”.

Un article en français peut être traduit en anglais et disponible sous /en/article-slug.

L’assistant de rédaction améliore la grammaire d’un article sans changer le sens.

Module 10 : Marketplace et Annonces Utilisateurs
Objectif : Permettre aux utilisateurs de publier des annonces classées par catégories marchandes.

Composants
Création d’annonce : Formulaire multi-étapes (catégorie : immobilier, emploi, véhicules, services…), images multiples, vidéos, géolocalisation, prix, description.

Pages listing : Recherche full-text (Elasticsearch ou Meilisearch) avec filtres (catégorie, pays, prix, date).

Page détail : Carrousel photo, contacter l’annonceur via formulaire (anonymisation email) ou chat en temps réel (Socket.IO).

Mise en avant payante : Paiement (intégration Stripe ou mobile money) pour mettre l’annonce en top ou en vedette.

Modération : Annonces en statut “pending” jusqu’à validation par un modérateur (ou IA de filtrage).

Tableau de bord vendeur : Gérer ses annonces, voir les contacts.

Tests de validation
Un utilisateur authentifié peut créer une annonce immobilière avec 5 photos, une vidéo et un prix.

L’annonce n’est pas visible publiquement avant validation par un admin.

Une fois publiée, elle apparaît dans les résultats de recherche par ville et catégorie.

La mise en avant payante est activée après un paiement test réussi, l’annonce remonte en haut de la liste.

Chemin Direct et Indépendance des Modules
Chaque module ci-dessus peut être développé, testé et livré en s’appuyant uniquement sur les modules précédents. Aucun retour en arrière n’est nécessaire, car :

Le schéma de base de données est extensible (migrations additives sans rupture).

L’API reste rétrocompatible (versionnée).

Les interfaces utilisateur s’enrichissent progressivement sans casser l’existant.
```


**Tool Call: List the `news/supabase` directory's contents**
Status: Completed

# Folders:
news\supabase\.temp
news\supabase\functions
news\supabase\migrations

# Files:
news\supabase\config.toml


## Assistant

<thinking>
**Inspecting configurations**

I need to continue analyzing things, specifically the Supabase configuration, migrations, and functions. I also want to check the package-lock file for accuracy. It's essential to confirm all the paths to make sure everything runs smoothly. I'll dive deeper into the configurations and get everything lined up correctly! It’s all about ensuring that the setup is just right so we can avoid any hiccups later on.
</thinking>

**Tool Call: List the `news/supabase/functions` directory's contents**
Status: Completed

# Folders:
news\supabase\functions\aggregate_news
news\supabase\functions\clear_db
news\supabase\functions\telegram_bot


**Tool Call: List the `news/supabase/migrations` directory's contents**
Status: Completed


# Files:
news\supabase\migrations\20240503194000_create_articles_and_sources.sql
news\supabase\migrations\20260503200222_setup_cron.sql
news\supabase\migrations\20260503200339_add_test_source.sql
news\supabase\migrations\20260503201158_add_telegram_source.sql
news\supabase\migrations\20260503201507_fix_telegram_source.sql
news\supabase\migrations\20260503202730_clear_articles.sql
news\supabase\migrations\20260503223812_add_media_groups.sql
news\supabase\migrations\20260503223941_add_media_info_to_media_groups.sql
news\supabase\migrations\20260511100000_add_telegram_message_id.sql
news\supabase\migrations\20260511190000_add_multi_tenant_users_flows.sql


**Tool Call: Read file `supabase\config.toml`**
Status: Completed

```news/supabase/config.toml
# For detailed configuration reference documentation, visit:
# https://supabase.com/docs/guides/local-development/cli/config
# A string used to distinguish different Supabase projects on the same host. Defaults to the
# working directory name when running `supabase init`.
project_id = "news"

[api]
enabled = true
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. `public` and `graphql_public` schemas are included by default.
schemas = ["public", "graphql_public"]
# Extra schemas to add to the search_path of every request.
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure. Limits payload size
# for accidental or malicious requests.
max_rows = 1000

[api.tls]
# Enable HTTPS endpoints locally using a self-signed certificate.
enabled = false
# Paths to self-signed certificate pair.
# cert_path = "../certs/my-cert.pem"
# key_path = "../certs/my-key.pem"

[db]
# Port to use for the local database URL.
port = 54322
# Port used by db diff command to initialize the shadow database.
shadow_port = 54320
# Maximum amount of time to wait for health check when starting the local database.
health_timeout = "2m"
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version;` on the remote database to check.
major_version = 17

[db.pooler]
enabled = false
# Port to use for the local connection pooler.
port = 54329
# Specifies when a server connection can be reused by other clients.
# Configure one of the supported pooler modes: `transaction`, `session`.
pool_mode = "transaction"
# How many server connections to allow per user/database pair.
default_pool_size = 20
# Maximum number of client connections allowed.
max_client_conn = 100

# [db.vault]
# secret_key = "env(SECRET_VALUE)"

[db.migrations]
# If disabled, migrations will be skipped during a db push or reset.
enabled = true
# Specifies an ordered list of schema files that describe your database.
# Supports glob patterns relative to supabase directory: "./schemas/*.sql"
schema_paths = []

[db.seed]
# If enabled, seeds the database after migrations during a db reset.
enabled = true
# Specifies an ordered list of seed files to load during db reset.
# Supports glob patterns relative to supabase directory: "./seeds/*.sql"
sql_paths = ["./seed.sql"]

[db.network_restrictions]
# Enable management of network restrictions.
enabled = false
# List of IPv4 CIDR blocks allowed to connect to the database.
# Defaults to allow all IPv4 connections. Set empty array to block all IPs.
allowed_cidrs = ["0.0.0.0/0"]
# List of IPv6 CIDR blocks allowed to connect to the database.
# Defaults to allow all IPv6 connections. Set empty array to block all IPs.
allowed_cidrs_v6 = ["::/0"]

# Uncomment to reject non-secure connections to the database.
# [db.ssl_enforcement]
# enabled = true

[realtime]
enabled = true
# Bind realtime via either IPv4 or IPv6. (default: IPv4)
# ip_version = "IPv6"
# The maximum length in bytes of HTTP request headers. (default: 4096)
# max_header_length = 4096

[studio]
enabled = true
# Port to use for Supabase Studio.
port = 54323
# External URL of the API server that frontend connects to.
api_url = "http://127.0.0.1"
# OpenAI API Key to use for Supabase AI in the Supabase Studio.
openai_api_key = "env(OPENAI_API_KEY)"

# Email testing server. Emails sent with the local dev setup are not actually sent - rather, they
# are monitored, and you can view the emails that would have been sent from the web interface.
[inbucket]
enabled = true
# Port to use for the email testing server web interface.
port = 54324
# Uncomment to expose additional ports for testing user applications that send emails.
# smtp_port = 54325
# pop3_port = 54326
# admin_email = "admin@email.com"
# sender_name = "Admin"

[storage]
enabled = true
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

# Uncomment to configure local storage buckets
# [storage.buckets.images]
# public = false
# file_size_limit = "50MiB"
# allowed_mime_types = ["image/png", "image/jpeg"]
# objects_path = "./images"

# Allow connections via S3 compatible clients
[storage.s3_protocol]
enabled = true

# Image transformation API is available to Supabase Pro plan.
# [storage.image_transformation]
# enabled = true

# Store analytical data in S3 for running ETL jobs over Iceberg Catalog
# This feature is only available on the hosted platform.
[storage.analytics]
enabled = false
max_namespaces = 5
max_tables = 10
max_catalogs = 2

# Analytics Buckets is available to Supabase Pro plan.
# [storage.analytics.buckets.my-warehouse]

# Store vector embeddings in S3 for large and durable datasets
# This feature is only available on the hosted platform.
[storage.vector]
enabled = false
max_buckets = 10
max_indexes = 5

# Vector Buckets is available to Supabase Pro plan.
# [storage.vector.buckets.documents-openai]

[auth]
enabled = true
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://127.0.0.1:3000"
# The public URL that Auth serves on. Defaults to the API external URL with `/auth/v1` appended.
# external_url = ""
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = ["https://127.0.0.1:3000"]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# JWT issuer URL. If not set, defaults to auth.external_url.
# jwt_issuer = ""
# Path to JWT signing key. DO NOT commit your signing keys file to git.
# signing_keys_path = "./signing_keys.json"
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
# Requires enable_refresh_token_rotation = true.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true
# Allow/disallow anonymous sign-ins to your project.
enable_anonymous_sign_ins = false
# Allow/disallow testing manual linking of accounts
enable_manual_linking = false
# Passwords shorter than this value will be rejected as weak. Minimum 6, recommended 8 or more.
minimum_password_length = 6
# Passwords that do not meet the following requirements will be rejected as weak. Supported values
# are: `letters_digits`, `lower_upper_letters_digits`, `lower_upper_letters_digits_symbols`
password_requirements = ""

# Configure passkey sign-ins.
# [auth.passkey]
# enabled = false

# Configure WebAuthn relying party settings (required when passkey is enabled).
# [auth.webauthn]
# rp_display_name = "Supabase"
# rp_id = "localhost"
# rp_origins = ["http://127.0.0.1:3000"]

[auth.rate_limit]
# Number of emails that can be sent per hour. Requires auth.email.smtp to be enabled.
email_sent = 2
# Number of SMS messages that can be sent per hour. Requires auth.sms to be enabled.
sms_sent = 30
# Number of anonymous sign-ins that can be made per hour per IP address. Requires enable_anonymous_sign_ins = true.
anonymous_users = 30
# Number of sessions that can be refreshed in a 5 minute interval per IP address.
token_refresh = 150
# Number of sign up and sign-in requests that can be made in a 5 minute interval per IP address (excludes anonymous users).
sign_in_sign_ups = 30
# Number of OTP / Magic link verifications that can be made in a 5 minute interval per IP address.
token_verifications = 30
# Number of Web3 logins that can be made in a 5 minute interval per IP address.
web3 = 30

# Configure one of the supported captcha providers: `hcaptcha`, `turnstile`.
# [auth.captcha]
# enabled = true
# provider = "hcaptcha"
# secret = ""

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# If enabled, a user will be required to confirm any email change on both the old, and new email
# addresses. If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = false
# If enabled, users will need to reauthenticate or have logged in recently to change their password.
secure_password_change = false
# Controls the minimum amount of time that must pass before sending another signup confirmation or password reset email.
max_frequency = "1s"
# Number of characters used in the email OTP.
otp_length = 6
# Number of seconds before the email OTP expires (defaults to 1 hour).
otp_expiry = 3600

# Use a production-ready SMTP server
# [auth.email.smtp]
# enabled = true
# host = "smtp.sendgrid.net"
# port = 587
# user = "apikey"
# pass = "env(SENDGRID_API_KEY)"
# admin_email = "admin@email.com"
# sender_name = "Admin"

# Uncomment to customize email template
# [auth.email.template.invite]
# subject = "You have been invited"
# content_path = "./supabase/templates/invite.html"

# Uncomment to customize notification email template
# [auth.email.notification.password_changed]
# enabled = true
# subject = "Your password has been changed"
# content_path = "./templates/password_changed_notification.html"

[auth.sms]
# Allow/disallow new user signups via SMS to your project.
enable_signup = false
# If enabled, users need to confirm their phone number before signing in.
enable_confirmations = false
# Template for sending OTP to users
template = "Your code is {{ .Code }}"
# Controls the minimum amount of time that must pass before sending another sms otp.
max_frequency = "5s"

# Use pre-defined map of phone number to OTP for testing.
# [auth.sms.test_otp]
# 4152127777 = "123456"

# Configure logged in session timeouts.
# [auth.sessions]
# Force log out after the specified duration.
# timebox = "24h"
# Force log out if the user has been inactive longer than the specified duration.
# inactivity_timeout = "8h"

# This hook runs before a new user is created and allows developers to reject the request based on the incoming user object.
# [auth.hook.before_user_created]
# enabled = true
# uri = "pg-functions://postgres/auth/before-user-created-hook"

# This hook runs before a token is issued and allows you to add additional claims based on the authentication method used.
# [auth.hook.custom_access_token]
# enabled = true
# uri = "pg-functions://<database>/<schema>/<hook_name>"

# Configure one of the supported SMS providers: `twilio`, `twilio_verify`, `messagebird`, `textlocal`, `vonage`.
[auth.sms.twilio]
enabled = false
account_sid = ""
message_service_sid = ""
# DO NOT commit your Twilio auth token to git. Use environment variable substitution instead:
auth_token = "env(SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN)"

# Multi-factor-authentication is available to Supabase Pro plan.
[auth.mfa]
# Control how many MFA factors can be enrolled at once per user.
max_enrolled_factors = 10

# Control MFA via App Authenticator (TOTP)
[auth.mfa.totp]
enroll_enabled = false
verify_enabled = false

# Configure MFA via Phone Messaging
[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false
otp_length = 6
template = "Your code is {{ .Code }}"
max_frequency = "5s"

# Configure MFA via WebAuthn
# [auth.mfa.web_authn]
# enroll_enabled = true
# verify_enabled = true

# Use an external OAuth provider. The full list of providers are: `apple`, `azure`, `bitbucket`,
# `discord`, `facebook`, `github`, `gitlab`, `google`, `keycloak`, `linkedin_oidc`, `notion`, `twitch`,
# `twitter`, `x`, `slack`, `spotify`, `workos`, `zoom`.
[auth.external.apple]
enabled = false
client_id = ""
# DO NOT commit your OAuth provider secret to git. Use environment variable substitution instead:
secret = "env(SUPABASE_AUTH_EXTERNAL_APPLE_SECRET)"
# Overrides the default auth callback URL derived from auth.external_url.
redirect_uri = ""
# Overrides the default auth provider URL. Used to support self-hosted gitlab, single-tenant Azure,
# or any other third-party OIDC providers.
url = ""
# If enabled, the nonce check will be skipped. Required for local sign in with Google auth.
skip_nonce_check = false
# If enabled, it will allow the user to successfully authenticate when the provider does not return an email address.
email_optional = false

# Allow Solana wallet holders to sign in to your project via the Sign in with Solana (SIWS, EIP-4361) standard.
# You can configure "web3" rate limit in the [auth.rate_limit] section and set up [auth.captcha] if self-hosting.
[auth.web3.solana]
enabled = false

# Use Firebase Auth as a third-party provider alongside Supabase Auth.
[auth.third_party.firebase]
enabled = false
# project_id = "my-firebase-project"

# Use Auth0 as a third-party provider alongside Supabase Auth.
[auth.third_party.auth0]
enabled = false
# tenant = "my-auth0-tenant"
# tenant_region = "us"

# Use AWS Cognito (Amplify) as a third-party provider alongside Supabase Auth.
[auth.third_party.aws_cognito]
enabled = false
# user_pool_id = "my-user-pool-id"
# user_pool_region = "us-east-1"

# Use Clerk as a third-party provider alongside Supabase Auth.
[auth.third_party.clerk]
enabled = false
# Obtain from https://clerk.com/setup/supabase
# domain = "example.clerk.accounts.dev"

# OAuth server configuration
[auth.oauth_server]
# Enable OAuth server functionality
enabled = false
# Path for OAuth consent flow UI
authorization_url_path = "/oauth/consent"
# Allow dynamic client registration
allow_dynamic_registration = false

[edge_runtime]
enabled = true
# Supported request policies: `oneshot`, `per_worker`.
# `per_worker` (default) — enables hot reload during local development.
# `oneshot` — fallback mode if hot reload causes issues (e.g. in large repos or with symlinks).
policy = "per_worker"
# Port to attach the Chrome inspector for debugging edge functions.
inspector_port = 8083
# The Deno major version to use.
deno_version = 2

# [edge_runtime.secrets]
# secret_key = "env(SECRET_VALUE)"

[analytics]
enabled = true
port = 54327
# Configure one of the supported backends: `postgres`, `bigquery`.
backend = "postgres"

# Experimental features may be deprecated any time
[experimental]
# Configures Postgres storage engine to use OrioleDB (S3)
orioledb_version = ""
# Configures S3 bucket URL, eg. <bucket_name>.s3-<region>.amazonaws.com
s3_host = "env(S3_HOST)"
# Configures S3 bucket region, eg. us-east-1
s3_region = "env(S3_REGION)"
# Configures AWS_ACCESS_KEY_ID for S3 bucket
s3_access_key = "env(S3_ACCESS_KEY)"
# Configures AWS_SECRET_ACCESS_KEY for S3 bucket
s3_secret_key = "env(S3_SECRET_KEY)"

# [experimental.pgdelta]
# When enabled, pg-delta becomes the active engine for supported schema flows.
# enabled = false
# Directory under `supabase/` where declarative files are written.
# declarative_schema_path = "./database"
# JSON string passed through to pg-delta SQL formatting.
# format_options = "{\"keywordCase\":\"upper\",\"indent\":2,\"maxWidth\":80,\"commaStyle\":\"trailing\"}"

[functions.aggregate_news]
enabled = true
verify_jwt = false
import_map = "./functions/aggregate_news/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/aggregate_news/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/aggregate_news/*.html" ]

[functions.clear_db]
enabled = true
verify_jwt = true
import_map = "./functions/clear_db/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/clear_db/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/clear_db/*.html" ]

[functions.telegram_bot]
enabled = true
verify_jwt = false
import_map = "./functions/telegram_bot/deno.json"
entrypoint = "./functions/telegram_bot/index.ts"

```


**Tool Call: Read file `package-lock.json` (lines 1-120)**
Status: Completed

```news/package-lock.json
{
  "name": "news",
  "lockfileVersion": 3,
  "requires": true,
  "packages": {
    "": {
      "devDependencies": {
        "supabase": "^2.98.0"
      }
    },
    "node_modules/@isaacs/fs-minipass": {
      "version": "4.0.1",
      "resolved": "https://registry.npmjs.org/@isaacs/fs-minipass/-/fs-minipass-4.0.1.tgz",
      "integrity": "sha512-wgm9Ehl2jpeqP3zw/7mo3kRHFp5MEDhqAdwy1fTGkHAwnkGOVsgpvQhL8B5n1qlb01jV3n/bI0ZfZp5lWA1k4w==",
      "dev": true,
      "license": "ISC",
      "dependencies": {
        "minipass": "^7.0.4"
      },
      "engines": {
        "node": ">=18.0.0"
      }
    },
    "node_modules/agent-base": {
      "version": "9.0.0",
      "resolved": "https://registry.npmjs.org/agent-base/-/agent-base-9.0.0.tgz",
      "integrity": "sha512-TQf59BsZnytt8GdJKLPfUZ54g/iaUL2OWDSFCCvMOhsHduDQxO8xC4PNeyIkVcA5KwL2phPSv0douC0fgWzmnA==",
      "dev": true,
      "license": "MIT",
      "engines": {
        "node": ">= 20"
      }
    },
    "node_modules/bin-links": {
      "version": "6.0.0",
      "resolved": "https://registry.npmjs.org/bin-links/-/bin-links-6.0.0.tgz",
      "integrity": "sha512-X4CiKlcV2GjnCMwnKAfbVWpHa++65th9TuzAEYtZoATiOE2DQKhSp4CJlyLoTqdhBKlXjpXjCTYPNNFS33Fi6w==",
      "dev": true,
      "license": "ISC",
      "dependencies": {
        "cmd-shim": "^8.0.0",
        "npm-normalize-package-bin": "^5.0.0",
        "proc-log": "^6.0.0",
        "read-cmd-shim": "^6.0.0",
        "write-file-atomic": "^7.0.0"
      },
      "engines": {
        "node": "^20.17.0 || >=22.9.0"
      }
    },
    "node_modules/chownr": {
      "version": "3.0.0",
      "resolved": "https://registry.npmjs.org/chownr/-/chownr-3.0.0.tgz",
      "integrity": "sha512-+IxzY9BZOQd/XuYPRmrvEVjF/nqj5kgT4kEq7VofrDoM1MxoRjEWkrCC3EtLi59TVawxTAn+orJwFQcrqEN1+g==",
      "dev": true,
      "license": "BlueOak-1.0.0",
      "engines": {
        "node": ">=18"
      }
    },
    "node_modules/cmd-shim": {
      "version": "8.0.0",
      "resolved": "https://registry.npmjs.org/cmd-shim/-/cmd-shim-8.0.0.tgz",
      "integrity": "sha512-Jk/BK6NCapZ58BKUxlSI+ouKRbjH1NLZCgJkYoab+vEHUY3f6OzpNBN9u7HFSv9J6TRDGs4PLOHezoKGaFRSCA==",
      "dev": true,
      "license": "ISC",
      "engines": {
        "node": "^20.17.0 || >=22.9.0"
      }
    },
    "node_modules/data-uri-to-buffer": {
      "version": "4.0.1",
      "resolved": "https://registry.npmjs.org/data-uri-to-buffer/-/data-uri-to-buffer-4.0.1.tgz",
      "integrity": "sha512-0R9ikRb668HB7QDxT1vkpuUBtqc53YyAwMwGeUFKRojY/NWKvdZ+9UYtRfGmhqNbRkTSVpMbmyhXipFFv2cb/A==",
      "dev": true,
      "license": "MIT",
      "engines": {
        "node": ">= 12"
      }
    },
    "node_modules/debug": {
      "version": "4.4.3",
      "resolved": "https://registry.npmjs.org/debug/-/debug-4.4.3.tgz",
      "integrity": "sha512-RGwwWnwQvkVfavKVt22FGLw+xYSdzARwm0ru6DhTVA3umU5hZc28V3kO4stgYryrTlLpuvgI9GiijltAjNbcqA==",
      "dev": true,
      "license": "MIT",
      "dependencies": {
        "ms": "^2.1.3"
      },
      "engines": {
        "node": ">=6.0"
      },
      "peerDependenciesMeta": {
        "supports-color": {
          "optional": true
        }
      }
    },
    "node_modules/fetch-blob": {
      "version": "3.2.0",
      "resolved": "https://registry.npmjs.org/fetch-blob/-/fetch-blob-3.2.0.tgz",
      "integrity": "sha512-7yAQpD2UMJzLi1Dqv7qFYnPbaPx7ZfFK6PiIxQ4PfkGPyNyl2Ugx+a/umUonmKqjhM4DnfbMvdX6otXq83soQQ==",
      "dev": true,
      "funding": [
        {
          "type": "github",
          "url": "https://github.com/sponsors/jimmywarting"
        },
        {
          "type": "paypal",
          "url": "https://paypal.me/jimmywarting"
        }
      ],
      "license": "MIT",
      "dependencies": {
        "node-domexception": "^1.0.0",
        "web-streams-polyfill": "^3.0.3"
      },
      "engines": {
        "node": "^12.20 || >= 14.13"

```


## Assistant

<thinking>
**Continuing project analysis**

I need to keep going with the project analysis by inspecting the function files and migrations. It's clear we have some found paths to work with. I'll consider using a "find" command to list the subdirectories involved. It seems like taking a systematic approach here will help me identify what needs to be checked more closely. I just hope I'm on the right track and not missing anything crucial!
</thinking>

**Tool Call: List the `news/supabase/functions/aggregate_news` directory's contents**
Status: Completed


# Files:
news\supabase\functions\aggregate_news\.npmrc
news\supabase\functions\aggregate_news\deno.json
news\supabase\functions\aggregate_news\index.ts


**Tool Call: List the `news/supabase/functions/clear_db` directory's contents**
Status: Completed


# Files:
news\supabase\functions\clear_db\.npmrc
news\supabase\functions\clear_db\deno.json
news\supabase\functions\clear_db\index.ts


**Tool Call: List the `news/supabase/functions/telegram_bot` directory's contents**
Status: Completed


# Files:
news\supabase\functions\telegram_bot\index.ts


**Tool Call: Read file `supabase\migrations\20240503194000_create_articles_and_sources.sql`**
Status: Completed

```news/supabase/migrations/20240503194000_create_articles_and_sources.sql
-- Create sources table
CREATE TABLE IF NOT EXISTS public.sources (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL,
    type TEXT NOT NULL, -- e.g., 'telegram', 'mediastack', 'gnews'
    config JSONB DEFAULT '{}'::jsonb, -- to store channel IDs, API keys, etc.
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Create articles table
CREATE TABLE IF NOT EXISTS public.articles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    title TEXT NOT NULL,
    summary TEXT,
    content TEXT,
    original_url TEXT,
    source_url TEXT,
    source_id UUID REFERENCES public.sources(id) ON DELETE SET NULL,
    is_certified BOOLEAN DEFAULT false,
    image_url TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    UNIQUE(title)
);

-- Enable RLS
ALTER TABLE public.sources ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.articles ENABLE ROW LEVEL SECURITY;

-- Create policies
CREATE POLICY "Allow public read access to articles"
    ON public.articles FOR SELECT
    USING (true);

CREATE POLICY "Allow public read access to sources"
    ON public.sources FOR SELECT
    USING (true);

```


**Tool Call: Read file `supabase\migrations\20260511190000_add_multi_tenant_users_flows.sql`**
Status: Completed

```news/supabase/migrations/20260511190000_add_multi_tenant_users_flows.sql
-- Multi-tenant users, flows, quotas, and subscriptions

CREATE TABLE IF NOT EXISTS public.app_users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  telegram_user_id BIGINT UNIQUE NOT NULL,
  username TEXT,
  is_admin BOOLEAN NOT NULL DEFAULT false,
  plan TEXT NOT NULL DEFAULT 'free',
  plan_expires_at TIMESTAMP WITH TIME ZONE,
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.user_targets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.app_users(id) ON DELETE CASCADE,
  chat_id TEXT NOT NULL,
  title TEXT,
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(),
  UNIQUE(user_id, chat_id)
);

CREATE TABLE IF NOT EXISTS public.user_sources (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.app_users(id) ON DELETE CASCADE,
  type TEXT NOT NULL,
  config JSONB NOT NULL DEFAULT '{}'::jsonb,
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.flows (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.app_users(id) ON DELETE CASCADE,
  source_id UUID NOT NULL REFERENCES public.user_sources(id) ON DELETE CASCADE,
  target_id UUID NOT NULL REFERENCES public.user_targets(id) ON DELETE CASCADE,
  mode TEXT NOT NULL DEFAULT 'new_only',
  initial_last_n INTEGER NOT NULL DEFAULT 5,
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(),
  UNIQUE(user_id, source_id, target_id)
);

CREATE TABLE IF NOT EXISTS public.usage_daily (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.app_users(id) ON DELETE CASCADE,
  day DATE NOT NULL,
  analyzed_count INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(),
  UNIQUE(user_id, day)
);

CREATE TABLE IF NOT EXISTS public.payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.app_users(id) ON DELETE CASCADE,
  provider TEXT NOT NULL DEFAULT 'nelsius',
  reference TEXT UNIQUE NOT NULL,
  amount INTEGER NOT NULL,
  currency TEXT NOT NULL DEFAULT 'XAF',
  status TEXT NOT NULL DEFAULT 'created',
  raw_payload JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.app_users(id) ON DELETE CASCADE,
  provider TEXT NOT NULL DEFAULT 'nelsius',
  status TEXT NOT NULL DEFAULT 'inactive',
  start_at TIMESTAMP WITH TIME ZONE,
  end_at TIMESTAMP WITH TIME ZONE,
  payment_reference TEXT,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_user_sources_user_id ON public.user_sources(user_id);
CREATE INDEX IF NOT EXISTS idx_user_targets_user_id ON public.user_targets(user_id);
CREATE INDEX IF NOT EXISTS idx_flows_user_id ON public.flows(user_id);
CREATE INDEX IF NOT EXISTS idx_usage_daily_user_day ON public.usage_daily(user_id, day);
CREATE INDEX IF NOT EXISTS idx_payments_user_id ON public.payments(user_id);
CREATE INDEX IF NOT EXISTS idx_subscriptions_user_id ON public.subscriptions(user_id);

ALTER TABLE public.app_users ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.user_targets ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.user_sources ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.flows ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.usage_daily ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.payments ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.subscriptions ENABLE ROW LEVEL SECURITY;

```


## Assistant

<thinking>
**Analyzing code and migrations**

I need to keep analyzing the function code and all migrations. Maybe I could use find_path to help with this. I want to avoid anything too huge that could complicate things further. It’s essential to take a careful look at the indexes to ensure everything is functioning properly. There's a lot to consider here, and I need to make sure I'm being thorough in my assessment to avoid any issues later on!
</thinking>

**Tool Call: Read file `supabase\functions\aggregate_news\index.ts`**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts
SUCCESS: File outline retrieved. This file is too large to read all at once, so the outline below shows the file's structure with line numbers.

IMPORTANT: Do NOT retry this call without line numbers - you will get the same outline.
Instead, use the line numbers below to read specific sections by calling this tool again with start_line and end_line parameters.

# File outline for E:\laragon\www\news\supabase\functions\aggregate_news\index.ts

const BOT_TOKEN [L3]
const OUTPUT_CHAT_ID [L4]
const GEMINI_API_KEY [L5]
const STORJ_ACCESS_KEY [L8]
const STORJ_SECRET_KEY [L9]
const STORJ_ENDPOINT [L10]
const STORJ_BUCKET [L11]
const STORJ_REGION [L12]
function isStorjConfigured() [L14-16]
function toHex( ) [L18-20]
function utf8Bytes( ) [L22-24]
function toArrayBuffer( ) [L26-28]
async function sha256( ) [L30-34]
 const bytes [L31]
 const digest [L32]
async function hmacSha256( ) [L36-46]
 const cryptoKey [L37-43]
  name [L40]
  hash [L40]
 const sig [L44]
function amzDate( ) [L48-58]
 const yyyy [L49]
 const mm [L50]
 const dd [L51]
 const HH [L52]
 const MM [L53]
 const SS [L54]
 const short [L55]
 const amz [L56]
function encodeRfc3986( ) [L60-62]
function canonicalUriPathStyle( ) [L64-67]
 const parts [L65]
async function getSigningKey( ) [L69-75]
 const kDate [L70]
 const kRegion [L71]
 const kService [L72]
 const kSigning [L73]
function buildCanonicalQuery( ) [L77-82]
interface ScrapedMessage [L84-91]
const AD_PATTERNS [L94-109]
const AD_KEYWORDS_STRICT [L110-113]
function isQuickAd( ) [L114-141]
 const lower [L115]
 let hits [L116]
 const capsRatio [L128]
 const emojiCount [L132]
 const digitCount [L137]
function extractPhotoUrlFromBlock( ) [L144-152]
 const bg [L145]
 const img [L147]
 const videoPoster [L149]
function extractAllPhotosFromBlock( ) [L155-201]
 const urls [L156]
 const seen [L157]
 const mediaContainer [L160]
 const isAlbum [L164-165]
 const photoBlocks [L170]
 const bgMatch [L172]
 const photoSection [L183]
 const bgMatch [L191]
function extractVideoFromBlock( ) [L204-225]
 const videoSection [L206]
 const url [L209]
 const durationMatch [L212-213]
 let duration [L214]
async function fetchChannelPage( ) [L227-237]
 const url [L228]
 const res [L229-231]
  headers [L230]
   User-Agent [L230]
async function scrapeChannel( ) [L239-310]
 const messages [L241]
 const seenIds [L242]
 let beforeId [L244]
 let reachedAfter [L245]
 const html [L248]
 const blocks [L251]
 let minIdOnPage [L252]
 let foundAny [L253]
 const idMatch [L256]
 const msgId [L258]
 let text [L270]
 const textMatch [L271]
 const allPhotos [L283]
 const videoInfo [L284]
 const hasMedia [L286]
 id [L289]
 photoUrls [L291]
 videoUrl [L292]
 videoDuration [L293]
async function restructureWithAI( ) [L313-340]
 title [L314]
 content [L314]
 isAd [L314]
 const res [L316-326]
  method [L319]
  headers [L320]
   Content-Type [L320]
  body [L321-324]
   contents [L322]
    parts [L322]
     text [L322]
   generationConfig [L323]
    responseMimeType [L323]
 const data [L327]
 const textResp [L328]
 let jsonStr [L330]
 const m [L331]
 const parsed [L333]
 title [L334]
 content [L334]
 isAd [L334]
 title [L339]
 content [L339]
 isAd [L339]
async function uploadToStorjAndGetSignedUrl( ) [L343-425]
 const endpoint [L353]
 const host [L354]
 const service [L355]
 const key [L357]
 const canonicalUri [L358]
 const amz [L361]
 const short [L361]
 const payloadHash [L362]
 const canonicalHeaders [L364-368]
 const signedHeaders [L369]
 const canonicalRequest [L371-372]
 const algorithm [L374]
 const credentialScope [L375]
 const stringToSign [L376-377]
 const signingKey [L379]
 const signature [L380]
 const authorization [L381-382]
 const putUrl [L384]
 const putRes [L385-394]
  method [L386]
  headers [L387-392]
   content-type [L388]
   x-amz-date [L389]
   x-amz-content-sha256 [L390]
   authorization [L391]
  body [L393]
 const errText [L397]
 const expires [L403]
 const getParams [L404-410]
  X-Amz-Algorithm [L405]
  X-Amz-Credential [L406]
  X-Amz-Date [L407]
  X-Amz-Expires [L408]
  X-Amz-SignedHeaders [L409]
 const canonicalQuery [L411]
 const getCanonicalRequest [L412-413]
 const getStringToSign [L414-415]
 const getSignature [L416]
 const signedGetUrl [L418]
async function downloadMedia( ) [L428-443]
 const res [L430-432]
  headers [L431]
   User-Agent [L431]
 const buffer [L437]
function buildMultipartForm( ) [L446-475]
 const encoder [L447]
 const parts [L448]
 let totalLength [L466]
 const body [L468]
 let offset [L469]
async function sendTextToTelegram( ) [L478-495]
 const res [L481-485]
  method [L482]
  headers [L483]
   Content-Type [L483]
  body [L484]
   chat_id [L484]
   text [L484]
   parse_mode [L484]
 const data [L486]
async function sendPhotoToTelegram( ) [L498-541]
 const mediaData [L502]
 const storjUrl [L510]
 const res [L519-528]
  method [L520]
  headers [L521]
   Content-Type [L521]
  body [L522-527]
   chat_id [L523]
   photo [L524]
   caption [L525]
   parse_mode [L526]
 const data [L530]
async function uploadPhotoDirect( ) [L544-565]
 const boundary [L545]
 const body [L546-551]
  name [L547]
  value [L547]
  name [L548]
  value [L548]
  name [L549]
  value [L549]
  name [L550]
  value [L550]
  filename [L550]
  contentType [L550]
 const res [L553-557]
  method [L554]
  headers [L555]
   Content-Type [L555]
  body [L556]
 const data [L559]
async function sendAlbumToTelegram( ) [L568-639]
 const mediaItems [L574]
 const data [L576]
 const validPhotos [L581]
 const boundary [L594]
 const fields [L595-597]
  name [L596]
  value [L596]
 const mediaArray [L600-605]
  type [L601]
  media [L602]
  caption [L603]
  parse_mode [L604]
 name [L606]
 value [L606]
 name [L611]
 value [L612]
 filename [L613]
 contentType [L614]
 const body [L618]
 const res [L620-624]
  method [L621]
  headers [L622]
   Content-Type [L622]
  body [L623]
 const data [L626]
async function sendVideoToTelegram( ) [L642-688]
 const mediaData [L646]
 const storjUrl [L656]
 const res [L665-675]
  method [L666]
  headers [L667]
   Content-Type [L667]
  body [L668-674]
   chat_id [L669]
   video [L670]
   caption [L671]
   parse_mode [L672]
   supports_streaming [L673]
 const data [L677]
async function uploadVideoDirect( ) [L691-713]
 const boundary [L692]
 const body [L693-699]
  name [L694]
  value [L694]
  name [L695]
  value [L695]
  name [L696]
  value [L696]
  name [L697]
  value [L697]
  name [L698]
  value [L698]
  filename [L698]
  contentType [L698]
 const res [L701-705]
  method [L702]
  headers [L703]
   Content-Type [L703]
  body [L704]
 const data [L707]
const supabase [L717-720]
const sources [L723]
success [L730]
new [L730]
message [L730]
let processedCount [L733]
let newArticlesCount [L734]
const channel [L737]
const lastMsgId [L741]
const messages [L744]
let messagesToProcess [L748]
let maxMsgId [L754]
const originalUrl [L763]
const existing [L764]
let finalContent [L784]
const aiRes [L786]
const textWithCredit [L794]
const isShortVideo [L798]
let telegramMsgId [L800]
const insertError [L819]
title [L822]
summary [L823]
content [L824]
original_url [L825]
source_id [L826]
is_certified [L827]
telegram_message_id [L828]
telegram_chat_id [L829]
method [L837]
headers [L838]
 Content-Type [L838]
body [L839]
 chat_id [L839]
 message_id [L839]
config [L858]
 last_message_id [L858]
success [L865]
processed [L865]
new [L865]
headers [L866]
 Content-Type [L866]
success [L870]
error [L870]
status [L870]

Showing symbols 1-313 (total symbols: 313)


NEXT STEPS: To read a specific symbol's implementation, call read_file with the same path plus start_line and end_line from the outline above.
For example, to read a function shown as [L100-150], use start_line: 100 and end_line: 150.
```


**Tool Call: Read file `supabase\functions\clear_db\index.ts`**
Status: Completed

```news/supabase/functions/clear_db/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

Deno.serve(async (req) => {
  const supabaseUrl = Deno.env.get('SUPABASE_URL') ?? ''
  const supabaseKey = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? ''
  const supabase = createClient(supabaseUrl, supabaseKey)

  const { error } = await supabase.from('articles').delete().neq('id', '00000000-0000-0000-0000-000000000000')

  if (error) return new Response(JSON.stringify({ error: error.message }), { status: 500 })
  return new Response(JSON.stringify({ success: true }))
})

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts`**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
SUCCESS: File outline retrieved. This file is too large to read all at once, so the outline below shows the file's structure with line numbers.

IMPORTANT: Do NOT retry this call without line numbers - you will get the same outline.
Instead, use the line numbers below to read specific sections by calling this tool again with start_line and end_line parameters.

# File outline for E:\laragon\www\news\supabase\functions\telegram_bot\index.ts

const BOT_TOKEN [L3]
const OUTPUT_CHAT_ID [L4]
const BASE [L5]
const ADMIN_IDS [L8-9]
function isAdmin( ) [L11-13]
async function getOrCreateAppUser( ) [L15-31]
 const existing [L16]
 const selErr [L16]
 const inserted [L24]
 const insErr [L24]
 telegram_user_id [L26]
 username [L26]
 is_admin [L26]
function isPremium( ) [L33-38]
async function reply( ) [L40-51]
 const res [L42-46]
  method [L43]
  headers [L44]
   Content-Type [L44]
  body [L45]
   chat_id [L45]
   text [L45]
   parse_mode [L45]
async function sendPhotoToChat( ) [L53-64]
 const res [L55-59]
  method [L56]
  headers [L57]
   Content-Type [L57]
  body [L58]
   chat_id [L58]
   photo [L58]
   caption [L58]
   parse_mode [L58]
async function copyMessageToChat( ) [L66-89]
 const body [L67-71]
  chat_id [L68]
  from_chat_id [L69]
  message_id [L70]
 const res [L77-81]
  method [L78]
  headers [L79]
   Content-Type [L79]
  body [L80]
 const data [L82]
 ok [L87]
 error [L87]
async function restructureWithAI( ) [L91-125]
 title [L92]
 content [L92]
 isAd [L92]
 const res [L94-105]
  method [L95]
  headers [L96]
   Content-Type [L96]
  body [L97-104]
   contents [L98-102]
    parts [L99-101]
     text [L100]
   generationConfig [L103]
    responseMimeType [L103]
 const data [L106]
 const textResp [L107]
 let jsonStr [L109]
 const match [L110]
 const parsed [L114]
 title [L116]
 content [L117]
 isAd [L118]
 title [L124]
 content [L124]
 isAd [L124]
const HELP [L128]
status [L132]
let body [L135]
status [L136]
const message [L138]
status [L139]
const chatId [L141]
const userId [L142]
const username [L143]
const messageId [L144]
const text [L145]
const photo [L146]
const hasMedia [L147]
const supabase [L149-152]
const parts [L154]
const command [L155]
const args [L156]
const adminMode [L158]
let appUser [L162]
const premium [L169]
status [L181]
const premium [L185]
const today [L186]
const usage [L187]
const used [L193]
const limit [L194]
status [L203]
status [L214]
status [L220]
count [L231]
head [L231]
count [L232]
head [L232]
const srcCount [L235]
count [L236]
head [L236]
const limit [L247]
const articles [L248]
const error [L248]
ascending [L250]
const lines [L255-259]
 const date [L256]
 const shortId [L257]
const a [L268]
const msg [L272-274]
const a [L288]
const error [L292]
const error [L301]
const forceMode [L308]
let deletedCount [L315]
let failedCount [L316]
let startMsgId [L323]
const latestRes [L325]
const latestData [L326]
const lastUpdate [L328]
const endMsgId [L338]
const delRes [L340-347]
 method [L341]
 headers [L342]
  Content-Type [L342]
 body [L343-346]
  chat_id [L344]
  message_id [L345]
const delData [L348]
const articles [L361]
const fetchError [L361]
ascending [L365]
status [L369]
const delRes [L376-383]
 method [L377]
 headers [L378]
  Content-Type [L378]
 body [L379-382]
  chat_id [L380]
  message_id [L381]
const delData [L384]
const deleteError [L398]
status [L405]
const sources [L409]
let resetCount [L414]
const newConfig [L417]
 last_message_id [L417]
const updateError [L418]
config [L420]
let content [L443]
let finalTitle [L446]
let finalContent [L447]
let isAd [L448]
const geminiKey [L451]
const aiRes [L454]
status [L463]
const uniqueSuffix [L466]
const uniqueTitle [L467]
const footer [L470]
const imageUrl [L477]
let imagePublicUrl [L478]
const fileRes [L480]
const fileData [L481]
const inserted [L488]
const error [L488]
title [L489]
summary [L490]
content [L491]
original_url [L492]
image_url [L493]
is_certified [L494]
method [L511]
headers [L512]
 Content-Type [L512]
body [L513]
 chat_id [L513]
 text [L513]
 parse_mode [L513]
const sources [L523]
ascending [L524]
const lines [L527-531]
 const icon [L528]
 const channel [L529]
let channel [L538]
const parts [L542]
const error [L547]
name [L548]
type [L549]
config [L550]
is_active [L551]
const src [L564]
is_active [L568]
const supabaseUrl [L577]
const serviceKey [L578]
const res [L579-582]
 method [L580]
 headers [L581]
  Authorization [L581]
const raw [L583]
let result [L584]
status [L594]
status [L600]
status [L622]

Showing symbols 1-209 (total symbols: 209)


NEXT STEPS: To read a specific symbol's implementation, call read_file with the same path plus start_line and end_line from the outline above.
For example, to read a function shown as [L100-150], use start_line: 100 and end_line: 150.
```


**Tool Call: Read file `supabase\functions\aggregate_news\deno.json`**
Status: Completed

```news/supabase/functions/aggregate_news/deno.json
{
  "imports": {
    "@supabase/functions-js": "jsr:@supabase/functions-js@^2"
  }
}

```


**Tool Call: Read file `supabase\functions\clear_db\deno.json`**
Status: Completed

```news/supabase/functions/clear_db/deno.json
{
  "imports": {
    "@supabase/functions-js": "jsr:@supabase/functions-js@^2"
  }
}

```


**Tool Call: Read file `supabase\migrations\20260503200222_setup_cron.sql`**
Status: Completed

```news/supabase/migrations/20260503200222_setup_cron.sql
-- Enable necessary extensions
CREATE EXTENSION IF NOT EXISTS pg_cron;
CREATE EXTENSION IF NOT EXISTS pg_net;

-- Create the cron job to call the Edge Function every 15 minutes
SELECT cron.schedule(
    'invoke-aggregate-news',
    '*/15 * * * *',
    $$
    SELECT net.http_post(
        url:='https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/aggregate_news',
        headers:='{"Content-Type": "application/json"}'::jsonb
    );
    $$
);

```


**Tool Call: Read file `supabase\migrations\20260503200339_add_test_source.sql`**
Status: Completed

```news/supabase/migrations/20260503200339_add_test_source.sql
-- Add a test RSS source
INSERT INTO public.sources (name, type, config, is_active)
VALUES ('Le Monde', 'rss', '{"url": "https://www.lemonde.fr/rss/une.xml"}', true)
ON CONFLICT DO NOTHING;

```


**Tool Call: Read file `supabase\migrations\20260503201158_add_telegram_source.sql`**
Status: Completed

```news/supabase/migrations/20260503201158_add_telegram_source.sql
-- Add N'ZUI MANTO telegram source
INSERT INTO public.sources (name, type, config, is_active)
VALUES ('N''ZUI MANTO', 'telegram', '{"channel": "nzuimanto"}', true)
ON CONFLICT DO NOTHING;

```


**Tool Call: Read file `supabase\migrations\20260503201507_fix_telegram_source.sql`**
Status: Completed

```news/supabase/migrations/20260503201507_fix_telegram_source.sql
-- Fix N'ZUI MANTO telegram source channel name
UPDATE public.sources 
SET name = 'N''ZUI MANTO 1', config = '{"channel": "NZUIMANTO1"}'
WHERE name = 'N''ZUI MANTO';

```


**Tool Call: Read file `supabase\migrations\20260503202730_clear_articles.sql`**
Status: Completed

```news/supabase/migrations/20260503202730_clear_articles.sql
DELETE FROM public.articles;

```


**Tool Call: Read file `supabase\migrations\20260503223812_add_media_groups.sql`**
Status: Completed

```news/supabase/migrations/20260503223812_add_media_groups.sql
CREATE TABLE media_groups (
  media_group_id text NOT NULL,
  message_id integer NOT NULL,
  created_at timestamp with time zone default now()
);
CREATE INDEX idx_media_groups_media_group_id ON media_groups (media_group_id);
```


**Tool Call: Read file `supabase\migrations\20260503223941_add_media_info_to_media_groups.sql`**
Status: Completed

```news/supabase/migrations/20260503223941_add_media_info_to_media_groups.sql
ALTER TABLE media_groups 
ADD COLUMN file_id text,
ADD COLUMN type text;
```


**Tool Call: Read file `supabase\migrations\20260511100000_add_telegram_message_id.sql`**
Status: Completed

```news/supabase/migrations/20260511100000_add_telegram_message_id.sql
-- Add telegram_message_id to track published messages for deletion
ALTER TABLE public.articles 
ADD COLUMN IF NOT EXISTS telegram_message_id BIGINT,
ADD COLUMN IF NOT EXISTS telegram_chat_id TEXT;

-- Index for faster lookups when clearing
CREATE INDEX IF NOT EXISTS idx_articles_telegram_msg 
ON public.articles(telegram_message_id) 
WHERE telegram_message_id IS NOT NULL;

```


## Assistant

<thinking>
**Reviewing understanding for project**

I see that I need to focus on finalizing the project in French, but first, I should understand the code better. The request essentially asks if I can analyze the project to understand it before we continue working on it. I'll summarize my current understanding and note what’s next. I should also inspect relevant snippets of function logic and read around the main bodies to grasp the aggregate and the Telegram bot elements.
</thinking>

**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 1-120)**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const BOT_TOKEN = Deno.env.get('TELEGRAM_BOT_TOKEN') || '';
const OUTPUT_CHAT_ID = Deno.env.get('TELEGRAM_CHAT_ID') || '';
const GEMINI_API_KEY = Deno.env.get('GEMINI_API_KEY') || '';

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get('STORJ_ACCESS_KEY') || '';
const STORJ_SECRET_KEY = Deno.env.get('STORJ_SECRET_KEY') || '';
const STORJ_ENDPOINT = Deno.env.get('STORJ_ENDPOINT') || 'https://gateway.storjshare.io';
const STORJ_BUCKET = Deno.env.get('STORJ_BUCKET') || '';
const STORJ_REGION = 'us-east-1'; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT);
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes).map((b) => b.toString(16).padStart(2, '0')).join('');
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(view.byteOffset, view.byteOffset + view.byteLength) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === 'string' ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest('SHA-256', toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    'raw',
    toArrayBuffer(key),
    { name: 'HMAC', hash: 'SHA-256' },
    false,
    ['sign'],
  );
  const sig = await crypto.subtle.sign('HMAC', cryptoKey, toArrayBuffer(utf8Bytes(data)));
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, '0');
  const dd = String(now.getUTCDate()).padStart(2, '0');
  const HH = String(now.getUTCHours()).padStart(2, '0');
  const MM = String(now.getUTCMinutes()).padStart(2, '0');
  const SS = String(now.getUTCSeconds()).padStart(2, '0');
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(/[!'()*]/g, (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`);
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split('/').map(encodeRfc3986));
  return parts.join('/').replace(/\/+/g, '/');
}

async function getSigningKey(secretKey: string, dateShort: string, region: string, service: string): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, 'aws4_request');
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join('&');
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[];  // All photos in album
  videoUrl?: string;     // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  'annonce', 'à vendre', 'en vente', 'prix:', 'prix :', 'contactez', 
  'recrutement', 'offre d\'emploi', ' Opportunité', 'gagner de l\'argent'
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;
  
  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {

```


**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 227-340)**
Status: Completed

````news/supabase/functions/aggregate_news/index.ts
async function fetchChannelPage(channel: string, beforeId?: number): Promise<string> {
  const url = beforeId ? `https://t.me/s/${channel}?before=${beforeId}` : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { 'User-Agent': 'Mozilla/5.0 (compatible; Googlebot/2.1)' },
  });
  if (!res.ok) {
    console.error(`[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ''}`);
    return '';
  }
  return await res.text();
}

async function scrapeChannel(channel: string, afterId = 0): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage) minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = '';
        const textMatch = block.match(/class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/);
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, '\n')
            .replace(/<\/p>/gi, '\n')
            .replace(/<[^>]+>/g, '')
            .replace(/&amp;/g, '&').replace(/&lt;/g, '<').replace(/&gt;/g, '>')
            .replace(/&quot;/g, '"').replace(/&#039;/g, "'").replace(/&nbsp;/g, ' ')
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);
        
        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;
        
        messages.push({ 
          id: msgId, 
          text, 
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia 
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(rawText: string): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10) return { title: '', content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          contents: [{ parts: [{ text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}` }] }],
          generationConfig: { responseMimeType: 'application/json' },
        }),
      }
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return { title: parsed.title || '', content: parsed.content || rawText, isAd: parsed.is_ad === true };
    }
  } catch (e) {
    console.error('[Gemini] Error:', e);
  }
  return { title: '', content: rawText, isAd: false };
}

````


**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 717-872)**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL') ?? '',
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? '',
  );

  try {
    const { data: sources } = await supabase
      .from('sources')
      .select('*')
      .eq('type', 'telegram')
      .eq('is_active', true);

    if (!sources || sources.length === 0) {
      return new Response(JSON.stringify({ success: true, new: 0, message: 'No active sources' }));
    }

    let processedCount = 0;
    let newArticlesCount = 0;

    for (const source of sources) {
      const channel = source.config?.channel;
      if (!channel) continue;

      // Get last processed message ID for this source (tracked in config)
      const lastMsgId = parseInt(source.config?.last_message_id || '0');

      console.log(`[Aggregate] Scraping @${channel}, after message ID: ${lastMsgId}`);
      const messages = await scrapeChannel(channel, lastMsgId);
      console.log(`[Aggregate] Found ${messages.length} new message(s) in @${channel}`);

      // FIRST RUN: limit to last 5 messages only to avoid spam
      let messagesToProcess = messages;
      if (lastMsgId === 0 && messages.length > 5) {
        messagesToProcess = messages.slice(-5); // Take only last 5
        console.log(`[Aggregate] First run - limiting to last 5 messages only`);
      }

      let maxMsgId = lastMsgId;

      for (const msg of messagesToProcess) {
        processedCount++;
        maxMsgId = Math.max(maxMsgId, msg.id);

        if (msg.text.length < 20 && !msg.hasMedia) continue;

        // ── Deduplication: use original_url (t.me/channel/msgId) as unique key
        const originalUrl = `https://t.me/${channel}/${msg.id}`;
        const { data: existing } = await supabase
          .from('articles')
          .select('id')
          .eq('original_url', originalUrl)
          .maybeSingle();

        if (existing) {
          console.log(`[Aggregate] Already processed: ${originalUrl}`);
          continue;
        }

        // ── Quick ad pre-filter (STRICT MODE)
        if (isQuickAd(msg.text)) {
          console.log(`[Aggregate] 🚫 AD BLOCKED: msg ${msg.id} from @${channel}`);
          // Mark as processed to avoid re-processing
          maxMsgId = Math.max(maxMsgId, msg.id);
          continue;
        }

        // ── AI restructuring
        let finalContent = msg.text;
        if (msg.text.length >= 10) {
          const aiRes = await restructureWithAI(msg.text);
          if (aiRes.isAd) {
            console.log(`[Aggregate] Gemini detected ad in msg ${msg.id}, skipping`);
            continue;
          }
          finalContent = aiRes.content.trim();
        }

        const textWithCredit = `${finalContent}\n\n📢 @izynews`;

        // ── Publish to Telegram channel FIRST (to get message_id)
        // Check for short video (< 60 seconds)
        const isShortVideo = msg.videoUrl && msg.videoDuration && msg.videoDuration <= 60;
        
        let telegramMsgId: number | null = null;
        
        if (isShortVideo && msg.videoUrl) {
          // Send video (short videos < 60s)
          telegramMsgId = await sendVideoToTelegram(textWithCredit, msg.videoUrl);
        } else if (msg.photoUrls && msg.photoUrls.length > 0) {
          // Send only the FIRST photo (not full album) - keeps it as single publication
          console.log(`[Aggregate] Album detected with ${msg.photoUrls.length} photos, using first only`);
          telegramMsgId = await sendPhotoToTelegram(textWithCredit, msg.photoUrls[0]);
        } else {
          // Text only
          telegramMsgId = await sendTextToTelegram(textWithCredit);
        }
        if (!telegramMsgId) {
          console.warn(`[Aggregate] Failed to publish msg ${msg.id} to Telegram, skipping DB insert`);
          continue;
        }

        // ── Save to DB with telegram_message_id
        const { error: insertError } = await supabase
          .from('articles')
          .insert({
            title: finalContent.replace(/<[^>]+>/g, '').substring(0, 100),
            summary: msg.text.substring(0, 200),
            content: textWithCredit,
            original_url: originalUrl,
            source_id: source.id,
            is_certified: false,
            telegram_message_id: telegramMsgId,
            telegram_chat_id: OUTPUT_CHAT_ID,
          });

        if (insertError) {
          console.error('[Aggregate] DB insert error:', insertError);
          // Try to delete the telegram message if DB insert failed
          try {
            await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/deleteMessage`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ chat_id: OUTPUT_CHAT_ID, message_id: telegramMsgId }),
            });
          } catch (e) {
            console.error('[Aggregate] Failed to cleanup telegram message:', e);
          }
          continue;
        }

        newArticlesCount++;
        console.log(`[Aggregate] Published msg ${msg.id} from @${channel} (TG msg_id: ${telegramMsgId})`);

        // Small delay to avoid rate limits
        await new Promise(r => setTimeout(r, 1500));
      }

      // ── Update last_message_id in source config so next run starts from here
      if (maxMsgId > lastMsgId) {
        await supabase
          .from('sources')
          .update({ config: { ...source.config, last_message_id: String(maxMsgId) } })
          .eq('id', source.id);
        console.log(`[Aggregate] Updated last_message_id for @${channel} to ${maxMsgId}`);
      }
    }

    return new Response(
      JSON.stringify({ success: true, processed: processedCount, new: newArticlesCount }),
      { headers: { 'Content-Type': 'application/json' } }
    );
  } catch (err) {
    console.error('[Aggregate] Fatal error:', err);
    return new Response(JSON.stringify({ success: false, error: (err as Error).message }), { status: 500 });
  }
});

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1-180)**
Status: Completed

````news/supabase/functions/telegram_bot/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const BOT_TOKEN = Deno.env.get('TELEGRAM_BOT_TOKEN') || '';
const OUTPUT_CHAT_ID = Deno.env.get('TELEGRAM_CHAT_ID') || '';
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get('TELEGRAM_ADMIN_IDS') || OUTPUT_CHAT_ID)
  .split(',').map(id => id.trim()).filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(supabase: any, telegramUserId: number, username?: string) {
  const { data: existing, error: selErr } = await supabase
    .from('app_users')
    .select('*')
    .eq('telegram_user_id', telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from('app_users')
    .insert({ telegram_user_id: telegramUserId, username: username || null, is_admin: false })
    .select('*')
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== 'premium') return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

async function reply(chatId: number | string, text: string, extra: Record<string, unknown> = {}) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ chat_id: chatId, text: text.substring(0, 4096), parse_mode: 'HTML', ...extra }),
    });
    if (!res.ok) console.error('[Bot] sendMessage failed:', await res.text());
  } catch (e) {
    console.error('[Bot] sendMessage exception:', e);
  }
}

async function sendPhotoToChat(chatId: number | string, photoFileId: string, caption: string) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ chat_id: chatId, photo: photoFileId, caption: caption.substring(0, 1024), parse_mode: 'HTML' }),
    });
    if (!res.ok) console.error('[Bot] sendPhoto failed:', await res.text());
  } catch (e) {
    console.error('[Bot] sendPhoto exception:', e);
  }
}

async function copyMessageToChat(chatId: number | string, fromChatId: number | string, messageId: number, caption?: string) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = 'HTML';
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error('[Bot] copyMessage failed:', data);
    return data;
  } catch (e) {
    console.error('[Bot] copyMessage exception:', e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(rawText: string, apiKey: string): Promise<{ title: string, content: string, isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5) return { title: '', content: rawText, isAd: false };
  try {
    const res = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        contents: [{
          parts: [{
            text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`
          }]
        }],
        generationConfig: { responseMimeType: "application/json" }
      })
    });
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return { 
        title: parsed.title || '', 
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true
      };
    }
  } catch(e) {
    console.error('[Gemini] Error:', e);
  }
  return { title: '', content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req) => {
  if (req.method !== 'POST') {
    return new Response('Telegram Bot Webhook — OK', { status: 200 });
  }

  let body: Record<string, unknown>;
  try { body = await req.json(); } catch { return new Response('Bad Request', { status: 400 }); }

  const message = (body.message || body.edited_message) as Record<string, unknown> | undefined;
  if (!message) return new Response('OK', { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username = ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || '') as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(photo || message.video || message.document || message.audio || message.animation || message.voice);

  const supabase = createClient(
    Deno.env.get('SUPABASE_URL') ?? '',
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? '',
  );

  const parts = text.split(/\s+/);
  const command = parts[0]?.toLowerCase() || '';
  const args = parts.slice(1);

  const adminMode = isAdmin(chatId) || isAdmin(userId);

  try {
    // ── Ensure app user exists for non-admins ─────────────────────────────
    let appUser: any = null;
    if (!adminMode) {
      appUser = await getOrCreateAppUser(supabase, userId, username);
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (!adminMode && (command === '/start' || command === '/help' || !command)) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? 'Premium' : 'Gratuit'}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`
      );

````


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 180-330)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
      );
      return new Response('OK', { status: 200 });
    }

    if (!adminMode && command === '/me') {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from('usage_daily')
        .select('analyzed_count')
        .eq('user_id', appUser.id)
        .eq('day', today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? '∞' : '5';
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? 'Premium' : 'Gratuit'}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ''}` +
          `Pour t'abonner: <code>/subscribe</code>`
      );
      return new Response('OK', { status: 200 });
    }

    if (!adminMode && command === '/subscribe') {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`
      );
      return new Response('OK', { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, '⛔ Accès refusé.');
      return new Response('OK', { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === '/start' || command === '/help' || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === '/stats') {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from('articles').select('*', { count: 'exact', head: true }),
        supabase.from('articles').select('*', { count: 'exact', head: true })
          .gte('created_at', new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase.from('sources')
        .select('*', { count: 'exact', head: true }).eq('is_active', true);
      await reply(chatId,
        `📊 <b>Statistiques</b>\n\n` +
        `📰 Articles total: <b>${total ?? 0}</b>\n` +
        `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
        `📡 Sources actives: <b>${srcCount ?? 0}</b>`
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === '/list') {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from('articles').select('id, title, created_at')
        .order('created_at', { ascending: false }).limit(limit);

      if (error) { await reply(chatId, `❌ Erreur: ${error.message}`); }
      else if (!articles?.length) { await reply(chatId, '📭 Aucun article.'); }
      else {
        const lines = articles.map((a, i) => {
          const date = new Date(a.created_at).toLocaleDateString('fr-FR');
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || 'Sans titre').substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(chatId, `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join('\n\n')}`);
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === '/article') {
      if (!args[0]) { await reply(chatId, '⚠️ Usage: /article &lt;id&gt;'); }
      else {
        const { data: a } = await supabase.from('articles').select('*')
          .ilike('id', `${args[0]}%`).limit(1).maybeSingle();
        if (!a) { await reply(chatId, '❌ Article non trouvé.'); }
        else {
          const msg = `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || '').substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === '/delete') {
      if (!args[0]) { await reply(chatId, '⚠️ Usage: /delete &lt;id&gt;'); }
      else {
        const { data: a } = await supabase.from('articles').select('id, title')
          .ilike('id', `${args[0]}%`).limit(1).maybeSingle();
        if (!a) { await reply(chatId, '❌ Article non trouvé.'); }
        else {
          const { error } = await supabase.from('articles').delete().eq('id', a.id);
          if (error) { await reply(chatId, `❌ Erreur: ${error.message}`); }
          else { await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`); }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === '/deleteall') {
      const { error } = await supabase.from('articles').delete().neq('id', '00000000-0000-0000-0000-000000000000');
      if (error) { await reply(chatId, `❌ Erreur lors de la suppression: ${error.message}`); }
      else { await reply(chatId, `✅ TOUS les articles ont été supprimés de la base de données.`); }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === '/clearall') {
      const forceMode = args[0] === 'force';
      await reply(chatId, forceMode 
        ? '⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes.' 
        : '⏳ Suppression en cours des messages du canal et de la base...'
      );
      
      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(chatId, '🔥 Mode FORCE: tentative suppression des 100 derniers messages...');
          
          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(`${BASE}/getUpdates?limit=1&offset=-1`);
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate = latestData.result[latestData.result.length - 1];
              if (lastUpdate.channel_post?.chat?.id?.toString() === OUTPUT_CHAT_ID.replace('@', '')) {
                startMsgId = lastUpdate.channel_post.message_id;

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 330-623)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log('[Bot] Could not get latest update, using default range');
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (!delData.description?.includes('message to delete not found')) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise(r => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from('articles')
          .select('id, telegram_message_id, telegram_chat_id, title')
          .not('telegram_message_id', 'is', null)
          .order('created_at', { ascending: false });
        
        if (fetchError) {
          await reply(chatId, `❌ Erreur récupération articles: ${fetchError.message}`);
          return new Response('OK', { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(`[Bot] Failed to delete message ${article.telegram_message_id}:`, delData.description);
              }
              // Small delay to avoid rate limits
              await new Promise(r => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from('articles')
          .delete()
          .neq('id', '00000000-0000-0000-0000-000000000000');
        
        if (deleteError) {
          await reply(chatId, `❌ Erreur suppression DB: ${deleteError.message}`);
          return new Response('OK', { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from('sources')
          .select('id, config')
          .eq('type', 'telegram');
        
        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: '0' };
            const { error: updateError } = await supabase
              .from('sources')
              .update({ config: newConfig })
              .eq('id', source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(chatId, 
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
          `${forceMode ? '🔥 Mode FORCE utilisé\n' : ''}` +
          `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
          `❌ Échecs: <b>${failedCount}</b>\n` +
          `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
          `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
          `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`
        );
      } catch (err) {
        console.error('[Bot] /clearall error:', err);
        await reply(chatId, `💥 Erreur lors du nettoyage: ${(err as Error).message}`);
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === '/publish') {
      let content = args.join(' ').trim();
      if (!content && !hasMedia) { await reply(chatId, '⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.'); }
      else {
        let finalTitle = 'Publication admin';
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get('GEMINI_API_KEY');
          if (geminiKey) {
            await reply(chatId, '⏳ Structuration avec l\'IA...');
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }
        
        if (isAd) {
          await reply(chatId, '❌ Ce contenu a été détecté comme une publicité/spam et n\'a pas été publié.');
          return new Response('OK', { status: 200 });
        }

        const uniqueSuffix = new Date().toISOString().replace(/[-:.TZ]/g, '').slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes('@izynews')) {
            finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
            finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase.from('articles').insert({
          title: uniqueTitle,
          summary: finalContent.substring(0, 500),
          content: finalContent,
          original_url: `https://t.me/admin_publish_${Date.now()}`,
          image_url: imagePublicUrl,
          is_certified: true,
        }).select('id').maybeSingle();

        if (error) { 
            console.error('[Bot] DB Insert Error:', error);
            await reply(chatId, `❌ Erreur DB: ${error.message}`); 
        }
        else {
          await reply(chatId, `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`);

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(OUTPUT_CHAT_ID, chatId, messageId, finalContent);
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: 'POST', 
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ chat_id: OUTPUT_CHAT_ID, text: finalContent.substring(0, 4096), parse_mode: 'HTML' }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === '/sources') {
      const { data: sources } = await supabase.from('sources').select('id, name, type, config, is_active')
        .order('created_at', { ascending: false });
      if (!sources?.length) { await reply(chatId, '📭 Aucune source.'); }
      else {
        const lines = sources.map(s => {
          const icon = s.is_active ? '🟢' : '🔴';
          const channel = s.config?.channel || '';
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ': @' + channel : ''}]`;
        });
        await reply(chatId, `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join('\n')}`);
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === '/addsource') {
      let channel = args[0]?.trim();
      if (!channel) { await reply(chatId, '⚠️ Usage: /addsource <@canal ou URL_TME>'); }
      else {
        if (channel.includes('t.me/')) {
          const parts = channel.split('t.me/');
          channel = parts[parts.length - 1].split('/')[0].split('?')[0];
        }
        channel = channel.replace(/^@/, '').toLowerCase();

        const { error } = await supabase.from('sources').insert({
          name: `@${channel}`,
          type: 'telegram',
          config: { channel },
          is_active: true,
        });
        if (error) { await reply(chatId, `❌ Erreur: ${error.message}`); }
        else { 
            await reply(chatId, `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`); 
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === '/delsource') {
      if (!args[0]) { await reply(chatId, '⚠️ Usage: /delsource &lt;id&gt;'); }
      else {
        const { data: src } = await supabase.from('sources').select('id, name')
          .ilike('id', `${args[0]}%`).limit(1).maybeSingle();
        if (!src) { await reply(chatId, '❌ Source non trouvée.'); }
        else {
          await supabase.from('sources').update({ is_active: false }).eq('id', src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === '/run') {
      await reply(chatId, '⏳ Agrégation en cours...');
      const supabaseUrl = Deno.env.get('SUPABASE_URL') ?? '';
      const serviceKey = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? '';
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: 'POST',
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error('[Bot] /run aggregate_news HTTP error:', res.status, raw?.slice(0, 500));
        await reply(chatId, `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`);
        return new Response('OK', { status: 200 });
      }

      if (!result) {
        console.error('[Bot] /run aggregate_news invalid JSON:', raw?.slice(0, 500));
        await reply(chatId, `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`);
        return new Response('OK', { status: 200 });
      }

      if (result.success) {
        await reply(chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`
        );
      } else {
        await reply(chatId, `❌ Erreur d'agrégation: ${result.error || 'inconnue'}`);
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(chatId, `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`);
    }

  } catch (err) {
    console.error('[Bot] Error:', err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response('OK', { status: 200 });
});

```


## Assistant

<thinking>
**Inspecting project settings**

I’m considering whether we have everything we need. I wonder if I should inspect the `.gitignore` file. Maybe there's something there that needs attention? I'm also thinking about the potential skills lock—if it’s not needed, we might want to address that too. Lastly, I want to run a `find` command to check if there’s no frontend/backend involved, just to make sure everything is organized properly.
</thinking>

**Tool Call: 34 matches**
Status: Completed

file://E:\laragon\www\news

file://E:\laragon\www\news\.env.local

file://E:\laragon\www\news\.gitignore

file://E:\laragon\www\news\.zed

file://E:\laragon\www\news\.zed\settings.json

file://E:\laragon\www\news\README.md

file://E:\laragon\www\news\package-lock.json

file://E:\laragon\www\news\package.json

file://E:\laragon\www\news\projet.md

file://E:\laragon\www\news\skills-lock.json

file://E:\laragon\www\news\supabase

file://E:\laragon\www\news\supabase\config.toml

file://E:\laragon\www\news\supabase\functions

file://E:\laragon\www\news\supabase\functions\aggregate_news

file://E:\laragon\www\news\supabase\functions\aggregate_news\.npmrc

file://E:\laragon\www\news\supabase\functions\aggregate_news\deno.json

file://E:\laragon\www\news\supabase\functions\aggregate_news\index.ts

file://E:\laragon\www\news\supabase\functions\clear_db

file://E:\laragon\www\news\supabase\functions\clear_db\.npmrc

file://E:\laragon\www\news\supabase\functions\clear_db\deno.json

file://E:\laragon\www\news\supabase\functions\clear_db\index.ts

file://E:\laragon\www\news\supabase\functions\telegram_bot

file://E:\laragon\www\news\supabase\functions\telegram_bot\index.ts

file://E:\laragon\www\news\supabase\migrations

file://E:\laragon\www\news\supabase\migrations\20240503194000_create_articles_and_sources.sql

file://E:\laragon\www\news\supabase\migrations\20260503200222_setup_cron.sql

file://E:\laragon\www\news\supabase\migrations\20260503200339_add_test_source.sql

file://E:\laragon\www\news\supabase\migrations\20260503201158_add_telegram_source.sql

file://E:\laragon\www\news\supabase\migrations\20260503201507_fix_telegram_source.sql

file://E:\laragon\www\news\supabase\migrations\20260503202730_clear_articles.sql

file://E:\laragon\www\news\supabase\migrations\20260503223812_add_media_groups.sql

file://E:\laragon\www\news\supabase\migrations\20260503223941_add_media_info_to_media_groups.sql

file://E:\laragon\www\news\supabase\migrations\20260511100000_add_telegram_message_id.sql

file://E:\laragon\www\news\supabase\migrations\20260511190000_add_multi_tenant_users_flows.sql

## Assistant

<thinking>
**Inspecting environment files**

I see we have a `.env.local` file, but I’m wondering if it’s been read yet. It might hold sensitive information, so I think we should avoid accessing it unless it’s absolutely necessary. I should probably check to see if it’s been inspected at all. Oh, and I need to ensure that it's included in `.gitignore`—that way we can be sure any secrets remain safe from unintended exposure.
</thinking>

**Tool Call: Read file `.gitignore`**
Status: Completed

```news/.gitignore
# Secrets and environment variables
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Supabase
supabase/.temp/

# OS
.DS_Store
Thumbs.db

# Agents and tools
.agents/
.aider-desk/
.codestudio/
.commandcode/
.continue/

# Logs
*.log

```


## Assistant

<thinking>
**Reviewing implementation details**

I’ve got multi-tenant tables that are partially implemented, but user flows aren’t complete. Currently, there aren’t any functions that support personal flows other than basic ones like /me or /subscribe. I should probably mention that we shouldn’t change anything just yet and ask what the next steps are. I also see that .env.local is present, but it’s not being read to avoid any secrets being exposed. I think using Markdown bullets could help clarify this.
</thinking>

Oui, j’ai analysé le projet pour comprendre sa structure avant qu’on continue. Je n’ai rien modifié.

## Vue d’ensemble du projet

Le projet `news` est actuellement beaucoup plus proche d’un **backend Supabase + bot Telegram d’agrégation de news** que d’une plateforme média complète avec frontend/backend/mobile.

Il contient principalement :

- une configuration Supabase locale ;
- des migrations SQL ;
- des Edge Functions Supabase ;
- un bot Telegram ;
- une fonction d’agrégation automatique de contenus Telegram ;
- un document `projet.md` qui décrit une vision beaucoup plus large de plateforme média mondiale.

## Structure actuelle

À la racine :

- `package.json`
- `package-lock.json`
- `README.md`
- `projet.md`
- `skills-lock.json`
- `supabase/`

Le `package.json` est très minimal :

- seule dépendance de développement : `supabase`.

Il n’y a pas encore de vrai dossier :

- `frontend/`
- `backend/`
- `mobile/`
- `packages/`

Donc le projet n’est pas encore le monorepo complet décrit dans `projet.md`.

## Ce que fait réellement le projet aujourd’hui

### 1. Base Supabase

Les migrations créent notamment :

- `sources`
- `articles`
- `media_groups`
- `app_users`
- `user_targets`
- `user_sources`
- `flows`
- `usage_daily`
- `payments`
- `subscriptions`

La base est donc orientée vers deux axes :

1. agrégation d’articles depuis des sources ;
2. début de système multi-utilisateur / abonnement / quotas.

### 2. Table `sources`

Elle stocke les sources d’information.

Exemples présents dans les migrations :

- une source RSS `Le Monde` ;
- une source Telegram `N'ZUI MANTO`.

Mais dans le code actuel, l’agrégation active semble surtout exploiter les sources de type `telegram`.

### 3. Table `articles`

Elle stocke les contenus agrégés ou publiés manuellement.

Champs importants :

- `title`
- `summary`
- `content`
- `original_url`
- `source_id`
- `is_certified`
- `image_url`
- `telegram_message_id`
- `telegram_chat_id`

Les colonnes `telegram_message_id` et `telegram_chat_id` servent à retrouver les messages publiés sur Telegram pour pouvoir les supprimer ensuite.

## Edge Functions présentes

### `aggregate_news`

C’est la fonction principale d’agrégation.

Elle fait globalement ceci :

1. lit les sources actives de type `telegram` dans Supabase ;
2. scrape les pages publiques `https://t.me/s/<channel>` ;
3. récupère les nouveaux messages depuis le dernier `last_message_id` ;
4. filtre les publicités avec :
   - des règles regex ;
   - éventuellement Gemini ;
5. reformule le contenu avec Gemini ;
6. ajoute le crédit `@izynews` ;
7. publie le contenu vers un canal Telegram cible ;
8. enregistre l’article en base avec le `telegram_message_id` ;
9. met à jour `last_message_id` dans la config de la source.

Elle gère aussi :

- les images ;
- les albums, mais ne publie actuellement que la première image ;
- les vidéos courtes ;
- l’upload média via Storj si configuré.

Variables d’environnement utilisées :

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`
- `GEMINI_API_KEY`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `STORJ_ACCESS_KEY`
- `STORJ_SECRET_KEY`
- `STORJ_ENDPOINT`
- `STORJ_BUCKET`

### `telegram_bot`

C’est le bot d’administration Telegram.

Il permet notamment :

- `/help`
- `/stats`
- `/list`
- `/article <id>`
- `/delete <id>`
- `/deleteall`
- `/clearall`
- `/publish <texte>`
- `/sources`
- `/addsource <@canal ou URL>`
- `/delsource <id>`
- `/run`

Il a aussi une petite partie utilisateur non-admin :

- `/start`
- `/help`
- `/me`
- `/subscribe`

Mais la partie abonnement est encore surtout déclarative : le paiement Nelsius est indiqué comme “en cours d’intégration”.

La sécurité admin repose sur :

- `TELEGRAM_ADMIN_IDS`
- ou par défaut `TELEGRAM_CHAT_ID`

### `clear_db`

Cette fonction supprime tous les articles en base.

Elle est protégée par `verify_jwt = true` dans `config.toml`.

## Plan théorique du projet

Le fichier `projet.md` décrit un projet beaucoup plus ambitieux :

- plateforme média complète ;
- frontend public ;
- backend API ;
- mobile React Native ;
- recommandations ;
- commentaires ;
- likes ;
- publicités ;
- IA ;
- marketplace ;
- modération ;
- monétisation ;
- conformité RGPD.

Mais ce document est actuellement un **plan de développement**, pas l’état réel du code.

En pratique, le projet est plutôt au stade :

> Bot Telegram + agrégateur automatique + base Supabase + début de modèle multi-utilisateur.

## Points importants repérés

### 1. `README.md` est presque vide

Il contient seulement :

- `# bot`

Il faudrait probablement créer une vraie documentation avec :

- objectif du projet ;
- architecture ;
- installation ;
- variables d’environnement ;
- déploiement ;
- commandes Supabase ;
- configuration Telegram ;
- configuration Gemini ;
- configuration Storj.

### 2. `.env.local` existe mais je ne l’ai pas lu

J’ai vu qu’un fichier `.env.local` existe, mais je ne l’ai pas ouvert pour éviter d’exposer des secrets.

Bonne nouvelle : `.env.local` est bien dans `.gitignore`.

### 3. Migration destructive

Il y a une migration :

- `20260503202730_clear_articles.sql`

qui fait :

- suppression de tous les articles.

C’est dangereux si cette migration est appliquée en production ou réappliquée dans un mauvais contexte.

À surveiller fortement.

### 4. URL Supabase hardcodée dans le cron

La migration `setup_cron.sql` appelle une URL Supabase distante hardcodée :

- `https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/aggregate_news`

Cela rend le projet moins portable entre environnements local, staging et production.

Idéalement, il faudrait gérer ça via configuration ou documenter clairement que cette migration cible un projet Supabase précis.

### 5. Dates de migrations en 2026

Plusieurs migrations ont des timestamps `2026...`.

Ce n’est pas forcément bloquant techniquement, mais c’est inhabituel et peut créer de la confusion dans l’historique.

### 6. RLS activé mais peu de policies

Les tables `articles` et `sources` ont des policies de lecture publique.

Les tables multi-tenant ont RLS activé, mais je n’ai pas vu de policies détaillées pour gérer les accès utilisateurs.

Comme les Edge Functions utilisent `SUPABASE_SERVICE_ROLE_KEY`, elles contournent RLS côté serveur. Mais si un frontend ou des clients directs Supabase arrivent plus tard, il faudra absolument ajouter des policies propres.

### 7. `aggregate_news` est public

Dans `supabase/config.toml`, la fonction `aggregate_news` a :

- `verify_jwt = false`

Donc elle peut être appelée sans JWT.

Selon le contexte, c’est pratique pour cron, mais risqué si l’URL est connue : quelqu’un pourrait déclencher l’agrégation plusieurs fois.

Il faudrait peut-être ajouter un secret d’appel ou un header de protection.

### 8. Le scraping Telegram dépend du HTML public

La fonction scrape les pages `t.me/s/...`.

C’est pratique, mais fragile :

- si Telegram change son HTML, le parsing peut casser ;
- certaines chaînes privées ne seront pas accessibles ;
- il peut y avoir des limites ou blocages.

### 9. Détection pub/spam déjà présente

Le projet a déjà une bonne base de filtrage :

- regex locales ;
- mots-clés stricts ;
- validation Gemini.

Mais il faudra probablement la rendre configurable depuis la base plus tard.

### 10. Début de modèle SaaS / multi-tenant

Les tables suivantes montrent une orientation SaaS :

- `app_users`
- `user_sources`
- `user_targets`
- `flows`
- `usage_daily`
- `payments`
- `subscriptions`

L’idée semble être :

> permettre à plusieurs utilisateurs Telegram d’avoir leurs propres sources, leurs propres canaux cibles, leurs quotas et abonnements.

Mais cette logique n’est pas encore complètement implémentée dans les fonctions.

## Résumé fonctionnel

Aujourd’hui, le projet permet surtout :

1. d’ajouter des sources Telegram ;
2. de scraper automatiquement les nouveaux posts ;
3. de reformuler les posts avec Gemini ;
4. de filtrer les pubs/spams ;
5. de publier les contenus sur un canal Telegram ;
6. de garder une trace des articles en base ;
7. de piloter tout ça depuis un bot Telegram admin.

## Ce qui manque pour atteindre le plan `projet.md`

Il manque encore :

- un frontend web ;
- une API REST classique ;
- une vraie authentification utilisateur web ;
- les catégories ;
- les pages articles ;
- les likes ;
- les commentaires ;
- les favoris ;
- l’historique de lecture ;
- l’administration web ;
- les publicités ;
- la traduction ;
- l’application mobile ;
- la marketplace ;
- les tests ;
- la CI/CD ;
- une vraie documentation.

## Ma recommandation pour la suite

Avant d’ajouter de grosses fonctionnalités, je te recommande de stabiliser la base actuelle.

Priorité proposée :

1. documenter le projet dans `README.md` ;
2. sécuriser `aggregate_news` avec un secret ou JWT ;
3. nettoyer les migrations dangereuses ou les déplacer hors du flux normal ;
4. clarifier l’architecture cible :
   - simple bot Telegram ?
   - plateforme web complète ?
   - SaaS d’automatisation Telegram ?
5. ajouter des policies RLS propres pour les tables multi-utilisateurs ;
6. améliorer la gestion des sources utilisateur ;
7. intégrer réellement les abonnements/paiements si le modèle SaaS est confirmé.

## Conclusion

Oui, j’ai maintenant une bonne compréhension du projet.

Le projet actuel est une base solide pour un **bot d’agrégation et de publication automatique de news sur Telegram**, avec IA et Supabase. Par contre, il n’est pas encore la plateforme média complète décrite dans `projet.md`.

On peut continuer dans deux directions possibles :

1. **stabiliser et finir le bot Telegram / agrégateur** ;
2. **transformer progressivement le projet vers la plateforme média complète**.

Dis-moi juste quelle direction tu veux prendre maintenant.

## User

non laisse dabord le document .md, voici ceque je veux que le projt donne actuellement. je veux que tout les utilisateurs, puissent  rejoindre le bot , ajouter les canaux sources et canaux cible , ajouter un systeme de filtre lors de recuperation dans differents de leur canaux. aussi, permettre aux utilisateur de gerer ses  activites telegram avec . et rassure toi que actuellement, sa recupere deja bien les media dans les cannax telegram et autres sans probleme . meme si c'est un album d'image, sa peux recuperer tout et republier en une seul pub aussi

## Assistant

Compris. On laisse d’abord le document `.md` de côté.

Ce que tu veux maintenant, ce n’est pas encore la grande plateforme média complète. Tu veux d’abord transformer le projet actuel en **bot Telegram multi-utilisateur** permettant à chaque utilisateur de gérer ses propres automatisations Telegram.

## Ce que je comprends de ton objectif actuel

Tu veux que :

1. **Tout utilisateur puisse rejoindre le bot**
   - il démarre le bot avec `/start` ;
   - le bot crée automatiquement son compte ;
   - il peut utiliser le bot sans être admin global.

2. **Chaque utilisateur puisse ajouter ses canaux sources**
   - exemple : canal A, canal B, canal C ;
   - le bot récupère les nouvelles publications depuis ces canaux ;
   - les sources sont propres à chaque utilisateur.

3. **Chaque utilisateur puisse ajouter ses canaux cibles**
   - exemple : son propre canal Telegram où les contenus seront republiés ;
   - le bot doit être admin dans le canal cible ;
   - chaque utilisateur peut avoir un ou plusieurs canaux de destination.

4. **Chaque utilisateur puisse créer des flux**
   - exemple :
     - source `@canal_source_1` vers cible `@mon_canal`
     - source `@canal_source_2` vers cible `@mon_autre_canal`
   - un flux relie donc :
     - un utilisateur ;
     - une source ;
     - une cible ;
     - des règles de filtrage ;
     - un état actif/inactif.

5. **Ajouter un système de filtres**
   Lors de la récupération, l’utilisateur doit pouvoir dire au bot quoi accepter ou refuser.

   Exemples possibles :
   - publier seulement si le texte contient certains mots ;
   - ignorer si le texte contient certains mots ;
   - ignorer les publicités ;
   - publier seulement les messages avec média ;
   - publier seulement les textes ;
   - publier les vidéos ;
   - publier les albums ;
   - remplacer certains mots ;
   - ajouter une signature ;
   - enlever les liens ;
   - enlever les mentions ;
   - reformuler avec IA ou publier tel quel.

6. **Permettre à l’utilisateur de gérer ses activités Telegram**
   L’utilisateur doit pouvoir voir et contrôler :
   - ses sources ;
   - ses canaux cibles ;
   - ses flux ;
   - ses publications récentes ;
   - ses statistiques ;
   - ses filtres ;
   - ses limites selon son plan ;
   - pause/reprise d’un flux ;
   - suppression d’une source ou cible ;
   - test manuel d’un flux.

7. **Bien gérer les médias**
   Tu veux conserver le comportement important suivant :

   - récupérer les images ;
   - récupérer les vidéos ;
   - récupérer les albums d’images ;
   - republier un album en une seule publication Telegram quand c’est possible ;
   - garder le média avec le texte/caption correspondant.

## Point important sur l’état actuel du code

Actuellement, le projet a déjà une bonne base :

- il sait scraper des canaux Telegram publics ;
- il sait détecter du texte ;
- il sait détecter des images ;
- il sait détecter des vidéos ;
- il a déjà une logique pour extraire plusieurs images d’un message ;
- il a déjà une fonction `sendAlbumToTelegram` dans `aggregate_news`.

Mais il y a une différence importante :

Dans le flux principal actuel de `aggregate_news`, quand un album est détecté, le code publie seulement **la première image**.

Donc techniquement, la base pour les albums existe, mais il faudra modifier le flux principal pour utiliser réellement l’envoi en album complet au lieu de prendre seulement la première image.

À noter aussi : Telegram limite un album `sendMediaGroup` à **10 médias maximum par groupe**. Donc si un album contient plus de 10 images, on devra soit :

- publier les 10 premières en un album ;
- soit découper en plusieurs albums ;
- soit garder une règle configurable.

## Changement principal à faire

Aujourd’hui, l’agrégation fonctionne plutôt comme ça :

- sources globales dans `public.sources` ;
- destination globale dans `TELEGRAM_CHAT_ID` ;
- admin global qui contrôle tout.

Mais ce que tu veux, c’est plutôt :

- sources par utilisateur dans `user_sources` ;
- cibles par utilisateur dans `user_targets` ;
- flux par utilisateur dans `flows` ;
- filtres par flux ou par utilisateur ;
- agrégation automatique pour chaque utilisateur ;
- publication vers le canal cible choisi par cet utilisateur.

La bonne nouvelle, c’est que certaines tables existent déjà :

- `app_users`
- `user_sources`
- `user_targets`
- `flows`
- `usage_daily`
- `payments`
- `subscriptions`

Mais elles ne sont pas encore pleinement utilisées dans le bot et dans `aggregate_news`.

## Architecture que je te propose

### 1. Utilisateur

Chaque personne qui lance le bot devient un `app_user`.

Le bot crée ou récupère l’utilisateur avec son :

- `telegram_user_id`
- `username`
- `plan`
- `is_active`

### 2. Source

Une source représente un canal Telegram à surveiller.

Exemple :

- `@source_news`
- `https://t.me/source_news`

Elle sera stockée dans `user_sources`.

Chaque source appartient à un utilisateur.

### 3. Cible

Une cible représente le canal où le bot doit republier.

Exemple :

- `@mon_canal`
- `-100xxxxxxxxxx`

Elle sera stockée dans `user_targets`.

Chaque cible appartient à un utilisateur.

### 4. Flux

Un flux relie une source à une cible.

Exemple :

- source : `@source_news`
- cible : `@mon_canal`
- filtre : ignorer les pubs, publier seulement les posts avec images
- statut : actif

Stocké dans `flows`.

### 5. Filtres

On peut stocker les filtres soit directement dans `flows`, via une colonne `filters JSONB`, soit créer une table dédiée `flow_filters`.

Pour commencer rapidement, je recommande `filters JSONB` dans `flows`.

Exemple de filtres possibles :

- `include_keywords`
- `exclude_keywords`
- `remove_links`
- `remove_mentions`
- `must_have_media`
- `allow_text`
- `allow_photos`
- `allow_videos`
- `allow_albums`
- `use_ai_rewrite`
- `block_ads`
- `append_signature`
- `signature_text`

## Commandes bot à ajouter ou améliorer

Je te propose cette logique de commandes.

### Compte

- `/start`
- `/me`
- `/subscribe`

### Sources

- `/addsource @canal`
- `/sources`
- `/delsource <id>`

### Cibles

- `/addtarget @moncanal`
- `/targets`
- `/deltarget <id>`

### Flux

- `/addflow <source_id> <target_id>`
- `/flows`
- `/pauseflow <id>`
- `/resumeflow <id>`
- `/deleteflow <id>`
- `/runflow <id>`

### Filtres

- `/filters <flow_id>`
- `/addinclude <flow_id> mot1,mot2,mot3`
- `/addexclude <flow_id> mot1,mot2,mot3`
- `/blockads <flow_id> on`
- `/mediaonly <flow_id> on`
- `/allowalbums <flow_id> on`
- `/rewriteai <flow_id> on`
- `/signature <flow_id> texte`

### Activité

- `/activity`
- `/stats`
- `/lastposts`
- `/usage`

## Exemple d’utilisation cible

Un utilisateur lance :

- `/start`

Puis :

- `/addsource @canal_source`
- `/addtarget @mon_canal`

Ensuite le bot répond avec les IDs.

Puis l’utilisateur fait :

- `/addflow source_id target_id`

Ensuite il configure :

- `/blockads flow_id on`
- `/allowalbums flow_id on`
- `/addexclude flow_id crypto,pari,casino`
- `/signature flow_id @mon_canal`

À partir de là, le bot surveille `@canal_source` et republie automatiquement vers `@mon_canal` selon les règles.

## Ce qu’il faut modifier dans le code

### Dans `telegram_bot`

Il faut transformer le bot actuel pour que les commandes non-admin permettent réellement de gérer :

- les sources utilisateur ;
- les cibles utilisateur ;
- les flux ;
- les filtres ;
- les abonnements plus tard.

Actuellement, beaucoup de commandes sont admin-only. Il faudra ouvrir certaines commandes aux utilisateurs, mais seulement sur leurs propres données.

### Dans `aggregate_news`

Il faut changer la logique.

Au lieu de lire seulement :

- `public.sources`

Il devra lire :

- `flows`
- `user_sources`
- `user_targets`
- `app_users`

Ensuite, pour chaque flux actif :

1. vérifier que l’utilisateur est actif ;
2. vérifier son plan ou son quota ;
3. lire la source ;
4. scraper les nouveaux messages ;
5. appliquer les filtres ;
6. gérer les médias ;
7. republier vers la cible du flux ;
8. enregistrer l’activité ;
9. mettre à jour le dernier message traité.

### Dans la base de données

Il faudra probablement ajouter quelques colonnes.

Par exemple dans `flows` :

- `filters JSONB DEFAULT '{}'`
- `last_message_id TEXT DEFAULT '0'`
- `last_run_at TIMESTAMP`
- `last_error TEXT`

Peut-être aussi créer une table :

- `flow_posts`
ou
- `published_messages`

Pour garder l’historique de ce qui a été publié par utilisateur/flux.

## Gestion des albums

Pour respecter ton besoin, l’agrégateur doit faire ceci :

- si le message contient plusieurs images :
  - utiliser `sendMediaGroup` ;
  - mettre la caption sur la première image ;
  - envoyer l’album comme une publication groupée ;
  - sauvegarder les `message_id` retournés si possible.

Important : Telegram considère un album comme plusieurs messages liés par un `media_group_id`, mais visuellement il apparaît comme une publication groupée.

Donc pour la suppression ou l’historique, il faudra garder tous les `message_id` associés à cet album.

## Ce que je recommande comme prochaine étape

Je propose qu’on fasse ça en plusieurs étapes propres.

### Étape 1 — Finaliser le modèle multi-utilisateur

Objectif :

- permettre à chaque utilisateur de créer ses sources, cibles et flux.

À faire :

- adapter les commandes `/addsource`, `/sources`, `/delsource` pour les utilisateurs ;
- ajouter `/addtarget`, `/targets`, `/deltarget` ;
- ajouter `/addflow`, `/flows`, `/pauseflow`, `/resumeflow`.

### Étape 2 — Ajouter les filtres

Objectif :

- permettre à chaque flux d’avoir ses règles.

À faire :

- ajouter `filters JSONB` dans `flows` ;
- ajouter les commandes de configuration ;
- appliquer les filtres dans l’agrégation.

### Étape 3 — Adapter l’agrégateur

Objectif :

- l’agrégateur traite les flux utilisateurs au lieu des sources globales.

À faire :

- lire `flows` actifs ;
- récupérer chaque source ;
- publier vers la cible correspondante ;
- gérer `last_message_id` par flux.

### Étape 4 — Corriger/activer la publication complète des albums

Objectif :

- republier tous les médias d’un album en un seul groupe Telegram.

À faire :

- utiliser réellement la fonction d’envoi d’album dans le flux principal ;
- gérer les albums jusqu’à 10 médias ;
- sauvegarder les IDs publiés.

### Étape 5 — Ajouter activité et historique

Objectif :

- permettre à l’utilisateur de suivre ce que le bot fait.

À faire :

- historique des publications ;
- erreurs par flux ;
- stats par jour ;
- nombre de messages analysés ;
- nombre de messages publiés ;
- nombre de messages ignorés par filtre.

## Conclusion

J’ai bien compris la direction : on ne part pas maintenant sur une plateforme média complète. On transforme d’abord ce projet en **bot Telegram automatisé multi-utilisateur**, où chaque utilisateur peut :

- ajouter ses propres sources ;
- ajouter ses propres canaux cibles ;
- créer ses flux ;
- appliquer ses filtres ;
- gérer son activité ;
- republier textes, images, vidéos et albums.

La prochaine étape logique, si tu valides, c’est que je commence par modifier la base et le bot pour rendre les **sources, cibles et flux utilisables par tous les utilisateurs**, pas seulement par l’admin.

## User (checkpoint)

ok fait dabod . ensuite, on vas utiliser un systeme par abonnement via nelsius pay pour queles utlisateur payene tpour avoir certaines fonctionalites pro

## Assistant

Je vais mettre en place la première base multi-utilisateur : migration SQL pour enrichir les `flows`, puis commandes bot côté utilisateur pour gérer sources, cibles, flux et filtres simples. Ensuite on gardera Nelsius Pay pour l’étape abonnement/pro.

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1-220)**
Status: Completed

````news/supabase/functions/telegram_bot/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const BOT_TOKEN = Deno.env.get('TELEGRAM_BOT_TOKEN') || '';
const OUTPUT_CHAT_ID = Deno.env.get('TELEGRAM_CHAT_ID') || '';
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get('TELEGRAM_ADMIN_IDS') || OUTPUT_CHAT_ID)
  .split(',').map(id => id.trim()).filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(supabase: any, telegramUserId: number, username?: string) {
  const { data: existing, error: selErr } = await supabase
    .from('app_users')
    .select('*')
    .eq('telegram_user_id', telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from('app_users')
    .insert({ telegram_user_id: telegramUserId, username: username || null, is_admin: false })
    .select('*')
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== 'premium') return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

async function reply(chatId: number | string, text: string, extra: Record<string, unknown> = {}) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ chat_id: chatId, text: text.substring(0, 4096), parse_mode: 'HTML', ...extra }),
    });
    if (!res.ok) console.error('[Bot] sendMessage failed:', await res.text());
  } catch (e) {
    console.error('[Bot] sendMessage exception:', e);
  }
}

async function sendPhotoToChat(chatId: number | string, photoFileId: string, caption: string) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ chat_id: chatId, photo: photoFileId, caption: caption.substring(0, 1024), parse_mode: 'HTML' }),
    });
    if (!res.ok) console.error('[Bot] sendPhoto failed:', await res.text());
  } catch (e) {
    console.error('[Bot] sendPhoto exception:', e);
  }
}

async function copyMessageToChat(chatId: number | string, fromChatId: number | string, messageId: number, caption?: string) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = 'HTML';
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error('[Bot] copyMessage failed:', data);
    return data;
  } catch (e) {
    console.error('[Bot] copyMessage exception:', e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(rawText: string, apiKey: string): Promise<{ title: string, content: string, isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5) return { title: '', content: rawText, isAd: false };
  try {
    const res = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        contents: [{
          parts: [{
            text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`
          }]
        }],
        generationConfig: { responseMimeType: "application/json" }
      })
    });
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return { 
        title: parsed.title || '', 
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true
      };
    }
  } catch(e) {
    console.error('[Gemini] Error:', e);
  }
  return { title: '', content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req) => {
  if (req.method !== 'POST') {
    return new Response('Telegram Bot Webhook — OK', { status: 200 });
  }

  let body: Record<string, unknown>;
  try { body = await req.json(); } catch { return new Response('Bad Request', { status: 400 }); }

  const message = (body.message || body.edited_message) as Record<string, unknown> | undefined;
  if (!message) return new Response('OK', { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username = ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || '') as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(photo || message.video || message.document || message.audio || message.animation || message.voice);

  const supabase = createClient(
    Deno.env.get('SUPABASE_URL') ?? '',
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? '',
  );

  const parts = text.split(/\s+/);
  const command = parts[0]?.toLowerCase() || '';
  const args = parts.slice(1);

  const adminMode = isAdmin(chatId) || isAdmin(userId);

  try {
    // ── Ensure app user exists for non-admins ─────────────────────────────
    let appUser: any = null;
    if (!adminMode) {
      appUser = await getOrCreateAppUser(supabase, userId, username);
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (!adminMode && (command === '/start' || command === '/help' || !command)) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? 'Premium' : 'Gratuit'}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`
      );
      return new Response('OK', { status: 200 });
    }

    if (!adminMode && command === '/me') {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from('usage_daily')
        .select('analyzed_count')
        .eq('user_id', appUser.id)
        .eq('day', today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? '∞' : '5';
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? 'Premium' : 'Gratuit'}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ''}` +
          `Pour t'abonner: <code>/subscribe</code>`
      );
      return new Response('OK', { status: 200 });
    }

    if (!adminMode && command === '/subscribe') {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`
      );
      return new Response('OK', { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, '⛔ Accès refusé.');
      return new Response('OK', { status: 200 });

````


**Tool Call: supabase\\migrations\\20260511200000\_add\_user\_flow\_filters\_activity.sql**
Status: Completed

Diff: supabase\migrations\20260511200000_add_user_flow_filters_activity.sql
```
-- Add user flow configuration, filters, and activity tracking

ALTER TABLE public.flows
  ADD COLUMN IF NOT EXISTS filters JSONB NOT NULL DEFAULT '{
    "include_keywords": [],
    "exclude_keywords": [],
    "block_ads": true,
    "media_only": false,
    "allow_text": true,
    "allow_photos": true,
    "allow_videos": true,
    "allow_albums": true,
    "use_ai_rewrite": false,
    "remove_links": false,
    "remove_mentions": false,
    "signature_text": ""
  }'::jsonb,
  ADD COLUMN IF NOT EXISTS last_message_id BIGINT NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS last_run_at TIMESTAMP WITH TIME ZONE,
  ADD COLUMN IF NOT EXISTS last_error TEXT;

CREATE INDEX IF NOT EXISTS idx_flows_active_last_run
  ON public.flows(is_active, last_run_at);

CREATE TABLE IF NOT EXISTS public.flow_activity (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.app_users(id) ON DELETE CASCADE,
  flow_id UUID REFERENCES public.flows(id) ON DELETE SET NULL,
  source_id UUID REFERENCES public.user_sources(id) ON DELETE SET NULL,
  target_id UUID REFERENCES public.user_targets(id) ON DELETE SET NULL,
  source_message_id BIGINT,
  target_message_ids BIGINT[] NOT NULL DEFAULT '{}',
  status TEXT NOT NULL DEFAULT 'published',
  reason TEXT,
  original_url TEXT,
  text_preview TEXT,
  media_count INTEGER NOT NULL DEFAULT 0,
  raw_payload JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_flow_activity_user_created_at
  ON public.flow_activity(user_id, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_flow_activity_flow_created_at
  ON public.flow_activity(flow_id, created_at DESC);

ALTER TABLE public.flow_activity ENABLE ROW LEVEL SECURITY;

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\n<b>📡 Sources</b>\n/addsource &lt;@canal ou URL t.me&gt; — Ajouter un canal source\n/sources — Mes sources\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>🎯 Canaux cibles</b>\n/addtarget &lt;@canal ou -100...&gt; [nom] — Ajouter un canal cible\n/targets — Mes canaux cibles\n/deltarget &lt;id&gt; — Désactiver un canal cible\n\n<b>🔁 Flux</b>\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Relier source → cible\n/flows — Mes flux\n/pauseflow &lt;id&gt; — Mettre en pause\n/resumeflow &lt;id&gt; — Reprendre\n/deleteflow &lt;id&gt; — Supprimer/désactiver\n\n<b>🧰 Filtres</b>\n/filters &lt;flow_id&gt; — Voir les filtres\n/include &lt;flow_id&gt; mot1,mot2 — Mots obligatoires\n/exclude &lt;flow_id&gt; mot1,mot2 — Mots interdits\n/blockads &lt;flow_id&gt; on|off — Bloquer les pubs\n/mediaonly &lt;flow_id&gt; on|off — Publier seulement les médias\n/allowalbums &lt;flow_id&gt; on|off — Autoriser les albums\n/rewriteai &lt;flow_id&gt; on|off — Reformulation IA\n/signature &lt;flow_id&gt; texte — Signature ajoutée\n\n<b>📊 Activité</b>\n/activity — Dernières actions\n/me — Mon compte\n/subscribe — Abonnement Premium`;

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const parts = text.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  const adminMode = isAdmin(chatId) || isAdmin(userId);

  try {
    // ── Ensure app user exists for non-admins ─────────────────────────────
    let appUser: any = null;
    if (!adminMode) {
      appUser = await getOrCreateAppUser(supabase, userId, username);
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
      );
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, "⛔ Accès refusé.");
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a, i) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`);
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

<thinking>
**Assessing formatting changes**

It seems like the edit_file made significant formatting changes, possibly because of some fuzzy input? It added helpers but also reformatted the entire file in the process. That's a bit concerning! I realize I need to implement non-admin commands before gate access; it might be critical to read around the user command section to ensure everything flows correctly. I'll focus on this next step so I can get it right!
</thinking>

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 280-380)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const parts = text.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  const adminMode = isAdmin(chatId) || isAdmin(userId);

  try {
    // ── Ensure app user exists for non-admins ─────────────────────────────
    let appUser: any = null;
    if (!adminMode) {
      appUser = await getOrCreateAppUser(supabase, userId, username);
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
      );
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, "⛔ Accès refusé.");
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\n<b>📡 Sources</b>\n/addsource &lt;@canal ou URL t.me&gt; — Ajouter un canal source\n/sources — Mes sources\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>🎯 Canaux cibles</b>\n/addtarget &lt;@canal ou -100...&gt; [nom] — Ajouter un canal cible\n/targets — Mes canaux cibles\n/deltarget &lt;id&gt; — Désactiver un canal cible\n\n<b>🔁 Flux</b>\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Relier source → cible\n/flows — Mes flux\n/pauseflow &lt;id&gt; — Mettre en pause\n/resumeflow &lt;id&gt; — Reprendre\n/deleteflow &lt;id&gt; — Supprimer/désactiver\n\n<b>🧰 Filtres</b>\n/filters &lt;flow_id&gt; — Voir les filtres\n/include &lt;flow_id&gt; mot1,mot2 — Mots obligatoires\n/exclude &lt;flow_id&gt; mot1,mot2 — Mots interdits\n/blockads &lt;flow_id&gt; on|off — Bloquer les pubs\n/mediaonly &lt;flow_id&gt; on|off — Publier seulement les médias\n/allowalbums &lt;flow_id&gt; on|off — Autoriser les albums\n/rewriteai &lt;flow_id&gt; on|off — Reformulation IA\n/signature &lt;flow_id&gt; texte — Signature ajoutée\n\n<b>📊 Activité</b>\n/activity — Dernières actions\n/me — Mon compte\n/subscribe — Abonnement Premium`;

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const parts = text.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  const adminMode = isAdmin(chatId) || isAdmin(userId);

  try {
    // ── Ensure app user exists for non-admins ─────────────────────────────
    let appUser: any = null;
    if (!adminMode) {
      appUser = await getOrCreateAppUser(supabase, userId, username);
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a, i) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`);
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Je continue avec l’agrégateur maintenant : il faut qu’il traite les `flows` des utilisateurs, applique les filtres et publie vers les canaux cibles de chaque utilisateur.

**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 478-688)**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts
async function sendTextToTelegram(text: string): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    const res = await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ chat_id: OUTPUT_CHAT_ID, text: text.substring(0, 4096), parse_mode: 'HTML' }),
    });
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error('[Telegram] sendMessage failed:', data.description);
  } catch (e) {
    console.error('[Telegram] sendText error:', e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(text: string, photoUrl: string): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(`[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`);
    const mediaData = await downloadMedia(photoUrl);
    
    if (!mediaData) {
      console.warn('[Telegram] Failed to download photo, falling back to text');
      return sendTextToTelegram(text);
    }
    
    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(mediaData, 'photo.jpg', 'image/jpeg');
    
    if (!storjUrl) {
      console.warn('[Telegram] Failed to upload to Storj, using direct upload');
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData);
    }
    
    // Send using Storj URL (permanent)
    const res = await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        chat_id: OUTPUT_CHAT_ID,
        photo: storjUrl,
        caption: text.substring(0, 1024),
        parse_mode: 'HTML'
      })
    });
    
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(`[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`);
      return data.result.message_id;
    }
    console.warn('[Telegram] sendPhoto with Storj URL failed:', data.description);
    return sendTextToTelegram(text);
  } catch (e) {
    console.error('[Telegram] sendPhoto error:', e);
    return sendTextToTelegram(text);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(text: string, mediaData: Uint8Array): Promise<number | null> {
  const boundary = '----FormBoundary' + Math.random().toString(36).substring(2);
  const body = buildMultipartForm([
    { name: 'chat_id', value: OUTPUT_CHAT_ID },
    { name: 'caption', value: text.substring(0, 1024) },
    { name: 'parse_mode', value: 'HTML' },
    { name: 'photo', value: mediaData, filename: 'image.jpg', contentType: 'image/jpeg' },
  ], boundary);
  
  const res = await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`, {
    method: 'POST',
    headers: { 'Content-Type': `multipart/form-data; boundary=${boundary}` },
    body: body as any,
  });
  
  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(`[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`);
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(text: string, photoUrls: string[]): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(`[Telegram] Processing album with ${photoUrls.length} photos...`);
    
    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise(r => setTimeout(r, 100)); // Rate limit protection
    }
    
    const validPhotos = mediaItems.filter(item => item.data !== null);
    console.log(`[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`);
    
    if (validPhotos.length === 0) {
      return sendTextToTelegram(text);
    }
    
    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url);
    }
    
    // Build media group - caption only on first item
    const boundary = '----FormBoundary' + Math.random().toString(36).substring(2);
    const fields: { name: string; value: string | Uint8Array; filename?: string; contentType?: string }[] = [
      { name: 'chat_id', value: OUTPUT_CHAT_ID },
    ];
    
    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: 'photo',
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? 'HTML' : undefined,
    }));
    fields.push({ name: 'media', value: JSON.stringify(mediaArray) });
    
    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: 'image/jpeg'
      });
    });
    
    const body = buildMultipartForm(fields, boundary);
    
    const res = await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`, {
      method: 'POST',
      headers: { 'Content-Type': `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    });
    
    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(`[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`);
      return data.result[0].message_id;
    }
    console.warn('[Telegram] sendMediaGroup failed:', data.description);
    
    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url);
  } catch (e) {
    console.error('[Telegram] sendAlbum error:', e);
    return sendTextToTelegram(text);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(text: string, videoUrl: string): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(`[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`);
    const mediaData = await downloadMedia(videoUrl);
    
    if (!mediaData) {
      console.warn('[Telegram] Failed to download video, falling back to text');
      return sendTextToTelegram(text);
    }
    
    console.log(`[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`);
    
    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(mediaData, 'video.mp4', 'video/mp4');
    
    if (!storjUrl) {
      console.warn('[Telegram] Failed to upload to Storj, using direct upload');
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData);
    }
    
    // Send using Storj URL (permanent)
    const res = await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        chat_id: OUTPUT_CHAT_ID,
        video: storjUrl,
        caption: text.substring(0, 1024),
        parse_mode: 'HTML',
        supports_streaming: true
      })
    });
    
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(`[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`);
      return data.result.message_id;
    }
    console.warn('[Telegram] sendVideo with Storj URL failed:', data.description);
    return sendTextToTelegram(text);
  } catch (e) {
    console.error('[Telegram] sendVideo error:', e);
    return sendTextToTelegram(text);
  }
}

```


**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 689-872)**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(text: string, mediaData: Uint8Array): Promise<number | null> {
  const boundary = '----FormBoundary' + Math.random().toString(36).substring(2);
  const body = buildMultipartForm([
    { name: 'chat_id', value: OUTPUT_CHAT_ID },
    { name: 'caption', value: text.substring(0, 1024) },
    { name: 'parse_mode', value: 'HTML' },
    { name: 'supports_streaming', value: 'true' },
    { name: 'video', value: mediaData, filename: 'video.mp4', contentType: 'video/mp4' },
  ], boundary);
  
  const res = await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`, {
    method: 'POST',
    headers: { 'Content-Type': `multipart/form-data; boundary=${boundary}` },
    body: body as any,
  });
  
  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(`[Telegram] Video sent directly, msg_id: ${data.result.message_id}`);
    return data.result.message_id;
  }
  return null;
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (req) => {
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL') ?? '',
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? '',
  );

  try {
    const { data: sources } = await supabase
      .from('sources')
      .select('*')
      .eq('type', 'telegram')
      .eq('is_active', true);

    if (!sources || sources.length === 0) {
      return new Response(JSON.stringify({ success: true, new: 0, message: 'No active sources' }));
    }

    let processedCount = 0;
    let newArticlesCount = 0;

    for (const source of sources) {
      const channel = source.config?.channel;
      if (!channel) continue;

      // Get last processed message ID for this source (tracked in config)
      const lastMsgId = parseInt(source.config?.last_message_id || '0');

      console.log(`[Aggregate] Scraping @${channel}, after message ID: ${lastMsgId}`);
      const messages = await scrapeChannel(channel, lastMsgId);
      console.log(`[Aggregate] Found ${messages.length} new message(s) in @${channel}`);

      // FIRST RUN: limit to last 5 messages only to avoid spam
      let messagesToProcess = messages;
      if (lastMsgId === 0 && messages.length > 5) {
        messagesToProcess = messages.slice(-5); // Take only last 5
        console.log(`[Aggregate] First run - limiting to last 5 messages only`);
      }

      let maxMsgId = lastMsgId;

      for (const msg of messagesToProcess) {
        processedCount++;
        maxMsgId = Math.max(maxMsgId, msg.id);

        if (msg.text.length < 20 && !msg.hasMedia) continue;

        // ── Deduplication: use original_url (t.me/channel/msgId) as unique key
        const originalUrl = `https://t.me/${channel}/${msg.id}`;
        const { data: existing } = await supabase
          .from('articles')
          .select('id')
          .eq('original_url', originalUrl)
          .maybeSingle();

        if (existing) {
          console.log(`[Aggregate] Already processed: ${originalUrl}`);
          continue;
        }

        // ── Quick ad pre-filter (STRICT MODE)
        if (isQuickAd(msg.text)) {
          console.log(`[Aggregate] 🚫 AD BLOCKED: msg ${msg.id} from @${channel}`);
          // Mark as processed to avoid re-processing
          maxMsgId = Math.max(maxMsgId, msg.id);
          continue;
        }

        // ── AI restructuring
        let finalContent = msg.text;
        if (msg.text.length >= 10) {
          const aiRes = await restructureWithAI(msg.text);
          if (aiRes.isAd) {
            console.log(`[Aggregate] Gemini detected ad in msg ${msg.id}, skipping`);
            continue;
          }
          finalContent = aiRes.content.trim();
        }

        const textWithCredit = `${finalContent}\n\n📢 @izynews`;

        // ── Publish to Telegram channel FIRST (to get message_id)
        // Check for short video (< 60 seconds)
        const isShortVideo = msg.videoUrl && msg.videoDuration && msg.videoDuration <= 60;
        
        let telegramMsgId: number | null = null;
        
        if (isShortVideo && msg.videoUrl) {
          // Send video (short videos < 60s)
          telegramMsgId = await sendVideoToTelegram(textWithCredit, msg.videoUrl);
        } else if (msg.photoUrls && msg.photoUrls.length > 0) {
          // Send only the FIRST photo (not full album) - keeps it as single publication
          console.log(`[Aggregate] Album detected with ${msg.photoUrls.length} photos, using first only`);
          telegramMsgId = await sendPhotoToTelegram(textWithCredit, msg.photoUrls[0]);
        } else {
          // Text only
          telegramMsgId = await sendTextToTelegram(textWithCredit);
        }
        if (!telegramMsgId) {
          console.warn(`[Aggregate] Failed to publish msg ${msg.id} to Telegram, skipping DB insert`);
          continue;
        }

        // ── Save to DB with telegram_message_id
        const { error: insertError } = await supabase
          .from('articles')
          .insert({
            title: finalContent.replace(/<[^>]+>/g, '').substring(0, 100),
            summary: msg.text.substring(0, 200),
            content: textWithCredit,
            original_url: originalUrl,
            source_id: source.id,
            is_certified: false,
            telegram_message_id: telegramMsgId,
            telegram_chat_id: OUTPUT_CHAT_ID,
          });

        if (insertError) {
          console.error('[Aggregate] DB insert error:', insertError);
          // Try to delete the telegram message if DB insert failed
          try {
            await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/deleteMessage`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ chat_id: OUTPUT_CHAT_ID, message_id: telegramMsgId }),
            });
          } catch (e) {
            console.error('[Aggregate] Failed to cleanup telegram message:', e);
          }
          continue;
        }

        newArticlesCount++;
        console.log(`[Aggregate] Published msg ${msg.id} from @${channel} (TG msg_id: ${telegramMsgId})`);

        // Small delay to avoid rate limits
        await new Promise(r => setTimeout(r, 1500));
      }

      // ── Update last_message_id in source config so next run starts from here
      if (maxMsgId > lastMsgId) {
        await supabase
          .from('sources')
          .update({ config: { ...source.config, last_message_id: String(maxMsgId) } })
          .eq('id', source.id);
        console.log(`[Aggregate] Updated last_message_id for @${channel} to ${maxMsgId}`);
      }
    }

    return new Response(
      JSON.stringify({ success: true, processed: processedCount, new: newArticlesCount }),
      { headers: { 'Content-Type': 'application/json' } }
    );
  } catch (err) {
    console.error('[Aggregate] Fatal error:', err);
    return new Response(JSON.stringify({ success: false, error: (err as Error).message }), { status: 500 });
  }
});

```


## Assistant

<thinking>
**Editing code functions**

I'm working on editing the code. I need to be careful not to include triple code blocks in the final version, especially since they may not be necessary. I’ll implement support for aggregate functions and consider adding an optional chatId parameter to the send functions. I also want to assess the current main handler for simplicity. It's essential to retain legacy functionality while integrating user flows, making sure everything flows smoothly. I’ll focus on creating target-specific media functions next.

**Refining function signatures**

I'm focusing on editing the function signatures, like updating `chat_id`. For sending photos, I’ll modify `sendPhotoToTelegram(text, photoUrl, chatId=OUTPUT)` and `uploadPhotoDirect(text, mediaData, chatId)`. I'll also include `sendVideoToTelegram` and `uploadVideoDirect`. There's a thought about how `sendAlbum` returns just a number, but we might want to keep the first ID or an array for tracking activity. If Telegram provides multiple IDs, I could adjust to return the first. I see I need to sort through the read outputs and make the necessary edits.
</thinking>

**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Failed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: OUTPUT_CHAT_ID,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: OUTPUT_CHAT_ID },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: OUTPUT_CHAT_ID }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: OUTPUT_CHAT_ID,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: OUTPUT_CHAT_ID },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (req) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: sources } = await supabase
      .from("sources")
      .select("*")
      .eq("type", "telegram")
      .eq("is_active", true);

    if (!sources || sources.length === 0) {
      return new Response(
        JSON.stringify({ success: true, new: 0, message: "No active sources" }),
      );
    }

    let processedCount = 0;
    let newArticlesCount = 0;

    for (const source of sources) {
      const channel = source.config?.channel;
      if (!channel) continue;

      // Get last processed message ID for this source (tracked in config)
      const lastMsgId = parseInt(source.config?.last_message_id || "0");

      console.log(
        `[Aggregate] Scraping @${channel}, after message ID: ${lastMsgId}`,
      );
      const messages = await scrapeChannel(channel, lastMsgId);
      console.log(
        `[Aggregate] Found ${messages.length} new message(s) in @${channel}`,
      );

      // FIRST RUN: limit to last 5 messages only to avoid spam
      let messagesToProcess = messages;
      if (lastMsgId === 0 && messages.length > 5) {
        messagesToProcess = messages.slice(-5); // Take only last 5
        console.log(`[Aggregate] First run - limiting to last 5 messages only`);
      }

      let maxMsgId = lastMsgId;

      for (const msg of messagesToProcess) {
        processedCount++;
        maxMsgId = Math.max(maxMsgId, msg.id);

        if (msg.text.length < 20 && !msg.hasMedia) continue;

        // ── Deduplication: use original_url (t.me/channel/msgId) as unique key
        const originalUrl = `https://t.me/${channel}/${msg.id}`;
        const { data: existing } = await supabase
          .from("articles")
          .select("id")
          .eq("original_url", originalUrl)
          .maybeSingle();

        if (existing) {
          console.log(`[Aggregate] Already processed: ${originalUrl}`);
          continue;
        }

        // ── Quick ad pre-filter (STRICT MODE)
        if (isQuickAd(msg.text)) {
          console.log(
            `[Aggregate] 🚫 AD BLOCKED: msg ${msg.id} from @${channel}`,
          );
          // Mark as processed to avoid re-processing
          maxMsgId = Math.max(maxMsgId, msg.id);
          continue;
        }

        // ── AI restructuring
        let finalContent = msg.text;
        if (msg.text.length >= 10) {
          const aiRes = await restructureWithAI(msg.text);
          if (aiRes.isAd) {
            console.log(
              `[Aggregate] Gemini detected ad in msg ${msg.id}, skipping`,
            );
            continue;
          }
          finalContent = aiRes.content.trim();
        }

        const textWithCredit = `${finalContent}\n\n📢 @izynews`;

        // ── Publish to Telegram channel FIRST (to get message_id)
        // Check for short video (< 60 seconds)
        const isShortVideo =
          msg.videoUrl && msg.videoDuration && msg.videoDuration <= 60;

        let telegramMsgId: number | null = null;

        if (isShortVideo && msg.videoUrl) {
          // Send video (short videos < 60s)
          telegramMsgId = await sendVideoToTelegram(
            textWithCredit,
            msg.videoUrl,
          );
        } else if (msg.photoUrls && msg.photoUrls.length > 0) {
          // Send only the FIRST photo (not full album) - keeps it as single publication
          console.log(
            `[Aggregate] Album detected with ${msg.photoUrls.length} photos, using first only`,
          );
          telegramMsgId = await sendPhotoToTelegram(
            textWithCredit,
            msg.photoUrls[0],
          );
        } else {
          // Text only
          telegramMsgId = await sendTextToTelegram(textWithCredit);
        }
        if (!telegramMsgId) {
          console.warn(
            `[Aggregate] Failed to publish msg ${msg.id} to Telegram, skipping DB insert`,
          );
          continue;
        }

        // ── Save to DB with telegram_message_id
        const { error: insertError } = await supabase.from("articles").insert({
          title: finalContent.replace(/<[^>]+>/g, "").substring(0, 100),
          summary: msg.text.substring(0, 200),
          content: textWithCredit,
          original_url: originalUrl,
          source_id: source.id,
          is_certified: false,
          telegram_message_id: telegramMsgId,
          telegram_chat_id: OUTPUT_CHAT_ID,
        });

        if (insertError) {
          console.error("[Aggregate] DB insert error:", insertError);
          // Try to delete the telegram message if DB insert failed
          try {
            await fetch(
              `https://api.telegram.org/bot${BOT_TOKEN}/deleteMessage`,
              {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  message_id: telegramMsgId,
                }),
              },
            );
          } catch (e) {
            console.error("[Aggregate] Failed to cleanup telegram message:", e);
          }
          continue;
        }

        newArticlesCount++;
        console.log(
          `[Aggregate] Published msg ${msg.id} from @${channel} (TG msg_id: ${telegramMsgId})`,
        );

        // Small delay to avoid rate limits
        await new Promise((r) => setTimeout(r, 1500));
      }

      // ── Update last_message_id in source config so next run starts from here
      if (maxMsgId > lastMsgId) {
        await supabase
          .from("sources")
          .update({
            config: { ...source.config, last_message_id: String(maxMsgId) },
          })
          .eq("id", source.id);
        console.log(
          `[Aggregate] Updated last_message_id for @${channel} to ${maxMsgId}`,
        );
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: newArticlesCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 615-910)**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts
// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: OUTPUT_CHAT_ID,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: OUTPUT_CHAT_ID },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: OUTPUT_CHAT_ID }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: OUTPUT_CHAT_ID,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: OUTPUT_CHAT_ID },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();

```


**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 583-614)**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}


```


**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Failed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: OUTPUT_CHAT_ID },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: OUTPUT_CHAT_ID }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: OUTPUT_CHAT_ID,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: OUTPUT_CHAT_ID },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (req) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: sources } = await supabase
      .from("sources")
      .select("*")
      .eq("type", "telegram")
      .eq("is_active", true);

    if (!sources || sources.length === 0) {
      return new Response(
        JSON.stringify({ success: true, new: 0, message: "No active sources" }),
      );
    }

    let processedCount = 0;
    let newArticlesCount = 0;

    for (const source of sources) {
      const channel = source.config?.channel;
      if (!channel) continue;

      // Get last processed message ID for this source (tracked in config)
      const lastMsgId = parseInt(source.config?.last_message_id || "0");

      console.log(
        `[Aggregate] Scraping @${channel}, after message ID: ${lastMsgId}`,
      );
      const messages = await scrapeChannel(channel, lastMsgId);
      console.log(
        `[Aggregate] Found ${messages.length} new message(s) in @${channel}`,
      );

      // FIRST RUN: limit to last 5 messages only to avoid spam
      let messagesToProcess = messages;
      if (lastMsgId === 0 && messages.length > 5) {
        messagesToProcess = messages.slice(-5); // Take only last 5
        console.log(`[Aggregate] First run - limiting to last 5 messages only`);
      }

      let maxMsgId = lastMsgId;

      for (const msg of messagesToProcess) {
        processedCount++;
        maxMsgId = Math.max(maxMsgId, msg.id);

        if (msg.text.length < 20 && !msg.hasMedia) continue;

        // ── Deduplication: use original_url (t.me/channel/msgId) as unique key
        const originalUrl = `https://t.me/${channel}/${msg.id}`;
        const { data: existing } = await supabase
          .from("articles")
          .select("id")
          .eq("original_url", originalUrl)
          .maybeSingle();

        if (existing) {
          console.log(`[Aggregate] Already processed: ${originalUrl}`);
          continue;
        }

        // ── Quick ad pre-filter (STRICT MODE)
        if (isQuickAd(msg.text)) {
          console.log(
            `[Aggregate] 🚫 AD BLOCKED: msg ${msg.id} from @${channel}`,
          );
          // Mark as processed to avoid re-processing
          maxMsgId = Math.max(maxMsgId, msg.id);
          continue;
        }

        // ── AI restructuring
        let finalContent = msg.text;
        if (msg.text.length >= 10) {
          const aiRes = await restructureWithAI(msg.text);
          if (aiRes.isAd) {
            console.log(
              `[Aggregate] Gemini detected ad in msg ${msg.id}, skipping`,
            );
            continue;
          }
          finalContent = aiRes.content.trim();
        }

        const textWithCredit = `${finalContent}\n\n📢 @izynews`;

        // ── Publish to Telegram channel FIRST (to get message_id)
        // Check for short video (< 60 seconds)
        const isShortVideo =
          msg.videoUrl && msg.videoDuration && msg.videoDuration <= 60;

        let telegramMsgId: number | null = null;

        if (isShortVideo && msg.videoUrl) {
          // Send video (short videos < 60s)
          telegramMsgId = await sendVideoToTelegram(
            textWithCredit,
            msg.videoUrl,
          );
        } else if (msg.photoUrls && msg.photoUrls.length > 0) {
          // Send only the FIRST photo (not full album) - keeps it as single publication
          console.log(
            `[Aggregate] Album detected with ${msg.photoUrls.length} photos, using first only`,
          );
          telegramMsgId = await sendPhotoToTelegram(
            textWithCredit,
            msg.photoUrls[0],
          );
        } else {
          // Text only
          telegramMsgId = await sendTextToTelegram(textWithCredit);
        }
        if (!telegramMsgId) {
          console.warn(
            `[Aggregate] Failed to publish msg ${msg.id} to Telegram, skipping DB insert`,
          );
          continue;
        }

        // ── Save to DB with telegram_message_id
        const { error: insertError } = await supabase.from("articles").insert({
          title: finalContent.replace(/<[^>]+>/g, "").substring(0, 100),
          summary: msg.text.substring(0, 200),
          content: textWithCredit,
          original_url: originalUrl,
          source_id: source.id,
          is_certified: false,
          telegram_message_id: telegramMsgId,
          telegram_chat_id: OUTPUT_CHAT_ID,
        });

        if (insertError) {
          console.error("[Aggregate] DB insert error:", insertError);
          // Try to delete the telegram message if DB insert failed
          try {
            await fetch(
              `https://api.telegram.org/bot${BOT_TOKEN}/deleteMessage`,
              {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  message_id: telegramMsgId,
                }),
              },
            );
          } catch (e) {
            console.error("[Aggregate] Failed to cleanup telegram message:", e);
          }
          continue;
        }

        newArticlesCount++;
        console.log(
          `[Aggregate] Published msg ${msg.id} from @${channel} (TG msg_id: ${telegramMsgId})`,
        );

        // Small delay to avoid rate limits
        await new Promise((r) => setTimeout(r, 1500));
      }

      // ── Update last_message_id in source config so next run starts from here
      if (maxMsgId > lastMsgId) {
        await supabase
          .from("sources")
          .update({
            config: { ...source.config, last_message_id: String(maxMsgId) },
          })
          .eq("id", source.id);
        console.log(
          `[Aggregate] Updated last_message_id for @${channel} to ${maxMsgId}`,
        );
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: newArticlesCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Failed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: OUTPUT_CHAT_ID }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
): Promise<number | null> {
  if (!OUTPUT_CHAT_ID || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: OUTPUT_CHAT_ID,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: OUTPUT_CHAT_ID },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (req) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: sources } = await supabase
      .from("sources")
      .select("*")
      .eq("type", "telegram")
      .eq("is_active", true);

    if (!sources || sources.length === 0) {
      return new Response(
        JSON.stringify({ success: true, new: 0, message: "No active sources" }),
      );
    }

    let processedCount = 0;
    let newArticlesCount = 0;

    for (const source of sources) {
      const channel = source.config?.channel;
      if (!channel) continue;

      // Get last processed message ID for this source (tracked in config)
      const lastMsgId = parseInt(source.config?.last_message_id || "0");

      console.log(
        `[Aggregate] Scraping @${channel}, after message ID: ${lastMsgId}`,
      );
      const messages = await scrapeChannel(channel, lastMsgId);
      console.log(
        `[Aggregate] Found ${messages.length} new message(s) in @${channel}`,
      );

      // FIRST RUN: limit to last 5 messages only to avoid spam
      let messagesToProcess = messages;
      if (lastMsgId === 0 && messages.length > 5) {
        messagesToProcess = messages.slice(-5); // Take only last 5
        console.log(`[Aggregate] First run - limiting to last 5 messages only`);
      }

      let maxMsgId = lastMsgId;

      for (const msg of messagesToProcess) {
        processedCount++;
        maxMsgId = Math.max(maxMsgId, msg.id);

        if (msg.text.length < 20 && !msg.hasMedia) continue;

        // ── Deduplication: use original_url (t.me/channel/msgId) as unique key
        const originalUrl = `https://t.me/${channel}/${msg.id}`;
        const { data: existing } = await supabase
          .from("articles")
          .select("id")
          .eq("original_url", originalUrl)
          .maybeSingle();

        if (existing) {
          console.log(`[Aggregate] Already processed: ${originalUrl}`);
          continue;
        }

        // ── Quick ad pre-filter (STRICT MODE)
        if (isQuickAd(msg.text)) {
          console.log(
            `[Aggregate] 🚫 AD BLOCKED: msg ${msg.id} from @${channel}`,
          );
          // Mark as processed to avoid re-processing
          maxMsgId = Math.max(maxMsgId, msg.id);
          continue;
        }

        // ── AI restructuring
        let finalContent = msg.text;
        if (msg.text.length >= 10) {
          const aiRes = await restructureWithAI(msg.text);
          if (aiRes.isAd) {
            console.log(
              `[Aggregate] Gemini detected ad in msg ${msg.id}, skipping`,
            );
            continue;
          }
          finalContent = aiRes.content.trim();
        }

        const textWithCredit = `${finalContent}\n\n📢 @izynews`;

        // ── Publish to Telegram channel FIRST (to get message_id)
        // Check for short video (< 60 seconds)
        const isShortVideo =
          msg.videoUrl && msg.videoDuration && msg.videoDuration <= 60;

        let telegramMsgId: number | null = null;

        if (isShortVideo && msg.videoUrl) {
          // Send video (short videos < 60s)
          telegramMsgId = await sendVideoToTelegram(
            textWithCredit,
            msg.videoUrl,
          );
        } else if (msg.photoUrls && msg.photoUrls.length > 0) {
          // Send only the FIRST photo (not full album) - keeps it as single publication
          console.log(
            `[Aggregate] Album detected with ${msg.photoUrls.length} photos, using first only`,
          );
          telegramMsgId = await sendPhotoToTelegram(
            textWithCredit,
            msg.photoUrls[0],
          );
        } else {
          // Text only
          telegramMsgId = await sendTextToTelegram(textWithCredit);
        }
        if (!telegramMsgId) {
          console.warn(
            `[Aggregate] Failed to publish msg ${msg.id} to Telegram, skipping DB insert`,
          );
          continue;
        }

        // ── Save to DB with telegram_message_id
        const { error: insertError } = await supabase.from("articles").insert({
          title: finalContent.replace(/<[^>]+>/g, "").substring(0, 100),
          summary: msg.text.substring(0, 200),
          content: textWithCredit,
          original_url: originalUrl,
          source_id: source.id,
          is_certified: false,
          telegram_message_id: telegramMsgId,
          telegram_chat_id: OUTPUT_CHAT_ID,
        });

        if (insertError) {
          console.error("[Aggregate] DB insert error:", insertError);
          // Try to delete the telegram message if DB insert failed
          try {
            await fetch(
              `https://api.telegram.org/bot${BOT_TOKEN}/deleteMessage`,
              {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  message_id: telegramMsgId,
                }),
              },
            );
          } catch (e) {
            console.error("[Aggregate] Failed to cleanup telegram message:", e);
          }
          continue;
        }

        newArticlesCount++;
        console.log(
          `[Aggregate] Published msg ${msg.id} from @${channel} (TG msg_id: ${telegramMsgId})`,
        );

        // Small delay to avoid rate limits
        await new Promise((r) => setTimeout(r, 1500));
      }

      // ── Update last_message_id in source config so next run starts from here
      if (maxMsgId > lastMsgId) {
        await supabase
          .from("sources")
          .update({
            config: { ...source.config, last_message_id: String(maxMsgId) },
          })
          .eq("id", source.id);
        console.log(
          `[Aggregate] Updated last_message_id for @${channel} to ${maxMsgId}`,
        );
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: newArticlesCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (req) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: sources } = await supabase
      .from("sources")
      .select("*")
      .eq("type", "telegram")
      .eq("is_active", true);

    if (!sources || sources.length === 0) {
      return new Response(
        JSON.stringify({ success: true, new: 0, message: "No active sources" }),
      );
    }

    let processedCount = 0;
    let newArticlesCount = 0;

    for (const source of sources) {
      const channel = source.config?.channel;
      if (!channel) continue;

      // Get last processed message ID for this source (tracked in config)
      const lastMsgId = parseInt(source.config?.last_message_id || "0");

      console.log(
        `[Aggregate] Scraping @${channel}, after message ID: ${lastMsgId}`,
      );
      const messages = await scrapeChannel(channel, lastMsgId);
      console.log(
        `[Aggregate] Found ${messages.length} new message(s) in @${channel}`,
      );

      // FIRST RUN: limit to last 5 messages only to avoid spam
      let messagesToProcess = messages;
      if (lastMsgId === 0 && messages.length > 5) {
        messagesToProcess = messages.slice(-5); // Take only last 5
        console.log(`[Aggregate] First run - limiting to last 5 messages only`);
      }

      let maxMsgId = lastMsgId;

      for (const msg of messagesToProcess) {
        processedCount++;
        maxMsgId = Math.max(maxMsgId, msg.id);

        if (msg.text.length < 20 && !msg.hasMedia) continue;

        // ── Deduplication: use original_url (t.me/channel/msgId) as unique key
        const originalUrl = `https://t.me/${channel}/${msg.id}`;
        const { data: existing } = await supabase
          .from("articles")
          .select("id")
          .eq("original_url", originalUrl)
          .maybeSingle();

        if (existing) {
          console.log(`[Aggregate] Already processed: ${originalUrl}`);
          continue;
        }

        // ── Quick ad pre-filter (STRICT MODE)
        if (isQuickAd(msg.text)) {
          console.log(
            `[Aggregate] 🚫 AD BLOCKED: msg ${msg.id} from @${channel}`,
          );
          // Mark as processed to avoid re-processing
          maxMsgId = Math.max(maxMsgId, msg.id);
          continue;
        }

        // ── AI restructuring
        let finalContent = msg.text;
        if (msg.text.length >= 10) {
          const aiRes = await restructureWithAI(msg.text);
          if (aiRes.isAd) {
            console.log(
              `[Aggregate] Gemini detected ad in msg ${msg.id}, skipping`,
            );
            continue;
          }
          finalContent = aiRes.content.trim();
        }

        const textWithCredit = `${finalContent}\n\n📢 @izynews`;

        // ── Publish to Telegram channel FIRST (to get message_id)
        // Check for short video (< 60 seconds)
        const isShortVideo =
          msg.videoUrl && msg.videoDuration && msg.videoDuration <= 60;

        let telegramMsgId: number | null = null;

        if (isShortVideo && msg.videoUrl) {
          // Send video (short videos < 60s)
          telegramMsgId = await sendVideoToTelegram(
            textWithCredit,
            msg.videoUrl,
          );
        } else if (msg.photoUrls && msg.photoUrls.length > 0) {
          // Send only the FIRST photo (not full album) - keeps it as single publication
          console.log(
            `[Aggregate] Album detected with ${msg.photoUrls.length} photos, using first only`,
          );
          telegramMsgId = await sendPhotoToTelegram(
            textWithCredit,
            msg.photoUrls[0],
          );
        } else {
          // Text only
          telegramMsgId = await sendTextToTelegram(textWithCredit);
        }
        if (!telegramMsgId) {
          console.warn(
            `[Aggregate] Failed to publish msg ${msg.id} to Telegram, skipping DB insert`,
          );
          continue;
        }

        // ── Save to DB with telegram_message_id
        const { error: insertError } = await supabase.from("articles").insert({
          title: finalContent.replace(/<[^>]+>/g, "").substring(0, 100),
          summary: msg.text.substring(0, 200),
          content: textWithCredit,
          original_url: originalUrl,
          source_id: source.id,
          is_certified: false,
          telegram_message_id: telegramMsgId,
          telegram_chat_id: OUTPUT_CHAT_ID,
        });

        if (insertError) {
          console.error("[Aggregate] DB insert error:", insertError);
          // Try to delete the telegram message if DB insert failed
          try {
            await fetch(
              `https://api.telegram.org/bot${BOT_TOKEN}/deleteMessage`,
              {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  message_id: telegramMsgId,
                }),
              },
            );
          } catch (e) {
            console.error("[Aggregate] Failed to cleanup telegram message:", e);
          }
          continue;
        }

        newArticlesCount++;
        console.log(
          `[Aggregate] Published msg ${msg.id} from @${channel} (TG msg_id: ${telegramMsgId})`,
        );

        // Small delay to avoid rate limits
        await new Promise((r) => setTimeout(r, 1500));
      }

      // ── Update last_message_id in source config so next run starts from here
      if (maxMsgId > lastMsgId) {
        await supabase
          .from("sources")
          .update({
            config: { ...source.config, last_message_id: String(maxMsgId) },
          })
          .eq("id", source.id);
        console.log(
          `[Aggregate] Updated last_message_id for @${channel} to ${maxMsgId}`,
        );
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: newArticlesCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


## Assistant

<thinking>
**Finalizing chat process**

I need to ensure that I've fixed all chat fallback issues. Next, I should focus on adding helper functions for filters. It makes sense to double-check everything after the `uploadVideo` function ends, along with the main functionalities. This systematic approach will help me keep things organized and running smoothly. I want to make sure nothing is overlooked, which is crucial for the user experience. Let's keep pushing forward!
</thinking>

**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 900-1125)**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (req) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: sources } = await supabase
      .from("sources")
      .select("*")
      .eq("type", "telegram")
      .eq("is_active", true);

    if (!sources || sources.length === 0) {
      return new Response(
        JSON.stringify({ success: true, new: 0, message: "No active sources" }),
      );
    }

    let processedCount = 0;
    let newArticlesCount = 0;

    for (const source of sources) {
      const channel = source.config?.channel;
      if (!channel) continue;

      // Get last processed message ID for this source (tracked in config)
      const lastMsgId = parseInt(source.config?.last_message_id || "0");

      console.log(
        `[Aggregate] Scraping @${channel}, after message ID: ${lastMsgId}`,
      );
      const messages = await scrapeChannel(channel, lastMsgId);
      console.log(
        `[Aggregate] Found ${messages.length} new message(s) in @${channel}`,
      );

      // FIRST RUN: limit to last 5 messages only to avoid spam
      let messagesToProcess = messages;
      if (lastMsgId === 0 && messages.length > 5) {
        messagesToProcess = messages.slice(-5); // Take only last 5
        console.log(`[Aggregate] First run - limiting to last 5 messages only`);
      }

      let maxMsgId = lastMsgId;

      for (const msg of messagesToProcess) {
        processedCount++;
        maxMsgId = Math.max(maxMsgId, msg.id);

        if (msg.text.length < 20 && !msg.hasMedia) continue;

        // ── Deduplication: use original_url (t.me/channel/msgId) as unique key
        const originalUrl = `https://t.me/${channel}/${msg.id}`;
        const { data: existing } = await supabase
          .from("articles")
          .select("id")
          .eq("original_url", originalUrl)
          .maybeSingle();

        if (existing) {
          console.log(`[Aggregate] Already processed: ${originalUrl}`);
          continue;
        }

        // ── Quick ad pre-filter (STRICT MODE)
        if (isQuickAd(msg.text)) {
          console.log(
            `[Aggregate] 🚫 AD BLOCKED: msg ${msg.id} from @${channel}`,
          );
          // Mark as processed to avoid re-processing
          maxMsgId = Math.max(maxMsgId, msg.id);
          continue;
        }

        // ── AI restructuring
        let finalContent = msg.text;
        if (msg.text.length >= 10) {
          const aiRes = await restructureWithAI(msg.text);
          if (aiRes.isAd) {
            console.log(
              `[Aggregate] Gemini detected ad in msg ${msg.id}, skipping`,
            );
            continue;
          }
          finalContent = aiRes.content.trim();
        }

        const textWithCredit = `${finalContent}\n\n📢 @izynews`;

        // ── Publish to Telegram channel FIRST (to get message_id)
        // Check for short video (< 60 seconds)
        const isShortVideo =
          msg.videoUrl && msg.videoDuration && msg.videoDuration <= 60;

        let telegramMsgId: number | null = null;

        if (isShortVideo && msg.videoUrl) {
          // Send video (short videos < 60s)
          telegramMsgId = await sendVideoToTelegram(
            textWithCredit,
            msg.videoUrl,
          );
        } else if (msg.photoUrls && msg.photoUrls.length > 0) {
          // Send only the FIRST photo (not full album) - keeps it as single publication
          console.log(
            `[Aggregate] Album detected with ${msg.photoUrls.length} photos, using first only`,
          );
          telegramMsgId = await sendPhotoToTelegram(
            textWithCredit,
            msg.photoUrls[0],
          );
        } else {
          // Text only
          telegramMsgId = await sendTextToTelegram(textWithCredit);
        }
        if (!telegramMsgId) {
          console.warn(
            `[Aggregate] Failed to publish msg ${msg.id} to Telegram, skipping DB insert`,
          );
          continue;
        }

        // ── Save to DB with telegram_message_id
        const { error: insertError } = await supabase.from("articles").insert({
          title: finalContent.replace(/<[^>]+>/g, "").substring(0, 100),
          summary: msg.text.substring(0, 200),
          content: textWithCredit,
          original_url: originalUrl,
          source_id: source.id,
          is_certified: false,
          telegram_message_id: telegramMsgId,
          telegram_chat_id: OUTPUT_CHAT_ID,
        });

        if (insertError) {
          console.error("[Aggregate] DB insert error:", insertError);
          // Try to delete the telegram message if DB insert failed
          try {
            await fetch(
              `https://api.telegram.org/bot${BOT_TOKEN}/deleteMessage`,
              {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  message_id: telegramMsgId,
                }),
              },
            );
          } catch (e) {
            console.error("[Aggregate] Failed to cleanup telegram message:", e);
          }
          continue;
        }

        newArticlesCount++;
        console.log(
          `[Aggregate] Published msg ${msg.id} from @${channel} (TG msg_id: ${telegramMsgId})`,
        );

        // Small delay to avoid rate limits
        await new Promise((r) => setTimeout(r, 1500));
      }

      // ── Update last_message_id in source config so next run starts from here
      if (maxMsgId > lastMsgId) {
        await supabase
          .from("sources")
          .update({
            config: { ...source.config, last_message_id: String(maxMsgId) },
          })
          .eq("id", source.id);
        console.log(
          `[Aggregate] Updated last_message_id for @${channel} to ${maxMsgId}`,
        );
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: newArticlesCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

function isPremiumUser(user: any): boolean {
  if (!user || user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}

function hasAnyKeyword(text: string, keywords: string[]): boolean {
  const lower = text.toLowerCase();
  return keywords.some((keyword) =>
    lower.includes(String(keyword).toLowerCase()),
  );
}

function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
  const isAlbum = Boolean(msg.photoUrls && msg.photoUrls.length > 1);
  const hasVideo = Boolean(msg.videoUrl);
  const hasText = msg.text.trim().length > 0;

  if (filters.media_only && !msg.hasMedia) return "media_only";
  if (!filters.allow_text && hasText && !msg.hasMedia)
    return "text_not_allowed";
  if (!filters.allow_photos && hasPhotos) return "photos_not_allowed";
  if (!filters.allow_videos && hasVideo) return "videos_not_allowed";
  if (!filters.allow_albums && isAlbum) return "albums_not_allowed";
  if (include.length > 0 && !hasAnyKeyword(msg.text, include))
    return "missing_include_keyword";
  if (exclude.length > 0 && hasAnyKeyword(msg.text, exclude))
    return "excluded_keyword";
  if (filters.block_ads && isQuickAd(msg.text)) return "ad_blocked";
  return null;
}

function transformContent(text: string, filters: any): string {
  let out = text.trim();
  if (filters.remove_links) out = out.replace(/https?:\/\/\S+/gi, "").trim();
  if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
  if (filters.signature_text)
    out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  return out;
}

async function recordActivity(supabase: any, payload: Record<string, unknown>) {
  const { error } = await supabase.from("flow_activity").insert(payload);
  if (error) console.error("[Aggregate] flow_activity insert error:", error);
}

async function incrementUsage(supabase: any, userId: string, amount: number) {
  const day = new Date().toISOString().slice(0, 10);
  const { data } = await supabase
    .from("usage_daily")
    .select("id, analyzed_count")
    .eq("user_id", userId)
    .eq("day", day)
    .maybeSingle();

  if (data) {
    await supabase
      .from("usage_daily")
      .update({ analyzed_count: (data.analyzed_count || 0) + amount })
      .eq("id", data.id);
  } else {
    await supabase
      .from("usage_daily")
      .insert({ user_id: userId, day, analyzed_count: amount });
  }
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (_req) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: flows, error: flowError } = await supabase
      .from("flows")
      .select(
        "*, user:app_users(*), source:user_sources(*), target:user_targets(*)",
      )
      .eq("is_active", true)
      .order("created_at", { ascending: true });

    if (flowError) throw flowError;

    if (!flows || flows.length === 0) {
      return new Response(
        JSON.stringify({
          success: true,
          processed: 0,
          new: 0,
          message: "No active user flows",
        }),
        { headers: { "Content-Type": "application/json" } },
      );
    }

    let processedCount = 0;
    let publishedCount = 0;
    let skippedCount = 0;

    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
      const channel = source.config?.channel;
      const targetChatId = target.chat_id;
      const lastMsgId = Number(flow.last_message_id || 0);
      if (!channel || !targetChatId) continue;

      console.log(
        `[Aggregate] Flow ${flow.id}: @${channel} → ${targetChatId}, after ${lastMsgId}`,
      );

      try {
        const messages = await scrapeChannel(channel, lastMsgId);
        let messagesToProcess = messages;
        if (
          lastMsgId === 0 &&
          messages.length > Number(flow.initial_last_n || 5)
        ) {
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (!premium && analyzedForThisFlow > 5) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason: "free_daily_limit",
              original_url: `https://t.me/${channel}/${msg.id}`,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")
            .maybeSingle();

          if (alreadyProcessed) continue;

          const reason = skipReasonForFilters(msg, filters);
          if (reason) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason,
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          let finalContent = msg.text;
          if (filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
                reason: "ai_ad_blocked",
                original_url: originalUrl,
                text_preview: msg.text.substring(0, 200),
                media_count:
                  (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
              });
              continue;
            }
            finalContent = aiRes.content.trim();
          }

          const textToPublish =
            transformContent(finalContent, filters) || originalUrl;
          const isShortVideo =
            msg.videoUrl && (!msg.videoDuration || msg.videoDuration <= 60);
          let telegramMsgId: number | null = null;

          if (isShortVideo && msg.videoUrl && filters.allow_videos) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 1 &&
            filters.allow_albums
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 0 &&
            filters.allow_photos
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (filters.allow_text) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }

          if (!telegramMsgId) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "failed",
              reason: "publish_failed",
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          publishedCount++;
          await recordActivity(supabase, {
            user_id: user.id,
            flow_id: flow.id,
            source_id: source.id,
            target_id: target.id,
            source_message_id: msg.id,
            target_message_ids: [telegramMsgId],
            status: "published",
            original_url: originalUrl,
            text_preview: msg.text.substring(0, 200),
            media_count: (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            raw_payload: {
              target_chat_id: targetChatId,
              source_channel: channel,
            },
          });

          await new Promise((r) => setTimeout(r, 1500));
        }

        if (analyzedForThisFlow > 0)
          await incrementUsage(supabase, user.id, analyzedForThisFlow);

        await supabase
          .from("flows")
          .update({
            last_message_id: maxMsgId,
            last_run_at: new Date().toISOString(),
            last_error: null,
          })
          .eq("id", flow.id);
      } catch (flowErr) {
        console.error(`[Aggregate] Flow ${flow.id} failed:`, flowErr);
        await supabase
          .from("flows")
          .update({
            last_run_at: new Date().toISOString(),
            last_error: (flowErr as Error).message,
          })
          .eq("id", flow.id);
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: publishedCount,
        skipped: skippedCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


## Assistant

<thinking>
**Verifying diagnostics**

I'm continuing to verify diagnostics. I also need to check for TypeScript errors that might be related to the Supabase select alias — maybe it’s an issue with the source property array? The Supabase-js relational aliases could return arrays unless the relationship is unique. In TypeScript, though, it seems that everything is likely okay with no diagnostics showing up? I’ll need to run the diagnostics to make sure everything checks out.
</thinking>

Je vais faire une passe de vérification rapide sur les diagnostics TypeScript pour corriger les erreurs évidentes avant de te résumer ce qui est prêt.

**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.
error at line 3: Cannot find name 'Deno'.
error at line 4: Cannot find name 'Deno'.
error at line 8: Cannot find name 'Deno'.
error at line 10: Parameter 'id' implicitly has an 'any' type.
error at line 250: Cannot find name 'Deno'.
error at line 250: Parameter 'req' implicitly has an 'any' type.
error at line 284: Cannot find name 'Deno'.
error at line 285: Cannot find name 'Deno'.
error at line 414: Parameter 's' implicitly has an 'any' type.
error at line 525: Parameter 't' implicitly has an 'any' type.
error at line 641: Parameter 'f' implicitly has an 'any' type.
error at line 783: Parameter 'r' implicitly has an 'any' type.
error at line 842: Parameter 'a' implicitly has an 'any' type.
error at line 842: Parameter 'i' implicitly has an 'any' type.
error at line 1104: Cannot find name 'Deno'.
error at line 1204: Parameter 's' implicitly has an 'any' type.
error at line 1271: Cannot find name 'Deno'.
error at line 1272: Cannot find name 'Deno'.


**Tool Call: Check diagnostics for `news/supabase/functions/aggregate_news/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.
error at line 3: Cannot find name 'Deno'.
error at line 4: Cannot find name 'Deno'.
error at line 5: Cannot find name 'Deno'.
error at line 8: Cannot find name 'Deno'.
error at line 9: Cannot find name 'Deno'.
error at line 11: Cannot find name 'Deno'.
error at line 12: Cannot find name 'Deno'.
error at line 1021: Cannot find name 'Deno'.
error at line 1021: Parameter '_req' implicitly has an 'any' type.
error at line 1023: Cannot find name 'Deno'.
error at line 1024: Cannot find name 'Deno'.


**Tool Call: Check diagnostics for `news/supabase/migrations/20260511200000_add_user_flow_filters_activity.sql`**
Status: Completed

File doesn't have errors or warnings!

## Assistant

<thinking>
**Finalizing in French**

I need to finalize something in French. I also want to mention that diagnostics show the Deno environment is set to false in the editor, but I think we might have altered the formatting. It could be helpful to run a grep command to check for OUTPUT_CHAT in the aggregated data for anything that might have been missed. It’s a little tricky, but I’ll figure this out!
</thinking>

**Tool Call: Search files for regex `OUTPUT_CHAT_ID|sendTextToTelegram\(|sendPhotoToTelegram\(|sendAlbumToTelegram\(|sendVideoToTelegram\(|uploadPhotoDirect\(|uploadVideoDirect\(` (case-sensitive)**
Status: Completed

Found 19 matches:

## Matches in news\supabase\functions\aggregate_news\index.ts

### const OUTPUT_CHAT_ID › L1-11
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
```

1273 lines remaining in ancestor node. Read the file to see all.

### async function sendTextToTelegram( ) › L586-589
```
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
```

24 lines remaining in ancestor node. Read the file to see all.

### async function sendPhotoToTelegram( ) › L616-620
```
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
```

57 lines remaining in ancestor node. Read the file to see all.

### async function sendPhotoToTelegram( ) › L628-631
```
    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }
```

### async function sendPhotoToTelegram( ) › L640-644
```
    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }
```

### async function sendPhotoToTelegram( ) › L670-676
```
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
```

### async function uploadPhotoDirect( ) › L680-684
```
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
```

34 lines remaining in ancestor node. Read the file to see all.

### async function sendAlbumToTelegram( ) › L721-725
```
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
```

86 lines remaining in ancestor node. Read the file to see all.

### async function sendAlbumToTelegram( ) › L745-747
```
    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }
```

### async function sendAlbumToTelegram( ) › L749-752
```
    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }
```

### async function sendAlbumToTelegram( ) › L804-810
```

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
```

### async function sendVideoToTelegram( ) › L814-818
```
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
```

62 lines remaining in ancestor node. Read the file to see all.

### async function sendVideoToTelegram( ) › L826-829
```
    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }
```

### async function sendVideoToTelegram( ) › L842-846
```
    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }
```

### async function sendVideoToTelegram( ) › L873-879
```
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
```

### async function uploadVideoDirect( ) › L883-887
```
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
```

35 lines remaining in ancestor node. Read the file to see all.

### L1171-1177
```
          if (isShortVideo && msg.videoUrl && filters.allow_videos) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
```

### L1181-1188
```
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
```

### L1192-1203
```
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (filters.allow_text) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }
```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\n<b>📡 Sources</b>\n/addsource &lt;@canal ou URL t.me&gt; — Ajouter un canal source\n/sources — Mes sources\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>🎯 Canaux cibles</b>\n/addtarget &lt;@canal ou -100...&gt; [nom] — Ajouter un canal cible\n/targets — Mes canaux cibles\n/deltarget &lt;id&gt; — Désactiver un canal cible\n\n<b>🔁 Flux</b>\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Relier source → cible\n/flows — Mes flux\n/pauseflow &lt;id&gt; — Mettre en pause\n/resumeflow &lt;id&gt; — Reprendre\n/deleteflow &lt;id&gt; — Supprimer/désactiver\n\n<b>🧰 Filtres</b>\n/filters &lt;flow_id&gt; — Voir les filtres\n/include &lt;flow_id&gt; mot1,mot2 — Mots obligatoires\n/exclude &lt;flow_id&gt; mot1,mot2 — Mots interdits\n/blockads &lt;flow_id&gt; on|off — Bloquer les pubs\n/mediaonly &lt;flow_id&gt; on|off — Publier seulement les médias\n/allowalbums &lt;flow_id&gt; on|off — Autoriser les albums\n/rewriteai &lt;flow_id&gt; on|off — Reformulation IA\n/signature &lt;flow_id&gt; texte — Signature ajoutée\n\n<b>📊 Activité</b>\n/activity — Dernières actions\n/me — Mon compte\n/subscribe — Abonnement Premium`;

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const parts = text.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  const adminMode = isAdmin(chatId) || isAdmin(userId);

  try {
    // ── Ensure app user exists for non-admins ─────────────────────────────
    let appUser: any = null;
    if (!adminMode) {
      appUser = await getOrCreateAppUser(supabase, userId, username);
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`);
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

function isPremiumUser(user: any): boolean {
  if (!user || user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}

function hasAnyKeyword(text: string, keywords: string[]): boolean {
  const lower = text.toLowerCase();
  return keywords.some((keyword) =>
    lower.includes(String(keyword).toLowerCase()),
  );
}

function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
  const isAlbum = Boolean(msg.photoUrls && msg.photoUrls.length > 1);
  const hasVideo = Boolean(msg.videoUrl);
  const hasText = msg.text.trim().length > 0;

  if (filters.media_only && !msg.hasMedia) return "media_only";
  if (!filters.allow_text && hasText && !msg.hasMedia)
    return "text_not_allowed";
  if (!filters.allow_photos && hasPhotos) return "photos_not_allowed";
  if (!filters.allow_videos && hasVideo) return "videos_not_allowed";
  if (!filters.allow_albums && isAlbum) return "albums_not_allowed";
  if (include.length > 0 && !hasAnyKeyword(msg.text, include))
    return "missing_include_keyword";
  if (exclude.length > 0 && hasAnyKeyword(msg.text, exclude))
    return "excluded_keyword";
  if (filters.block_ads && isQuickAd(msg.text)) return "ad_blocked";
  return null;
}

function transformContent(text: string, filters: any): string {
  let out = text.trim();
  if (filters.remove_links) out = out.replace(/https?:\/\/\S+/gi, "").trim();
  if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
  if (filters.signature_text)
    out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  return out;
}

async function recordActivity(supabase: any, payload: Record<string, unknown>) {
  const { error } = await supabase.from("flow_activity").insert(payload);
  if (error) console.error("[Aggregate] flow_activity insert error:", error);
}

async function incrementUsage(supabase: any, userId: string, amount: number) {
  const day = new Date().toISOString().slice(0, 10);
  const { data } = await supabase
    .from("usage_daily")
    .select("id, analyzed_count")
    .eq("user_id", userId)
    .eq("day", day)
    .maybeSingle();

  if (data) {
    await supabase
      .from("usage_daily")
      .update({ analyzed_count: (data.analyzed_count || 0) + amount })
      .eq("id", data.id);
  } else {
    await supabase
      .from("usage_daily")
      .insert({ user_id: userId, day, analyzed_count: amount });
  }
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (_req: Request) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: flows, error: flowError } = await supabase
      .from("flows")
      .select(
        "*, user:app_users(*), source:user_sources(*), target:user_targets(*)",
      )
      .eq("is_active", true)
      .order("created_at", { ascending: true });

    if (flowError) throw flowError;

    if (!flows || flows.length === 0) {
      return new Response(
        JSON.stringify({
          success: true,
          processed: 0,
          new: 0,
          message: "No active user flows",
        }),
        { headers: { "Content-Type": "application/json" } },
      );
    }

    let processedCount = 0;
    let publishedCount = 0;
    let skippedCount = 0;

    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
      const channel = source.config?.channel;
      const targetChatId = target.chat_id;
      const lastMsgId = Number(flow.last_message_id || 0);
      if (!channel || !targetChatId) continue;

      console.log(
        `[Aggregate] Flow ${flow.id}: @${channel} → ${targetChatId}, after ${lastMsgId}`,
      );

      try {
        const messages = await scrapeChannel(channel, lastMsgId);
        let messagesToProcess = messages;
        if (
          lastMsgId === 0 &&
          messages.length > Number(flow.initial_last_n || 5)
        ) {
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (!premium && analyzedForThisFlow > 5) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason: "free_daily_limit",
              original_url: `https://t.me/${channel}/${msg.id}`,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")
            .maybeSingle();

          if (alreadyProcessed) continue;

          const reason = skipReasonForFilters(msg, filters);
          if (reason) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason,
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          let finalContent = msg.text;
          if (filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
                reason: "ai_ad_blocked",
                original_url: originalUrl,
                text_preview: msg.text.substring(0, 200),
                media_count:
                  (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
              });
              continue;
            }
            finalContent = aiRes.content.trim();
          }

          const textToPublish =
            transformContent(finalContent, filters) || originalUrl;
          const isShortVideo =
            msg.videoUrl && (!msg.videoDuration || msg.videoDuration <= 60);
          let telegramMsgId: number | null = null;

          if (isShortVideo && msg.videoUrl && filters.allow_videos) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 1 &&
            filters.allow_albums
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 0 &&
            filters.allow_photos
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (filters.allow_text) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }

          if (!telegramMsgId) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "failed",
              reason: "publish_failed",
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          publishedCount++;
          await recordActivity(supabase, {
            user_id: user.id,
            flow_id: flow.id,
            source_id: source.id,
            target_id: target.id,
            source_message_id: msg.id,
            target_message_ids: [telegramMsgId],
            status: "published",
            original_url: originalUrl,
            text_preview: msg.text.substring(0, 200),
            media_count: (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            raw_payload: {
              target_chat_id: targetChatId,
              source_channel: channel,
            },
          });

          await new Promise((r) => setTimeout(r, 1500));
        }

        if (analyzedForThisFlow > 0)
          await incrementUsage(supabase, user.id, analyzedForThisFlow);

        await supabase
          .from("flows")
          .update({
            last_message_id: maxMsgId,
            last_run_at: new Date().toISOString(),
            last_error: null,
          })
          .eq("id", flow.id);
      } catch (flowErr) {
        console.error(`[Aggregate] Flow ${flow.id} failed:`, flowErr);
        await supabase
          .from("flows")
          .update({
            last_run_at: new Date().toISOString(),
            last_error: (flowErr as Error).message,
          })
          .eq("id", flow.id);
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: publishedCount,
        skipped: skippedCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/functions/aggregate_news/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


## Assistant

C’est fait pour la première base multi-utilisateur.

J’ai laissé le document `.md` tranquille et j’ai travaillé directement sur le fonctionnement actuel du bot.

## Ce qui a été ajouté

### 1. Nouvelle migration Supabase

J’ai ajouté :

`news/supabase/migrations/20260511200000_add_user_flow_filters_activity.sql`

Elle ajoute :

- des filtres sur chaque `flow` via `filters JSONB` ;
- `last_message_id` par flux ;
- `last_run_at` ;
- `last_error` ;
- une table `flow_activity` pour suivre les actions du bot.

Cette table permettra à chaque utilisateur de voir :

- les messages publiés ;
- les messages ignorés ;
- les erreurs ;
- les médias traités ;
- les publications liées à un flux.

## 2. Commandes utilisateur ajoutées au bot Telegram

J’ai modifié :

`news/supabase/functions/telegram_bot/index.ts`

Maintenant un utilisateur non-admin peut gérer ses propres données.

### Sources

Commandes disponibles :

- `/addsource @canal`
- `/sources`
- `/delsource <id>`

Les sources sont stockées dans `user_sources`, donc chaque utilisateur a ses propres canaux sources.

### Canaux cibles

Commandes disponibles :

- `/addtarget @moncanal`
- `/addtarget -100xxxxxxxxxx`
- `/targets`
- `/deltarget <id>`

Les canaux cibles sont stockés dans `user_targets`.

Le bot essaie aussi de vérifier le canal cible avec `getChat`. Si Telegram refuse, le bot prévient l’utilisateur qu’il doit ajouter le bot comme admin du canal cible.

### Flux

Commandes disponibles :

- `/addflow <source_id> <target_id>`
- `/flows`
- `/pauseflow <id>`
- `/resumeflow <id>`
- `/deleteflow <id>`

Un flux relie :

- une source ;
- une cible ;
- un utilisateur ;
- des filtres ;
- un état actif/inactif.

Pour le plan gratuit, j’ai déjà mis une première limite :

- 1 seul flux actif par utilisateur gratuit.

Le Premium n’est pas encore relié à Nelsius Pay, mais la base est prête.

### Filtres

Commandes disponibles :

- `/filters <flow_id>`
- `/include <flow_id> mot1,mot2`
- `/exclude <flow_id> mot1,mot2`
- `/blockads <flow_id> on|off`
- `/mediaonly <flow_id> on|off`
- `/allowalbums <flow_id> on|off`
- `/rewriteai <flow_id> on|off`
- `/signature <flow_id> texte`

Les filtres sont enregistrés dans `flows.filters`.

### Activité

Commande ajoutée :

- `/activity`

Elle affiche les dernières activités de l’utilisateur depuis `flow_activity`.

## 3. Agrégateur transformé en multi-utilisateur

J’ai modifié :

`news/supabase/functions/aggregate_news/index.ts`

Avant, l’agrégateur lisait seulement la table globale `sources` et publiait vers un seul `TELEGRAM_CHAT_ID`.

Maintenant, il lit les `flows` actifs.

Pour chaque flux actif, il fait :

1. récupère l’utilisateur ;
2. récupère sa source ;
3. récupère son canal cible ;
4. scrape le canal source Telegram ;
5. applique les filtres du flux ;
6. publie vers le canal cible de l’utilisateur ;
7. enregistre l’activité dans `flow_activity` ;
8. met à jour `last_message_id` sur le flux ;
9. enregistre les erreurs éventuelles dans `last_error`.

## 4. Gestion des albums

J’ai aussi adapté la logique pour publier les albums.

Maintenant, si un message contient plusieurs images et que le filtre `allow_albums` est activé, l’agrégateur utilise `sendMediaGroup`.

Donc il peut republier un album d’images en une seule publication groupée Telegram.

Important : Telegram limite les albums à 10 médias par `sendMediaGroup`. J’ai donc limité à 10 images maximum par album publié :

- `msg.photoUrls.slice(0, 10)`

C’est le comportement correct pour rester compatible avec Telegram.

## 5. Gestion des médias par canal cible

J’ai modifié les fonctions d’envoi pour accepter un `chatId` dynamique :

- `sendTextToTelegram`
- `sendPhotoToTelegram`
- `sendAlbumToTelegram`
- `sendVideoToTelegram`
- `uploadPhotoDirect`
- `uploadVideoDirect`

Donc maintenant les médias ne sont plus envoyés seulement vers le canal global `TELEGRAM_CHAT_ID`, mais vers le canal cible choisi par l’utilisateur.

## 6. Ce qui reste volontairement pour l’étape suivante

Je n’ai pas encore intégré Nelsius Pay, comme tu as dit qu’on le ferait ensuite.

Actuellement, le système prépare déjà :

- `plan = free` ou `premium` ;
- `plan_expires_at` ;
- table `payments` ;
- table `subscriptions` ;
- commande `/subscribe` ;
- limitation gratuite à 1 flux actif.

Mais l’étape suivante sera de connecter réellement Nelsius Pay pour :

- créer un paiement ;
- recevoir le callback/webhook ;
- valider le paiement ;
- activer `plan = premium` ;
- définir `plan_expires_at` ;
- débloquer les fonctionnalités pro.

## Diagnostics

Il reste uniquement un diagnostic local sur les imports Deno :

- `Cannot find module 'https://esm.sh/@supabase/supabase-js@2'`

C’est lié à l’analyse TypeScript locale qui ne comprend pas les imports URL Deno/Supabase. Ce n’est pas forcément bloquant pour Supabase Edge Functions.

J’ai ajouté `declare const Deno: any;` pour réduire les autres erreurs liées à l’environnement Deno.

## Ce que tu dois faire côté Supabase

Pour appliquer ce que j’ai ajouté, il faudra :

- appliquer la migration ;
- redéployer `telegram_bot` ;
- redéployer `aggregate_news`.

Commandes typiques :

- `supabase db push`
- `supabase functions deploy telegram_bot`
- `supabase functions deploy aggregate_news`

## Résultat actuel attendu

Maintenant, le bot est orienté vers ton objectif :

- chaque utilisateur peut rejoindre le bot ;
- chaque utilisateur peut ajouter ses sources ;
- chaque utilisateur peut ajouter ses canaux cibles ;
- chaque utilisateur peut créer ses flux ;
- chaque utilisateur peut configurer ses filtres ;
- l’agrégateur publie vers les canaux cibles de chaque utilisateur ;
- les albums d’images peuvent être republiés en groupe ;
- l’activité est enregistrée.

La prochaine étape logique : intégrer Nelsius Pay pour transformer le plan gratuit en abonnement Premium réel.

## User

maintenant, comment faire pour que ces modifications partent sur supabase? sachant que il y avais deja la version avant qu tu ne modifie

## Assistant

Pour envoyer ces modifications sur Supabase alors qu’une ancienne version existe déjà, il faut faire 2 choses :

1. **appliquer la nouvelle migration SQL** pour mettre à jour la base ;
2. **redéployer les Edge Functions** modifiées :
   - `telegram_bot`
   - `aggregate_news`

Comme l’ancienne version existe déjà, Supabase va simplement remplacer le code des fonctions déployées et appliquer uniquement les nouvelles migrations non encore exécutées.

---

## 1. Vérifier que tu es dans le bon dossier

Dans ton terminal, place-toi dans le dossier du projet :

`E:\laragon\www\news`

Avec ton shell, ça peut être :

```/dev/null/terminal.txt#L1
cd E:\laragon\www\news
```

Si tu utilises Git Bash ou Laragon avec un shell Unix, ça peut être plutôt :

```/dev/null/terminal.txt#L1
cd /e/laragon/www/news
```

---

## 2. Vérifier que Supabase CLI fonctionne

Lance :

```/dev/null/terminal.txt#L1
npx supabase --version
```

Ou si Supabase est installé globalement :

```/dev/null/terminal.txt#L1
supabase --version
```

Ton projet utilise déjà le package `supabase` dans `package.json`, donc `npx supabase ...` devrait marcher.

---

## 3. Vérifier que ton projet est lié au bon projet Supabase distant

Lance :

```/dev/null/terminal.txt#L1
npx supabase status
```

Puis vérifie aussi si le projet est linké :

```/dev/null/terminal.txt#L1
npx supabase projects list
```

Si le projet n’est pas encore lié, il faut le lier avec ton project ref Supabase.

Dans ton code, l’ancien cron pointe vers :

`nxgryaqncfjuazfvekro`

Donc ton project ref semble être :

```/dev/null/text.txt#L1
nxgryaqncfjuazfvekro
```

Tu peux lier avec :

```/dev/null/terminal.txt#L1
npx supabase link --project-ref nxgryaqncfjuazfvekro
```

Supabase peut te demander ton mot de passe database ou une connexion.

---

## 4. Vérifier les migrations avant de pousser

Comme il y avait déjà une version avant, il faut faire attention aux migrations existantes.

Tu as maintenant une nouvelle migration :

```/dev/null/text.txt#L1
supabase/migrations/20260511200000_add_user_flow_filters_activity.sql
```

Elle est additive : elle ajoute des colonnes et une table. Donc elle ne devrait pas casser l’ancien système.

Tu peux vérifier les migrations distantes avec :

```/dev/null/terminal.txt#L1
npx supabase migration list
```

Cette commande te montrera quelles migrations sont :

- locales ;
- distantes ;
- déjà appliquées ;
- manquantes.

---

## 5. Appliquer la migration sur Supabase

Pour envoyer la nouvelle migration vers la base Supabase distante :

```/dev/null/terminal.txt#L1
npx supabase db push
```

Cette commande va appliquer les migrations locales qui ne sont pas encore sur le projet distant.

### Important

Si Supabase te montre une liste de migrations à appliquer, vérifie bien qu’il n’essaie pas d’appliquer une migration destructive comme :

```/dev/null/text.txt#L1
20260503202730_clear_articles.sql
```

Cette migration contient une suppression des articles.

Si elle a déjà été appliquée avant, pas de souci.

Mais si elle n’a jamais été appliquée sur le Supabase distant, `db push` pourrait vouloir l’exécuter maintenant, ce qui supprimerait les articles.

Donc avant de lancer `db push`, fais vraiment :

```/dev/null/terminal.txt#L1
npx supabase migration list
```

Et regarde si `20260503202730_clear_articles.sql` est déjà côté remote.

---

## 6. Cas important : si `db push` veut appliquer d’anciennes migrations dangereuses

Si `npx supabase migration list` montre que des anciennes migrations locales ne sont pas appliquées en remote, notamment :

```/dev/null/text.txt#L1
20260503202730_clear_articles.sql
```

Alors ne lance pas directement `db push`.

Dans ce cas, tu as 2 options.

### Option A — Marquer l’ancienne migration comme déjà appliquée

Si la base distante est déjà dans le bon état et que tu ne veux pas rejouer certaines anciennes migrations, tu peux réparer l’historique des migrations.

Exemple :

```/dev/null/terminal.txt#L1
npx supabase migration repair --status applied 20260503202730
```

Mais il faut faire ça seulement si tu es sûr que cette migration ne doit pas être exécutée.

### Option B — Appliquer seulement le SQL de la nouvelle migration manuellement

Si tu veux éviter tout risque, tu peux copier le contenu de la nouvelle migration et l’exécuter directement dans Supabase SQL Editor.

Dans Supabase Dashboard :

1. va dans ton projet ;
2. ouvre **SQL Editor** ;
3. colle le contenu de :
   - `supabase/migrations/20260511200000_add_user_flow_filters_activity.sql`
4. clique sur **Run**.

Cette option est souvent la plus sûre si l’historique des migrations est incertain.

---

## 7. Redéployer les Edge Functions

Après la base, redéploie les fonctions.

### Déployer `telegram_bot`

```/dev/null/terminal.txt#L1
npx supabase functions deploy telegram_bot
```

### Déployer `aggregate_news`

```/dev/null/terminal.txt#L1
npx supabase functions deploy aggregate_news
```

Tu peux aussi ajouter `--project-ref` si nécessaire :

```/dev/null/terminal.txt#L1
npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro
```

```/dev/null/terminal.txt#L1
npx supabase functions deploy aggregate_news --project-ref nxgryaqncfjuazfvekro
```

---

## 8. Vérifier les variables d’environnement Supabase

Les Edge Functions utilisent toujours ces secrets :

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`
- `TELEGRAM_ADMIN_IDS`
- `GEMINI_API_KEY`
- `STORJ_ACCESS_KEY`
- `STORJ_SECRET_KEY`
- `STORJ_ENDPOINT`
- `STORJ_BUCKET`

Vérifie les secrets existants :

```/dev/null/terminal.txt#L1
npx supabase secrets list
```

Si une variable manque, ajoute-la comme ceci :

```/dev/null/terminal.txt#L1
npx supabase secrets set NOM_DE_LA_VARIABLE=valeur
```

Exemple :

```/dev/null/terminal.txt#L1
npx supabase secrets set TELEGRAM_ADMIN_IDS=123456789
```

Si tu mets plusieurs admins :

```/dev/null/terminal.txt#L1
npx supabase secrets set TELEGRAM_ADMIN_IDS=123456789,987654321
```

---

## 9. Vérifier le webhook Telegram

Comme tu as redéployé `telegram_bot`, l’URL devrait rester la même :

```/dev/null/text.txt#L1
https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/telegram_bot
```

Mais si le webhook Telegram n’est pas configuré ou a été modifié, remets-le :

```/dev/null/terminal.txt#L1
curl "https://api.telegram.org/botTON_BOT_TOKEN/setWebhook?url=https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/telegram_bot"
```

Remplace `TON_BOT_TOKEN` par ton vrai token Telegram.

Si tu es sous Windows PowerShell, tu peux utiliser plutôt :

```/dev/null/terminal.txt#L1
Invoke-WebRequest "https://api.telegram.org/botTON_BOT_TOKEN/setWebhook?url=https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/telegram_bot"
```

---

## 10. Tester le bot

Dans Telegram, avec un compte non-admin, teste :

```/dev/null/text.txt#L1
/start
```

Puis :

```/dev/null/text.txt#L1
/addsource @un_canal_public
```

Ensuite :

```/dev/null/text.txt#L1
/sources
```

Ajoute ton canal cible :

```/dev/null/text.txt#L1
/addtarget @ton_canal_cible
```

Puis :

```/dev/null/text.txt#L1
/targets
```

Crée le flux :

```/dev/null/text.txt#L1
/addflow source_id target_id
```

Exemple avec des IDs courts retournés par le bot :

```/dev/null/text.txt#L1
/addflow a1b2c3d4 e5f6g7h8
```

Configure les filtres :

```/dev/null/text.txt#L1
/blockads a1b2c3d4 on
```

```/dev/null/text.txt#L1
/allowalbums a1b2c3d4 on
```

Puis lance l’agrégation avec ton compte admin :

```/dev/null/text.txt#L1
/run
```

Ou attends le cron automatique.

---

## 11. Tester directement `aggregate_news`

Tu peux tester la fonction via Supabase :

```/dev/null/terminal.txt#L1
npx supabase functions invoke aggregate_news
```

Pour la version distante, tu peux utiliser curl :

```/dev/null/terminal.txt#L1
curl -X POST https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/aggregate_news
```

Comme actuellement `verify_jwt = false` pour `aggregate_news`, elle peut être appelée sans token.

Plus tard, je recommande de sécuriser ça.

---

## 12. Vérifier les logs

Après déploiement, si quelque chose ne marche pas :

```/dev/null/terminal.txt#L1
npx supabase functions logs telegram_bot
```

```/dev/null/terminal.txt#L1
npx supabase functions logs aggregate_news
```

Ou dans Supabase Dashboard :

- Edge Functions ;
- choisir la fonction ;
- Logs.

---

## Méthode recommandée dans ton cas

Vu qu’il y avait déjà une version avant, je te recommande cette séquence prudente :

1. Vérifier les migrations :

```/dev/null/terminal.txt#L1
npx supabase migration list
```

2. Si aucune ancienne migration dangereuse n’est en attente :

```/dev/null/terminal.txt#L1
npx supabase db push
```

3. Si une ancienne migration dangereuse est en attente, appliquer manuellement seulement la nouvelle migration dans SQL Editor.

4. Déployer les fonctions :

```/dev/null/terminal.txt#L1
npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro
```

```/dev/null/terminal.txt#L1
npx supabase functions deploy aggregate_news --project-ref nxgryaqncfjuazfvekro
```

5. Tester dans Telegram :

```/dev/null/text.txt#L1
/start
/sources
/targets
/flows
```

---

## Attention importante

Avant `db push`, vérifie bien que cette migration ne va pas s’exécuter si elle n’a jamais été appliquée :

```/dev/null/text.txt#L1
20260503202730_clear_articles.sql
```

Car elle supprime les articles.

Si tu veux, je peux aussi te préparer une version “safe deploy” : on renomme/déplace les migrations dangereuses ou on prépare uniquement un script SQL propre à exécuter dans Supabase SQL Editor.

## User (checkpoint)

dans le bot cote utilisateur et admin, il faut ajouter les boutons directement en bas . meme pour le bot cote admin et les scenariot du genre si il veux par exemple ajouter un nouveau systeme, il tape sur le bouton nouveau par exemple, ensuite, on demande d'entre le canl source. ensuite, on demande de choisir le canll cyble ou d'ajouter un nouveau canal cyble ainsi de suite. donc arange bien les fonctionnements

## Assistant

Je vais ajouter un vrai fonctionnement avec boutons Telegram (`reply_markup.keyboard`) et un état de conversation par utilisateur, pour permettre des scénarios guidés comme “Nouveau flux → entrer source → choisir/ajouter cible → créer le flux”. Je vais aussi prévoir un menu admin séparé.

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1-180)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\n<b>📡 Sources</b>\n/addsource &lt;@canal ou URL t.me&gt; — Ajouter un canal source\n/sources — Mes sources\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>🎯 Canaux cibles</b>\n/addtarget &lt;@canal ou -100...&gt; [nom] — Ajouter un canal cible\n/targets — Mes canaux cibles\n/deltarget &lt;id&gt; — Désactiver un canal cible\n\n<b>🔁 Flux</b>\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Relier source → cible\n/flows — Mes flux\n/pauseflow &lt;id&gt; — Mettre en pause\n/resumeflow &lt;id&gt; — Reprendre\n/deleteflow &lt;id&gt; — Supprimer/désactiver\n\n<b>🧰 Filtres</b>\n/filters &lt;flow_id&gt; — Voir les filtres\n/include &lt;flow_id&gt; mot1,mot2 — Mots obligatoires\n/exclude &lt;flow_id&gt; mot1,mot2 — Mots interdits\n/blockads &lt;flow_id&gt; on|off — Bloquer les pubs\n/mediaonly &lt;flow_id&gt; on|off — Publier seulement les médias\n/allowalbums &lt;flow_id&gt; on|off — Autoriser les albums\n/rewriteai &lt;flow_id&gt; on|off — Reformulation IA\n/signature &lt;flow_id&gt; texte — Signature ajoutée\n\n<b>📊 Activité</b>\n/activity — Dernières actions\n/me — Mon compte\n/subscribe — Abonnement Premium`;

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 250-820)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const parts = text.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  const adminMode = isAdmin(chatId) || isAdmin(userId);

  try {
    // ── Ensure app user exists for non-admins ─────────────────────────────
    let appUser: any = null;
    if (!adminMode) {
      appUser = await getOrCreateAppUser(supabase, userId, username);
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1180-1320)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,

```


## Assistant

<thinking>
**Considering state management**

I need to edit and possibly add a migration for bot_state to make it easier to store state in the app_users configuration. Currently, existing app_users don't have state. I think I should add a migration for bot_state as jsonb. Workflow implementation is also necessary, including mapping commands from button labels to slash commands and handling state before commands.

Oh, should I track admin state with app_users too? But, if I modify to always create an app_user for admins, I need to update the is_admin flag properly. Let's clarify this!

**Planning menu and flow**

I need to set up a reply with a custom keyboard featuring specific buttons. The user menu buttons include options like "➕ Nouveau flux" and "📡 Mes sources," while the admin menu contains "🛠 Admin" and "▶️ Lancer agrégation," among others. 

For scenarios, when a user selects a new flow, I’ll have it set the state to await_source and prompt for the source. After adding or locating it, they would choose a target with buttons displaying existing choices. 

I also want to ensure state handling occurs early in the process to avoid issues!

**Implementing helper functions**

I need to implement several helper functions, like menuMarkup for admin menus and state management from appUser.bot_state. I’ll also set up functions like setUserState, which takes in parameters like supabase, userId, and the desired state.

I think ensuring the user has a source and a target is crucial, along with creating a user flow. Lastly, I'll need a function to generate a keyboard for listing targets. This should help streamline the overall process!
</thinking>

**Tool Call: supabase\\migrations\\20260511201000\_add\_bot\_state\_to\_app\_users.sql**
Status: Completed

Diff: supabase\migrations\20260511201000_add_bot_state_to_app_users.sql
```
-- Store Telegram bot guided workflow state per user/admin
ALTER TABLE public.app_users
  ADD COLUMN IF NOT EXISTS bot_state JSONB NOT NULL DEFAULT '{}'::jsonb;

```


## Assistant

Now need edit telegram_bot. Add helpers after USER_HELP maybe. Need ensure admins also appUser. Currently appUser only non-admin. We'll change to always get/create app user; if admin and not is_admin maybe update. Need avoid using appUser null.

Add keyboard helpers. Then button normalization after command? Need use text button label to command if exact. But for state choices text is like "🎯 xxxxx — title", need handler state.

State shape {step:'new_flow_source'|'new_flow_target'|'new_flow_new_target'|..., draft:{source_id, channel}} maybe.

Simplify: "➕ Nouveau flux" starts guided flow. It asks source. User enters source. Then we add/get source, set state target and show target keyboard. If no targets, ask to send target. If user presses "➕ Ajouter une cible", state new_flow_target_input. If user presses target button, parse target ID from prefix maybe using code tag? Reply keyboard doesn't support HTML. Use label `🎯 ${shortId(t.id)} - title`. Then state handler picks regex `[0-9a-f]{8}`.

Need add reply with menus.

Button mapping:
- user buttons:
`➕ Nouveau flux`,`🔁 Mes flux`
`📡 Mes sources`,`🎯 Mes cibles`
`📊 Activité`,`👤 Mon compte`
`💳 Premium`,`❓ Aide`
- admin buttons:
`🛠 Menu admin`, `▶️ Lancer agrégation`
`📊 Stats`, `📡 Sources globales`
`📰 Articles`, `🧹 Nettoyage`
`👤 Menu utilisateur`

Need since admin currently commands are global. But admin can also test user menu? Hmm admin gate skips user commands; user commands only !admin. Maybe admin needs admin menu only. User flow for admins? The user said admin buttons too. Fine.

Map button to commands early:
if text == button, command = '/...'. But const command currently. Change to let normalizedText. I'll implement simple function commandFromButton(text, adminMode). Then parts use normalizedText. Need if state input shouldn't translate source channel unless it matches buttons. If state exists and no slash command and not button? handle state before normal command.

Need helpers for state and flow.

Need edit after findOwnedRecord maybe.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["📡 Sources globales", "📰 Articles"],
        ["🧹 Nettoyage", "❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/clearall",
    "❓ Aide admin": "/help",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const parts = text.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  const adminMode = isAdmin(chatId) || isAdmin(userId);

  try {
    // ── Ensure app user exists for non-admins ─────────────────────────────
    let appUser: any = null;
    if (!adminMode) {
      appUser = await getOrCreateAppUser(supabase, userId, username);
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`);
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

<thinking>
**Considering command parsing needs**

I see that I need to focus on editing the command parsing and incorporating appUser for admins. It looks like I'll also need to implement a guided handler. I realize I should insert this after adminMode to ensure everything works smoothly together. There's a lot to consider to make sure the system operates efficiently! Let's get going on this edit and see how it all ties together.
</thinking>

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["📡 Sources globales", "📰 Articles"],
        ["🧹 Nettoyage", "❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/clearall",
    "❓ Aide admin": "/help",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`);
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["📡 Sources globales", "📰 Articles"],
        ["🧹 Nettoyage", "❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "❓ Aide admin": "/help",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nSource → Cible configurées.\n\nTu peux voir tes flux avec <b>🔁 Mes flux</b>.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${target.chat_id}</b>\n\n⚠️ Vérifie que le bot est admin du canal cible.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP);
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`);
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["📡 Sources globales", "📰 Articles"],
        ["🧹 Nettoyage", "❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
        "🗑 DB seulement": "/deleteall",
        "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [["🗑 DB seulement", "🔥 Tout supprimer"], ["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nSource → Cible configurées.\n\nTu peux voir tes flux avec <b>🔁 Mes flux</b>.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${target.chat_id}</b>\n\n⚠️ Vérifie que le bot est admin du canal cible.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            else { await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`, adminMenuMarkup()); }
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
                  chatId,
                  `✅ TOUS les articles ont été supprimés de la base de données.`,
                  adminMenuMarkup(),
                );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

<thinking>
**Identifying syntax errors**

I noticed there's a syntax error with "else { else {" that I need to fix. I should inspect the lines around that part to see what's going wrong. Additionally, the indentation in the map looks a bit off, but I guess it's manageable for now. I’ll run some diagnostics to get more insights. Let’s figure this out step by step to ensure everything works smoothly!
</thinking>

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 110-135)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
        "🗑 DB seulement": "/deleteall",
        "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1270-1315)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            else { await reply(chatId, `✅ Article supprimé:\n<i>${a.title}</i>`, adminMenuMarkup()); }
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["📡 Sources globales", "📰 Articles"],
        ["🧹 Nettoyage", "❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nSource → Cible configurées.\n\nTu peux voir tes flux avec <b>🔁 Mes flux</b>.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${target.chat_id}</b>\n\n⚠️ Vérifie que le bot est admin du canal cible.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

<thinking>
**Adding admin features**

I'm considering adding a button labeled "➕ Source globale" to the admin menu. The command /newglobalsource should set the state to admin_global_source_input while handling the admin state. It's important to note that admins are now app_users. I think I'll need to implement an admin state handler before the admin gate, possibly after cleaning or canceling the admin state. This should all come together to create a global source while being integrated into the admin menu.
</thinking>

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nSource → Cible configurées.\n\nTu peux voir tes flux avec <b>🔁 Mes flux</b>.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${target.chat_id}</b>\n\n⚠️ Vérifie que le bot est admin du canal cible.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Je vérifie maintenant que le fichier reste cohérent après l’ajout des scénarios guidés et des boutons.

**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/migrations/20260511201000_add_bot_state_to_app_users.sql`**
Status: Completed

File doesn't have errors or warnings!

## Assistant

C’est fait. J’ai arrangé le fonctionnement du bot avec des **boutons en bas** et des **scénarios guidés**, côté utilisateur et côté admin.

## Ce qui a été ajouté

### 1. Boutons côté utilisateur

Le menu utilisateur affiche maintenant des boutons en bas :

- `➕ Nouveau flux`
- `🔁 Mes flux`
- `📡 Mes sources`
- `🎯 Mes cibles`
- `📊 Activité`
- `👤 Mon compte`
- `💳 Premium`
- `❓ Aide`

Donc l’utilisateur n’est plus obligé de taper les commandes manuellement.

---

## 2. Scénario guidé : création d’un nouveau flux

Quand l’utilisateur clique sur :

`➕ Nouveau flux`

Le bot lance un parcours guidé.

### Étape 1 — Demander le canal source

Le bot demande :

> Envoie maintenant le canal source à surveiller.

L’utilisateur peut répondre par exemple :

- `@canal_source`
- `https://t.me/canal_source`

Le bot ajoute automatiquement cette source dans `user_sources`.

---

### Étape 2 — Choisir ou ajouter un canal cible

Après la source, le bot regarde si l’utilisateur a déjà des canaux cibles.

#### Cas 1 : l’utilisateur a déjà des cibles

Le bot affiche des boutons avec ses canaux cibles existants.

Exemple :

- `🎯 a1b2c3d4 - Mon Canal`
- `🎯 e5f6g7h8 - Business News`
- `➕ Ajouter une cible`
- `❌ Annuler`

L’utilisateur clique sur le canal cible voulu.

Ensuite le bot crée automatiquement le flux.

---

#### Cas 2 : l’utilisateur n’a pas encore de canal cible

Le bot demande directement :

> Envoie le canal cible où republier.

L’utilisateur peut envoyer :

- `@mon_canal`
- `-100xxxxxxxxxx`

Le bot ajoute la cible dans `user_targets`, puis crée automatiquement le flux.

---

### Étape 3 — Création automatique du flux

Le bot crée le lien :

> source → cible

Avec les filtres par défaut :

- blocage pub activé ;
- texte autorisé ;
- photos autorisées ;
- vidéos autorisées ;
- albums autorisés ;
- reformulation IA désactivée par défaut.

Le bot répond ensuite :

> Flux créé avec succès.

Puis il remet le menu utilisateur normal.

---

## 3. Bouton Annuler

J’ai ajouté :

`❌ Annuler`

Pendant les scénarios guidés.

Quand l’utilisateur clique dessus :

- l’action en cours est annulée ;
- l’état temporaire est supprimé ;
- le menu normal revient.

---

## 4. Stockage de l’état de conversation

J’ai ajouté une nouvelle migration :

`news/supabase/migrations/20260511201000_add_bot_state_to_app_users.sql`

Elle ajoute la colonne :

`bot_state JSONB`

dans `app_users`.

Cette colonne sert à savoir où l’utilisateur se trouve dans le scénario :

- il est en train d’ajouter une source ;
- il doit choisir une cible ;
- il doit envoyer une nouvelle cible ;
- etc.

Donc le bot peut gérer des conversations étape par étape.

---

## 5. Boutons côté admin

Le menu admin affiche maintenant aussi des boutons en bas :

- `▶️ Lancer agrégation`
- `📊 Stats`
- `➕ Source globale`
- `📡 Sources globales`
- `📰 Articles`
- `🧹 Nettoyage`
- `❓ Aide admin`

---

## 6. Scénario guidé admin : ajouter une source globale

Quand l’admin clique sur :

`➕ Source globale`

Le bot demande :

> Envoie le canal Telegram source à ajouter au système global.

L’admin peut envoyer :

- `@canal_source`
- `https://t.me/canal_source`

Le bot ajoute alors la source dans la table globale `sources`.

---

## 7. Scénario admin : nettoyage

Quand l’admin clique sur :

`🧹 Nettoyage`

Le bot affiche des boutons sensibles :

- `🗑 DB seulement`
- `🔥 Tout supprimer`
- `❌ Annuler`

`🗑 DB seulement` correspond à `/deleteall`.

`🔥 Tout supprimer` correspond à `/clearall`.

Donc l’admin peut gérer ces actions sans taper la commande.

---

## 8. Admin aussi enregistré dans `app_users`

J’ai aussi ajusté le fonctionnement pour que les admins soient également enregistrés dans `app_users`.

Pourquoi ?

Parce que maintenant le bot a besoin de stocker un `bot_state` même pour les scénarios admin.

Si un admin démarre le bot, il sera créé dans `app_users`, puis marqué `is_admin = true`.

---

## Fichiers modifiés

J’ai modifié :

- `news/supabase/functions/telegram_bot/index.ts`

J’ai ajouté :

- `news/supabase/migrations/20260511201000_add_bot_state_to_app_users.sql`

---

## Diagnostic

Il reste seulement le diagnostic connu :

`Cannot find module 'https://esm.sh/@supabase/supabase-js@2'`

C’est lié à l’analyse TypeScript locale qui ne comprend pas les imports URL Deno/Supabase.

Ce n’est pas une erreur de logique du code Supabase Edge Function.

---

## À faire pour envoyer sur Supabase

Comme on a ajouté une nouvelle migration, il faut l’appliquer avant de redéployer le bot.

Ordre recommandé :

1. appliquer les migrations ;
2. déployer `telegram_bot`.

Commandes :

- `npx supabase db push`
- `npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

Si tu veux éviter les anciennes migrations dangereuses, applique manuellement cette migration dans le SQL Editor Supabase :

`news/supabase/migrations/20260511201000_add_bot_state_to_app_users.sql`

Ensuite déploie seulement la fonction :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

---

## Fonctionnement actuel attendu

Maintenant, un utilisateur peut faire :

1. ouvrir le bot ;
2. cliquer sur `➕ Nouveau flux` ;
3. envoyer le canal source ;
4. choisir ou ajouter le canal cible ;
5. le bot crée le flux automatiquement ;
6. l’utilisateur peut gérer ensuite avec les boutons :
   - `🔁 Mes flux`
   - `📡 Mes sources`
   - `🎯 Mes cibles`
   - `📊 Activité`

Côté admin, il peut utiliser les boutons pour :

- lancer l’agrégation ;
- voir les stats ;
- ajouter une source globale ;
- voir les articles ;
- nettoyer la base ou tout supprimer.

## User

actuellement, c'est l'utilisateur qui doit chercher commen mettre le bot admin de son canal. il ya une possibilite de lui facilliter la tache ou meme le nommer admin depuis le bot? tres simplement

## Assistant

Oui, on peut **lui faciliter beaucoup la tâche**, mais il y a une limite importante :

## Peut-on nommer automatiquement le bot admin depuis le bot ?

**Non, pas directement.**

Telegram ne permet pas à un bot de se nommer lui-même administrateur d’un canal ou d’un groupe.

Pour des raisons de sécurité, seul :

- le propriétaire du canal ;
- ou un admin qui a déjà les droits nécessaires ;

peut ajouter le bot comme administrateur.

Donc le bot ne peut pas faire tout seul :

> “Je deviens admin de ton canal.”

---

## Ce qu’on peut faire très simplement

On peut rendre le parcours beaucoup plus facile avec un bouton.

### Option simple recommandée

Quand l’utilisateur doit ajouter un canal cible, le bot peut afficher un message comme :

> Pour que je puisse publier dans ton canal, tu dois m’ajouter comme administrateur.

Puis afficher un bouton :

`➕ Ajouter le bot à mon canal`

Ce bouton peut ouvrir directement l’interface Telegram pour choisir un canal/groupe où ajouter le bot.

Telegram supporte ce type de lien :

```/dev/null/text.txt#L1
https://t.me/NOM_DU_BOT?startchannel=setup
```

ou selon le cas :

```/dev/null/text.txt#L1
https://t.me/NOM_DU_BOT?startgroup=setup
```

Pour un canal, `startchannel` est le plus intéressant.

L’utilisateur clique, Telegram lui propose d’ajouter le bot à un canal.

---

## Encore mieux : demander les droits admin nécessaires

On peut aussi utiliser un lien avec les permissions demandées.

Exemple :

```/dev/null/text.txt#L1
https://t.me/NOM_DU_BOT?startchannel=setup&admin=post_messages+edit_messages+delete_messages
```

Pour ton cas, le bot a surtout besoin de :

- publier des messages ;
- publier des médias ;
- éventuellement supprimer les messages qu’il a publiés.

Donc les droits utiles sont :

```/dev/null/text.txt#L1
post_messages
edit_messages
delete_messages
```

Le lien peut être affiché dans un bouton inline.

---

## Ce que je te propose d’ajouter dans le bot

Quand l’utilisateur ajoute une cible avec :

- `➕ Nouveau flux`
- ou `/addtarget`

Le bot peut répondre :

> Étape importante : ajoute-moi comme admin dans ton canal cible.

Avec deux boutons :

- `➕ Ajouter le bot comme admin`
- `✅ J’ai ajouté le bot`

Ensuite :

1. l’utilisateur clique sur `➕ Ajouter le bot comme admin` ;
2. Telegram ouvre l’interface pour ajouter le bot au canal ;
3. l’utilisateur revient dans le bot ;
4. il clique sur `✅ J’ai ajouté le bot` ;
5. le bot vérifie avec `getChatMember` ou `getChat` si possible ;
6. si c’est bon, on continue ;
7. sinon le bot explique quoi corriger.

---

## Peut-on vérifier que le bot est admin ?

Oui, si l’utilisateur donne le canal cible en `@username` ou `chat_id`, le bot peut appeler :

```/dev/null/text.txt#L1
getChatMember
```

Pour vérifier son propre statut dans le canal.

Le bot doit vérifier que son statut est :

- `administrator`

ou éventuellement :

- `creator`

Pour récupérer l’ID du bot, on peut utiliser :

```/dev/null/text.txt#L1
getMe
```

Puis vérifier :

```/dev/null/text.txt#L1
getChatMember?chat_id=@canal&user_id=BOT_ID
```

---

## Parcours utilisateur très simple

Je te propose ce scénario :

### 1. L’utilisateur clique sur `➕ Nouveau flux`

Le bot demande le canal source.

### 2. L’utilisateur donne la source

Exemple :

```/dev/null/text.txt#L1
@source_news
```

### 3. Le bot demande la cible

Exemple :

```/dev/null/text.txt#L1
@mon_canal
```

### 4. Le bot répond

> Pour publier dans `@mon_canal`, ajoute-moi comme administrateur.

Boutons :

- `➕ Ajouter le bot à mon canal`
- `✅ J’ai ajouté le bot`
- `❌ Annuler`

### 5. L’utilisateur clique sur le bouton Telegram

Telegram l’aide à ajouter le bot.

### 6. L’utilisateur revient et clique sur `✅ J’ai ajouté le bot`

Le bot vérifie.

Si OK :

> Super, le bot est admin. Ton flux est prêt.

Si pas OK :

> Je ne suis pas encore admin de ce canal. Vérifie que tu m’as ajouté avec le droit de publier.

---

## Ce qu’il me faut pour l’ajouter proprement

Il me faut connaître le **username exact du bot**, par exemple :

```/dev/null/text.txt#L1
@MonBotName
```

On peut aussi le récupérer automatiquement avec `getMe`, donc ce n’est pas obligatoire de le mettre en variable d’environnement.

Mais pour générer le lien, il faut le username du bot.

Le bot peut appeler Telegram `getMe` et construire le lien automatiquement.

---

## Exemple de bouton inline à envoyer

Le bot peut envoyer un bouton de ce type :

```/dev/null/json.txt#L1-12
{
  "reply_markup": {
    "inline_keyboard": [
      [
        {
          "text": "➕ Ajouter le bot comme admin",
          "url": "https://t.me/NOM_DU_BOT?startchannel=setup&admin=post_messages+edit_messages+delete_messages"
        }
      ]
    ]
  }
}
```

Ce bouton n’apparaît pas comme clavier en bas, mais sous le message, ce qui est mieux pour un lien.

---

## Conclusion

Donc :

- **le bot ne peut pas se nommer admin tout seul** ;
- mais on peut ajouter un bouton qui ouvre directement l’interface Telegram pour l’ajouter comme admin ;
- ensuite le bot peut vérifier si l’utilisateur l’a bien ajouté ;
- ça rend l’expérience beaucoup plus simple.

Je te recommande qu’on ajoute maintenant :

1. le bouton `➕ Ajouter le bot comme admin` ;
2. la vérification `✅ J’ai ajouté le bot` ;
3. l’étape guidée dans le scénario `Nouveau flux`.

## User (checkpoint)

ok

## Assistant

Je vais ajouter l’étape guidée : après le choix du canal cible, le bot donnera un bouton/lien pour ajouter le bot comme admin, puis un bouton “✅ J’ai ajouté le bot” qui déclenche une vérification avant de créer le flux.

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1-260)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)

```


## Assistant

Need add admin verify functions after createFlow maybe. Also commandFromButton map add "✅ J’ai ajouté le bot". We need inline URL to add bot admin. Need get bot username via getMe. Helper getBotAdminUrl. Check membership. For channels maybe getChatMember can work only if bot has access? If not admin getChatMember maybe returns not enough? Actually if bot is member maybe ok; channels if not member, fail.

Need after target selection / input, instead of createFlow immediately, set state await_bot_admin with draft {source_id,target_id} and prompt. Create helper promptVerifyTargetAdmin.

Need handle command "/verifytargetadmin" from button. Add reply keyboard with "✅ J’ai ajouté le bot".

Since Telegram button URL cannot be keyboard; send inline + keyboard. reply supports extra with reply_markup one object only, cannot have both inline and keyboard. Telegram sendMessage only one reply_markup. Can send first inline then second keyboard? Simpler: Use inline button URL in message plus instruct to press bottom "✅..."? But cannot show both in one message. Maybe use inline_keyboard with two buttons URL and callback? Callback not handled. Could use URL + "J'ai ajouté" as bottom keyboard in follow-up message. We'll implement helper sends two messages: promptAdminInstructions: first reply with inline URL. second reply with keyboard including "✅ J’ai ajouté le bot", "❌ Annuler".

Need helper sendBotAdminPrompt(chatId,target). It calls getMe.

Need direct command fromButton.

Implement `commandFromButton`: "✅ J’ai ajouté le bot": "/verifytargetadmin".

Add helpers:
async getBotInfo(): username,id. request BASE/getMe.
botAdminLink(botUsername): https://t.me/${username}?startchannel=setup&admin=post_messages+edit_messages+delete_messages
async isBotAdminInChat(chatId): getMe then getChatMember with chat_id, user_id; ok admin/creator and can_post_messages not false maybe. If false and chatId starts @, perhaps fail. Return {ok, reason}. 
async sendAdminPrompt(chatId,targetChatId) { getMe, link; await reply with inline_markup; await reply with keyboard; }

Need note if user already added? We should verify after target input and if ok create immediately? Good. But process says ask verify. Simpler always prompt, then user clicks verify.

Need modify flow choose_target: after selecting target, set state new_flow_verify_target and send prompt. 
target_input same.
Maybe add handler for `/verifytargetadmin` regardless buttonCommand and state. Since buttonCommand not null, current state handler requires !buttonCommand and won't catch. Add before state text handler:
if !adminMode && command === '/verifytargetadmin' && appUser.bot_state?.step==='new_flow_verify_target' { verify, create flow }. 
Also if user sends that text button, commandFromButton maps.
Add to user menu? no.

Need ensure "➕ Ajouter une cible" is not command mapping. Current state handler condition !buttonCommand; if text is "➕ Ajouter une cible" buttonCommand null, fine. 
Need implement.



**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  return `https://t.me/${username}?startchannel=setup&admin=post_messages+edit_messages+delete_messages`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nSource → Cible configurées.\n\nTu peux voir tes flux avec <b>🔁 Mes flux</b>.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${target.chat_id}</b>\n\n⚠️ Vérifie que le bot est admin du canal cible.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need modify flow state handlers. Read relevant segment.

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 650-870)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nSource → Cible configurées.\n\nTu peux voir tes flux avec <b>🔁 Mes flux</b>.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const result = await createFlowForUser(
          supabase,
          appUser,
          draft.source_id,
          target.id,
        );
        if (result.error || !result.data) {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `❌ ${result.error || "Impossible de créer le flux."}`,
            userMenuMarkup(),
          );
        } else {
          await clearBotState(supabase, appUser.id);
          await reply(
            chatId,
            `✅ <b>Flux créé avec succès</b>\n\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${target.chat_id}</b>\n\n⚠️ Vérifie que le bot est admin du canal cible.`,
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  return `https://t.me/${username}?startchannel=setup&admin=post_messages+edit_messages+delete_messages`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need also /addtarget command could prompt? But user's Ok likely guided only. Should modify /addtarget direct to mention add admin button maybe. Not necessary but useful. Also /addflow direct currently creates without verifying. maybe okay, but user asks simplify. Could be okay.

Need diagnostics.

**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


## Assistant

C’est bon, j’ai ajouté l’assistance pour que l’utilisateur ajoute facilement le bot comme admin de son canal.

## Ce qui a été ajouté

Dans `news/supabase/functions/telegram_bot/index.ts`, j’ai ajouté :

### 1. Bouton `➕ Ajouter le bot comme admin`

Quand l’utilisateur crée un nouveau flux et arrive à l’étape du canal cible, le bot ne crée plus directement le flux.

Il affiche d’abord une étape :

> Pour publier dans ce canal, ajoute le bot comme administrateur.

Puis il affiche un bouton inline :

`➕ Ajouter le bot comme admin`

Ce bouton ouvre Telegram avec un lien du type :

`https://t.me/NOM_DU_BOT?startchannel=setup&admin=post_messages+edit_messages+delete_messages`

Donc l’utilisateur peut plus facilement ajouter le bot à son canal.

---

## 2. Bouton `✅ J’ai ajouté le bot`

Après le bouton d’ajout, le bot affiche aussi un bouton en bas :

`✅ J’ai ajouté le bot`

Quand l’utilisateur clique dessus, le bot vérifie réellement s’il est admin du canal cible.

---

## 3. Vérification automatique avec Telegram

J’ai ajouté une vérification via l’API Telegram :

- `getMe` pour récupérer l’ID et le username du bot ;
- `getChatMember` pour vérifier si le bot est admin dans le canal cible.

Le bot accepte le statut :

- `administrator`
- `creator`

Si le bot n’est pas admin, il répond :

> Je ne suis pas encore admin de ce canal.

Et il demande de recommencer après avoir ajouté le bot.

---

## 4. Création du flux seulement après vérification

Avant, le flux était créé directement après le choix de la cible.

Maintenant, dans le scénario guidé :

1. l’utilisateur clique sur `➕ Nouveau flux` ;
2. il envoie le canal source ;
3. il choisit ou ajoute le canal cible ;
4. le bot lui donne le bouton pour ajouter le bot comme admin ;
5. l’utilisateur clique sur `✅ J’ai ajouté le bot` ;
6. le bot vérifie ;
7. si c’est OK, le flux est créé.

Donc ça évite de créer des flux vers des canaux où le bot ne peut pas publier.

---

## 5. Nouvel état conversationnel ajouté

J’ai ajouté l’étape :

`new_flow_verify_target`

Elle garde temporairement :

- `source_id`
- `target_id`
- `target_chat_id`

Le flux n’est créé que quand le bot confirme qu’il est bien admin du canal cible.

---

## Limite Telegram importante

Le bot ne peut toujours pas se nommer admin tout seul.

Mais maintenant l’utilisateur est guidé avec un bouton direct, donc il n’a plus besoin de chercher manuellement comment faire.

---

## À redéployer

Tu dois redéployer seulement la fonction :

`telegram_bot`

Commande :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

Si tu n’as pas encore appliqué la migration `bot_state`, applique aussi :

`news/supabase/migrations/20260511201000_add_bot_state_to_app_users.sql`

Sinon le scénario guidé ne pourra pas stocker l’étape en cours.

---

## Diagnostic

Il reste uniquement le diagnostic local connu :

`Cannot find module 'https://esm.sh/@supabase/supabase-js@2'`

C’est lié à l’environnement Deno/Supabase dans l’éditeur local, pas à la logique du bot.

## User (checkpoint)

un utilisateur doit aussi pouvoir  desaciver un flux. aussi, lorque on veux ajouter le bot, par defaut, tout les roles sont activer. c'est l'utilisateur qui peux desaciver si il veux

## Assistant

<thinking>
**Evaluating markup adjustments**

I'm thinking about adding the flowChoiceMarkup helper after the targetChoiceMarkup. It might make sense to include scenarios before the state text handler, similar to what's done in newflow. I also want to introduce commands for /pauseflowbutton and /resumeflowbutton. That includes state steps like choose_flow_pause and choose_flow_resume, allowing flow selection through a label with a shortId. Finally, I'll need to update and reply while ensuring botAdminUrl has all roles. I think I need to edit this all!
</thinking>

Je vais ajouter des boutons pour désactiver/réactiver un flux facilement, puis changer le lien d’ajout admin pour demander tous les rôles par défaut. Telegram affichera ensuite les permissions à l’utilisateur, qu’il pourra retirer s’il le souhaite.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooser resumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need button state handling has !buttonCommand condition, when choose flow button has no command map? label starts 🔁 shortId, commandFromButton returns null, good. 

Need mention direct /pauseflow exists.

But should maybe add all roles URL. Unsure invalid admin params for startchannel? Telegram Bot Admin rights maybe include specific parameters: `admin=change_info+post_messages+edit_messages+delete_messages+invite_users+restrict_members+pin_messages+promote_members+manage_video_chats` Maybe `manage_chat`, stories, topics may not be valid? Telegram deep link admin parameter supports: change_info, post_messages, edit_messages, delete_messages, invite_users, restrict_members, pin_messages, promote_members, manage_video_chats, anonymous, manage_chat? Need not include invalid parameters? Could link fail if invalid? Hmm user asked by default all roles. Include all known maybe okay. Let's search? No web needed. Safer include official widely-supported rights: `change_info+post_messages+edit_messages+delete_messages+invite_users+restrict_members+pin_messages+promote_members+manage_video_chats`. For channels topics don't apply. Stories might be newer not sure. If invalid, potentially ignored or fail. "tout les roles activés" channel roles mainly those. Let's leave? Maybe manage_chat invalid? Deep linking docs? I recall `admin=change_info+post_messages+edit_messages+delete_messages+invite_users+restrict_members+pin_messages+promote_members+manage_video_chats+anonymous+manage_chat`. Not sure. I might keep conservative all standard. But if user wants all roles, better all available. However if Telegram rejects invalid roles, link could be bad. Maybe use `startchannel` and `admin=change_info+post_messages+edit_messages+delete_messages+invite_users+restrict_members+pin_messages+promote_members+manage_video_chats` exactly. Users can add more manually. But "par defaut tout les roles sont activer." Hmm "tout" within supported deep link. I'd include `manage_chat`? maybe default role. Let's not risk.

Need run diagnostics.

**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


## Assistant

C’est fait.

## 1. L’utilisateur peut maintenant désactiver/réactiver un flux avec des boutons

J’ai ajouté deux boutons dans le menu utilisateur :

- `⏸ Désactiver flux`
- `▶️ Réactiver flux`

### Fonctionnement

Quand l’utilisateur clique sur :

`⏸ Désactiver flux`

Le bot liste uniquement ses flux actifs sous forme de boutons.

Exemple :

`🔁 a1b2c3d4 - @source → @cible`

L’utilisateur clique sur le flux, et le bot le désactive.

Quand l’utilisateur clique sur :

`▶️ Réactiver flux`

Le bot liste uniquement ses flux désactivés.

L’utilisateur clique sur le flux, et le bot le réactive.

Donc l’utilisateur n’a plus besoin de taper :

- `/pauseflow <id>`
- `/resumeflow <id>`

Mais ces commandes restent disponibles.

---

## 2. Le lien “Ajouter le bot comme admin” demande maintenant tous les rôles par défaut

J’ai modifié la génération du lien Telegram.

Avant, le lien demandait seulement :

- publier ;
- modifier ;
- supprimer.

Maintenant, le lien demande les permissions admin disponibles par défaut :

- changer les infos ;
- publier des messages ;
- modifier des messages ;
- supprimer des messages ;
- inviter des utilisateurs ;
- restreindre des membres ;
- épingler des messages ;
- promouvoir des membres ;
- gérer les chats vidéo ;
- gérer le chat ;
- gérer les topics ;
- publier/modifier/supprimer les stories.

L’utilisateur verra ces permissions dans Telegram et pourra en désactiver certaines s’il veut.

Important : même si tout est demandé par défaut, l’utilisateur garde le contrôle final dans l’interface Telegram.

---

## 3. Fichier modifié

J’ai modifié :

`news/supabase/functions/telegram_bot/index.ts`

---

## 4. Redéploiement nécessaire

Tu dois redéployer seulement la fonction du bot :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

Pas besoin de nouvelle migration pour cette modification.

---

## 5. Diagnostic

Il reste seulement le diagnostic local connu :

`Cannot find module 'https://esm.sh/@supabase/supabase-js@2'`

C’est normal dans l’éditeur local avec les imports Deno/Supabase.

## User

ok j'ai mis en production et tout fonctionne bien. maintenant,  dit moi ce qu'un utilisateur peux faire

## Assistant

Actuellement, un utilisateur peut utiliser le bot comme un **outil d’automatisation Telegram** pour récupérer des contenus depuis des canaux sources et les republier vers ses propres canaux cibles.

Voici ce qu’il peut faire concrètement.

## 1. Démarrer le bot

Quand il lance le bot avec :

`/start`

Le bot crée automatiquement son compte utilisateur dans `app_users`.

Il voit ensuite le menu avec les boutons :

- `➕ Nouveau flux`
- `🔁 Mes flux`
- `⏸ Désactiver flux`
- `▶️ Réactiver flux`
- `📡 Mes sources`
- `🎯 Mes cibles`
- `📊 Activité`
- `👤 Mon compte`
- `💳 Premium`
- `❓ Aide`

---

## 2. Créer un nouveau flux automatiquement

Le bouton principal est :

`➕ Nouveau flux`

Avec ce scénario :

### Étape 1

Le bot demande le **canal source** à surveiller.

L’utilisateur peut envoyer :

- `@canal_source`
- `https://t.me/canal_source`

Le bot enregistre ce canal comme source personnelle de l’utilisateur.

---

### Étape 2

Le bot demande ou propose le **canal cible**.

Si l’utilisateur a déjà des canaux cibles, le bot les affiche sous forme de boutons.

Sinon, il lui demande d’en envoyer un :

- `@mon_canal`
- `-100xxxxxxxxxx`

---

### Étape 3

Le bot demande à l’utilisateur d’ajouter le bot comme admin du canal cible.

Il affiche un bouton :

`➕ Ajouter le bot comme admin`

Ce bouton ouvre Telegram pour faciliter l’ajout du bot comme administrateur.

Par défaut, le lien demande tous les rôles admin disponibles. L’utilisateur peut désactiver certains droits s’il veut.

---

### Étape 4

L’utilisateur revient dans le bot et clique :

`✅ J’ai ajouté le bot`

Le bot vérifie qu’il est bien admin du canal cible.

Si c’est bon, le flux est créé.

---

## 3. Ajouter une source manuellement

L’utilisateur peut ajouter une source avec :

`/addsource @canal`

ou via le scénario guidé.

Une source correspond au canal Telegram d’où le bot va récupérer les publications.

Exemples :

- `@actualites`
- `https://t.me/actualites`

---

## 4. Voir ses sources

Avec le bouton :

`📡 Mes sources`

ou la commande :

`/sources`

Il voit la liste de ses canaux sources.

Chaque source a un ID court, par exemple :

`a1b2c3d4`

---

## 5. Désactiver une source

Avec :

`/delsource <id>`

Exemple :

`/delsource a1b2c3d4`

Le bot désactive la source et désactive aussi les flux liés à cette source.

---

## 6. Ajouter un canal cible

L’utilisateur peut ajouter un canal cible avec :

`/addtarget @mon_canal`

ou :

`/addtarget -100xxxxxxxxxx`

Un canal cible est le canal où le bot republie les contenus.

Le bot essaie de vérifier le canal avec Telegram. Si le bot n’a pas encore accès, il prévient l’utilisateur qu’il doit l’ajouter comme admin.

---

## 7. Voir ses canaux cibles

Avec le bouton :

`🎯 Mes cibles`

ou la commande :

`/targets`

Il voit ses canaux cibles enregistrés.

---

## 8. Désactiver un canal cible

Avec :

`/deltarget <id>`

Exemple :

`/deltarget e5f6g7h8`

Le bot désactive le canal cible et désactive aussi les flux liés à cette cible.

---

## 9. Voir ses flux

Avec le bouton :

`🔁 Mes flux`

ou la commande :

`/flows`

Il voit ses flux.

Un flux correspond à :

> canal source → canal cible

Exemple :

`@source_news → @mon_canal`

Chaque flux montre aussi :

- s’il est actif ou en pause ;
- le dernier message traité ;
- une erreur éventuelle.

---

## 10. Désactiver un flux

Avec le bouton :

`⏸ Désactiver flux`

Le bot affiche les flux actifs de l’utilisateur.

L’utilisateur clique sur celui qu’il veut désactiver.

Le flux est mis en pause.

Le bot ne récupérera plus de nouveaux contenus pour ce flux.

Il peut aussi utiliser :

`/pauseflow <id>`

---

## 11. Réactiver un flux

Avec le bouton :

`▶️ Réactiver flux`

Le bot affiche les flux désactivés de l’utilisateur.

L’utilisateur clique sur celui qu’il veut réactiver.

Le flux recommence à fonctionner.

Il peut aussi utiliser :

`/resumeflow <id>`

---

## 12. Configurer les filtres d’un flux

L’utilisateur peut gérer les filtres avec des commandes.

### Voir les filtres

`/filters <flow_id>`

---

### Ajouter des mots obligatoires

`/include <flow_id> mot1,mot2,mot3`

Exemple :

`/include a1b2c3d4 sport,football,coupe`

Le bot publiera seulement les messages contenant au moins un de ces mots.

---

### Ajouter des mots interdits

`/exclude <flow_id> mot1,mot2,mot3`

Exemple :

`/exclude a1b2c3d4 crypto,pari,casino`

Le bot ignorera les messages contenant ces mots.

---

### Bloquer les publicités

`/blockads <flow_id> on`

ou :

`/blockads <flow_id> off`

Par défaut, c’est activé.

---

### Publier seulement les médias

`/mediaonly <flow_id> on`

Si activé, le bot ignore les messages texte sans média.

---

### Autoriser ou bloquer les albums

`/allowalbums <flow_id> on`

ou :

`/allowalbums <flow_id> off`

Par défaut, les albums sont autorisés.

Le bot peut republier un album d’images en une publication groupée Telegram.

---

### Activer la reformulation IA

`/rewriteai <flow_id> on`

ou :

`/rewriteai <flow_id> off`

Si activé, le bot peut reformuler les contenus avec Gemini.

---

### Ajouter une signature

`/signature <flow_id> texte`

Exemple :

`/signature a1b2c3d4 @moncanal`

Le texte sera ajouté à la fin des publications.

---

## 13. Voir son activité

Avec le bouton :

`📊 Activité`

ou la commande :

`/activity`

L’utilisateur voit les dernières actions du bot :

- publication réussie ;
- publication ignorée ;
- raison du filtrage ;
- nombre de médias ;
- date ;
- aperçu du texte.

---

## 14. Voir son compte

Avec le bouton :

`👤 Mon compte`

ou la commande :

`/me`

Il voit :

- son plan actuel ;
- son usage du jour ;
- sa limite ;
- la date d’expiration s’il est Premium.

---

## 15. Voir l’offre Premium

Avec le bouton :

`💳 Premium`

ou la commande :

`/subscribe`

Il voit l’offre :

- 500 FCFA / mois ;
- flux illimités ;
- plusieurs sources/cibles ;
- accès aux fonctions pro.

Pour l’instant, Nelsius Pay n’est pas encore intégré, donc l’abonnement n’est pas encore activé automatiquement par paiement.

---

## 16. Ce que fait l’agrégateur pour lui

Quand l’agrégateur tourne, il traite ses flux actifs.

Pour chaque flux :

1. lit le canal source ;
2. récupère les nouveaux messages ;
3. applique ses filtres ;
4. vérifie texte/médias/albums ;
5. republie dans son canal cible ;
6. garde l’historique dans `flow_activity` ;
7. met à jour le dernier message traité.

---

## 17. Gestion des médias

L’utilisateur peut profiter de la récupération de médias :

- texte ;
- images ;
- vidéos courtes ;
- albums d’images.

Pour les albums, le bot republie jusqu’à 10 images dans un même groupe Telegram, conformément aux limites Telegram.

---

## En résumé

Un utilisateur peut actuellement :

- créer son compte automatiquement ;
- ajouter des canaux sources ;
- ajouter des canaux cibles ;
- être guidé pour mettre le bot admin dans son canal ;
- créer un flux source → cible ;
- désactiver un flux ;
- réactiver un flux ;
- gérer ses sources ;
- gérer ses cibles ;
- configurer des filtres ;
- voir ses activités ;
- consulter son plan ;
- préparer son passage Premium.

Le bot est donc déjà utilisable comme système multi-utilisateur d’automatisation Telegram.

## User

ok quand l'admin tape /run, sa lance automatiquement  toutes les recuperation et les publication dont le cronjob n'avait pas ancore executer pour tout les utilisateurs?

## Assistant

Oui. Actuellement, quand l’admin tape :

`/run`

le bot appelle la fonction Supabase :

`aggregate_news`

Et cette fonction traite maintenant **tous les flux actifs de tous les utilisateurs**.

Donc oui :

> `/run` lance manuellement toutes les récupérations/publications en attente pour tous les utilisateurs, comme le ferait le cronjob.

## Ce que `/run` fait exactement

Quand l’admin tape `/run` :

1. `telegram_bot` appelle l’Edge Function `aggregate_news` ;
2. `aggregate_news` lit tous les `flows` actifs ;
3. pour chaque flux, il récupère :
   - l’utilisateur ;
   - la source ;
   - la cible ;
   - les filtres ;
4. il scrape le canal source Telegram ;
5. il récupère seulement les messages plus récents que `last_message_id` du flux ;
6. il applique les filtres ;
7. il republie vers le canal cible de l’utilisateur ;
8. il enregistre l’activité dans `flow_activity` ;
9. il met à jour `last_message_id`.

Donc si le cronjob n’a pas encore tourné, `/run` permet de déclencher le traitement tout de suite.

---

## Exemple simple

Imaginons :

### Utilisateur A

Flux :

`@source1 → @canalA`

Dernier message traité :

`100`

Source actuelle :

messages `101`, `102`, `103`

---

### Utilisateur B

Flux :

`@source2 → @canalB`

Dernier message traité :

`50`

Source actuelle :

messages `51`, `52`

---

Quand l’admin tape `/run`, le bot va traiter :

- pour utilisateur A :
  - `101`
  - `102`
  - `103`

- pour utilisateur B :
  - `51`
  - `52`

Puis il mettra à jour :

- flux A : `last_message_id = 103`
- flux B : `last_message_id = 52`

---

## Est-ce que ça republie les anciens messages ?

Normalement non.

Le système utilise :

`last_message_id`

sur chaque flux.

Donc il ne republie que les messages plus récents que le dernier message traité.

---

## Cas spécial : premier lancement d’un flux

Si un flux vient d’être créé et que :

`last_message_id = 0`

alors l’agrégateur ne prend pas tout l’historique du canal.

Il prend seulement les derniers messages selon :

`initial_last_n`

Actuellement par défaut :

`5`

Donc au premier lancement, il récupère au maximum les 5 derniers messages du canal source pour éviter de spammer le canal cible.

---

## Est-ce que `/run` respecte les filtres de chaque utilisateur ?

Oui.

Pour chaque flux, il applique les filtres propres au flux :

- mots obligatoires ;
- mots interdits ;
- blocage pub ;
- média seulement ;
- albums autorisés ou non ;
- reformulation IA ou non ;
- signature ;
- suppression liens/mentions si activée.

---

## Est-ce que `/run` publie dans les bons canaux cibles ?

Oui.

Chaque flux a son propre `target_id`.

L’agrégateur récupère le `chat_id` du canal cible dans `user_targets`, puis publie vers ce canal.

Donc ça ne publie pas tout dans le canal global `TELEGRAM_CHAT_ID`.

---

## Est-ce que `/run` traite aussi les flux désactivés ?

Non.

Il lit seulement les flux avec :

`is_active = true`

Donc les flux désactivés par l’utilisateur ne sont pas traités.

---

## Est-ce que `/run` traite les utilisateurs inactifs ?

Non.

Il vérifie aussi que :

- l’utilisateur est actif ;
- la source est active ;
- la cible est active.

Si un de ces éléments est inactif, le flux est ignoré.

---

## Attention importante

Si beaucoup d’utilisateurs ont beaucoup de flux, `/run` peut devenir long.

Actuellement, il traite les flux en boucle, les uns après les autres.

Donc plus tard, il faudra peut-être ajouter :

- pagination des flux ;
- batch processing ;
- limitation par exécution ;
- verrou anti-double exécution ;
- queue ;
- logs détaillés ;
- exécution par utilisateur ;
- exécution par flux.

Mais pour l’état actuel :

> Oui, `/run` sert bien à lancer manuellement tout ce que le cron n’a pas encore exécuté pour les flux actifs de tous les utilisateurs.

## User (checkpoint)

maintenant , nous allons integrer le systeme de paiement via nelsius pay pour faire les depots ?

## Assistant

<thinking>
**Looking into API integration**

I see that I need to respond in French, and it feels like we're starting the integration process. I should check the current payment tables to see if they’re already set up. I need to understand the Nelsius Pay API better, which might mean doing a web search. The user’s question hints at asking for API details or how to implement a scaffold. I should consider using web search for the Nelsius Pay documentation.
</thinking>

Oui, on peut intégrer Nelsius Pay maintenant. Je vais d’abord vérifier s’il y a une doc publique exploitable pour l’API Nelsius Pay, puis je te proposerai le flux exact à intégrer dans le bot.

**Tool Call: Searched the web: 5 results**
Status: Completed

https://pypi.org/project/nelsius-pay/

https://nelsius.com/

https://pypi.org/project/nelsius-pay/1.0.0/

https://docs.nexiopay.com/reference/registerwebhookurls

https://docs.ngenius-payments.com/reference/consuming-web-hooks

## Assistant

<thinking>
**Planning response and implementation**

I need to figure out how to respond, maybe start by planning and asking for key details. It seems like I'm going to implement something related, so I should know about the Nelsius endpoints. A recent search provided an SDK, which could be useful. Fetching documentation might help clarify things. I'll take that step to ensure I'm covering all bases as I work through this.
</thinking>

**Tool Call: Fetch `https://pypi.org/project/nelsius-pay/`**
Status: Completed

Skip to main content      Switch to mobile version                                        <div class="notification-bar notification-bar--warning" role="status">             <span class="notification-bar__icon">               <i class="fa fa-exclamation-triangle" aria-hidden="true"></i>               <span class="sr-only">Warning</span>             </span>             <span class="notification-bar__message">Some features may not work without JavaScript. Please try enabling it if you encounter problems.</span>           </div>                                                                                                           Search PyPI                                                    Search                                                              Search PyPI                                   Search                                

# nelsius-pay 1.0.0

                      

              pip install nelsius-pay                                              Copy PIP instructions                                                                          Latest version                    

Released:     Mar 21, 2026                                         

Official Python SDK for the Nelsius Developer API                                

### Navigation

                    

### Verified details      

  These details have been verified by PyPI    

###### Maintainers

                                                        nelsius                        

###### Meta

      
          -                           **Author:** Nelson Siebi                      
      
      

### Unverified details

  These details have not been verified by PyPI    

###### Project links

    
          -               Bug Tracker          
          -               Homepage          
          -               Source          
    
          

###### Meta

      
          -                           **License:** MIT License                       
          -                                         Tags                                  nelsius ,                                                  payments ,                                                  sdk ,                                                  api                                       
          -                           **Requires:** Python >=3.7                       
      
              

###### Classifiers

      
          -             **License**            
                -                                       OSI Approved :: MIT License                                   
            
          
          -             **Operating System**            
                -                                       OS Independent                                   
            
          
          -             **Programming Language**            
                -                                       Python :: 3                                   
            
          
      
                        Report project as malware                                            

## Project description

  

# Nelsius Python SDK

Official Python SDK for the Nelsius Developer API.

## Installation

```
pip install nelsius-pay

```

## Quick Start

```
from nelsius import Nelsius

# Initialize the client
nelsius = Nelsius('your_secret_key', {
    'verify_ssl': False  # Set to False for local development
})

# 1. Get available payment methods
methods = nelsius.methods('CM', 'XAF')
print("Available methods:", methods)

# 2. Create a Checkout Session
session = nelsius.checkout.create_session({
    'amount': 5000,
    'currency': 'XAF',
    'reference': 'ORDER_12345',
    'return_url': 'https://yoursite.com/success',
    'customer': {
        'email': 'client@example.com',
        'name': 'Jean Dupont'
    }
})

if session.get('success'):
    print("Checkout URL:", session['data']['checkout_url'])

# 3. Direct Charge (Mobile Money)
charge = nelsius.charge.create({
    'amount': 2000,
    'currency': 'XAF',
    'payment_method': 'orange_money_cm',
    'phone': '690000000',
    'reference': 'REF_999'
})
print("Charge Result:", charge)

```

## Documentation

For full details on the parameters and responses, visit the official documentation.  

## Project details

  

### Verified details      

  These details have been verified by PyPI    

###### Maintainers

                                                        nelsius                        

###### Meta

      
          -                           **Author:** Nelson Siebi                      
      
      

### Unverified details

  These details have not been verified by PyPI    

###### Project links

    
          -               Bug Tracker          
          -               Homepage          
          -               Source          
    
          

###### Meta

      
          -                           **License:** MIT License                       
          -                                         Tags                                  nelsius ,                                                  payments ,                                                  sdk ,                                                  api                                       
          -                           **Requires:** Python >=3.7                       
      
              

###### Classifiers

      
          -             **License**            
                -                                       OSI Approved :: MIT License                                   
            
          
          -             **Operating System**            
                -                                       OS Independent                                   
            
          
          -             **Programming Language**            
                -                                       Python :: 3                                   
            
          
      
        

##     Release history          Release notifications |       RSS feed       

                            This version                                                      

            1.0.0                     

    Mar 21, 2026                         

## Download files

    

Download the file for your platform. If you're not sure which to choose, learn more about installing packages.        

### Source Distribution  

                                          nelsius_pay-1.0.0.tar.gz        (4.1 kB         view details)         

          Uploaded     Mar 21, 2026   `Source`                      

### Built Distribution  

  

Filter files by name, interpreter, ABI, and platform.    

If you're not sure about the file name format, learn more about wheel file names.        <p class="initial-toggle-visibility"> The dropdown lists show the available interpreters, ABIs, and platforms.    </p>     <p class="initial-toggle-visibility"> Enable javascript to be able to filter the list of wheel files.    </p>     

Copy a direct link to the current filters        Copy        

                  File name                      Interpreter      Interpreter      py3          ABI      ABI      none          Platform      Platform      any                                                nelsius_pay-1.0.0-py3-none-any.whl        (5.5 kB         view details)         

          Uploaded     Mar 21, 2026   `Python 3`                    

## File details

  

    Details for the file `nelsius_pay-1.0.0.tar.gz`.     

### File metadata

      
      -         Download URL: nelsius_pay-1.0.0.tar.gz      
      - Upload date:     Mar 21, 2026   
      - Size: 4.1 kB      
      - Tags: Source
      - Uploaded using Trusted Publishing? No    
      - Uploaded via: twine/6.2.0 CPython/3.13.0
  

### File hashes

      Hashes for nelsius_pay-1.0.0.tar.gz    

      
        | Algorithm         | Hash digest         |        |    
| --- | --- | --- |          
        | SHA256        |           `be33804a5cb7d4041305bf8ac95d588a885148e0ad89b7a34ae794d08380ca2f`                 |           Copy                         |      
         | MD5        |           `f2b20e3503a9d3dedd16db2f907a6911`                 |           Copy                         |      
         | BLAKE2b-256        |           `d3d53df564fc0ae1d0673db2e088cb5399716e0242e2a62d4d623fa726197911`                 |           Copy                         |        

See more details on using hashes here.  

## File details

  

    Details for the file `nelsius_pay-1.0.0-py3-none-any.whl`.     

### File metadata

      
      -         Download URL: nelsius_pay-1.0.0-py3-none-any.whl      
      - Upload date:     Mar 21, 2026   
      - Size: 5.5 kB      
      - Tags: Python 3
      - Uploaded using Trusted Publishing? No    
      - Uploaded via: twine/6.2.0 CPython/3.13.0
  

### File hashes

      Hashes for nelsius_pay-1.0.0-py3-none-any.whl    

      
         | Algorithm         | Hash digest         |        |    
| --- | --- | --- |          
        | SHA256        |           `f988d8380131ae27bb35ed3f5f892f40da4b923ce2a1f0e9d41d721fe2ede205`                 |           Copy                         |      
         | MD5        |           `72f737ce6dfa8a8431b2308306eb6d8f`                 |           Copy                         |      
         | BLAKE2b-256        |           `8759f10f52156d1eaf91f68f43514d08689909030a3eddfb6978f7ee468ed5f4`                 |           Copy                         |        

See more details on using hashes here.                    

## Help

                    

## About PyPI

                    

## Contributing to PyPI

                    

## Using PyPI

                      

Status:         all systems operational      

Developed and maintained by the Python community, for the Python community.        Donate today!    

    "PyPI", "Python Package Index", and the blocks logos are registered trademarks of the Python Software Foundation.         

    © 2026 Python Software Foundation        Site map      

Deployed from `c4c32e5`      Switch to desktop version      
        -           English        
        -           español        
        -           français        
        -           日本語        
        -           português (Brasil)        
        -           українська        
        -           Ελληνικά        
        -           Deutsch        
        -           中文 (简体)        
        -           中文 (繁體)        
        -           русский        
        -           עברית        
        -           Esperanto        
        -           한국어        
    
    

Supported by                              AWS                        Cloud computing and Security Sponsor                                               Datadog                        Monitoring                                               Depot                        Continuous Integration                                               Fastly                        CDN                                               Google                        Download Analytics                                               Pingdom                        Monitoring                                               Sentry                        Error logging                                               StatusPage                        Status page

## Assistant

<thinking>
**Considering Nelsius Pay integration**

The user is asking about integrating payments through Nelsius Pay, particularly in relation to "dépôts." They might want to start the integration process, so I should clarify the difference between deposits and subscriptions. I have tools available to help with this and can decide how to implement the integration. I’ll need API details, but my search only provided the SDK without endpoints. It might be helpful to check GitHub’s source for those endpoints. Should I use fetch on GitHub?
</thinking>

Oui. Pour l’intégration, il faut distinguer 2 choses :

1. **paiement d’abonnement Premium** : l’utilisateur paie 500 FCFA/mois, puis son compte devient Premium ;
2. **dépôt / solde utilisateur** : l’utilisateur recharge un solde interne, puis ce solde peut servir à payer des options.

Je vais vérifier rapidement la structure de l’API Nelsius Pay depuis leur SDK pour éviter de deviner les endpoints.

**Tool Call: Fetch `https://raw.githubusercontent.com/nelson-siebi/nelsius-pay-python/main/nelsius/client.py`**
Status: Completed

import requests
from .managers.checkout import CheckoutManager
from .managers.charge import ChargeManager
from .managers.balance import BalanceManager

class Nelsius:
    """
    Nelsius Pay Python SDK Client
    """
    def __init__(self, secret_key, options=None):
        self.secret_key = secret_key
        self.options = options or {}
        self.base_url = self.options.get('base_url', 'https://api.nelsius.com')
        self.verify_ssl = self.options.get('verify_ssl', True)
        
        # Initialize Managers
        self.checkout = CheckoutManager(self)
        self.charge = ChargeManager(self)
        self.balance = BalanceManager(self)

    def request(self, method, endpoint, data=None, params=None):
        url = f"{self.base_url.rstrip('/')}/{endpoint.lstrip('/')}"
        
        headers = {
            'Authorization': f'Bearer {self.secret_key}',
            'Accept': 'application/json',
            'Content-Type': 'application/json',
            'X-SDK-Platform': 'python'
        }
        
        try:
            response = requests.request(
                method=method,
                url=url,
                json=data,
                params=params,
                headers=headers,
                verify=self.verify_ssl
            )
            
            # Simple error handling (matching Node.js/PHP behavior)
            return response.json()
            
        except requests.exceptions.RequestException as e:
            return {
                'success': False,
                'message': f'Request failed: {str(e)}',
                'error': str(e)
            }

    def methods(self, country=None, currency=None):
        """
        Get available payment methods
        """
        params = {}
        if country:
            params['country'] = country
        if currency:
            params['currency'] = currency
            
        return self.request('GET', '/api/v1/payment-methods', params=params)


**Tool Call: Fetch `https://raw.githubusercontent.com/nelson-siebi/nelsius-pay-python/main/nelsius/managers/checkout.py`**
Status: Completed

class CheckoutManager:
    def __init__(self, client):
        self.client = client

    def create_session(self, data):
        """
        Create a hosted checkout session
        """
        return self.client.request('POST', '/api/v1/checkout/sessions', data=data)

    def retrieve(self, session_id):
        """
        Retrieve a checkout session status
        """
        return self.client.request('GET', f'/api/v1/checkout/sessions/{session_id}')


**Tool Call: Fetch `https://raw.githubusercontent.com/nelson-siebi/nelsius-pay-python/main/nelsius/managers/charge.py`**
Status: Completed

class ChargeManager:
    def __init__(self, client):
        self.client = client

    def create(self, data):
        """
        Initiate a direct charge (Mobile Money, etc.)
        """
        return self.client.request('POST', '/api/v1/charges', data=data)

    def retrieve(self, charge_id):
        """
        Retrieve a charge status
        """
        return self.client.request('GET', f'/api/v1/charges/{charge_id}')


**Tool Call: Fetch `https://raw.githubusercontent.com/nelson-siebi/nelsius-pay-python/main/nelsius/managers/balance.py`**
Status: Completed

class BalanceManager:
    def __init__(self, client):
        self.client = client

    def retrieve(self):
        """
        Get merchant balance
        """
        return self.client.request('GET', '/api/v1/balance')


## Assistant

<thinking>
**Setting up subscription plan**

I’m thinking through the subscription process. Once the webhook is successful, I should set the plan to premium for one month. I need to handle the Nelsius webhook and understand the payload that likely includes the reference, status, amount, and currency. It seems crucial to make sure everything aligns for a smooth transition to the premium plan. I'll double-check these details to ensure accuracy!

**Planning database migration**

I'm looking at adding several new components to the database. I need to alter the payments table to include columns for payment_type, paid_at, checkout_url, and external_id, ensuring defaults where necessary. I also need to update the app_users table to include a wallet_balance column. Then, I'll create a new wallet_transactions table and a function for nelsius_webhook. Finally, I'll need to update the telegram_bot to handle the createCheckoutSession and related commands. This feels like a big task, but I’ll tackle it step by step.
</thinking>

Je vais intégrer la base Nelsius Pay proprement : table de paiement améliorée, wallet/dépôts, webhook Nelsius, puis commande/bouton `/subscribe` et `/deposit` côté bot pour générer un lien de paiement.

**Tool Call: supabase\\migrations\\20260511202000\_add\_nelsius\_payments\_wallet.sql**
Status: Completed

Diff: supabase\migrations\20260511202000_add_nelsius_payments_wallet.sql
```
-- Nelsius Pay integration: subscription payments and user wallet deposits

ALTER TABLE public.app_users
  ADD COLUMN IF NOT EXISTS wallet_balance INTEGER NOT NULL DEFAULT 0;

ALTER TABLE public.payments
  ADD COLUMN IF NOT EXISTS payment_type TEXT NOT NULL DEFAULT 'subscription',
  ADD COLUMN IF NOT EXISTS checkout_url TEXT,
  ADD COLUMN IF NOT EXISTS external_id TEXT,
  ADD COLUMN IF NOT EXISTS paid_at TIMESTAMP WITH TIME ZONE,
  ADD COLUMN IF NOT EXISTS metadata JSONB NOT NULL DEFAULT '{}'::jsonb;

CREATE INDEX IF NOT EXISTS idx_payments_reference ON public.payments(reference);
CREATE INDEX IF NOT EXISTS idx_payments_status_type ON public.payments(status, payment_type);

CREATE TABLE IF NOT EXISTS public.wallet_transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.app_users(id) ON DELETE CASCADE,
  payment_id UUID REFERENCES public.payments(id) ON DELETE SET NULL,
  type TEXT NOT NULL DEFAULT 'deposit',
  amount INTEGER NOT NULL,
  currency TEXT NOT NULL DEFAULT 'XAF',
  balance_after INTEGER NOT NULL DEFAULT 0,
  description TEXT,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_wallet_transactions_user_created_at
  ON public.wallet_transactions(user_id, created_at DESC);

ALTER TABLE public.wallet_transactions ENABLE ROW LEVEL SECURITY;

```


## Assistant

Need add function nelsius_webhook. Directory. Use create_directory then write.

**Tool Call: Create directory `news/supabase/functions/nelsius_webhook`**
Status: Completed

Created directory news/supabase/functions/nelsius_webhook

**Tool Call: supabase\\functions\\nelsius\_webhook\\index.ts**
Status: Completed

Diff: supabase\functions\nelsius_webhook\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

function isPaidStatus(status: string): boolean {
  return ["paid", "success", "successful", "completed", "approved", "succeeded"].includes(status.toLowerCase());
}

function extractPayloadValue(payload: any, keys: string[]): string | null {
  for (const key of keys) {
    const value = key.split(".").reduce((acc: any, part: string) => acc?.[part], payload);
    if (value !== undefined && value !== null && String(value).trim()) return String(value);
  }
  return null;
}

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Nelsius webhook OK", { status: 200 });
  }

  const webhookSecret = Deno.env.get("NELSIUS_WEBHOOK_SECRET") || "";
  if (webhookSecret) {
    const incomingSecret = req.headers.get("x-nelsius-secret") || req.headers.get("x-webhook-secret") || "";
    if (incomingSecret !== webhookSecret) {
      return new Response(JSON.stringify({ success: false, error: "Unauthorized webhook" }), { status: 401 });
    }
  }

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  let payload: any;
  try {
    payload = await req.json();
  } catch (_e) {
    return new Response(JSON.stringify({ success: false, error: "Invalid JSON" }), { status: 400 });
  }

  const reference = extractPayloadValue(payload, ["reference", "data.reference", "payment.reference", "transaction.reference"]);
  const status = extractPayloadValue(payload, ["status", "data.status", "payment.status", "transaction.status"]) || "unknown";
  const externalId = extractPayloadValue(payload, ["id", "data.id", "charge_id", "session_id", "data.charge_id", "data.session_id"]);

  if (!reference) {
    return new Response(JSON.stringify({ success: false, error: "Missing reference" }), { status: 400 });
  }

  const { data: payment, error: paymentError } = await supabase
    .from("payments")
    .select("*")
    .eq("reference", reference)
    .maybeSingle();

  if (paymentError) {
    return new Response(JSON.stringify({ success: false, error: paymentError.message }), { status: 500 });
  }

  if (!payment) {
    return new Response(JSON.stringify({ success: false, error: "Payment not found" }), { status: 404 });
  }

  const updatePayload: Record<string, unknown> = {
    status,
    raw_payload: payload,
    external_id: externalId,
  };

  if (isPaidStatus(status)) updatePayload.paid_at = new Date().toISOString();

  const { error: updateError } = await supabase
    .from("payments")
    .update(updatePayload)
    .eq("id", payment.id);

  if (updateError) {
    return new Response(JSON.stringify({ success: false, error: updateError.message }), { status: 500 });
  }

  if (!isPaidStatus(status) || payment.status === "paid" || payment.status === "success" || payment.status === "completed") {
    return new Response(JSON.stringify({ success: true, message: "Payment status recorded" }));
  }

  if (payment.payment_type === "subscription") {
    const now = new Date();
    const end = new Date(now);
    end.setMonth(end.getMonth() + 1);

    await supabase
      .from("app_users")
      .update({ plan: "premium", plan_expires_at: end.toISOString(), is_active: true })
      .eq("id", payment.user_id);

    await supabase.from("subscriptions").insert({
      user_id: payment.user_id,
      provider: "nelsius",
      status: "active",
      start_at: now.toISOString(),
      end_at: end.toISOString(),
      payment_reference: reference,
    });
  }

  if (payment.payment_type === "deposit") {
    const { data: user } = await supabase
      .from("app_users")
      .select("wallet_balance")
      .eq("id", payment.user_id)
      .maybeSingle();

    const balanceAfter = (user?.wallet_balance || 0) + Number(payment.amount || 0);

    await supabase
      .from("app_users")
      .update({ wallet_balance: balanceAfter })
      .eq("id", payment.user_id);

    await supabase.from("wallet_transactions").insert({
      user_id: payment.user_id,
      payment_id: payment.id,
      type: "deposit",
      amount: payment.amount,
      currency: payment.currency,
      balance_after: balanceAfter,
      description: `Dépôt Nelsius Pay ${reference}`,
    });
  }

  return new Response(JSON.stringify({ success: true }));
});

```


## Assistant

Need add config function entry in supabase/config.toml. Read end maybe.

**Tool Call: Read file `supabase\config.toml` (lines 260-340)**
Status: Completed

```news/supabase/config.toml
max_frequency = "5s"

# Use pre-defined map of phone number to OTP for testing.
# [auth.sms.test_otp]
# 4152127777 = "123456"

# Configure logged in session timeouts.
# [auth.sessions]
# Force log out after the specified duration.
# timebox = "24h"
# Force log out if the user has been inactive longer than the specified duration.
# inactivity_timeout = "8h"

# This hook runs before a new user is created and allows developers to reject the request based on the incoming user object.
# [auth.hook.before_user_created]
# enabled = true
# uri = "pg-functions://postgres/auth/before-user-created-hook"

# This hook runs before a token is issued and allows you to add additional claims based on the authentication method used.
# [auth.hook.custom_access_token]
# enabled = true
# uri = "pg-functions://<database>/<schema>/<hook_name>"

# Configure one of the supported SMS providers: `twilio`, `twilio_verify`, `messagebird`, `textlocal`, `vonage`.
[auth.sms.twilio]
enabled = false
account_sid = ""
message_service_sid = ""
# DO NOT commit your Twilio auth token to git. Use environment variable substitution instead:
auth_token = "env(SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN)"

# Multi-factor-authentication is available to Supabase Pro plan.
[auth.mfa]
# Control how many MFA factors can be enrolled at once per user.
max_enrolled_factors = 10

# Control MFA via App Authenticator (TOTP)
[auth.mfa.totp]
enroll_enabled = false
verify_enabled = false

# Configure MFA via Phone Messaging
[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false
otp_length = 6
template = "Your code is {{ .Code }}"
max_frequency = "5s"

# Configure MFA via WebAuthn
# [auth.mfa.web_authn]
# enroll_enabled = true
# verify_enabled = true

# Use an external OAuth provider. The full list of providers are: `apple`, `azure`, `bitbucket`,
# `discord`, `facebook`, `github`, `gitlab`, `google`, `keycloak`, `linkedin_oidc`, `notion`, `twitch`,
# `twitter`, `x`, `slack`, `spotify`, `workos`, `zoom`.
[auth.external.apple]
enabled = false
client_id = ""
# DO NOT commit your OAuth provider secret to git. Use environment variable substitution instead:
secret = "env(SUPABASE_AUTH_EXTERNAL_APPLE_SECRET)"
# Overrides the default auth callback URL derived from auth.external_url.
redirect_uri = ""
# Overrides the default auth provider URL. Used to support self-hosted gitlab, single-tenant Azure,
# or any other third-party OIDC providers.
url = ""
# If enabled, the nonce check will be skipped. Required for local sign in with Google auth.
skip_nonce_check = false
# If enabled, it will allow the user to successfully authenticate when the provider does not return an email address.
email_optional = false

# Allow Solana wallet holders to sign in to your project via the Sign in with Solana (SIWS, EIP-4361) standard.
# You can configure "web3" rate limit in the [auth.rate_limit] section and set up [auth.captcha] if self-hosting.
[auth.web3.solana]
enabled = false

# Use Firebase Auth as a third-party provider alongside Supabase Auth.
[auth.third_party.firebase]
enabled = false
# project_id = "my-firebase-project"

```


**Tool Call: Read file `supabase\config.toml` (lines 420-520)**
Status: Completed

```news/supabase/config.toml

[functions.clear_db]
enabled = true
verify_jwt = true
import_map = "./functions/clear_db/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/clear_db/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/clear_db/*.html" ]

[functions.telegram_bot]
enabled = true
verify_jwt = false
import_map = "./functions/telegram_bot/deno.json"
entrypoint = "./functions/telegram_bot/index.ts"

```


**Tool Call: supabase\\config.toml**
Status: Completed

Diff: supabase\config.toml
```
# For detailed configuration reference documentation, visit:
# https://supabase.com/docs/guides/local-development/cli/config
# A string used to distinguish different Supabase projects on the same host. Defaults to the
# working directory name when running `supabase init`.
project_id = "news"

[api]
enabled = true
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. `public` and `graphql_public` schemas are included by default.
schemas = ["public", "graphql_public"]
# Extra schemas to add to the search_path of every request.
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure. Limits payload size
# for accidental or malicious requests.
max_rows = 1000

[api.tls]
# Enable HTTPS endpoints locally using a self-signed certificate.
enabled = false
# Paths to self-signed certificate pair.
# cert_path = "../certs/my-cert.pem"
# key_path = "../certs/my-key.pem"

[db]
# Port to use for the local database URL.
port = 54322
# Port used by db diff command to initialize the shadow database.
shadow_port = 54320
# Maximum amount of time to wait for health check when starting the local database.
health_timeout = "2m"
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version;` on the remote database to check.
major_version = 17

[db.pooler]
enabled = false
# Port to use for the local connection pooler.
port = 54329
# Specifies when a server connection can be reused by other clients.
# Configure one of the supported pooler modes: `transaction`, `session`.
pool_mode = "transaction"
# How many server connections to allow per user/database pair.
default_pool_size = 20
# Maximum number of client connections allowed.
max_client_conn = 100

# [db.vault]
# secret_key = "env(SECRET_VALUE)"

[db.migrations]
# If disabled, migrations will be skipped during a db push or reset.
enabled = true
# Specifies an ordered list of schema files that describe your database.
# Supports glob patterns relative to supabase directory: "./schemas/*.sql"
schema_paths = []

[db.seed]
# If enabled, seeds the database after migrations during a db reset.
enabled = true
# Specifies an ordered list of seed files to load during db reset.
# Supports glob patterns relative to supabase directory: "./seeds/*.sql"
sql_paths = ["./seed.sql"]

[db.network_restrictions]
# Enable management of network restrictions.
enabled = false
# List of IPv4 CIDR blocks allowed to connect to the database.
# Defaults to allow all IPv4 connections. Set empty array to block all IPs.
allowed_cidrs = ["0.0.0.0/0"]
# List of IPv6 CIDR blocks allowed to connect to the database.
# Defaults to allow all IPv6 connections. Set empty array to block all IPs.
allowed_cidrs_v6 = ["::/0"]

# Uncomment to reject non-secure connections to the database.
# [db.ssl_enforcement]
# enabled = true

[realtime]
enabled = true
# Bind realtime via either IPv4 or IPv6. (default: IPv4)
# ip_version = "IPv6"
# The maximum length in bytes of HTTP request headers. (default: 4096)
# max_header_length = 4096

[studio]
enabled = true
# Port to use for Supabase Studio.
port = 54323
# External URL of the API server that frontend connects to.
api_url = "http://127.0.0.1"
# OpenAI API Key to use for Supabase AI in the Supabase Studio.
openai_api_key = "env(OPENAI_API_KEY)"

# Email testing server. Emails sent with the local dev setup are not actually sent - rather, they
# are monitored, and you can view the emails that would have been sent from the web interface.
[inbucket]
enabled = true
# Port to use for the email testing server web interface.
port = 54324
# Uncomment to expose additional ports for testing user applications that send emails.
# smtp_port = 54325
# pop3_port = 54326
# admin_email = "admin@email.com"
# sender_name = "Admin"

[storage]
enabled = true
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

# Uncomment to configure local storage buckets
# [storage.buckets.images]
# public = false
# file_size_limit = "50MiB"
# allowed_mime_types = ["image/png", "image/jpeg"]
# objects_path = "./images"

# Allow connections via S3 compatible clients
[storage.s3_protocol]
enabled = true

# Image transformation API is available to Supabase Pro plan.
# [storage.image_transformation]
# enabled = true

# Store analytical data in S3 for running ETL jobs over Iceberg Catalog
# This feature is only available on the hosted platform.
[storage.analytics]
enabled = false
max_namespaces = 5
max_tables = 10
max_catalogs = 2

# Analytics Buckets is available to Supabase Pro plan.
# [storage.analytics.buckets.my-warehouse]

# Store vector embeddings in S3 for large and durable datasets
# This feature is only available on the hosted platform.
[storage.vector]
enabled = false
max_buckets = 10
max_indexes = 5

# Vector Buckets is available to Supabase Pro plan.
# [storage.vector.buckets.documents-openai]

[auth]
enabled = true
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://127.0.0.1:3000"
# The public URL that Auth serves on. Defaults to the API external URL with `/auth/v1` appended.
# external_url = ""
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = ["https://127.0.0.1:3000"]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# JWT issuer URL. If not set, defaults to auth.external_url.
# jwt_issuer = ""
# Path to JWT signing key. DO NOT commit your signing keys file to git.
# signing_keys_path = "./signing_keys.json"
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
# Requires enable_refresh_token_rotation = true.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true
# Allow/disallow anonymous sign-ins to your project.
enable_anonymous_sign_ins = false
# Allow/disallow testing manual linking of accounts
enable_manual_linking = false
# Passwords shorter than this value will be rejected as weak. Minimum 6, recommended 8 or more.
minimum_password_length = 6
# Passwords that do not meet the following requirements will be rejected as weak. Supported values
# are: `letters_digits`, `lower_upper_letters_digits`, `lower_upper_letters_digits_symbols`
password_requirements = ""

# Configure passkey sign-ins.
# [auth.passkey]
# enabled = false

# Configure WebAuthn relying party settings (required when passkey is enabled).
# [auth.webauthn]
# rp_display_name = "Supabase"
# rp_id = "localhost"
# rp_origins = ["http://127.0.0.1:3000"]

[auth.rate_limit]
# Number of emails that can be sent per hour. Requires auth.email.smtp to be enabled.
email_sent = 2
# Number of SMS messages that can be sent per hour. Requires auth.sms to be enabled.
sms_sent = 30
# Number of anonymous sign-ins that can be made per hour per IP address. Requires enable_anonymous_sign_ins = true.
anonymous_users = 30
# Number of sessions that can be refreshed in a 5 minute interval per IP address.
token_refresh = 150
# Number of sign up and sign-in requests that can be made in a 5 minute interval per IP address (excludes anonymous users).
sign_in_sign_ups = 30
# Number of OTP / Magic link verifications that can be made in a 5 minute interval per IP address.
token_verifications = 30
# Number of Web3 logins that can be made in a 5 minute interval per IP address.
web3 = 30

# Configure one of the supported captcha providers: `hcaptcha`, `turnstile`.
# [auth.captcha]
# enabled = true
# provider = "hcaptcha"
# secret = ""

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# If enabled, a user will be required to confirm any email change on both the old, and new email
# addresses. If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = false
# If enabled, users will need to reauthenticate or have logged in recently to change their password.
secure_password_change = false
# Controls the minimum amount of time that must pass before sending another signup confirmation or password reset email.
max_frequency = "1s"
# Number of characters used in the email OTP.
otp_length = 6
# Number of seconds before the email OTP expires (defaults to 1 hour).
otp_expiry = 3600

# Use a production-ready SMTP server
# [auth.email.smtp]
# enabled = true
# host = "smtp.sendgrid.net"
# port = 587
# user = "apikey"
# pass = "env(SENDGRID_API_KEY)"
# admin_email = "admin@email.com"
# sender_name = "Admin"

# Uncomment to customize email template
# [auth.email.template.invite]
# subject = "You have been invited"
# content_path = "./supabase/templates/invite.html"

# Uncomment to customize notification email template
# [auth.email.notification.password_changed]
# enabled = true
# subject = "Your password has been changed"
# content_path = "./templates/password_changed_notification.html"

[auth.sms]
# Allow/disallow new user signups via SMS to your project.
enable_signup = false
# If enabled, users need to confirm their phone number before signing in.
enable_confirmations = false
# Template for sending OTP to users
template = "Your code is {{ .Code }}"
# Controls the minimum amount of time that must pass before sending another sms otp.
max_frequency = "5s"

# Use pre-defined map of phone number to OTP for testing.
# [auth.sms.test_otp]
# 4152127777 = "123456"

# Configure logged in session timeouts.
# [auth.sessions]
# Force log out after the specified duration.
# timebox = "24h"
# Force log out if the user has been inactive longer than the specified duration.
# inactivity_timeout = "8h"

# This hook runs before a new user is created and allows developers to reject the request based on the incoming user object.
# [auth.hook.before_user_created]
# enabled = true
# uri = "pg-functions://postgres/auth/before-user-created-hook"

# This hook runs before a token is issued and allows you to add additional claims based on the authentication method used.
# [auth.hook.custom_access_token]
# enabled = true
# uri = "pg-functions://<database>/<schema>/<hook_name>"

# Configure one of the supported SMS providers: `twilio`, `twilio_verify`, `messagebird`, `textlocal`, `vonage`.
[auth.sms.twilio]
enabled = false
account_sid = ""
message_service_sid = ""
# DO NOT commit your Twilio auth token to git. Use environment variable substitution instead:
auth_token = "env(SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN)"

# Multi-factor-authentication is available to Supabase Pro plan.
[auth.mfa]
# Control how many MFA factors can be enrolled at once per user.
max_enrolled_factors = 10

# Control MFA via App Authenticator (TOTP)
[auth.mfa.totp]
enroll_enabled = false
verify_enabled = false

# Configure MFA via Phone Messaging
[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false
otp_length = 6
template = "Your code is {{ .Code }}"
max_frequency = "5s"

# Configure MFA via WebAuthn
# [auth.mfa.web_authn]
# enroll_enabled = true
# verify_enabled = true

# Use an external OAuth provider. The full list of providers are: `apple`, `azure`, `bitbucket`,
# `discord`, `facebook`, `github`, `gitlab`, `google`, `keycloak`, `linkedin_oidc`, `notion`, `twitch`,
# `twitter`, `x`, `slack`, `spotify`, `workos`, `zoom`.
[auth.external.apple]
enabled = false
client_id = ""
# DO NOT commit your OAuth provider secret to git. Use environment variable substitution instead:
secret = "env(SUPABASE_AUTH_EXTERNAL_APPLE_SECRET)"
# Overrides the default auth callback URL derived from auth.external_url.
redirect_uri = ""
# Overrides the default auth provider URL. Used to support self-hosted gitlab, single-tenant Azure,
# or any other third-party OIDC providers.
url = ""
# If enabled, the nonce check will be skipped. Required for local sign in with Google auth.
skip_nonce_check = false
# If enabled, it will allow the user to successfully authenticate when the provider does not return an email address.
email_optional = false

# Allow Solana wallet holders to sign in to your project via the Sign in with Solana (SIWS, EIP-4361) standard.
# You can configure "web3" rate limit in the [auth.rate_limit] section and set up [auth.captcha] if self-hosting.
[auth.web3.solana]
enabled = false

# Use Firebase Auth as a third-party provider alongside Supabase Auth.
[auth.third_party.firebase]
enabled = false
# project_id = "my-firebase-project"

# Use Auth0 as a third-party provider alongside Supabase Auth.
[auth.third_party.auth0]
enabled = false
# tenant = "my-auth0-tenant"
# tenant_region = "us"

# Use AWS Cognito (Amplify) as a third-party provider alongside Supabase Auth.
[auth.third_party.aws_cognito]
enabled = false
# user_pool_id = "my-user-pool-id"
# user_pool_region = "us-east-1"

# Use Clerk as a third-party provider alongside Supabase Auth.
[auth.third_party.clerk]
enabled = false
# Obtain from https://clerk.com/setup/supabase
# domain = "example.clerk.accounts.dev"

# OAuth server configuration
[auth.oauth_server]
# Enable OAuth server functionality
enabled = false
# Path for OAuth consent flow UI
authorization_url_path = "/oauth/consent"
# Allow dynamic client registration
allow_dynamic_registration = false

[edge_runtime]
enabled = true
# Supported request policies: `oneshot`, `per_worker`.
# `per_worker` (default) — enables hot reload during local development.
# `oneshot` — fallback mode if hot reload causes issues (e.g. in large repos or with symlinks).
policy = "per_worker"
# Port to attach the Chrome inspector for debugging edge functions.
inspector_port = 8083
# The Deno major version to use.
deno_version = 2

# [edge_runtime.secrets]
# secret_key = "env(SECRET_VALUE)"

[analytics]
enabled = true
port = 54327
# Configure one of the supported backends: `postgres`, `bigquery`.
backend = "postgres"

# Experimental features may be deprecated any time
[experimental]
# Configures Postgres storage engine to use OrioleDB (S3)
orioledb_version = ""
# Configures S3 bucket URL, eg. <bucket_name>.s3-<region>.amazonaws.com
s3_host = "env(S3_HOST)"
# Configures S3 bucket region, eg. us-east-1
s3_region = "env(S3_REGION)"
# Configures AWS_ACCESS_KEY_ID for S3 bucket
s3_access_key = "env(S3_ACCESS_KEY)"
# Configures AWS_SECRET_ACCESS_KEY for S3 bucket
s3_secret_key = "env(S3_SECRET_KEY)"

# [experimental.pgdelta]
# When enabled, pg-delta becomes the active engine for supported schema flows.
# enabled = false
# Directory under `supabase/` where declarative files are written.
# declarative_schema_path = "./database"
# JSON string passed through to pg-delta SQL formatting.
# format_options = "{\"keywordCase\":\"upper\",\"indent\":2,\"maxWidth\":80,\"commaStyle\":\"trailing\"}"

[functions.aggregate_news]
enabled = true
verify_jwt = false
import_map = "./functions/aggregate_news/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/aggregate_news/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/aggregate_news/*.html" ]

[functions.clear_db]
enabled = true
verify_jwt = true
import_map = "./functions/clear_db/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/clear_db/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/clear_db/*.html" ]

[functions.telegram_bot]
enabled = true
verify_jwt = false
import_map = "./functions/telegram_bot/deno.json"
entrypoint = "./functions/telegram_bot/index.ts"

[functions.nelsius_webhook]
enabled = true
verify_jwt = false
entrypoint = "./functions/nelsius_webhook/index.ts"

```


## Assistant

<thinking>
**Considering deposit command**

I'm thinking about adding a command for deposits, like /deposit followed by the amount, or maybe a button for it. I want to ask the user for the amount clearly and state it as deposit_amount. I'm also considering if I should include /wallet in this process. Let’s see, it might be time for some editing to make everything clearer and more user-friendly! I'm excited to refine this feature.
</thinking>

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "💰 Dépôt"],
        ["❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "💰 Dépôt": "/deposit",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        `💳 <b>Abonnement Premium</b>\n\n` +
          `Prix: <b>500 FCFA / mois</b>\n` +
          `Avantages: flux illimités, plusieurs canaux/sources.\n\n` +
          `⏳ Paiement Nelsius Pay: en cours d'intégration.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need add commands handling /deposit and updated /subscribe. Current /subscribe simple response. Need replace for non-admin. Need add deposit state maybe if no amount ask. For subscription generate checkout immediately. Deposit with arg or state: /deposit amount. Button sets ask amount.

Add command /wallet maybe and /deposit input state. Need update /me to include wallet_balance? It selects usage only, appUser maybe includes wallet_balance if column exists after migration. We can show. Also if migration not applied, appUser.wallet_balance undefined.

Edit in early non-admin.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "💰 Dépôt"],
        ["❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "💰 Dépôt": "/deposit",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et tape /me pour vérifier ton statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need create config no deno json maybe ok. Need diagnostics.

**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/functions/nelsius_webhook/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/migrations/20260511202000_add_nelsius_payments_wallet.sql`**
Status: Completed

File doesn't have errors or warnings!

## Assistant

<thinking>
**Explaining webhook deployment**

I need to finalize my explanation about deploying the secrets and webhook. It's important to mention that it's not fully guaranteed without the actual payload, and that a robust webhook is necessary. I also need to highlight the Nelsius callback, which should be configured to `/functions/v1/nelsius_webhook` with an optional secret header. I should also clarify that we implemented checkout instead of direct charge. The user mentioned depots, but we focused on deposit wallet and subscription, which is good.
</thinking>

Oui, j’ai intégré une première version complète du système de paiement via **Nelsius Pay** pour :

1. **payer l’abonnement Premium** ;
2. **faire des dépôts dans un wallet utilisateur**.

## Ce qui a été ajouté

### 1. Migration base de données

Nouveau fichier :

`news/supabase/migrations/20260511202000_add_nelsius_payments_wallet.sql`

Cette migration ajoute :

- `wallet_balance` dans `app_users` ;
- des champs supplémentaires dans `payments` :
  - `payment_type`
  - `checkout_url`
  - `external_id`
  - `paid_at`
  - `metadata`
- une nouvelle table :
  - `wallet_transactions`

Cette table garde l’historique des dépôts utilisateurs.

---

## 2. Paiement Premium via Nelsius Pay

Côté utilisateur, le bouton :

`💳 Premium`

ou la commande :

`/subscribe`

génère maintenant un vrai lien de paiement Nelsius Pay.

Le montant actuel est :

`500 FCFA`

Quand l’utilisateur clique, il est envoyé vers le checkout Nelsius Pay.

Après confirmation du paiement par webhook, son compte passe en :

`premium`

avec une expiration à +1 mois.

---

## 3. Dépôt wallet via Nelsius Pay

J’ai ajouté le bouton :

`💰 Dépôt`

et la commande :

`/deposit`

### Utilisation simple

L’utilisateur clique sur :

`💰 Dépôt`

Le bot demande :

> Entre le montant à déposer en FCFA.

Exemple :

`1000`

Le bot génère ensuite un lien de paiement Nelsius Pay.

### Utilisation directe

L’utilisateur peut aussi taper :

`/deposit 1000`

Le bot génère directement le lien de dépôt.

Après confirmation du paiement par webhook :

- `wallet_balance` est crédité ;
- une ligne est créée dans `wallet_transactions`.

---

## 4. Webhook Nelsius Pay ajouté

J’ai créé une nouvelle Edge Function :

`news/supabase/functions/nelsius_webhook/index.ts`

Elle reçoit les notifications de paiement Nelsius.

Elle fait ceci :

1. lit la référence de paiement ;
2. retrouve le paiement dans `payments` ;
3. met à jour son statut ;
4. si le paiement est confirmé :
   - active Premium pour les abonnements ;
   - crédite le wallet pour les dépôts.

---

## 5. Configuration Supabase ajoutée

J’ai aussi ajouté la fonction dans :

`news/supabase/config.toml`

Nouvelle section :

`functions.nelsius_webhook`

La fonction est publique côté JWT :

`verify_jwt = false`

C’est nécessaire pour que Nelsius puisse appeler le webhook.

---

## 6. Variables d’environnement nécessaires

Tu dois configurer ces secrets Supabase :

### Obligatoire

`NELSIUS_SECRET_KEY`

C’est ta clé API secrète Nelsius.

Commande :

`npx supabase secrets set NELSIUS_SECRET_KEY=ta_cle_secrete --project-ref nxgryaqncfjuazfvekro`

---

### Recommandé

`NELSIUS_WEBHOOK_SECRET`

Un secret partagé pour protéger le webhook.

Commande :

`npx supabase secrets set NELSIUS_WEBHOOK_SECRET=un_secret_fort --project-ref nxgryaqncfjuazfvekro`

Ensuite, dans Nelsius, il faudra configurer le webhook pour envoyer ce secret dans un header :

- `x-nelsius-secret`

ou :

- `x-webhook-secret`

Si Nelsius ne permet pas d’ajouter un header personnalisé, on pourra adapter plus tard.

---

### Optionnel

`NELSIUS_BASE_URL`

Par défaut, j’ai utilisé :

`https://api.nelsius.com`

Donc tu n’es pas obligé de le configurer, sauf si Nelsius te donne une URL sandbox ou différente.

Commande optionnelle :

`npx supabase secrets set NELSIUS_BASE_URL=https://api.nelsius.com --project-ref nxgryaqncfjuazfvekro`

---

### Optionnel

`NELSIUS_RETURN_URL`

URL où l’utilisateur revient après paiement.

Si absent, le bot essaie de mettre le lien Telegram du bot.

Commande optionnelle :

`npx supabase secrets set NELSIUS_RETURN_URL=https://t.me/TonBotUsername --project-ref nxgryaqncfjuazfvekro`

---

## 7. URL du webhook à configurer dans Nelsius Pay

Après déploiement, ton webhook sera :

`https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/nelsius_webhook`

C’est cette URL qu’il faut mettre dans le dashboard Nelsius Pay comme webhook/callback.

---

## 8. Déploiement à faire

### Appliquer la migration

Si ton historique de migrations est propre :

`npx supabase db push`

Sinon, applique manuellement le SQL de :

`news/supabase/migrations/20260511202000_add_nelsius_payments_wallet.sql`

dans le SQL Editor Supabase.

---

### Déployer le bot

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

---

### Déployer le webhook

`npx supabase functions deploy nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

---

## 9. Tester côté utilisateur

Dans Telegram :

### Tester Premium

L’utilisateur clique :

`💳 Premium`

ou tape :

`/subscribe`

Le bot doit répondre avec un bouton :

`💳 Payer 500 FCFA`

---

### Tester dépôt

L’utilisateur clique :

`💰 Dépôt`

Puis entre :

`1000`

ou tape directement :

`/deposit 1000`

Le bot doit répondre avec un bouton :

`💰 Déposer 1000 FCFA`

---

### Vérifier le compte

Après paiement validé par webhook, l’utilisateur tape :

`/me`

Il doit voir :

- son plan Premium si abonnement ;
- son solde wallet si dépôt.

---

## 10. Point important sur la confirmation des paiements

Comme je n’ai pas encore vu la documentation complète du webhook Nelsius, j’ai rendu le webhook assez flexible.

Il cherche la référence dans plusieurs formats possibles :

- `reference`
- `data.reference`
- `payment.reference`
- `transaction.reference`

Et le statut dans :

- `status`
- `data.status`
- `payment.status`
- `transaction.status`

Il considère comme paiement réussi les statuts :

- `paid`
- `success`
- `successful`
- `completed`
- `approved`
- `succeeded`

Si Nelsius utilise un autre nom exact pour les statuts, on ajustera facilement.

---

## 11. Ce qui est maintenant possible

Un utilisateur peut maintenant :

- payer son abonnement Premium ;
- déposer de l’argent dans son wallet ;
- voir son solde avec `/me` ;
- être crédité automatiquement après webhook ;
- devenir Premium automatiquement après paiement.

---

## 12. Étape suivante recommandée

Après test du webhook réel, on devrait ajouter :

1. une commande `/payments` pour afficher les paiements récents ;
2. une commande `/wallet` pour afficher l’historique des dépôts ;
3. une vérification manuelle `/checkpayment <reference>` si le webhook tarde ;
4. l’utilisation du solde wallet pour payer Premium sans refaire un paiement externe.

## User

sur nelsius pay, je met quel url pour le webhook, comsa nelsius pay envoie les webhook laba. aussi, comment tester et me rassurer que les webhooks fonctionnent bien. et aussi, je met ma cle api  nelsius pay ou?

## Assistant

Oui. Pour Nelsius Pay, tu dois configurer **3 choses** :

1. l’URL du webhook ;
2. ta clé API Nelsius Pay dans les secrets Supabase ;
3. éventuellement un secret de sécurité webhook.

---

## 1. URL du webhook à mettre dans Nelsius Pay

Ton URL webhook est :

`https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/nelsius_webhook`

C’est cette URL que tu dois mettre dans le dashboard Nelsius Pay comme :

- webhook URL ;
- callback URL ;
- notification URL ;
- payment notification URL ;

selon le nom qu’ils utilisent.

---

## 2. Où mettre ta clé API Nelsius Pay ?

Tu dois mettre ta clé API Nelsius dans les **secrets Supabase**, pas dans le code.

Commande :

`npx supabase secrets set NELSIUS_SECRET_KEY=TA_CLE_API_NELSIUS --project-ref nxgryaqncfjuazfvekro`

Exemple :

`npx supabase secrets set NELSIUS_SECRET_KEY=sk_live_xxxxxxxxxxxxxxxxx --project-ref nxgryaqncfjuazfvekro`

Après ça, redéploie le bot :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

Le bot utilise `NELSIUS_SECRET_KEY` pour créer les liens de paiement.

---

## 3. Secret webhook optionnel mais recommandé

J’ai prévu dans le code un secret optionnel :

`NELSIUS_WEBHOOK_SECRET`

Il sert à vérifier que les appels viennent bien de Nelsius.

Tu peux le définir comme ceci :

`npx supabase secrets set NELSIUS_WEBHOOK_SECRET=un_secret_tres_long --project-ref nxgryaqncfjuazfvekro`

Exemple :

`npx supabase secrets set NELSIUS_WEBHOOK_SECRET=nelsius_izynews_2026_secret --project-ref nxgryaqncfjuazfvekro`

Ensuite, si Nelsius Pay permet d’ajouter un header personnalisé dans le webhook, ajoute :

Header :

`x-nelsius-secret`

Valeur :

`nelsius_izynews_2026_secret`

ou bien :

Header :

`x-webhook-secret`

Valeur :

`nelsius_izynews_2026_secret`

Le code accepte les deux headers :

- `x-nelsius-secret`
- `x-webhook-secret`

### Important

Si Nelsius Pay ne permet pas d’ajouter un header personnalisé, ne configure pas `NELSIUS_WEBHOOK_SECRET` pour l’instant.

Sinon, le webhook va refuser les appels avec erreur `401 Unauthorized webhook`.

Donc :

- si Nelsius permet les headers personnalisés : configure `NELSIUS_WEBHOOK_SECRET` ;
- si Nelsius ne permet pas les headers personnalisés : ne mets pas encore `NELSIUS_WEBHOOK_SECRET`.

---

## 4. Déployer le webhook

Si tu ne l’as pas encore fait :

`npx supabase functions deploy nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

Et aussi :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

---

## 5. Vérifier que le webhook répond

Tu peux tester dans ton navigateur cette URL :

`https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/nelsius_webhook`

Si tout est bon, tu dois voir une réponse du genre :

`Nelsius webhook OK`

Ou utiliser `curl` :

`curl https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/nelsius_webhook`

Réponse attendue :

`Nelsius webhook OK`

---

## 6. Tester un webhook manuellement

Le webhook a besoin d’une `reference` qui existe déjà dans ta table `payments`.

Donc le test complet se fait en 2 étapes.

---

### Étape A — Créer un paiement depuis Telegram

Dans le bot, tape :

`/deposit 100`

ou :

`/subscribe`

Le bot va créer une ligne dans la table `payments`.

Ensuite, dans Supabase, va dans :

`Table Editor` → `payments`

Récupère la valeur de la colonne :

`reference`

Exemple :

`DEPOSIT_123456789_1760000000000`

---

### Étape B — Simuler le webhook avec cette référence

Utilise `curl` :

`curl -X POST https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/nelsius_webhook -H "Content-Type: application/json" -d "{\"reference\":\"DEPOSIT_123456789_1760000000000\",\"status\":\"paid\"}"`

Remplace bien la référence par celle qui existe dans ta table.

Si tu as configuré `NELSIUS_WEBHOOK_SECRET`, ajoute aussi le header :

`curl -X POST https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/nelsius_webhook -H "Content-Type: application/json" -H "x-nelsius-secret: nelsius_izynews_2026_secret" -d "{\"reference\":\"DEPOSIT_123456789_1760000000000\",\"status\":\"paid\"}"`

---

## 7. Résultat attendu pour un dépôt

Si tu testes avec une référence de dépôt :

`payment_type = deposit`

Alors le webhook doit :

1. mettre le paiement en statut `paid` ;
2. remplir `paid_at` ;
3. créditer `wallet_balance` dans `app_users` ;
4. ajouter une ligne dans `wallet_transactions`.

Tu peux vérifier dans Supabase :

- table `payments`
- table `app_users`
- table `wallet_transactions`

Puis dans Telegram :

`/me`

Tu dois voir le solde augmenter.

---

## 8. Résultat attendu pour un abonnement

Si tu testes avec une référence d’abonnement :

`payment_type = subscription`

Alors le webhook doit :

1. mettre le paiement en statut `paid` ;
2. remplir `paid_at` ;
3. passer l’utilisateur en `plan = premium` ;
4. mettre `plan_expires_at` à +1 mois ;
5. créer une ligne dans `subscriptions`.

Puis dans Telegram :

`/me`

Tu dois voir :

`Plan: Premium`

---

## 9. Voir les logs du webhook

Pour vérifier que Nelsius appelle bien ton webhook :

`npx supabase functions logs nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

Ou dans Supabase Dashboard :

- Edge Functions ;
- `nelsius_webhook`;
- Logs.

---

## 10. Tester depuis le bot

### Test dépôt

Dans Telegram :

`/deposit 100`

Le bot doit générer un bouton de paiement.

Tu paies.

Ensuite tu attends le webhook.

Puis :

`/me`

Tu dois voir :

`Solde wallet: 100 XAF`

---

### Test Premium

Dans Telegram :

`/subscribe`

Le bot doit générer un bouton :

`Payer 500 FCFA`

Tu paies.

Puis :

`/me`

Tu dois voir :

`Plan: Premium`

---

## 11. Si le paiement marche mais le compte ne change pas

Vérifie dans cet ordre :

### 1. Le webhook est-il appelé ?

Logs :

`npx supabase functions logs nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

### 2. La référence reçue correspond-elle à une ligne dans `payments` ?

Dans `payments`, vérifie :

`reference`

La référence envoyée par Nelsius doit être exactement la même.

### 3. Le statut envoyé par Nelsius est-il reconnu ?

Le code reconnaît actuellement :

- `paid`
- `success`
- `successful`
- `completed`
- `approved`
- `succeeded`

Si Nelsius envoie par exemple :

- `SUCCESSFUL_PAYMENT`
- `COMPLETED_PAYMENT`
- `confirmed`

il faudra l’ajouter dans le code.

### 4. Le secret webhook bloque-t-il ?

Si tu as mis `NELSIUS_WEBHOOK_SECRET` mais que Nelsius n’envoie pas le header, le webhook répondra :

`401 Unauthorized webhook`

Dans ce cas, retire temporairement le secret :

`npx supabase secrets unset NELSIUS_WEBHOOK_SECRET --project-ref nxgryaqncfjuazfvekro`

Ou configure le bon header côté Nelsius.

---

## Résumé rapide

### URL webhook à mettre dans Nelsius

`https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/nelsius_webhook`

### Clé API Nelsius à mettre dans Supabase

`npx supabase secrets set NELSIUS_SECRET_KEY=TA_CLE_API_NELSIUS --project-ref nxgryaqncfjuazfvekro`

### Déployer

`npx supabase functions deploy nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

### Tester

`/deposit 100`

Puis simuler webhook :

`curl -X POST https://nxgryaqncfjuazfvekro.supabase.co/functions/v1/nelsius_webhook -H "Content-Type: application/json" -d "{\"reference\":\"TA_REFERENCE\",\"status\":\"paid\"}"`

## User (checkpoint)

l'initiation de paiement donne bien mais on dit que apres le paiement, je viens taper /me pour voir le statut de paiement or /me renvie vers le profil. et aussi, tu est bien sur que la verification de statut de paiement fonctionne bien avec le webhok?

## Assistant

Tu as raison : `/me` montre le profil général, pas le détail d’un paiement précis. Je vais ajouter une commande/bouton `🧾 Paiements` pour voir les paiements récents, et sécuriser l’idempotence du webhook pour éviter qu’un même webhook crédité plusieurs fois ne double le solde.

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 60-160)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "💰 Dépôt"],
        ["❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "💰 Dépôt": "/deposit",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1090-1250)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et tape /me pour vérifier ton statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,

```


**Tool Call: Read file `supabase\functions\nelsius_webhook\index.ts` (lines 1-140)**
Status: Completed

```news/supabase/functions/nelsius_webhook/index.ts
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

function isPaidStatus(status: string): boolean {
  return ["paid", "success", "successful", "completed", "approved", "succeeded"].includes(status.toLowerCase());
}

function extractPayloadValue(payload: any, keys: string[]): string | null {
  for (const key of keys) {
    const value = key.split(".").reduce((acc: any, part: string) => acc?.[part], payload);
    if (value !== undefined && value !== null && String(value).trim()) return String(value);
  }
  return null;
}

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Nelsius webhook OK", { status: 200 });
  }

  const webhookSecret = Deno.env.get("NELSIUS_WEBHOOK_SECRET") || "";
  if (webhookSecret) {
    const incomingSecret = req.headers.get("x-nelsius-secret") || req.headers.get("x-webhook-secret") || "";
    if (incomingSecret !== webhookSecret) {
      return new Response(JSON.stringify({ success: false, error: "Unauthorized webhook" }), { status: 401 });
    }
  }

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  let payload: any;
  try {
    payload = await req.json();
  } catch (_e) {
    return new Response(JSON.stringify({ success: false, error: "Invalid JSON" }), { status: 400 });
  }

  const reference = extractPayloadValue(payload, ["reference", "data.reference", "payment.reference", "transaction.reference"]);
  const status = extractPayloadValue(payload, ["status", "data.status", "payment.status", "transaction.status"]) || "unknown";
  const externalId = extractPayloadValue(payload, ["id", "data.id", "charge_id", "session_id", "data.charge_id", "data.session_id"]);

  if (!reference) {
    return new Response(JSON.stringify({ success: false, error: "Missing reference" }), { status: 400 });
  }

  const { data: payment, error: paymentError } = await supabase
    .from("payments")
    .select("*")
    .eq("reference", reference)
    .maybeSingle();

  if (paymentError) {
    return new Response(JSON.stringify({ success: false, error: paymentError.message }), { status: 500 });
  }

  if (!payment) {
    return new Response(JSON.stringify({ success: false, error: "Payment not found" }), { status: 404 });
  }

  const updatePayload: Record<string, unknown> = {
    status,
    raw_payload: payload,
    external_id: externalId,
  };

  if (isPaidStatus(status)) updatePayload.paid_at = new Date().toISOString();

  const { error: updateError } = await supabase
    .from("payments")
    .update(updatePayload)
    .eq("id", payment.id);

  if (updateError) {
    return new Response(JSON.stringify({ success: false, error: updateError.message }), { status: 500 });
  }

  if (!isPaidStatus(status) || payment.status === "paid" || payment.status === "success" || payment.status === "completed") {
    return new Response(JSON.stringify({ success: true, message: "Payment status recorded" }));
  }

  if (payment.payment_type === "subscription") {
    const now = new Date();
    const end = new Date(now);
    end.setMonth(end.getMonth() + 1);

    await supabase
      .from("app_users")
      .update({ plan: "premium", plan_expires_at: end.toISOString(), is_active: true })
      .eq("id", payment.user_id);

    await supabase.from("subscriptions").insert({
      user_id: payment.user_id,
      provider: "nelsius",
      status: "active",
      start_at: now.toISOString(),
      end_at: end.toISOString(),
      payment_reference: reference,
    });
  }

  if (payment.payment_type === "deposit") {
    const { data: user } = await supabase
      .from("app_users")
      .select("wallet_balance")
      .eq("id", payment.user_id)
      .maybeSingle();

    const balanceAfter = (user?.wallet_balance || 0) + Number(payment.amount || 0);

    await supabase
      .from("app_users")
      .update({ wallet_balance: balanceAfter })
      .eq("id", payment.user_id);

    await supabase.from("wallet_transactions").insert({
      user_id: payment.user_id,
      payment_id: payment.id,
      type: "deposit",
      amount: payment.amount,
      currency: payment.currency,
      balance_after: balanceAfter,
      description: `Dépôt Nelsius Pay ${reference}`,
    });
  }

  return new Response(JSON.stringify({ success: true }));
});

```


## Assistant

Tu as raison : `/me` doit rester le profil, et le suivi des paiements doit être séparé. Je vais ajouter `🧾 Paiements` / `/payments` pour voir les paiements récents, et renforcer le webhook pour éviter les doubles validations si Nelsius renvoie le même webhook plusieurs fois.

**Tool Call: supabase\\functions\\nelsius\_webhook\\index.ts**
Status: Completed

Diff: supabase\functions\nelsius_webhook\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

function isPaidStatus(status: string): boolean {
  return [
    "paid",
    "success",
    "successful",
    "completed",
    "approved",
    "succeeded",
  ].includes(status.toLowerCase());
}

function extractPayloadValue(payload: any, keys: string[]): string | null {
  for (const key of keys) {
    const value = key
      .split(".")
      .reduce((acc: any, part: string) => acc?.[part], payload);
    if (value !== undefined && value !== null && String(value).trim())
      return String(value);
  }
  return null;
}

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Nelsius webhook OK", { status: 200 });
  }

  const webhookSecret = Deno.env.get("NELSIUS_WEBHOOK_SECRET") || "";
  if (webhookSecret) {
    const incomingSecret =
      req.headers.get("x-nelsius-secret") ||
      req.headers.get("x-webhook-secret") ||
      "";
    if (incomingSecret !== webhookSecret) {
      return new Response(
        JSON.stringify({ success: false, error: "Unauthorized webhook" }),
        { status: 401 },
      );
    }
  }

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  let payload: any;
  try {
    payload = await req.json();
  } catch (_e) {
    return new Response(
      JSON.stringify({ success: false, error: "Invalid JSON" }),
      { status: 400 },
    );
  }

  const reference = extractPayloadValue(payload, [
    "reference",
    "data.reference",
    "payment.reference",
    "transaction.reference",
  ]);
  const status =
    extractPayloadValue(payload, [
      "status",
      "data.status",
      "payment.status",
      "transaction.status",
    ]) || "unknown";
  const externalId = extractPayloadValue(payload, [
    "id",
    "data.id",
    "charge_id",
    "session_id",
    "data.charge_id",
    "data.session_id",
  ]);

  if (!reference) {
    return new Response(
      JSON.stringify({ success: false, error: "Missing reference" }),
      { status: 400 },
    );
  }

  const { data: payment, error: paymentError } = await supabase
    .from("payments")
    .select("*")
    .eq("reference", reference)
    .maybeSingle();

  if (paymentError) {
    return new Response(
      JSON.stringify({ success: false, error: paymentError.message }),
      { status: 500 },
    );
  }

  if (!payment) {
    return new Response(
      JSON.stringify({ success: false, error: "Payment not found" }),
      { status: 404 },
    );
  }

  const incomingPaid = isPaidStatus(status);
  const wasAlreadyPaid = isPaidStatus(String(payment.status || ""));

  const updatePayload: Record<string, unknown> = {
    status,
    raw_payload: payload,
    external_id: externalId,
  };

  if (incomingPaid && !payment.paid_at)
    updatePayload.paid_at = new Date().toISOString();

  const { error: updateError } = await supabase
    .from("payments")
    .update(updatePayload)
    .eq("id", payment.id);

  if (updateError) {
    return new Response(
      JSON.stringify({ success: false, error: updateError.message }),
      { status: 500 },
    );
  }

  if (!incomingPaid || wasAlreadyPaid) {
    return new Response(
      JSON.stringify({
        success: true,
        message: wasAlreadyPaid
          ? "Payment already processed"
          : "Payment status recorded",
      }),
    );
  }

  if (payment.payment_type === "subscription") {
    const now = new Date();
    const end = new Date(now);
    end.setMonth(end.getMonth() + 1);

    await supabase
      .from("app_users")
      .update({
        plan: "premium",
        plan_expires_at: end.toISOString(),
        is_active: true,
      })
      .eq("id", payment.user_id);

    await supabase.from("subscriptions").insert({
      user_id: payment.user_id,
      provider: "nelsius",
      status: "active",
      start_at: now.toISOString(),
      end_at: end.toISOString(),
      payment_reference: reference,
    });
  }

  if (payment.payment_type === "deposit") {
    const { data: user } = await supabase
      .from("app_users")
      .select("wallet_balance")
      .eq("id", payment.user_id)
      .maybeSingle();

    const balanceAfter =
      (user?.wallet_balance || 0) + Number(payment.amount || 0);

    await supabase
      .from("app_users")
      .update({ wallet_balance: balanceAfter })
      .eq("id", payment.user_id);

    await supabase.from("wallet_transactions").insert({
      user_id: payment.user_id,
      payment_id: payment.id,
      type: "deposit",
      amount: payment.amount,
      currency: payment.currency,
      balance_after: balanceAfter,
      description: `Dépôt Nelsius Pay ${reference}`,
    });
  }

  return new Response(JSON.stringify({ success: true }));
});

```


## Assistant

Need add payments button and command, update messages after payment to say /payments.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Failed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "💰 Dépôt"],
        ["🧾 Paiements", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need add /payments manually and modify remaining messages. Read after 1190-1320.

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1180-1325)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Failed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "💰 Dépôt"],
        ["🧾 Paiements", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

It added /payments good. Need update message in deposit.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Failed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "💰 Dépôt"],
        ["🧾 Paiements", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 940-985)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1288-1325)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et tape /me pour vérifier ton solde.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPremium(user: any): boolean {
  if (!user) return false;
  if (user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "💰 Dépôt"],
        ["🧾 Paiements", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Tu as raison sur les deux points.

## 1. `/me` ne doit pas être utilisé comme suivi de paiement

`/me` doit rester le **profil utilisateur** :

- plan ;
- usage ;
- solde wallet ;
- expiration Premium.

Donc j’ai ajouté un vrai suivi séparé :

## Nouveau bouton ajouté

`🧾 Paiements`

Et commande :

`/payments`

L’utilisateur peut maintenant voir ses paiements récents avec :

- type : `Premium` ou `Dépôt` ;
- montant ;
- statut ;
- référence ;
- date de création ;
- date de paiement si confirmé.

Exemple de résultat :

`✅ Premium — 500 XAF`

`Statut: paid`

`Réf: SUBSCRIPTION_123456_...`

ou :

`⏳ Dépôt — 1000 XAF`

`Statut: pending`

Donc maintenant, après paiement, le bot dit plutôt :

> Clique sur 🧾 Paiements pour suivre le statut. `/me` affichera ton profil une fois le paiement confirmé.

---

## 2. Est-ce que la vérification webhook fonctionne vraiment ?

Réponse honnête : **oui, la logique est correcte**, mais elle dépend d’une chose importante :

> Nelsius Pay doit renvoyer dans son webhook la même `reference` que celle envoyée lors de la création du paiement.

Dans notre intégration, quand le bot crée un paiement Nelsius, il envoie une référence unique du genre :

`SUBSCRIPTION_123456789_1760000000000`

ou :

`DEPOSIT_123456789_1760000000000`

Cette référence est aussi enregistrée dans la table `payments`.

Quand Nelsius appelle le webhook, le code cherche cette référence dans le payload reçu.

S’il trouve la référence et un statut payé, alors il valide.

---

## 3. Ce que j’ai renforcé dans le webhook

J’ai amélioré le webhook pour éviter un problème classique :

> Nelsius peut envoyer plusieurs fois le même webhook.

Avant, si le même webhook arrivait deux fois, un dépôt pouvait potentiellement être crédité deux fois.

Maintenant, le webhook est plus sûr :

- il vérifie si le paiement était déjà payé avant ;
- si oui, il ne crédite pas une deuxième fois ;
- il répond seulement :
  - `Payment already processed`.

Donc les dépôts sont maintenant protégés contre les doubles crédits.

---

## 4. Statuts reconnus comme paiement réussi

Le webhook considère ces statuts comme paiement réussi :

- `paid`
- `success`
- `successful`
- `completed`
- `approved`
- `succeeded`

Si Nelsius Pay envoie un autre statut, par exemple :

- `confirmed`
- `SUCCESSFUL_PAYMENT`
- `PAYMENT_SUCCESS`

alors il faudra juste l’ajouter dans le code.

Donc on devra observer le premier vrai webhook dans les logs pour confirmer le statut exact.

---

## 5. Où le webhook cherche la référence

Le webhook est flexible. Il cherche la référence dans :

- `reference`
- `data.reference`
- `payment.reference`
- `transaction.reference`

Et le statut dans :

- `status`
- `data.status`
- `payment.status`
- `transaction.status`

Donc il couvre plusieurs formats possibles.

Mais si Nelsius utilise un format complètement différent, il faudra adapter.

---

## 6. Ce qui se passe après webhook confirmé

### Si c’est un paiement Premium

Le webhook :

1. met le paiement à jour ;
2. met `paid_at` ;
3. passe l’utilisateur en `plan = premium` ;
4. met `plan_expires_at` à +1 mois ;
5. crée une ligne dans `subscriptions`.

### Si c’est un dépôt

Le webhook :

1. met le paiement à jour ;
2. met `paid_at` ;
3. augmente `wallet_balance` ;
4. crée une ligne dans `wallet_transactions`.

---

## 7. Ce que l’utilisateur fait maintenant après paiement

Après paiement, il peut cliquer :

`🧾 Paiements`

ou taper :

`/payments`

Pour voir :

- paiement en attente ;
- paiement confirmé ;
- paiement échoué.

Ensuite seulement, il peut taper :

`/me`

pour voir son profil mis à jour.

---

## 8. Fichiers modifiés

J’ai modifié :

`news/supabase/functions/telegram_bot/index.ts`

et :

`news/supabase/functions/nelsius_webhook/index.ts`

---

## 9. À redéployer

Tu dois redéployer :

`telegram_bot`

et :

`nelsius_webhook`

Commandes :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

`npx supabase functions deploy nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

---

## 10. Comment être sûr à 100% que le webhook marche ?

Il faut faire un test réel.

### Étape 1

Dans Telegram :

`/deposit 100`

Tu paies réellement ou en mode test.

### Étape 2

Tu regardes :

`/payments`

Tu dois d’abord voir :

`pending`

ou le statut retourné par Nelsius.

### Étape 3

Quand Nelsius appelle le webhook, tu dois voir :

`paid`

ou :

`success`

selon le statut envoyé.

### Étape 4

Tu vérifies les logs :

`npx supabase functions logs nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

Dans les logs, on pourra voir si Nelsius envoie bien :

- la référence ;
- le statut ;
- le payload attendu.

---

## Conclusion

Je ne vais pas te dire “oui sûr à 100%” sans avoir vu le vrai payload de Nelsius.

Mais techniquement :

- la création du paiement fonctionne ;
- le webhook est prêt ;
- la référence est bien utilisée ;
- le statut est bien traité ;
- les doubles webhooks sont protégés ;
- l’utilisateur a maintenant `/payments` pour suivre précisément.

La seule chose à confirmer en production, c’est le **format exact du webhook Nelsius Pay**. Une fois qu’on voit un vrai payload dans les logs, on pourra verrouiller définitivement la validation.

## User (checkpoint)

aussi, maintenant, ajoute les fonctionalites  de pro plus qui offre les fonctionalites de traduction. du genre, sa peux traduire meme le message d'un canal qui est en autre langue en une langue cible et autres. et u rend lutlisation pour les utilisateurs  facile. aussi,  pour ceux ui n'ont pas payer l'abonnement, tu ajoute notre signature  en bas des publicatons. aussi, pour les autres canaux utilisateur, on ne filtre pas les  publications. on recupere tout les messages . maintenat dans l'abonnement pro plus, on offre aussi la possibiliter a l'utilisateur de filtrer les messages dans chaques flux. du genre filtrer et retirer les publicites  ou  remplacer les mots cles par leur identifiant personnel . ou meme remplacer les liens par des liens qu'ils vont configurer ou suprimer simplement et autres truck du genre. ce paln pro plus coute 1000fcfa par mois. si les 30 jours de l'abonnment veux deja expirer , on avertie deja l'utilisateur quelques jour avant. aussi, l'admin peux notifier les  utilisateurs du bot avec  des messages soit annonces et autres. et pour les messages, sa commence automatiquement par salut cher(nom utilisateur), message admin..

## Assistant

<thinking>
**Considering AI rewrite implementation**

I’m thinking about how to use AI for rewriting. Maybe I should separate the translation process after transforming the content? I definitely need to implement that! I’ll need to consider replacements using filters and make sure the replacements object includes actions like "keep," "remove," or "replace." Also, I should focus on filtering ads or mots, particularly for pro_plus users. There’s a lot to keep in mind here!
</thinking>

Je vais ajouter le plan **Pro Plus** à 1000 FCFA/mois, les options de traduction/filtres avancés par flux, la signature automatique pour les non-abonnés, les alertes avant expiration, et la notification admin aux utilisateurs. Je vais faire ça en plusieurs modifications ciblées.

**Tool Call: supabase\\migrations\\20260511203000\_add\_pro\_plus\_features.sql**
Status: Completed

Diff: supabase\migrations\20260511203000_add_pro_plus_features.sql
```
-- Pro Plus features: translation, advanced filters, subscription warnings

ALTER TABLE public.app_users
  ADD COLUMN IF NOT EXISTS last_subscription_warning_at TIMESTAMP WITH TIME ZONE;

ALTER TABLE public.flows
  ALTER COLUMN filters SET DEFAULT '{
    "include_keywords": [],
    "exclude_keywords": [],
    "block_ads": false,
    "media_only": false,
    "allow_text": true,
    "allow_photos": true,
    "allow_videos": true,
    "allow_albums": true,
    "use_ai_rewrite": false,
    "remove_links": false,
    "remove_mentions": false,
    "signature_text": "",
    "translate_enabled": false,
    "target_language": "fr",
    "replacements": {},
    "link_action": "keep",
    "link_replacement": ""
  }'::jsonb;

```


**Tool Call: Search files for regex `DEFAULT_FLOW_FILTERS|isPremiumUser|skipReasonForFilters|transformContent|restructureWithAI|textToPublish|filters\.use_ai_rewrite` (case-sensitive)**
Status: Completed

Found 14 matches:

## Matches in news\supabase\functions\aggregate_news\index.ts

### async function restructureWithAI( ) › L377-387
```
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
```

32 lines remaining in ancestor node. Read the file to see all.

### const DEFAULT_FLOW_FILTERS › L926-936
```
const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: true,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
```

3 lines remaining in ancestor node. Read the file to see all.

### function isPremiumUser( ) › L941-945
```
function isPremiumUser(user: any): boolean {
  if (!user || user.plan !== "premium") return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}
```

### function normalizeFilters( ) › L947-949
```
function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}
```

### function skipReasonForFilters( ) › L958-968
```
function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
```

17 lines remaining in ancestor node. Read the file to see all.

### function transformContent( ) › L987-994
```
function transformContent(text: string, filters: any): string {
  let out = text.trim();
  if (filters.remove_links) out = out.replace(/https?:\/\/\S+/gi, "").trim();
  if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
  if (filters.signature_text)
    out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  return out;
}
```

### const premium › L1056-1066
```
    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
```

201 lines remaining in ancestor node. Read the file to see all.

### const reason › L1123-1127
```
          if (alreadyProcessed) continue;

          const reason = skipReasonForFilters(msg, filters);
          if (reason) {
            skippedCount++;
```

### L1143-1155
```

          let finalContent = msg.text;
          if (filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
```

### const textToPublish › L1165-1168
```
          }

          const textToPublish =
            transformContent(finalContent, filters) || originalUrl;
```

### L1174-1178
```
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
```

### L1185-1189
```
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
```

### L1195-1199
```
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
```

### L1201-1204
```
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
```


## Assistant

Need edit aggregate. Add pro plus checks and translations. Pro vs ProPlus. Signature for non-subscribed. "Pour autres canaux utilisateur, on ne filtre pas publications" means default block_ads false and filters only if pro_plus. For non pro plus ignore filters completely? "pro plus offers possibility filter", so only pro_plus can filter. But maybe signature applies to free. Premium maybe no signature? "ceux qui n'ont pas payer l'abonnement" means free. Premium and pro_plus no signature. Need pro plus plan value "pro_plus", premium (500) still multiple flows; pro_plus includes premium plus filters/translation. Modify isPremiumUser => premium or pro_plus. Add isProPlusUser. Default block_ads false.

Translation using Gemini. Add function translateWithAI. Maybe combine with rewrite. We'll add after restructure. Need target_language.

Transform content should take proPlus and userPaid. For free add "\n\n🤖 Propulsé par @..." We don't know signature text. Use env DEFAULT_PUBLIC_SIGNATURE or "🤖 Propulsé par IzyNews". Need maybe bot username? At aggregate no getMe. Use Deno.env.get("FREE_USER_SIGNATURE") || "🤖 Propulsé par @izynews". Good.

For pro plus filters: only call skipReasonForFilters if proPlus. Transform advanced only if proPlus, else raw with signature if free. However text content maybe translate if proPlus. Translation: if filters.translate_enabled and GEMINI. Need "translateWithAI(finalContent, target_language)".
Replacements: object {word: replacement}; link_action. Implement in transformContent.

Need edit.

**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";
const FREE_USER_SIGNATURE =
  Deno.env.get("FREE_USER_SIGNATURE") || "🤖 Propulsé par IzyNews";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

function isPlanActive(user: any, planNames: string[]): boolean {
  if (!user || !planNames.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremiumUser(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlusUser(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}

function hasAnyKeyword(text: string, keywords: string[]): boolean {
  const lower = text.toLowerCase();
  return keywords.some((keyword) =>
    lower.includes(String(keyword).toLowerCase()),
  );
}

function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
  const isAlbum = Boolean(msg.photoUrls && msg.photoUrls.length > 1);
  const hasVideo = Boolean(msg.videoUrl);
  const hasText = msg.text.trim().length > 0;

  if (filters.media_only && !msg.hasMedia) return "media_only";
  if (!filters.allow_text && hasText && !msg.hasMedia)
    return "text_not_allowed";
  if (!filters.allow_photos && hasPhotos) return "photos_not_allowed";
  if (!filters.allow_videos && hasVideo) return "videos_not_allowed";
  if (!filters.allow_albums && isAlbum) return "albums_not_allowed";
  if (include.length > 0 && !hasAnyKeyword(msg.text, include))
    return "missing_include_keyword";
  if (exclude.length > 0 && hasAnyKeyword(msg.text, exclude))
    return "excluded_keyword";
  if (filters.block_ads && isQuickAd(msg.text)) return "ad_blocked";
  return null;
}

function transformContent(text: string, filters: any): string {
  let out = text.trim();
  if (filters.remove_links) out = out.replace(/https?:\/\/\S+/gi, "").trim();
  if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
  if (filters.signature_text)
    out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  return out;
}

async function recordActivity(supabase: any, payload: Record<string, unknown>) {
  const { error } = await supabase.from("flow_activity").insert(payload);
  if (error) console.error("[Aggregate] flow_activity insert error:", error);
}

async function incrementUsage(supabase: any, userId: string, amount: number) {
  const day = new Date().toISOString().slice(0, 10);
  const { data } = await supabase
    .from("usage_daily")
    .select("id, analyzed_count")
    .eq("user_id", userId)
    .eq("day", day)
    .maybeSingle();

  if (data) {
    await supabase
      .from("usage_daily")
      .update({ analyzed_count: (data.analyzed_count || 0) + amount })
      .eq("id", data.id);
  } else {
    await supabase
      .from("usage_daily")
      .insert({ user_id: userId, day, analyzed_count: amount });
  }
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (_req: Request) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: flows, error: flowError } = await supabase
      .from("flows")
      .select(
        "*, user:app_users(*), source:user_sources(*), target:user_targets(*)",
      )
      .eq("is_active", true)
      .order("created_at", { ascending: true });

    if (flowError) throw flowError;

    if (!flows || flows.length === 0) {
      return new Response(
        JSON.stringify({
          success: true,
          processed: 0,
          new: 0,
          message: "No active user flows",
        }),
        { headers: { "Content-Type": "application/json" } },
      );
    }

    let processedCount = 0;
    let publishedCount = 0;
    let skippedCount = 0;

    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
      const channel = source.config?.channel;
      const targetChatId = target.chat_id;
      const lastMsgId = Number(flow.last_message_id || 0);
      if (!channel || !targetChatId) continue;

      console.log(
        `[Aggregate] Flow ${flow.id}: @${channel} → ${targetChatId}, after ${lastMsgId}`,
      );

      try {
        const messages = await scrapeChannel(channel, lastMsgId);
        let messagesToProcess = messages;
        if (
          lastMsgId === 0 &&
          messages.length > Number(flow.initial_last_n || 5)
        ) {
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (!premium && analyzedForThisFlow > 5) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason: "free_daily_limit",
              original_url: `https://t.me/${channel}/${msg.id}`,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")
            .maybeSingle();

          if (alreadyProcessed) continue;

          const reason = skipReasonForFilters(msg, filters);
          if (reason) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason,
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          let finalContent = msg.text;
          if (filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
                reason: "ai_ad_blocked",
                original_url: originalUrl,
                text_preview: msg.text.substring(0, 200),
                media_count:
                  (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
              });
              continue;
            }
            finalContent = aiRes.content.trim();
          }

          const textToPublish =
            transformContent(finalContent, filters) || originalUrl;
          const isShortVideo =
            msg.videoUrl && (!msg.videoDuration || msg.videoDuration <= 60);
          let telegramMsgId: number | null = null;

          if (isShortVideo && msg.videoUrl && filters.allow_videos) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 1 &&
            filters.allow_albums
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 0 &&
            filters.allow_photos
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (filters.allow_text) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }

          if (!telegramMsgId) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "failed",
              reason: "publish_failed",
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          publishedCount++;
          await recordActivity(supabase, {
            user_id: user.id,
            flow_id: flow.id,
            source_id: source.id,
            target_id: target.id,
            source_message_id: msg.id,
            target_message_ids: [telegramMsgId],
            status: "published",
            original_url: originalUrl,
            text_preview: msg.text.substring(0, 200),
            media_count: (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            raw_payload: {
              target_chat_id: targetChatId,
              source_channel: channel,
            },
          });

          await new Promise((r) => setTimeout(r, 1500));
        }

        if (analyzedForThisFlow > 0)
          await incrementUsage(supabase, user.id, analyzedForThisFlow);

        await supabase
          .from("flows")
          .update({
            last_message_id: maxMsgId,
            last_run_at: new Date().toISOString(),
            last_error: null,
          })
          .eq("id", flow.id);
      } catch (flowErr) {
        console.error(`[Aggregate] Flow ${flow.id} failed:`, flowErr);
        await supabase
          .from("flows")
          .update({
            last_run_at: new Date().toISOString(),
            last_error: (flowErr as Error).message,
          })
          .eq("id", flow.id);
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: publishedCount,
        skipped: skippedCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";
const FREE_USER_SIGNATURE =
  Deno.env.get("FREE_USER_SIGNATURE") || "🤖 Propulsé par IzyNews";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

async function translateWithAI(
  rawText: string,
  targetLanguage: string,
): Promise<string> {
  if (!GEMINI_API_KEY || rawText.length < 3) return rawText;
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Traduis le message suivant en ${targetLanguage}. Conserve le sens, les noms propres, les emojis pertinents et les liens si présents. Réponds uniquement avec le texte traduit.\n\n${rawText}`,
                },
              ],
            },
          ],
        }),
      },
    );
    const data = await res.json();
    return data.candidates?.[0]?.content?.parts?.[0]?.text?.trim() || rawText;
  } catch (e) {
    console.error("[Gemini] Translation error:", e);
    return rawText;
  }
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

function isPlanActive(user: any, planNames: string[]): boolean {
  if (!user || !planNames.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremiumUser(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlusUser(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}

function hasAnyKeyword(text: string, keywords: string[]): boolean {
  const lower = text.toLowerCase();
  return keywords.some((keyword) =>
    lower.includes(String(keyword).toLowerCase()),
  );
}

function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
  const isAlbum = Boolean(msg.photoUrls && msg.photoUrls.length > 1);
  const hasVideo = Boolean(msg.videoUrl);
  const hasText = msg.text.trim().length > 0;

  if (filters.media_only && !msg.hasMedia) return "media_only";
  if (!filters.allow_text && hasText && !msg.hasMedia)
    return "text_not_allowed";
  if (!filters.allow_photos && hasPhotos) return "photos_not_allowed";
  if (!filters.allow_videos && hasVideo) return "videos_not_allowed";
  if (!filters.allow_albums && isAlbum) return "albums_not_allowed";
  if (include.length > 0 && !hasAnyKeyword(msg.text, include))
    return "missing_include_keyword";
  if (exclude.length > 0 && hasAnyKeyword(msg.text, exclude))
    return "excluded_keyword";
  if (filters.block_ads && isQuickAd(msg.text)) return "ad_blocked";
  return null;
}

function transformContent(
  text: string,
  filters: any,
  proPlus: boolean,
  paidUser: boolean,
): string {
  let out = text.trim();

  if (proPlus) {
    const replacements = filters.replacements || {};
    for (const [from, to] of Object.entries(replacements)) {
      if (!from) continue;
      out = out.replace(
        new RegExp(String(from).replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi"),
        String(to),
      );
    }

    if (filters.link_action === "remove" || filters.remove_links) {
      out = out.replace(/https?:\/\/\S+/gi, "").trim();
    } else if (filters.link_action === "replace" && filters.link_replacement) {
      out = out
        .replace(/https?:\/\/\S+/gi, String(filters.link_replacement))
        .trim();
    }

    if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
    if (filters.signature_text)
      out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  }

  if (!paidUser) out = `${out}\n\n${FREE_USER_SIGNATURE}`.trim();
  return out;
}

async function recordActivity(supabase: any, payload: Record<string, unknown>) {
  const { error } = await supabase.from("flow_activity").insert(payload);
  if (error) console.error("[Aggregate] flow_activity insert error:", error);
}

async function incrementUsage(supabase: any, userId: string, amount: number) {
  const day = new Date().toISOString().slice(0, 10);
  const { data } = await supabase
    .from("usage_daily")
    .select("id, analyzed_count")
    .eq("user_id", userId)
    .eq("day", day)
    .maybeSingle();

  if (data) {
    await supabase
      .from("usage_daily")
      .update({ analyzed_count: (data.analyzed_count || 0) + amount })
      .eq("id", data.id);
  } else {
    await supabase
      .from("usage_daily")
      .insert({ user_id: userId, day, analyzed_count: amount });
  }
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (_req: Request) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: flows, error: flowError } = await supabase
      .from("flows")
      .select(
        "*, user:app_users(*), source:user_sources(*), target:user_targets(*)",
      )
      .eq("is_active", true)
      .order("created_at", { ascending: true });

    if (flowError) throw flowError;

    if (!flows || flows.length === 0) {
      return new Response(
        JSON.stringify({
          success: true,
          processed: 0,
          new: 0,
          message: "No active user flows",
        }),
        { headers: { "Content-Type": "application/json" } },
      );
    }

    let processedCount = 0;
    let publishedCount = 0;
    let skippedCount = 0;

    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
      const channel = source.config?.channel;
      const targetChatId = target.chat_id;
      const lastMsgId = Number(flow.last_message_id || 0);
      if (!channel || !targetChatId) continue;

      console.log(
        `[Aggregate] Flow ${flow.id}: @${channel} → ${targetChatId}, after ${lastMsgId}`,
      );

      try {
        const messages = await scrapeChannel(channel, lastMsgId);
        let messagesToProcess = messages;
        if (
          lastMsgId === 0 &&
          messages.length > Number(flow.initial_last_n || 5)
        ) {
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (!premium && analyzedForThisFlow > 5) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason: "free_daily_limit",
              original_url: `https://t.me/${channel}/${msg.id}`,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")
            .maybeSingle();

          if (alreadyProcessed) continue;

          const reason = skipReasonForFilters(msg, filters);
          if (reason) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason,
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          let finalContent = msg.text;
          if (filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
                reason: "ai_ad_blocked",
                original_url: originalUrl,
                text_preview: msg.text.substring(0, 200),
                media_count:
                  (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
              });
              continue;
            }
            finalContent = aiRes.content.trim();
          }

          const textToPublish =
            transformContent(finalContent, filters) || originalUrl;
          const isShortVideo =
            msg.videoUrl && (!msg.videoDuration || msg.videoDuration <= 60);
          let telegramMsgId: number | null = null;

          if (isShortVideo && msg.videoUrl && filters.allow_videos) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 1 &&
            filters.allow_albums
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 0 &&
            filters.allow_photos
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (filters.allow_text) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }

          if (!telegramMsgId) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "failed",
              reason: "publish_failed",
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          publishedCount++;
          await recordActivity(supabase, {
            user_id: user.id,
            flow_id: flow.id,
            source_id: source.id,
            target_id: target.id,
            source_message_id: msg.id,
            target_message_ids: [telegramMsgId],
            status: "published",
            original_url: originalUrl,
            text_preview: msg.text.substring(0, 200),
            media_count: (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            raw_payload: {
              target_chat_id: targetChatId,
              source_channel: channel,
            },
          });

          await new Promise((r) => setTimeout(r, 1500));
        }

        if (analyzedForThisFlow > 0)
          await incrementUsage(supabase, user.id, analyzedForThisFlow);

        await supabase
          .from("flows")
          .update({
            last_message_id: maxMsgId,
            last_run_at: new Date().toISOString(),
            last_error: null,
          })
          .eq("id", flow.id);
      } catch (flowErr) {
        console.error(`[Aggregate] Flow ${flow.id} failed:`, flowErr);
        await supabase
          .from("flows")
          .update({
            last_run_at: new Date().toISOString(),
            last_error: (flowErr as Error).message,
          })
          .eq("id", flow.id);
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: publishedCount,
        skipped: skippedCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


## Assistant

<thinking>
**Modifying function parameters**

I need to adjust some main function parameters, specifically for premium const and proPlus. The reasoning should only be applicable if the proPlus condition is met. Also, I should make sure to use the AI rewrite or translation features solely for proPlus. The textToPublish arguments will need specific attention as well. It's all about keeping things organized and ensuring the functionality is clear based on the proPlus requirement.
</thinking>

**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";
const FREE_USER_SIGNATURE =
  Deno.env.get("FREE_USER_SIGNATURE") || "🤖 Propulsé par IzyNews";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

async function translateWithAI(
  rawText: string,
  targetLanguage: string,
): Promise<string> {
  if (!GEMINI_API_KEY || rawText.length < 3) return rawText;
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Traduis le message suivant en ${targetLanguage}. Conserve le sens, les noms propres, les emojis pertinents et les liens si présents. Réponds uniquement avec le texte traduit.\n\n${rawText}`,
                },
              ],
            },
          ],
        }),
      },
    );
    const data = await res.json();
    return data.candidates?.[0]?.content?.parts?.[0]?.text?.trim() || rawText;
  } catch (e) {
    console.error("[Gemini] Translation error:", e);
    return rawText;
  }
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

function isPlanActive(user: any, planNames: string[]): boolean {
  if (!user || !planNames.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremiumUser(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlusUser(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}

function hasAnyKeyword(text: string, keywords: string[]): boolean {
  const lower = text.toLowerCase();
  return keywords.some((keyword) =>
    lower.includes(String(keyword).toLowerCase()),
  );
}

function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
  const isAlbum = Boolean(msg.photoUrls && msg.photoUrls.length > 1);
  const hasVideo = Boolean(msg.videoUrl);
  const hasText = msg.text.trim().length > 0;

  if (filters.media_only && !msg.hasMedia) return "media_only";
  if (!filters.allow_text && hasText && !msg.hasMedia)
    return "text_not_allowed";
  if (!filters.allow_photos && hasPhotos) return "photos_not_allowed";
  if (!filters.allow_videos && hasVideo) return "videos_not_allowed";
  if (!filters.allow_albums && isAlbum) return "albums_not_allowed";
  if (include.length > 0 && !hasAnyKeyword(msg.text, include))
    return "missing_include_keyword";
  if (exclude.length > 0 && hasAnyKeyword(msg.text, exclude))
    return "excluded_keyword";
  if (filters.block_ads && isQuickAd(msg.text)) return "ad_blocked";
  return null;
}

function transformContent(
  text: string,
  filters: any,
  proPlus: boolean,
  paidUser: boolean,
): string {
  let out = text.trim();

  if (proPlus) {
    const replacements = filters.replacements || {};
    for (const [from, to] of Object.entries(replacements)) {
      if (!from) continue;
      out = out.replace(
        new RegExp(String(from).replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi"),
        String(to),
      );
    }

    if (filters.link_action === "remove" || filters.remove_links) {
      out = out.replace(/https?:\/\/\S+/gi, "").trim();
    } else if (filters.link_action === "replace" && filters.link_replacement) {
      out = out
        .replace(/https?:\/\/\S+/gi, String(filters.link_replacement))
        .trim();
    }

    if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
    if (filters.signature_text)
      out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  }

  if (!paidUser) out = `${out}\n\n${FREE_USER_SIGNATURE}`.trim();
  return out;
}

async function recordActivity(supabase: any, payload: Record<string, unknown>) {
  const { error } = await supabase.from("flow_activity").insert(payload);
  if (error) console.error("[Aggregate] flow_activity insert error:", error);
}

async function incrementUsage(supabase: any, userId: string, amount: number) {
  const day = new Date().toISOString().slice(0, 10);
  const { data } = await supabase
    .from("usage_daily")
    .select("id, analyzed_count")
    .eq("user_id", userId)
    .eq("day", day)
    .maybeSingle();

  if (data) {
    await supabase
      .from("usage_daily")
      .update({ analyzed_count: (data.analyzed_count || 0) + amount })
      .eq("id", data.id);
  } else {
    await supabase
      .from("usage_daily")
      .insert({ user_id: userId, day, analyzed_count: amount });
  }
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (_req: Request) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: flows, error: flowError } = await supabase
      .from("flows")
      .select(
        "*, user:app_users(*), source:user_sources(*), target:user_targets(*)",
      )
      .eq("is_active", true)
      .order("created_at", { ascending: true });

    if (flowError) throw flowError;

    if (!flows || flows.length === 0) {
      return new Response(
        JSON.stringify({
          success: true,
          processed: 0,
          new: 0,
          message: "No active user flows",
        }),
        { headers: { "Content-Type": "application/json" } },
      );
    }

    let processedCount = 0;
    let publishedCount = 0;
    let skippedCount = 0;

    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
      const proPlus = isProPlusUser(user);
      const channel = source.config?.channel;
      const targetChatId = target.chat_id;
      const lastMsgId = Number(flow.last_message_id || 0);
      if (!channel || !targetChatId) continue;

      console.log(
        `[Aggregate] Flow ${flow.id}: @${channel} → ${targetChatId}, after ${lastMsgId}`,
      );

      try {
        const messages = await scrapeChannel(channel, lastMsgId);
        let messagesToProcess = messages;
        if (
          lastMsgId === 0 &&
          messages.length > Number(flow.initial_last_n || 5)
        ) {
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (!premium && analyzedForThisFlow > 5) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason: "free_daily_limit",
              original_url: `https://t.me/${channel}/${msg.id}`,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")
            .maybeSingle();

          if (alreadyProcessed) continue;

          const reason = proPlus ? skipReasonForFilters(msg, filters) : null;
          if (reason) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason,
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          let finalContent = msg.text;
          if (proPlus && filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
                reason: "ai_ad_blocked",
                original_url: originalUrl,
                text_preview: msg.text.substring(0, 200),
                media_count:
                  (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
              });
              continue;
            }
            finalContent = aiRes.content.trim();
          }

          if (proPlus && filters.translate_enabled) {
            finalContent = await translateWithAI(
              finalContent,
              filters.target_language || "fr",
            );
          }

          const textToPublish =
            transformContent(finalContent, filters, proPlus, premium) ||
            originalUrl;
          const isShortVideo =
            msg.videoUrl && (!msg.videoDuration || msg.videoDuration <= 60);
          let telegramMsgId: number | null = null;

          if (isShortVideo && msg.videoUrl && filters.allow_videos) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 1 &&
            filters.allow_albums
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 0 &&
            filters.allow_photos
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (filters.allow_text) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }

          if (!telegramMsgId) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "failed",
              reason: "publish_failed",
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          publishedCount++;
          await recordActivity(supabase, {
            user_id: user.id,
            flow_id: flow.id,
            source_id: source.id,
            target_id: target.id,
            source_message_id: msg.id,
            target_message_ids: [telegramMsgId],
            status: "published",
            original_url: originalUrl,
            text_preview: msg.text.substring(0, 200),
            media_count: (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            raw_payload: {
              target_chat_id: targetChatId,
              source_channel: channel,
            },
          });

          await new Promise((r) => setTimeout(r, 1500));
        }

        if (analyzedForThisFlow > 0)
          await incrementUsage(supabase, user.id, analyzedForThisFlow);

        await supabase
          .from("flows")
          .update({
            last_message_id: maxMsgId,
            last_run_at: new Date().toISOString(),
            last_error: null,
          })
          .eq("id", flow.id);
      } catch (flowErr) {
        console.error(`[Aggregate] Flow ${flow.id} failed:`, flowErr);
        await supabase
          .from("flows")
          .update({
            last_run_at: new Date().toISOString(),
            last_error: (flowErr as Error).message,
          })
          .eq("id", flow.id);
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: publishedCount,
        skipped: skippedCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need commandFromButton map "⚙️ Filtres Pro+" missing. Add. Also createNelsiusCheckout call for proplus plan. Webhook must activate plan from metadata. Modify nelsius_webhook subscription plan. Also bot commands proplus, pro filters maybe enough with commands. Need add filter commands: /translate <flow_id> language on/off? /replace <flow_id> old=>new ; /links <flow_id> keep|remove|replace url ; /blockads already exists but allow only proPlus? Currently filter commands exist for all. Need guard: if not proplus and filter command -> require proplus. But /filters view okay. Add to filter command block. Also add /profilters help.

Need subscription warning function maybe complicated. Implement in aggregate? better in bot /me? Requirement warn before expiration. Need scheduled can be in aggregate to send Telegram messages. Add maybe simple in aggregate on each run: if plan_expires_at <= 3 days and last_warning null or >1day send message to user. Needs BOT_TOKEN; user.telegram_user_id. Implement in aggregate maybe later. Also admin broadcast. Need do manageable.

First edit bot: map, proplus command, filter advanced.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Failed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need read filter block.

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1728-1805)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Failed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: supabase\\functions\\nelsius\_webhook\\index.ts**
Status: Completed

Diff: supabase\functions\nelsius_webhook\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

function isPaidStatus(status: string): boolean {
  return [
    "paid",
    "success",
    "successful",
    "completed",
    "approved",
    "succeeded",
  ].includes(status.toLowerCase());
}

function extractPayloadValue(payload: any, keys: string[]): string | null {
  for (const key of keys) {
    const value = key
      .split(".")
      .reduce((acc: any, part: string) => acc?.[part], payload);
    if (value !== undefined && value !== null && String(value).trim())
      return String(value);
  }
  return null;
}

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Nelsius webhook OK", { status: 200 });
  }

  const webhookSecret = Deno.env.get("NELSIUS_WEBHOOK_SECRET") || "";
  if (webhookSecret) {
    const incomingSecret =
      req.headers.get("x-nelsius-secret") ||
      req.headers.get("x-webhook-secret") ||
      "";
    if (incomingSecret !== webhookSecret) {
      return new Response(
        JSON.stringify({ success: false, error: "Unauthorized webhook" }),
        { status: 401 },
      );
    }
  }

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  let payload: any;
  try {
    payload = await req.json();
  } catch (_e) {
    return new Response(
      JSON.stringify({ success: false, error: "Invalid JSON" }),
      { status: 400 },
    );
  }

  const reference = extractPayloadValue(payload, [
    "reference",
    "data.reference",
    "payment.reference",
    "transaction.reference",
  ]);
  const status =
    extractPayloadValue(payload, [
      "status",
      "data.status",
      "payment.status",
      "transaction.status",
    ]) || "unknown";
  const externalId = extractPayloadValue(payload, [
    "id",
    "data.id",
    "charge_id",
    "session_id",
    "data.charge_id",
    "data.session_id",
  ]);

  if (!reference) {
    return new Response(
      JSON.stringify({ success: false, error: "Missing reference" }),
      { status: 400 },
    );
  }

  const { data: payment, error: paymentError } = await supabase
    .from("payments")
    .select("*")
    .eq("reference", reference)
    .maybeSingle();

  if (paymentError) {
    return new Response(
      JSON.stringify({ success: false, error: paymentError.message }),
      { status: 500 },
    );
  }

  if (!payment) {
    return new Response(
      JSON.stringify({ success: false, error: "Payment not found" }),
      { status: 404 },
    );
  }

  const incomingPaid = isPaidStatus(status);
  const wasAlreadyPaid = isPaidStatus(String(payment.status || ""));

  const updatePayload: Record<string, unknown> = {
    status,
    raw_payload: payload,
    external_id: externalId,
  };

  if (incomingPaid && !payment.paid_at)
    updatePayload.paid_at = new Date().toISOString();

  const { error: updateError } = await supabase
    .from("payments")
    .update(updatePayload)
    .eq("id", payment.id);

  if (updateError) {
    return new Response(
      JSON.stringify({ success: false, error: updateError.message }),
      { status: 500 },
    );
  }

  if (!incomingPaid || wasAlreadyPaid) {
    return new Response(
      JSON.stringify({
        success: true,
        message: wasAlreadyPaid
          ? "Payment already processed"
          : "Payment status recorded",
      }),
    );
  }

  if (payment.payment_type === "subscription") {
    const now = new Date();
    const end = new Date(now);
    end.setMonth(end.getMonth() + 1);
    const requestedPlan =
      payment.metadata?.plan === "pro_plus" ? "pro_plus" : "premium";

    await supabase
      .from("app_users")
      .update({
        plan: requestedPlan,
        plan_expires_at: end.toISOString(),
        is_active: true,
      })
      .eq("id", payment.user_id);

    await supabase.from("subscriptions").insert({
      user_id: payment.user_id,
      provider: "nelsius",
      status: requestedPlan,
      start_at: now.toISOString(),
      end_at: end.toISOString(),
      payment_reference: reference,
    });
  }

  if (payment.payment_type === "deposit") {
    const { data: user } = await supabase
      .from("app_users")
      .select("wallet_balance")
      .eq("id", payment.user_id)
      .maybeSingle();

    const balanceAfter =
      (user?.wallet_balance || 0) + Number(payment.amount || 0);

    await supabase
      .from("app_users")
      .update({ wallet_balance: balanceAfter })
      .eq("id", payment.user_id);

    await supabase.from("wallet_transactions").insert({
      user_id: payment.user_id,
      payment_id: payment.id,
      type: "deposit",
      amount: payment.amount,
      currency: payment.currency,
      balance_after: balanceAfter,
      description: `Dépôt Nelsius Pay ${reference}`,
    });
  }

  return new Response(JSON.stringify({ success: true }));
});

```


## Assistant

Need admin broadcast and expiration warnings. Admin broadcast in bot state. Need add in bot. Also subscription warning in aggregate. Let's implement admin broadcast.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

<thinking>
**Considering expiration warning logic**

I need to create an expiration warning mechanism in the system. I think adding a function to send a direct message to the user's Telegram ID using `sendTextToTelegram(message, user.telegram_user_id)` is essential. It might be better to identify unique users before entering the flow loop; otherwise, I could warn users multiple times if they’re enrolled in various flows. I want to ensure the warning status updates correctly based on subscription dates. Let’s refine this!
</thinking>

**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";
const FREE_USER_SIGNATURE =
  Deno.env.get("FREE_USER_SIGNATURE") || "🤖 Propulsé par IzyNews";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

async function translateWithAI(
  rawText: string,
  targetLanguage: string,
): Promise<string> {
  if (!GEMINI_API_KEY || rawText.length < 3) return rawText;
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Traduis le message suivant en ${targetLanguage}. Conserve le sens, les noms propres, les emojis pertinents et les liens si présents. Réponds uniquement avec le texte traduit.\n\n${rawText}`,
                },
              ],
            },
          ],
        }),
      },
    );
    const data = await res.json();
    return data.candidates?.[0]?.content?.parts?.[0]?.text?.trim() || rawText;
  } catch (e) {
    console.error("[Gemini] Translation error:", e);
    return rawText;
  }
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

function isPlanActive(user: any, planNames: string[]): boolean {
  if (!user || !planNames.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremiumUser(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlusUser(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}

function hasAnyKeyword(text: string, keywords: string[]): boolean {
  const lower = text.toLowerCase();
  return keywords.some((keyword) =>
    lower.includes(String(keyword).toLowerCase()),
  );
}

function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
  const isAlbum = Boolean(msg.photoUrls && msg.photoUrls.length > 1);
  const hasVideo = Boolean(msg.videoUrl);
  const hasText = msg.text.trim().length > 0;

  if (filters.media_only && !msg.hasMedia) return "media_only";
  if (!filters.allow_text && hasText && !msg.hasMedia)
    return "text_not_allowed";
  if (!filters.allow_photos && hasPhotos) return "photos_not_allowed";
  if (!filters.allow_videos && hasVideo) return "videos_not_allowed";
  if (!filters.allow_albums && isAlbum) return "albums_not_allowed";
  if (include.length > 0 && !hasAnyKeyword(msg.text, include))
    return "missing_include_keyword";
  if (exclude.length > 0 && hasAnyKeyword(msg.text, exclude))
    return "excluded_keyword";
  if (filters.block_ads && isQuickAd(msg.text)) return "ad_blocked";
  return null;
}

function transformContent(
  text: string,
  filters: any,
  proPlus: boolean,
  paidUser: boolean,
): string {
  let out = text.trim();

  if (proPlus) {
    const replacements = filters.replacements || {};
    for (const [from, to] of Object.entries(replacements)) {
      if (!from) continue;
      out = out.replace(
        new RegExp(String(from).replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi"),
        String(to),
      );
    }

    if (filters.link_action === "remove" || filters.remove_links) {
      out = out.replace(/https?:\/\/\S+/gi, "").trim();
    } else if (filters.link_action === "replace" && filters.link_replacement) {
      out = out
        .replace(/https?:\/\/\S+/gi, String(filters.link_replacement))
        .trim();
    }

    if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
    if (filters.signature_text)
      out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  }

  if (!paidUser) out = `${out}\n\n${FREE_USER_SIGNATURE}`.trim();
  return out;
}

async function recordActivity(supabase: any, payload: Record<string, unknown>) {
  const { error } = await supabase.from("flow_activity").insert(payload);
  if (error) console.error("[Aggregate] flow_activity insert error:", error);
}

async function incrementUsage(supabase: any, userId: string, amount: number) {
  const day = new Date().toISOString().slice(0, 10);
  const { data } = await supabase
    .from("usage_daily")
    .select("id, analyzed_count")
    .eq("user_id", userId)
    .eq("day", day)
    .maybeSingle();

  if (data) {
    await supabase
      .from("usage_daily")
      .update({ analyzed_count: (data.analyzed_count || 0) + amount })
      .eq("id", data.id);
  } else {
    await supabase
      .from("usage_daily")
      .insert({ user_id: userId, day, analyzed_count: amount });
  }
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (_req: Request) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: flows, error: flowError } = await supabase
      .from("flows")
      .select(
        "*, user:app_users(*), source:user_sources(*), target:user_targets(*)",
      )
      .eq("is_active", true)
      .order("created_at", { ascending: true });

    if (flowError) throw flowError;

    if (!flows || flows.length === 0) {
      return new Response(
        JSON.stringify({
          success: true,
          processed: 0,
          new: 0,
          message: "No active user flows",
        }),
        { headers: { "Content-Type": "application/json" } },
      );
    }

    let processedCount = 0;
    let publishedCount = 0;
    let skippedCount = 0;
    const warnedUserIds = new Set<string>();

    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
      const proPlus = isProPlusUser(user);

      if (premium && user.plan_expires_at && !warnedUserIds.has(user.id)) {
        const expiresAt = new Date(user.plan_expires_at).getTime();
        const daysLeft = Math.ceil((expiresAt - Date.now()) / 86400000);
        const lastWarn = user.last_subscription_warning_at
          ? new Date(user.last_subscription_warning_at).getTime()
          : 0;
        if (daysLeft > 0 && daysLeft <= 5 && Date.now() - lastWarn > 86400000) {
          await sendTextToTelegram(
            `⏰ Salut ${user.username ? "@" + user.username : "cher utilisateur"},\n\nTon abonnement <b>${user.plan === "pro_plus" ? "Pro Plus" : "Premium"}</b> expire dans <b>${daysLeft} jour(s)</b>. Pense à renouveler pour garder tes fonctionnalités actives.`,
            String(user.telegram_user_id),
          );
          await supabase
            .from("app_users")
            .update({ last_subscription_warning_at: new Date().toISOString() })
            .eq("id", user.id);
          warnedUserIds.add(user.id);
        }
      }

      const channel = source.config?.channel;
      const targetChatId = target.chat_id;
      const lastMsgId = Number(flow.last_message_id || 0);
      if (!channel || !targetChatId) continue;

      console.log(
        `[Aggregate] Flow ${flow.id}: @${channel} → ${targetChatId}, after ${lastMsgId}`,
      );

      try {
        const messages = await scrapeChannel(channel, lastMsgId);
        let messagesToProcess = messages;
        if (
          lastMsgId === 0 &&
          messages.length > Number(flow.initial_last_n || 5)
        ) {
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (!premium && analyzedForThisFlow > 5) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason: "free_daily_limit",
              original_url: `https://t.me/${channel}/${msg.id}`,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")
            .maybeSingle();

          if (alreadyProcessed) continue;

          const reason = proPlus ? skipReasonForFilters(msg, filters) : null;
          if (reason) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason,
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          let finalContent = msg.text;
          if (proPlus && filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
                reason: "ai_ad_blocked",
                original_url: originalUrl,
                text_preview: msg.text.substring(0, 200),
                media_count:
                  (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
              });
              continue;
            }
            finalContent = aiRes.content.trim();
          }

          if (proPlus && filters.translate_enabled) {
            finalContent = await translateWithAI(
              finalContent,
              filters.target_language || "fr",
            );
          }

          const textToPublish =
            transformContent(finalContent, filters, proPlus, premium) ||
            originalUrl;
          const isShortVideo =
            msg.videoUrl && (!msg.videoDuration || msg.videoDuration <= 60);
          let telegramMsgId: number | null = null;

          if (isShortVideo && msg.videoUrl && filters.allow_videos) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 1 &&
            filters.allow_albums
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 0 &&
            filters.allow_photos
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (filters.allow_text) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }

          if (!telegramMsgId) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "failed",
              reason: "publish_failed",
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          publishedCount++;
          await recordActivity(supabase, {
            user_id: user.id,
            flow_id: flow.id,
            source_id: source.id,
            target_id: target.id,
            source_message_id: msg.id,
            target_message_ids: [telegramMsgId],
            status: "published",
            original_url: originalUrl,
            text_preview: msg.text.substring(0, 200),
            media_count: (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            raw_payload: {
              target_chat_id: targetChatId,
              source_channel: channel,
            },
          });

          await new Promise((r) => setTimeout(r, 1500));
        }

        if (analyzedForThisFlow > 0)
          await incrementUsage(supabase, user.id, analyzedForThisFlow);

        await supabase
          .from("flows")
          .update({
            last_message_id: maxMsgId,
            last_run_at: new Date().toISOString(),
            last_error: null,
          })
          .eq("id", flow.id);
      } catch (flowErr) {
        console.error(`[Aggregate] Flow ${flow.id} failed:`, flowErr);
        await supabase
          .from("flows")
          .update({
            last_run_at: new Date().toISOString(),
            last_error: (flowErr as Error).message,
          })
          .eq("id", flow.id);
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: publishedCount,
        skipped: skippedCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


## Assistant

Need webhook metadata maybe raw_payload JSONB, payment.metadata. Supabase returns maybe object.

Need add /me plan display for proplus. Currently premium ? Premium : Gratuit. Change. Also isPremium includes proplus.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Failed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1258-1276)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.id || data?.id || data?.data?.session_id || null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Need /start display Premium vs proplus? Not necessary.

Need fix regex in aggregate transform maybe okay. Need diagnostics.

**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/functions/aggregate_news/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/functions/nelsius_webhook/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/migrations/20260511203000_add_pro_plus_features.sql`**
Status: Completed

File doesn't have errors or warnings!

## Assistant

C’est fait. J’ai ajouté une première version du plan **Pro Plus** avec :

- traduction automatique ;
- filtres avancés ;
- remplacement de mots ;
- gestion des liens ;
- suppression pub configurable ;
- signature automatique pour les utilisateurs gratuits ;
- alerte avant expiration ;
- notification admin à tous les utilisateurs.

## 1. Nouveau plan Pro Plus

Le plan Pro Plus coûte maintenant :

`1000 FCFA / mois`

J’ai ajouté le bouton :

`🚀 Pro Plus`

Quand l’utilisateur clique dessus, le bot génère un lien de paiement Nelsius Pay de :

`1000 FCFA`

Après paiement validé par webhook, son `plan` devient :

`pro_plus`

---

## 2. Premium reste à 500 FCFA

Le plan Premium reste disponible avec :

`💳 Premium`

Montant :

`500 FCFA / mois`

Le plan Premium donne les fonctionnalités payantes de base, mais pas les filtres avancés/traduction.

---

## 3. Traduction automatique pour Pro Plus

Les utilisateurs Pro Plus peuvent activer la traduction sur un flux.

Commande :

`/translate flow_id langue on`

Exemples :

`/translate a1b2c3d4 fr on`

`/translate a1b2c3d4 en on`

`/translate a1b2c3d4 es on`

Pour désactiver :

`/translate a1b2c3d4 fr off`

Quand c’est activé, le bot traduit le message récupéré depuis le canal source vers la langue cible avant de republier.

La traduction utilise `GEMINI_API_KEY`.

---

## 4. Filtres avancés réservés Pro Plus

J’ai ajouté le bouton :

`⚙️ Filtres Pro+`

Il affiche les commandes disponibles.

Les filtres avancés sont maintenant réservés au plan `pro_plus`.

Commandes disponibles :

### Traduction

`/translate flow_id fr on`

### Remplacement de mots

`/replace flow_id ancien => nouveau`

Exemple :

`/replace a1b2c3d4 WhatsApp => @moncontact`

### Gestion des liens

Garder les liens :

`/links flow_id keep`

Supprimer les liens :

`/links flow_id remove`

Remplacer tous les liens par un lien personnel :

`/links flow_id replace https://mon-lien.com`

### Bloquer les publicités

`/blockads flow_id on`

ou :

`/blockads flow_id off`

### Mots obligatoires

`/include flow_id mot1,mot2`

### Mots interdits

`/exclude flow_id mot1,mot2`

### Signature personnalisée

`/signature flow_id texte`

---

## 5. Les utilisateurs gratuits ne filtrent pas les publications

J’ai changé le comportement côté agrégateur.

Maintenant :

- les utilisateurs gratuits récupèrent les publications normalement ;
- les filtres avancés ne s’appliquent pas pour eux ;
- les contenus ne sont pas bloqués par défaut ;
- les publications passent plus librement.

Les filtres comme :

- suppression pub ;
- mots interdits ;
- mots obligatoires ;
- traduction ;
- remplacement de liens ;
- remplacement de mots ;

sont appliqués uniquement si l’utilisateur est en Pro Plus actif.

---

## 6. Signature automatique pour les utilisateurs non abonnés

Pour ceux qui n’ont pas payé d’abonnement actif, le bot ajoute automatiquement une signature en bas des publications.

Par défaut :

`🤖 Propulsé par IzyNews`

Tu peux changer cette signature avec un secret Supabase :

`FREE_USER_SIGNATURE`

Exemple :

`npx supabase secrets set FREE_USER_SIGNATURE="🤖 Propulsé par @TonBot" --project-ref nxgryaqncfjuazfvekro`

Cette signature ne s’applique pas aux utilisateurs Premium ou Pro Plus.

---

## 7. Alerte avant expiration d’abonnement

J’ai ajouté une colonne :

`last_subscription_warning_at`

dans `app_users`.

L’agrégateur peut maintenant avertir l’utilisateur quand son abonnement expire bientôt.

Actuellement, il avertit si l’abonnement expire dans les **5 prochains jours**.

Message envoyé :

> Ton abonnement Premium/Pro Plus expire dans X jour(s). Pense à renouveler.

Le bot évite de répéter l’alerte plus d’une fois par jour.

---

## 8. Notification admin à tous les utilisateurs

Côté admin, j’ai ajouté le bouton :

`📣 Notifier utilisateurs`

Quand l’admin clique dessus :

1. le bot demande le message à envoyer ;
2. l’admin écrit le message ;
3. le bot l’envoie à tous les utilisateurs actifs.

Chaque message commence automatiquement par :

`Salut cher/chère {nom utilisateur},`

Puis le message admin.

Exemple :

`Salut cher/chère @username,`

`Message admin...`

---

## 9. Paiement Pro Plus via webhook

J’ai ajusté le webhook Nelsius.

Maintenant, quand un paiement d’abonnement est confirmé :

- si le paiement vient de `Premium`, le plan devient `premium` ;
- si le paiement vient de `Pro Plus`, le plan devient `pro_plus`.

Le plan est stocké dans `payments.metadata.plan`.

---

## 10. Nouvelle migration ajoutée

J’ai ajouté :

`news/supabase/migrations/20260511203000_add_pro_plus_features.sql`

Elle ajoute :

- `last_subscription_warning_at` dans `app_users` ;
- les nouvelles valeurs par défaut des filtres de flux :
  - traduction ;
  - langue cible ;
  - remplacements ;
  - action sur les liens ;
  - suppression pub désactivée par défaut.

---

## 11. Fichiers modifiés

J’ai modifié :

- `news/supabase/functions/telegram_bot/index.ts`
- `news/supabase/functions/aggregate_news/index.ts`
- `news/supabase/functions/nelsius_webhook/index.ts`

J’ai ajouté :

- `news/supabase/migrations/20260511203000_add_pro_plus_features.sql`

---

## 12. À déployer

Applique la migration :

`npx supabase db push`

Ou manuellement dans le SQL Editor Supabase :

`news/supabase/migrations/20260511203000_add_pro_plus_features.sql`

Puis redéploie :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

`npx supabase functions deploy aggregate_news --project-ref nxgryaqncfjuazfvekro`

`npx supabase functions deploy nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

---

## 13. Secret recommandé pour la signature gratuite

Optionnel :

`npx supabase secrets set FREE_USER_SIGNATURE="🤖 Propulsé par IzyNews" --project-ref nxgryaqncfjuazfvekro`

---

## 14. Résumé des plans

### Gratuit

- créer un flux ;
- récupérer les messages ;
- republier les médias ;
- albums supportés ;
- signature automatique ajoutée en bas ;
- pas de filtres avancés ;
- pas de traduction.

### Premium — 500 FCFA/mois

- pas de signature automatique ;
- plusieurs flux selon la logique Premium existante ;
- accès payant de base.

### Pro Plus — 1000 FCFA/mois

- tout Premium ;
- traduction automatique ;
- suppression pub configurable ;
- mots interdits/obligatoires ;
- remplacement de mots ;
- remplacement/suppression des liens ;
- signature personnalisée par flux ;
- filtres avancés par flux.

## User (checkpoint)

notre webhook supporte bien cette structure? {"event":"payment.failed","data":{"transaction_code":"API202605131111491762","reference_id":"REQ2605131011NO8QQR8","status":"failed","amount":"650.00","net_amount":"600.00","currency":"XAF","operator":"mtn_money","operator_transaction_id":null,"description":null,"failure_reason":"\u00c9chec du paiement","payer_phone":"676676120","payer_email":"nelsonsiebi237@gmail.com","created_at":"2026-05-13T10:11:49.000000Z"},"timestamp":1778667138,"horodatage":1778667138}

## Assistant

Non, pas complètement : avec cette structure, notre webhook lit bien `data.status = failed`, mais il ne retrouvait pas encore la référence car Nelsius envoie `data.reference_id` au lieu de `reference` ou `data.reference`. Je vais l’adapter pour supporter `reference_id`, `transaction_code`, et aussi retrouver un paiement via `external_id` si besoin.

**Tool Call: supabase\\functions\\nelsius\_webhook\\index.ts**
Status: Completed

Diff: supabase\functions\nelsius_webhook\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

function isPaidStatus(status: string): boolean {
  return [
    "paid",
    "success",
    "successful",
    "completed",
    "approved",
    "succeeded",
  ].includes(status.toLowerCase());
}

function extractPayloadValue(payload: any, keys: string[]): string | null {
  for (const key of keys) {
    const value = key
      .split(".")
      .reduce((acc: any, part: string) => acc?.[part], payload);
    if (value !== undefined && value !== null && String(value).trim())
      return String(value);
  }
  return null;
}

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Nelsius webhook OK", { status: 200 });
  }

  const webhookSecret = Deno.env.get("NELSIUS_WEBHOOK_SECRET") || "";
  if (webhookSecret) {
    const incomingSecret =
      req.headers.get("x-nelsius-secret") ||
      req.headers.get("x-webhook-secret") ||
      "";
    if (incomingSecret !== webhookSecret) {
      return new Response(
        JSON.stringify({ success: false, error: "Unauthorized webhook" }),
        { status: 401 },
      );
    }
  }

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  let payload: any;
  try {
    payload = await req.json();
  } catch (_e) {
    return new Response(
      JSON.stringify({ success: false, error: "Invalid JSON" }),
      { status: 400 },
    );
  }

  const reference = extractPayloadValue(payload, [
    "reference",
    "reference_id",
    "data.reference",
    "data.reference_id",
    "payment.reference",
    "payment.reference_id",
    "transaction.reference",
    "transaction.reference_id",
  ]);
  const status =
    extractPayloadValue(payload, [
      "status",
      "data.status",
      "payment.status",
      "transaction.status",
    ]) || "unknown";
  const externalId = extractPayloadValue(payload, [
    "id",
    "data.id",
    "transaction_code",
    "data.transaction_code",
    "charge_id",
    "session_id",
    "data.charge_id",
    "data.session_id",
    "data.operator_transaction_id",
  ]);

  if (!reference) {
    return new Response(
      JSON.stringify({ success: false, error: "Missing reference" }),
      { status: 400 },
    );
  }

  let { data: payment, error: paymentError } = await supabase
    .from("payments")
    .select("*")
    .eq("reference", reference)
    .maybeSingle();

  if (!payment && reference) {
    const fallback = await supabase
      .from("payments")
      .select("*")
      .eq("external_id", reference)
      .maybeSingle();
    payment = fallback.data;
    paymentError = fallback.error;
  }

  if (!payment && externalId) {
    const fallback = await supabase
      .from("payments")
      .select("*")
      .eq("external_id", externalId)
      .maybeSingle();
    payment = fallback.data;
    paymentError = fallback.error;
  }

  if (paymentError) {
    return new Response(
      JSON.stringify({ success: false, error: paymentError.message }),
      { status: 500 },
    );
  }

  if (!payment) {
    return new Response(
      JSON.stringify({ success: false, error: "Payment not found" }),
      { status: 404 },
    );
  }

  const incomingPaid = isPaidStatus(status);
  const wasAlreadyPaid = isPaidStatus(String(payment.status || ""));

  const updatePayload: Record<string, unknown> = {
    status,
    raw_payload: payload,
    external_id: externalId,
  };

  if (incomingPaid && !payment.paid_at)
    updatePayload.paid_at = new Date().toISOString();

  const { error: updateError } = await supabase
    .from("payments")
    .update(updatePayload)
    .eq("id", payment.id);

  if (updateError) {
    return new Response(
      JSON.stringify({ success: false, error: updateError.message }),
      { status: 500 },
    );
  }

  if (!incomingPaid || wasAlreadyPaid) {
    return new Response(
      JSON.stringify({
        success: true,
        message: wasAlreadyPaid
          ? "Payment already processed"
          : "Payment status recorded",
      }),
    );
  }

  if (payment.payment_type === "subscription") {
    const now = new Date();
    const end = new Date(now);
    end.setMonth(end.getMonth() + 1);
    const requestedPlan =
      payment.metadata?.plan === "pro_plus" ? "pro_plus" : "premium";

    await supabase
      .from("app_users")
      .update({
        plan: requestedPlan,
        plan_expires_at: end.toISOString(),
        is_active: true,
      })
      .eq("id", payment.user_id);

    await supabase.from("subscriptions").insert({
      user_id: payment.user_id,
      provider: "nelsius",
      status: requestedPlan,
      start_at: now.toISOString(),
      end_at: end.toISOString(),
      payment_reference: reference,
    });
  }

  if (payment.payment_type === "deposit") {
    const { data: user } = await supabase
      .from("app_users")
      .select("wallet_balance")
      .eq("id", payment.user_id)
      .maybeSingle();

    const balanceAfter =
      (user?.wallet_balance || 0) + Number(payment.amount || 0);

    await supabase
      .from("app_users")
      .update({ wallet_balance: balanceAfter })
      .eq("id", payment.user_id);

    await supabase.from("wallet_transactions").insert({
      user_id: payment.user_id,
      payment_id: payment.id,
      type: "deposit",
      amount: payment.amount,
      currency: payment.currency,
      balance_after: balanceAfter,
      description: `Dépôt Nelsius Pay ${reference}`,
    });
  }

  return new Response(JSON.stringify({ success: true }));
});

```


## Assistant

<thinking>
**Updating code for reference storage**

I need to update the code to ensure we store the Nelsius reference_id if the response includes data.reference_id. Right now, it looks like we're using externalId from a couple of different places, such as data?.data?.id or data?.id. I’ll want to add that new data.data.reference_id as well. Oh, and I should check if the reference shows up in the description, but it seems it doesn't. Let's make these updates!
</thinking>

Bonne remarque : j’ai adapté le webhook pour lire `data.reference_id`, mais il faut aussi que l’initiation de paiement stocke cette référence Nelsius si elle est retournée à la création du checkout. Je fais cet ajustement aussi.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.reference_id ||
      data?.reference_id ||
      data?.data?.transaction_code ||
      data?.transaction_code ||
      data?.data?.id ||
      data?.id ||
      data?.data?.session_id ||
      null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Avant cette modification : **non, pas totalement**.

Avec ce payload :

```/dev/null/nelsius-webhook.json#L1-22
{
  "event": "payment.failed",
  "data": {
    "transaction_code": "API202605131111491762",
    "reference_id": "REQ2605131011NO8QQR8",
    "status": "failed",
    "amount": "650.00",
    "net_amount": "600.00",
    "currency": "XAF",
    "operator": "mtn_money",
    "operator_transaction_id": null,
    "description": null,
    "failure_reason": "Échec du paiement",
    "payer_phone": "676676120",
    "payer_email": "nelsonsiebi237@gmail.com",
    "created_at": "2026-05-13T10:11:49.000000Z"
  },
  "timestamp": 1778667138,
  "horodatage": 1778667138
}
```

Notre webhook lisait déjà correctement :

`data.status`

Donc il pouvait comprendre que le statut est :

`failed`

Mais il ne lisait pas encore :

`data.reference_id`

Donc il risquait de répondre :

`Payment not found`

car il cherchait seulement :

- `reference`
- `data.reference`
- `payment.reference`
- `transaction.reference`

---

## Ce que j’ai corrigé

J’ai adapté :

`news/supabase/functions/nelsius_webhook/index.ts`

Maintenant le webhook supporte aussi :

- `reference_id`
- `data.reference_id`
- `payment.reference_id`
- `transaction.reference_id`

Il supporte aussi :

- `transaction_code`
- `data.transaction_code`
- `data.operator_transaction_id`

comme identifiants externes.

---

## Recherche du paiement améliorée

Maintenant, le webhook cherche le paiement dans cet ordre :

1. `payments.reference = reference`
2. si non trouvé :
   - `payments.external_id = reference`
3. si non trouvé :
   - `payments.external_id = externalId`

Donc si Nelsius envoie :

`data.reference_id = REQ2605131011NO8QQR8`

et que cette valeur a été stockée comme `external_id`, le paiement pourra être retrouvé.

---

## J’ai aussi corrigé l’initiation de paiement

Dans :

`news/supabase/functions/telegram_bot/index.ts`

j’ai modifié la création du paiement pour stocker comme `external_id` :

- `data.reference_id`
- `data.transaction_code`
- `data.id`
- `data.session_id`

selon ce que Nelsius retourne.

Donc si à l’initiation Nelsius renvoie :

`reference_id`

on le stocke maintenant.

---

## Pour ton exemple exact

Avec ton webhook :

- `event = payment.failed`
- `data.status = failed`
- `data.reference_id = REQ2605131011NO8QQR8`
- `data.transaction_code = API202605131111491762`

Le webhook va maintenant :

1. lire `data.reference_id` ;
2. chercher un paiement correspondant ;
3. mettre le statut à `failed` ;
4. enregistrer le payload brut dans `payments.raw_payload` ;
5. ne pas activer Premium ;
6. ne pas créditer le wallet.

Donc comportement correct.

---

## Attention importante

Pour que ça marche, il faut que la référence envoyée par Nelsius dans le webhook corresponde à une valeur stockée chez nous.

Il y a deux possibilités :

### Cas 1 — Nelsius renvoie notre référence initiale

Nous envoyons une référence du type :

`DEPOSIT_123456789_...`

Si Nelsius renvoie cette même référence dans `data.reference_id`, c’est parfait.

### Cas 2 — Nelsius remplace notre référence par `REQ...`

Si Nelsius crée sa propre référence comme :

`REQ2605131011NO8QQR8`

alors il faut que cette référence soit bien retournée dès l’initiation du paiement, pour qu’on puisse la stocker dans :

`payments.external_id`

J’ai ajouté cette prise en charge.

---

## Ce qu’il faut redéployer

Tu dois redéployer :

`telegram_bot`

et :

`nelsius_webhook`

Commandes :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

`npx supabase functions deploy nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

---

## Test recommandé

1. Lance un dépôt :

`/deposit 100`

2. Va dans Supabase → table `payments`.

Vérifie :

- `reference`
- `external_id`
- `raw_payload`

3. Fais un paiement test qui échoue ou réussit.

4. Vérifie que Nelsius envoie bien :

`data.reference_id`

5. Vérifie que dans `payments`, le statut devient :

`failed`

ou :

`paid/successful/completed`

selon le cas.

---

## Conclusion

Maintenant oui, le webhook supporte cette structure Nelsius avec :

`data.reference_id`

`data.transaction_code`

`data.status`

Et il gère correctement le cas :

`payment.failed` sans activer ni créditer l’utilisateur.

## User (checkpoint)

maintenant, verifie et rassure toi que tout est synchroniser. et sur le message d'accueil au bot, actuellement il ya seuleemnt   le free  et le pro simple. ajuste le message et stylise bien . tout en sachant que meme le plan gratuit  fait le recuperation illimite mais sur un seul flux

## Assistant

<thinking>
**Adjusting welcome text**

I need to modify the welcome text to make it clearer. Also, I might want to check "verifie synchronise" to ensure everything’s working properly. Running diagnostics sounds like a good idea, and I'll definitely need to grep for the relevant plan. I should inspect the current /start section, as I already have the files needed for this task. Using grep will help me find what I need. Let's get started!
</thinking>

Je vais vérifier les endroits liés aux plans (`free`, `premium`, `pro_plus`) puis ajuster le message d’accueil pour présenter clairement les 3 offres, avec le gratuit en récupération illimitée sur 1 flux.

**Tool Call: Search files for regex `Gratuit|Premium|Pro Plus|pro_plus|500 FCFA|1000 FCFA|1 flux|5 analyses|illimit`**
Status: Completed

Showing matches 1-20 (there were more matches found; use offset: 20 to see next page):

## Matches in news\supabase\functions\aggregate_news\index.ts

### const AD_PATTERNS › L129-133
```
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
```

### function isPremiumUser( ) › L986-988
```
function isPremiumUser(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}
```

### function isProPlusUser( ) › L990-992
```
function isProPlusUser(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}
```

### const premium › L1130-1140
```
    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
```

230 lines remaining in ancestor node. Read the file to see all.

### L1141-1145
```
      const proPlus = isProPlusUser(user);

      if (premium && user.plan_expires_at && !warnedUserIds.has(user.id)) {
        const expiresAt = new Date(user.plan_expires_at).getTime();
        const daysLeft = Math.ceil((expiresAt - Date.now()) / 86400000);
```

### L1150-1153
```
          await sendTextToTelegram(
            `⏰ Salut ${user.username ? "@" + user.username : "cher utilisateur"},\n\nTon abonnement <b>${user.plan === "pro_plus" ? "Pro Plus" : "Premium"}</b> expire dans <b>${daysLeft} jour(s)</b>. Pense à renouveler pour garder tes fonctionnalités actives.`,
            String(user.telegram_user_id),
          );
```

### L1184-1194
```
        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (!premium && analyzedForThisFlow > 5) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
```

153 lines remaining in ancestor node. Read the file to see all.

### const textToPublish › L1269-1271
```
          const textToPublish =
            transformContent(finalContent, filters, proPlus, premium) ||
            originalUrl;
```

## Matches in news\supabase\functions\nelsius_webhook\index.ts

### const requestedPlan › L176-177
```
    const requestedPlan =
      payment.metadata?.plan === "pro_plus" ? "pro_plus" : "premium";
```

## Matches in news\supabase\functions\telegram_bot\index.ts

### function isPremium( ) › L51-53
```
function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}
```

### function isProPlus( ) › L55-57
```
function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}
```

### const USER_HELP › L77-81
```
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
```

### function userMenuMarkup() › reply_markup › keyboard › L84-92
```
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
```

### function commandFromButton( ) › const map › 💳 Premium › L143-153
```
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
```

17 lines remaining in ancestor node. Read the file to see all.

### async function createFlowForUser( ) › const premium › L331-344
```
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
```

16 lines remaining in ancestor node. Read the file to see all.

### async function createNelsiusCheckout( ) › L428-434
```
async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
```

### const premium › L1236-1244
```
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
```

7 lines remaining in ancestor node. Read the file to see all.

### const premium › L1253-1267
```
    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
```

10 lines remaining in ancestor node. Read the file to see all.

### L1268-1273
```
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
```

### L1279-1289
```
    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
```

23 lines remaining in ancestor node. Read the file to see all.


**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/functions/aggregate_news/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/functions/nelsius_webhook/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1228-1278)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      await reply(
        chatId,
        `👋 Bienvenue !\n\n` +
          `Plan: <b>${premium ? "Premium" : "Gratuit"}</b>\n` +
          `- Gratuit: 1 flux (1 source → 1 canal cible), 5 analyses/jour\n` +
          `- Premium: illimité (500 FCFA / mois)\n\n` +
          `Commandes:\n` +
          `<code>/me</code> — Mon compte\n` +
          `<code>/subscribe</code> — S'abonner\n\n` +
          `ℹ️ Pour publier vers ton canal, ajoute ce bot comme admin dans ton canal cible.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = premium ? "∞" : "5";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Analyses aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }


```


**Tool Call: Read file `supabase\functions\aggregate_news\index.ts` (lines 1178-1215)**
Status: Completed

```news/supabase/functions/aggregate_news/index.ts
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (!premium && analyzedForThisFlow > 5) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason: "free_daily_limit",
              original_url: `https://t.me/${channel}/${msg.id}`,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")

```


**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: { telegram_user_id: appUser.telegram_user_id, plan },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.reference_id ||
      data?.reference_id ||
      data?.data?.transaction_code ||
      data?.transaction_code ||
      data?.data?.id ||
      data?.id ||
      data?.data?.session_id ||
      null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus
        ? "🚀 Pro Plus"
        : premium
          ? "💳 Premium"
          : "🆓 Gratuit";
      await reply(
        chatId,
        `👋 <b>Bienvenue sur ton assistant Telegram Auto</b>\n\n` +
          `Ton plan actuel : <b>${planLabel}</b>\n\n` +
          `🆓 <b>Gratuit</b>\n` +
          `• Récupération illimitée\n` +
          `• 1 flux actif : 1 source → 1 canal cible\n` +
          `• Signature IzyNews ajoutée en bas des publications\n\n` +
          `💳 <b>Premium — 500 FCFA/mois</b>\n` +
          `• Plusieurs flux actifs\n` +
          `• Pas de signature automatique\n\n` +
          `🚀 <b>Pro Plus — 1000 FCFA/mois</b>\n` +
          `• Tout Premium\n` +
          `• Traduction automatique\n` +
          `• Filtres avancés par flux\n` +
          `• Remplacement mots/liens et suppression pubs\n\n` +
          `👉 Clique sur <b>➕ Nouveau flux</b> pour commencer.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = "∞";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Messages analysés aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";
const FREE_USER_SIGNATURE =
  Deno.env.get("FREE_USER_SIGNATURE") || "🤖 Propulsé par IzyNews";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

async function translateWithAI(
  rawText: string,
  targetLanguage: string,
): Promise<string> {
  if (!GEMINI_API_KEY || rawText.length < 3) return rawText;
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Traduis le message suivant en ${targetLanguage}. Conserve le sens, les noms propres, les emojis pertinents et les liens si présents. Réponds uniquement avec le texte traduit.\n\n${rawText}`,
                },
              ],
            },
          ],
        }),
      },
    );
    const data = await res.json();
    return data.candidates?.[0]?.content?.parts?.[0]?.text?.trim() || rawText;
  } catch (e) {
    console.error("[Gemini] Translation error:", e);
    return rawText;
  }
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

function isPlanActive(user: any, planNames: string[]): boolean {
  if (!user || !planNames.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremiumUser(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlusUser(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}

function hasAnyKeyword(text: string, keywords: string[]): boolean {
  const lower = text.toLowerCase();
  return keywords.some((keyword) =>
    lower.includes(String(keyword).toLowerCase()),
  );
}

function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
  const isAlbum = Boolean(msg.photoUrls && msg.photoUrls.length > 1);
  const hasVideo = Boolean(msg.videoUrl);
  const hasText = msg.text.trim().length > 0;

  if (filters.media_only && !msg.hasMedia) return "media_only";
  if (!filters.allow_text && hasText && !msg.hasMedia)
    return "text_not_allowed";
  if (!filters.allow_photos && hasPhotos) return "photos_not_allowed";
  if (!filters.allow_videos && hasVideo) return "videos_not_allowed";
  if (!filters.allow_albums && isAlbum) return "albums_not_allowed";
  if (include.length > 0 && !hasAnyKeyword(msg.text, include))
    return "missing_include_keyword";
  if (exclude.length > 0 && hasAnyKeyword(msg.text, exclude))
    return "excluded_keyword";
  if (filters.block_ads && isQuickAd(msg.text)) return "ad_blocked";
  return null;
}

function transformContent(
  text: string,
  filters: any,
  proPlus: boolean,
  paidUser: boolean,
): string {
  let out = text.trim();

  if (proPlus) {
    const replacements = filters.replacements || {};
    for (const [from, to] of Object.entries(replacements)) {
      if (!from) continue;
      out = out.replace(
        new RegExp(String(from).replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi"),
        String(to),
      );
    }

    if (filters.link_action === "remove" || filters.remove_links) {
      out = out.replace(/https?:\/\/\S+/gi, "").trim();
    } else if (filters.link_action === "replace" && filters.link_replacement) {
      out = out
        .replace(/https?:\/\/\S+/gi, String(filters.link_replacement))
        .trim();
    }

    if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
    if (filters.signature_text)
      out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  }

  if (!paidUser) out = `${out}\n\n${FREE_USER_SIGNATURE}`.trim();
  return out;
}

async function recordActivity(supabase: any, payload: Record<string, unknown>) {
  const { error } = await supabase.from("flow_activity").insert(payload);
  if (error) console.error("[Aggregate] flow_activity insert error:", error);
}

async function incrementUsage(supabase: any, userId: string, amount: number) {
  const day = new Date().toISOString().slice(0, 10);
  const { data } = await supabase
    .from("usage_daily")
    .select("id, analyzed_count")
    .eq("user_id", userId)
    .eq("day", day)
    .maybeSingle();

  if (data) {
    await supabase
      .from("usage_daily")
      .update({ analyzed_count: (data.analyzed_count || 0) + amount })
      .eq("id", data.id);
  } else {
    await supabase
      .from("usage_daily")
      .insert({ user_id: userId, day, analyzed_count: amount });
  }
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (_req: Request) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: flows, error: flowError } = await supabase
      .from("flows")
      .select(
        "*, user:app_users(*), source:user_sources(*), target:user_targets(*)",
      )
      .eq("is_active", true)
      .order("created_at", { ascending: true });

    if (flowError) throw flowError;

    if (!flows || flows.length === 0) {
      return new Response(
        JSON.stringify({
          success: true,
          processed: 0,
          new: 0,
          message: "No active user flows",
        }),
        { headers: { "Content-Type": "application/json" } },
      );
    }

    let processedCount = 0;
    let publishedCount = 0;
    let skippedCount = 0;
    const warnedUserIds = new Set<string>();

    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
      const proPlus = isProPlusUser(user);

      if (premium && user.plan_expires_at && !warnedUserIds.has(user.id)) {
        const expiresAt = new Date(user.plan_expires_at).getTime();
        const daysLeft = Math.ceil((expiresAt - Date.now()) / 86400000);
        const lastWarn = user.last_subscription_warning_at
          ? new Date(user.last_subscription_warning_at).getTime()
          : 0;
        if (daysLeft > 0 && daysLeft <= 5 && Date.now() - lastWarn > 86400000) {
          await sendTextToTelegram(
            `⏰ Salut ${user.username ? "@" + user.username : "cher utilisateur"},\n\nTon abonnement <b>${user.plan === "pro_plus" ? "Pro Plus" : "Premium"}</b> expire dans <b>${daysLeft} jour(s)</b>. Pense à renouveler pour garder tes fonctionnalités actives.`,
            String(user.telegram_user_id),
          );
          await supabase
            .from("app_users")
            .update({ last_subscription_warning_at: new Date().toISOString() })
            .eq("id", user.id);
          warnedUserIds.add(user.id);
        }
      }

      const channel = source.config?.channel;
      const targetChatId = target.chat_id;
      const lastMsgId = Number(flow.last_message_id || 0);
      if (!channel || !targetChatId) continue;

      console.log(
        `[Aggregate] Flow ${flow.id}: @${channel} → ${targetChatId}, after ${lastMsgId}`,
      );

      try {
        const messages = await scrapeChannel(channel, lastMsgId);
        let messagesToProcess = messages;
        if (
          lastMsgId === 0 &&
          messages.length > Number(flow.initial_last_n || 5)
        ) {
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")
            .maybeSingle();

          if (alreadyProcessed) continue;

          const reason = proPlus ? skipReasonForFilters(msg, filters) : null;
          if (reason) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason,
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          let finalContent = msg.text;
          if (proPlus && filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
                reason: "ai_ad_blocked",
                original_url: originalUrl,
                text_preview: msg.text.substring(0, 200),
                media_count:
                  (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
              });
              continue;
            }
            finalContent = aiRes.content.trim();
          }

          if (proPlus && filters.translate_enabled) {
            finalContent = await translateWithAI(
              finalContent,
              filters.target_language || "fr",
            );
          }

          const textToPublish =
            transformContent(finalContent, filters, proPlus, premium) ||
            originalUrl;
          const isShortVideo =
            msg.videoUrl && (!msg.videoDuration || msg.videoDuration <= 60);
          let telegramMsgId: number | null = null;

          if (isShortVideo && msg.videoUrl && filters.allow_videos) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 1 &&
            filters.allow_albums
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 0 &&
            filters.allow_photos
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (filters.allow_text) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }

          if (!telegramMsgId) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "failed",
              reason: "publish_failed",
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          publishedCount++;
          await recordActivity(supabase, {
            user_id: user.id,
            flow_id: flow.id,
            source_id: source.id,
            target_id: target.id,
            source_message_id: msg.id,
            target_message_ids: [telegramMsgId],
            status: "published",
            original_url: originalUrl,
            text_preview: msg.text.substring(0, 200),
            media_count: (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            raw_payload: {
              target_chat_id: targetChatId,
              source_channel: channel,
            },
          });

          await new Promise((r) => setTimeout(r, 1500));
        }

        if (analyzedForThisFlow > 0)
          await incrementUsage(supabase, user.id, analyzedForThisFlow);

        await supabase
          .from("flows")
          .update({
            last_message_id: maxMsgId,
            last_run_at: new Date().toISOString(),
            last_error: null,
          })
          .eq("id", flow.id);
      } catch (flowErr) {
        console.error(`[Aggregate] Flow ${flow.id} failed:`, flowErr);
        await supabase
          .from("flows")
          .update({
            last_run_at: new Date().toISOString(),
            last_error: (flowErr as Error).message,
          })
          .eq("id", flow.id);
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: publishedCount,
        skipped: skippedCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


## Assistant

Need "synchronise": direct filter command has proPlus guard. But /filters display? ok. Maybe default DB migration old existing flows keep block_ads true? We changed default, but existing flows have old filters with block_ads true. For free users, aggregate ignores filters because proPlus? yes reason null and transform ignores. But allow media booleans are still used for all! At publish step, `filters.allow_videos`, `allow_albums`, `allow_photos`, `allow_text` still applies to non-proplus. Defaults true but if old changed they could affect free. But command restricted now, old free could have changed previous filters. Need make publishing allow all for non-proplus. Add effective booleans: if !proPlus true.

**Tool Call: supabase\\functions\\aggregate\_news\\index.ts**
Status: Completed

Diff: supabase\functions\aggregate_news\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const GEMINI_API_KEY = Deno.env.get("GEMINI_API_KEY") || "";
const FREE_USER_SIGNATURE =
  Deno.env.get("FREE_USER_SIGNATURE") || "🤖 Propulsé par IzyNews";

// Storj S3-compatible configuration
const STORJ_ACCESS_KEY = Deno.env.get("STORJ_ACCESS_KEY") || "";
const STORJ_SECRET_KEY = Deno.env.get("STORJ_SECRET_KEY") || "";
const STORJ_ENDPOINT =
  Deno.env.get("STORJ_ENDPOINT") || "https://gateway.storjshare.io";
const STORJ_BUCKET = Deno.env.get("STORJ_BUCKET") || "";
const STORJ_REGION = "us-east-1"; // Storj uses this region by default

function isStorjConfigured(): boolean {
  return Boolean(
    STORJ_ACCESS_KEY && STORJ_SECRET_KEY && STORJ_BUCKET && STORJ_ENDPOINT,
  );
}

function toHex(bytes: Uint8Array): string {
  return Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

function utf8Bytes(s: string): Uint8Array {
  return new TextEncoder().encode(s);
}

function toArrayBuffer(view: Uint8Array): ArrayBuffer {
  return view.buffer.slice(
    view.byteOffset,
    view.byteOffset + view.byteLength,
  ) as ArrayBuffer;
}

async function sha256(data: Uint8Array | string): Promise<Uint8Array> {
  const bytes = typeof data === "string" ? utf8Bytes(data) : data;
  const digest = await crypto.subtle.digest("SHA-256", toArrayBuffer(bytes));
  return new Uint8Array(digest);
}

async function hmacSha256(key: Uint8Array, data: string): Promise<Uint8Array> {
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    toArrayBuffer(key),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign(
    "HMAC",
    cryptoKey,
    toArrayBuffer(utf8Bytes(data)),
  );
  return new Uint8Array(sig);
}

function amzDate(now = new Date()): { amz: string; short: string } {
  const yyyy = now.getUTCFullYear();
  const mm = String(now.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(now.getUTCDate()).padStart(2, "0");
  const HH = String(now.getUTCHours()).padStart(2, "0");
  const MM = String(now.getUTCMinutes()).padStart(2, "0");
  const SS = String(now.getUTCSeconds()).padStart(2, "0");
  const short = `${yyyy}${mm}${dd}`;
  const amz = `${short}T${HH}${MM}${SS}Z`;
  return { amz, short };
}

function encodeRfc3986(str: string): string {
  return encodeURIComponent(str).replace(
    /[!'()*]/g,
    (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`,
  );
}

function canonicalUriPathStyle(bucket: string, key: string): string {
  const parts = [`/${bucket}`].concat(key.split("/").map(encodeRfc3986));
  return parts.join("/").replace(/\/+/g, "/");
}

async function getSigningKey(
  secretKey: string,
  dateShort: string,
  region: string,
  service: string,
): Promise<Uint8Array> {
  const kDate = await hmacSha256(utf8Bytes(`AWS4${secretKey}`), dateShort);
  const kRegion = await hmacSha256(kDate, region);
  const kService = await hmacSha256(kRegion, service);
  const kSigning = await hmacSha256(kService, "aws4_request");
  return kSigning;
}

function buildCanonicalQuery(params: Record<string, string>): string {
  return Object.keys(params)
    .sort()
    .map((k) => `${encodeRfc3986(k)}=${encodeRfc3986(params[k])}`)
    .join("&");
}

interface ScrapedMessage {
  id: number;
  text: string;
  photoUrls?: string[]; // All photos in album
  videoUrl?: string; // Video if present
  videoDuration?: number; // Duration in seconds (if available)
  hasMedia: boolean;
}

// ── Quick keyword-based ad pre-filter ──────────────────────────────────────
const AD_PATTERNS = [
  /rejoignez.{0,30}(groupe|chaîne|canal)/i,
  /cliquez?\s+ici/i,
  /lien\s+(en\s+bio|affilié)/i,
  /promo|soldes|réduction|rabais/i,
  /invest|trading|crypto|bitcoin|forex/i,
  /gagn\w+\s+de\s+l.argent/i,
  /sponsor|partenariat\s+commercial/i,
  /annonce|à\s+vendre|en\s+vente|prix\s*[:\-]|[📞📱]\s*contact|whatsap/i,
  /cherche\s+.*\s+(emploi|travail|job)|offre\s+d'?emploi/i,
  /recrute|recrutement|urgent|limité|disponible\s+maintenant/i,
  /offre\s+spéciale|exceptionnel|dernière\s+chance|promotion/i,
  /faire\s+de\s+l'argent|gagner\s+(de\s+l')?argent|revenu\s+passif/i,
  /inscription\s+gratuite|inscrivez[-\s]vous/i,
  /💰|💵|💲|🏷️|🛒|🛍️|📢/,
];
const AD_KEYWORDS_STRICT = [
  "annonce",
  "à vendre",
  "en vente",
  "prix:",
  "prix :",
  "contactez",
  "recrutement",
  "offre d'emploi",
  " Opportunité",
  "gagner de l'argent",
];
function isQuickAd(text: string): boolean {
  const lower = text.toLowerCase();
  let hits = 0;
  for (const p of AD_PATTERNS) if (p.test(lower)) hits++;

  // Strict keyword check (immediate detection)
  for (const keyword of AD_KEYWORDS_STRICT) {
    if (lower.includes(keyword.toLowerCase())) {
      console.log(`[Filter] Strict keyword detected: "${keyword}"`);
      return true;
    }
  }

  // Check for excessive caps (typical of ads)
  const capsRatio = (text.match(/[A-Z]/g) || []).length / text.length;
  if (capsRatio > 0.6 && text.length > 15) hits += 2;

  // Check for excessive emoji (ads often have many)
  const emojiCount = (text.match(/[\u{1F300}-\u{1F9FF}]/gu) || []).length;
  if (emojiCount > 6) hits++;
  if (emojiCount > 10) hits += 2;

  // Check for excessive numbers (prices, phone numbers)
  const digitCount = (text.match(/\d/g) || []).length;
  if (digitCount > 15) hits++;

  return hits >= 2;
}

// ── Scrape t.me/s/<channel> ──────────────────────────────────────────────
function extractPhotoUrlFromBlock(block: string): string | undefined {
  const bg = block.match(/background-image:url\('([^']+)'\)/);
  if (bg?.[1]) return bg[1];
  const img = block.match(/<img[^>]+src="([^"]+)"[^>]*>/i);
  if (img?.[1]) return img[1];
  const videoPoster = block.match(/<video[^>]+poster="([^"]+)"[^>]*>/i);
  if (videoPoster?.[1]) return videoPoster[1];
  return undefined;
}

// Extract ALL photos from an album message - STRICT filtering
function extractAllPhotosFromBlock(block: string): string[] {
  const urls: string[] = [];
  const seen = new Set<string>();

  // Find the main media container for this message
  const mediaContainer = block.match(
    /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
  );
  if (!mediaContainer) return [];

  // Look for grouped media (album indicator)
  const isAlbum =
    block.includes("tgme_widget_message_grouped_wrap") ||
    block.includes("tgme_widget_message_album");

  // Extract photos from grouped containers (albums)
  if (isAlbum) {
    // Find all photo containers in grouped layout
    const photoBlocks = block.matchAll(
      /class="tgme_widget_message_photo_wrap[^"]*"[^>]*>/g,
    );
    for (const photoBlock of photoBlocks) {
      const bgMatch = photoBlock[0].match(/background-image:url\('([^']+)'\)/);
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Single photo or backup extraction
  if (urls.length === 0) {
    // Extract from the main widget_photo div only (not nested)
    const photoSection = block.match(
      /<a[^>]*class="tgme_widget_message_photo_wrap[^"]*"[^>]*background-image:url\('([^']+)'\)/,
    );
    if (photoSection?.[1] && !seen.has(photoSection[1])) {
      urls.push(photoSection[1]);
      seen.add(photoSection[1]);
    }

    // Fallback: find background in style attribute (first one only)
    if (urls.length === 0) {
      const bgMatch = block.match(
        /background-image:url\('([^']+\/(?:photos|video_thumbnails)\/[^']+)'\)/,
      );
      if (bgMatch?.[1] && !seen.has(bgMatch[1])) {
        urls.push(bgMatch[1]);
        seen.add(bgMatch[1]);
      }
    }
  }

  // Clean URLs - remove size constraints for full quality
  return urls.map((url) =>
    url.replace(/\?size=[^&]*/, "").replace(/&size=[^&]*/, ""),
  );
}

// Extract video info from block - STRICT filtering
function extractVideoFromBlock(block: string): {
  url?: string;
  duration?: number;
} {
  // Look for video element with src - must be inside tgme_widget_message_video
  const videoSection = block.match(
    /class="tgme_widget_message_video[^"]*"[^>]*>.*?<video[^>]+src="([^"]+)"[^>]*>/is,
  );
  if (!videoSection?.[1]) return {};

  const url = videoSection[1];

  // Try to extract duration from data-duration or time element
  const durationMatch =
    block.match(/data-duration="(\d+)"/) ||
    block.match(/<time[^>]*>(\d+):(\d+)<\/time>/);
  let duration: number | undefined;
  if (durationMatch) {
    if (durationMatch[2]) {
      // MM:SS format
      duration = parseInt(durationMatch[1]) * 60 + parseInt(durationMatch[2]);
    } else {
      duration = parseInt(durationMatch[1]);
    }
  }

  return { url, duration };
}

async function fetchChannelPage(
  channel: string,
  beforeId?: number,
): Promise<string> {
  const url = beforeId
    ? `https://t.me/s/${channel}?before=${beforeId}`
    : `https://t.me/s/${channel}`;
  const res = await fetch(url, {
    headers: { "User-Agent": "Mozilla/5.0 (compatible; Googlebot/2.1)" },
  });
  if (!res.ok) {
    console.error(
      `[Scrape] HTTP ${res.status} for ${channel}${beforeId ? ` before=${beforeId}` : ""}`,
    );
    return "";
  }
  return await res.text();
}

async function scrapeChannel(
  channel: string,
  afterId = 0,
): Promise<ScrapedMessage[]> {
  try {
    const messages: ScrapedMessage[] = [];
    const seenIds = new Set<number>();

    let beforeId: number | undefined = undefined;
    let reachedAfter = false;

    for (let page = 0; page < 20; page++) {
      const html = await fetchChannelPage(channel, beforeId);
      if (!html) break;

      const blocks = html.split('<div class="tgme_widget_message_wrap');
      let minIdOnPage: number | undefined;
      let foundAny = false;

      for (const block of blocks.slice(1)) {
        const idMatch = block.match(/data-post="[^"]*\/(\d+)"/);
        if (!idMatch) continue;
        const msgId = parseInt(idMatch[1]);
        if (!Number.isFinite(msgId)) continue;
        foundAny = true;

        if (minIdOnPage === undefined || msgId < minIdOnPage)
          minIdOnPage = msgId;
        if (msgId <= afterId) {
          reachedAfter = true;
          continue;
        }
        if (seenIds.has(msgId)) continue;
        seenIds.add(msgId);

        let text = "";
        const textMatch = block.match(
          /class="tgme_widget_message_text[^"]*"[^>]*>([\s\S]*?)<\/div>/,
        );
        if (textMatch) {
          text = textMatch[1]
            .replace(/<br\s*\/?>/gi, "\n")
            .replace(/<\/p>/gi, "\n")
            .replace(/<[^>]+>/g, "")
            .replace(/&amp;/g, "&")
            .replace(/&lt;/g, "<")
            .replace(/&gt;/g, ">")
            .replace(/&quot;/g, '"')
            .replace(/&#039;/g, "'")
            .replace(/&nbsp;/g, " ")
            .trim();
        }

        // Extract all media from the message
        const allPhotos = extractAllPhotosFromBlock(block);
        const videoInfo = extractVideoFromBlock(block);

        const hasMedia = allPhotos.length > 0 || videoInfo.url !== undefined;

        messages.push({
          id: msgId,
          text,
          photoUrls: allPhotos.length > 0 ? allPhotos : undefined,
          videoUrl: videoInfo.url,
          videoDuration: videoInfo.duration,
          hasMedia,
        });
      }

      if (!foundAny) break;
      if (reachedAfter) break;
      if (minIdOnPage === undefined) break;

      beforeId = minIdOnPage;
    }

    return messages.sort((a, b) => a.id - b.id);
  } catch (e) {
    console.error(`[Scrape] Exception for ${channel}:`, e);
    return [];
  }
}

// ── Gemini AI restructuring ──────────────────────────────────────────────
async function restructureWithAI(
  rawText: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!GEMINI_API_KEY || rawText.length < 10)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel pour le canal Telegram @izynews.\nVoici une information brute :\n\n${rawText}\n\nRègles STRICTES :\n1. Si c'est une publicité, spam, crypto promo, recrutement MLM ou contenu commercial : "is_ad": true OBLIGATOIRE.\n2. Sinon reformule de façon claire, percutante, professionnelle avec émojis.\n3. Titre court et accrocheur (max 100 chars) avec émojis.\n4. Mets le titre en <b>gras HTML</b> au début du contenu.\n5. NE mets PAS "@izynews" à la fin.\n\nJSON STRICT uniquement :\n{"is_ad":false,"title":"Titre 🔥","content":"<b>Titre 🔥</b>\\n\\nContenu..."}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const m = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (m) jsonStr = m[1].trim();
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

async function translateWithAI(
  rawText: string,
  targetLanguage: string,
): Promise<string> {
  if (!GEMINI_API_KEY || rawText.length < 3) return rawText;
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Traduis le message suivant en ${targetLanguage}. Conserve le sens, les noms propres, les emojis pertinents et les liens si présents. Réponds uniquement avec le texte traduit.\n\n${rawText}`,
                },
              ],
            },
          ],
        }),
      },
    );
    const data = await res.json();
    return data.candidates?.[0]?.content?.parts?.[0]?.text?.trim() || rawText;
  } catch (e) {
    console.error("[Gemini] Translation error:", e);
    return rawText;
  }
}

// ── Upload to Storj (S3 REST + SigV4) + presigned GET URL for Telegram ──
async function uploadToStorjAndGetSignedUrl(
  buffer: Uint8Array,
  filename: string,
  contentType: string,
): Promise<string | null> {
  if (!isStorjConfigured()) {
    console.warn(
      "[Storj] Not configured (missing env vars), skipping Storj upload",
    );
    return null;
  }

  const endpoint = new URL(STORJ_ENDPOINT);
  const host = endpoint.host;
  const service = "s3";

  const key = `${Date.now()}_${Math.random().toString(36).slice(2)}_${filename}`;
  const canonicalUri = canonicalUriPathStyle(STORJ_BUCKET, key);

  try {
    const { amz, short } = amzDate();
    const payloadHash = toHex(await sha256(buffer));

    const canonicalHeaders =
      `content-type:${contentType}\n` +
      `host:${host}\n` +
      `x-amz-content-sha256:${payloadHash}\n` +
      `x-amz-date:${amz}\n`;
    const signedHeaders = "content-type;host;x-amz-content-sha256;x-amz-date";

    const canonicalRequest = `PUT\n${canonicalUri}\n\n${canonicalHeaders}\n${signedHeaders}\n${payloadHash}`;

    const algorithm = "AWS4-HMAC-SHA256";
    const credentialScope = `${short}/${STORJ_REGION}/${service}/aws4_request`;
    const stringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(canonicalRequest))}`;

    const signingKey = await getSigningKey(
      STORJ_SECRET_KEY,
      short,
      STORJ_REGION,
      service,
    );
    const signature = toHex(await hmacSha256(signingKey, stringToSign));
    const authorization = `${algorithm} Credential=${STORJ_ACCESS_KEY}/${credentialScope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;

    const putUrl = `${endpoint.origin}${canonicalUri}`;
    const putRes = await fetch(putUrl, {
      method: "PUT",
      headers: {
        "content-type": contentType,
        "x-amz-date": amz,
        "x-amz-content-sha256": payloadHash,
        authorization: authorization,
      },
      body: buffer as any,
    });

    if (!putRes.ok) {
      const errText = await putRes.text().catch(() => "");
      console.error(
        "[Storj] PUT failed:",
        putRes.status,
        errText?.slice(0, 300),
      );
      return null;
    }

    // Presigned GET for 1 hour so Telegram can download the object.
    const expires = 60 * 60;
    const getParams: Record<string, string> = {
      "X-Amz-Algorithm": algorithm,
      "X-Amz-Credential": `${STORJ_ACCESS_KEY}/${credentialScope}`,
      "X-Amz-Date": amz,
      "X-Amz-Expires": String(expires),
      "X-Amz-SignedHeaders": "host",
    };
    const canonicalQuery = buildCanonicalQuery(getParams);
    const getCanonicalRequest = `GET\n${canonicalUri}\n${canonicalQuery}\nhost:${host}\n\nhost\nUNSIGNED-PAYLOAD`;
    const getStringToSign = `${algorithm}\n${amz}\n${credentialScope}\n${toHex(await sha256(getCanonicalRequest))}`;
    const getSignature = toHex(await hmacSha256(signingKey, getStringToSign));

    const signedGetUrl = `${endpoint.origin}${canonicalUri}?${canonicalQuery}&X-Amz-Signature=${getSignature}`;
    console.log(
      `[Storj] Uploaded: ${key} (${(buffer.length / 1024 / 1024).toFixed(2)} MB)`,
    );
    return signedGetUrl;
  } catch (e) {
    console.error("[Storj] Upload/sign error:", e);
    return null;
  }
}

// ── Download media from URL ──────────────────────────────────────────────
async function downloadMedia(url: string): Promise<Uint8Array | null> {
  try {
    const res = await fetch(url, {
      headers: {
        "User-Agent":
          "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
      },
    });
    if (!res.ok) {
      console.error(`[Download] HTTP ${res.status} for ${url}`);
      return null;
    }
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  } catch (e) {
    console.error("[Download] Error:", e);
    return null;
  }
}

// ── Helper: Build multipart form data ────────────────────────────────────
function buildMultipartForm(
  fields: {
    name: string;
    value: string | Uint8Array;
    filename?: string;
    contentType?: string;
  }[],
  boundary: string,
): Uint8Array {
  const encoder = new TextEncoder();
  const parts: Uint8Array[] = [];

  for (const field of fields) {
    parts.push(encoder.encode(`--${boundary}\r\n`));

    if (field.filename) {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"; filename="${field.filename}"\r\n`,
        ),
      );
      parts.push(
        encoder.encode(
          `Content-Type: ${field.contentType || "application/octet-stream"}\r\n\r\n`,
        ),
      );
      parts.push(field.value as Uint8Array);
    } else {
      parts.push(
        encoder.encode(
          `Content-Disposition: form-data; name="${field.name}"\r\n\r\n`,
        ),
      );
      parts.push(encoder.encode(field.value as string));
    }
    parts.push(encoder.encode(`\r\n`));
  }

  parts.push(encoder.encode(`--${boundary}--\r\n`));

  let totalLength = 0;
  for (const part of parts) totalLength += part.length;
  const body = new Uint8Array(totalLength);
  let offset = 0;
  for (const part of parts) {
    body.set(part, offset);
    offset += part.length;
  }
  return body;
}

// ── Send text-only ────────────────────────────────────────────────────────
async function sendTextToTelegram(
  text: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          text: text.substring(0, 4096),
          parse_mode: "HTML",
        }),
      },
    );
    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      return data.result.message_id;
    }
    console.error("[Telegram] sendMessage failed:", data.description);
  } catch (e) {
    console.error("[Telegram] sendText error:", e);
  }
  return null;
}

// ── Send single photo ────────────────────────────────────────────────────
async function sendPhotoToTelegram(
  text: string,
  photoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading photo: ${photoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(photoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download photo, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "photo.jpg",
      "image/jpeg",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadPhotoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          photo: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Photo sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendPhoto with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendPhoto error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload photo directly to Telegram (fallback) ───────────────────────
async function uploadPhotoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      {
        name: "photo",
        value: mediaData,
        filename: "image.jpg",
        contentType: "image/jpeg",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Photo sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

// ── Send album (multiple photos) ───────────────────────────────────────────
async function sendAlbumToTelegram(
  text: string,
  photoUrls: string[],
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Processing album with ${photoUrls.length} photos...`,
    );

    // Download all photos
    const mediaItems: { url: string; data: Uint8Array | null }[] = [];
    for (const url of photoUrls) {
      const data = await downloadMedia(url);
      mediaItems.push({ url, data });
      await new Promise((r) => setTimeout(r, 100)); // Rate limit protection
    }

    const validPhotos = mediaItems.filter((item) => item.data !== null);
    console.log(
      `[Telegram] Downloaded ${validPhotos.length}/${photoUrls.length} photos`,
    );

    if (validPhotos.length === 0) {
      return sendTextToTelegram(text, chatId);
    }

    if (validPhotos.length === 1) {
      // Single photo fallback
      return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
    }

    // Build media group - caption only on first item
    const boundary =
      "----FormBoundary" + Math.random().toString(36).substring(2);
    const fields: {
      name: string;
      value: string | Uint8Array;
      filename?: string;
      contentType?: string;
    }[] = [{ name: "chat_id", value: chatId }];

    // Build media array JSON
    const mediaArray = validPhotos.map((photo, index) => ({
      type: "photo",
      media: `attach://photo${index}`,
      caption: index === 0 ? text.substring(0, 1024) : undefined,
      parse_mode: index === 0 ? "HTML" : undefined,
    }));
    fields.push({ name: "media", value: JSON.stringify(mediaArray) });

    // Add photo files
    validPhotos.forEach((photo, index) => {
      fields.push({
        name: `photo${index}`,
        value: photo.data!,
        filename: `photo${index}.jpg`,
        contentType: "image/jpeg",
      });
    });

    const body = buildMultipartForm(fields, boundary);

    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendMediaGroup`,
      {
        method: "POST",
        headers: {
          "Content-Type": `multipart/form-data; boundary=${boundary}`,
        },
        body: body as any,
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.length > 0) {
      console.log(
        `[Telegram] Album sent (${validPhotos.length} photos), first msg_id: ${data.result[0].message_id}`,
      );
      return data.result[0].message_id;
    }
    console.warn("[Telegram] sendMediaGroup failed:", data.description);

    // Fallback: send first photo with caption
    return sendPhotoToTelegram(text, validPhotos[0].url, chatId);
  } catch (e) {
    console.error("[Telegram] sendAlbum error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Send video ───────────────────────────────────────────────────────────
async function sendVideoToTelegram(
  text: string,
  videoUrl: string,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  if (!chatId || !BOT_TOKEN) return null;
  try {
    console.log(
      `[Telegram] Downloading video: ${videoUrl.substring(0, 50)}...`,
    );
    const mediaData = await downloadMedia(videoUrl);

    if (!mediaData) {
      console.warn("[Telegram] Failed to download video, falling back to text");
      return sendTextToTelegram(text, chatId);
    }

    console.log(
      `[Telegram] Video downloaded: ${(mediaData.length / 1024 / 1024).toFixed(2)} MB`,
    );

    // Upload to Storj (S3) and get a signed GET URL for Telegram
    const storjUrl = await uploadToStorjAndGetSignedUrl(
      mediaData,
      "video.mp4",
      "video/mp4",
    );

    if (!storjUrl) {
      console.warn("[Telegram] Failed to upload to Storj, using direct upload");
      // Fallback: direct upload to Telegram
      return await uploadVideoDirect(text, mediaData, chatId);
    }

    // Send using Storj URL (permanent)
    const res = await fetch(
      `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: chatId,
          video: storjUrl,
          caption: text.substring(0, 1024),
          parse_mode: "HTML",
          supports_streaming: true,
        }),
      },
    );

    const data = await res.json();
    if (data.ok && data.result?.message_id) {
      console.log(
        `[Telegram] Video sent via Storj, msg_id: ${data.result.message_id}`,
      );
      return data.result.message_id;
    }
    console.warn(
      "[Telegram] sendVideo with Storj URL failed:",
      data.description,
    );
    return sendTextToTelegram(text, chatId);
  } catch (e) {
    console.error("[Telegram] sendVideo error:", e);
    return sendTextToTelegram(text, chatId);
  }
}

// ── Upload video directly to Telegram (fallback) ───────────────────────
async function uploadVideoDirect(
  text: string,
  mediaData: Uint8Array,
  chatId = OUTPUT_CHAT_ID,
): Promise<number | null> {
  const boundary = "----FormBoundary" + Math.random().toString(36).substring(2);
  const body = buildMultipartForm(
    [
      { name: "chat_id", value: chatId },
      { name: "caption", value: text.substring(0, 1024) },
      { name: "parse_mode", value: "HTML" },
      { name: "supports_streaming", value: "true" },
      {
        name: "video",
        value: mediaData,
        filename: "video.mp4",
        contentType: "video/mp4",
      },
    ],
    boundary,
  );

  const res = await fetch(
    `https://api.telegram.org/bot${BOT_TOKEN}/sendVideo`,
    {
      method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
      body: body as any,
    },
  );

  const data = await res.json();
  if (data.ok && data.result?.message_id) {
    console.log(
      `[Telegram] Video sent directly, msg_id: ${data.result.message_id}`,
    );
    return data.result.message_id;
  }
  return null;
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

function isPlanActive(user: any, planNames: string[]): boolean {
  if (!user || !planNames.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremiumUser(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlusUser(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

function normalizeFilters(filters: any) {
  return { ...DEFAULT_FLOW_FILTERS, ...(filters || {}) };
}

function hasAnyKeyword(text: string, keywords: string[]): boolean {
  const lower = text.toLowerCase();
  return keywords.some((keyword) =>
    lower.includes(String(keyword).toLowerCase()),
  );
}

function skipReasonForFilters(
  msg: ScrapedMessage,
  filters: any,
): string | null {
  const include = Array.isArray(filters.include_keywords)
    ? filters.include_keywords
    : [];
  const exclude = Array.isArray(filters.exclude_keywords)
    ? filters.exclude_keywords
    : [];
  const hasPhotos = Boolean(msg.photoUrls?.length);
  const isAlbum = Boolean(msg.photoUrls && msg.photoUrls.length > 1);
  const hasVideo = Boolean(msg.videoUrl);
  const hasText = msg.text.trim().length > 0;

  if (filters.media_only && !msg.hasMedia) return "media_only";
  if (!filters.allow_text && hasText && !msg.hasMedia)
    return "text_not_allowed";
  if (!filters.allow_photos && hasPhotos) return "photos_not_allowed";
  if (!filters.allow_videos && hasVideo) return "videos_not_allowed";
  if (!filters.allow_albums && isAlbum) return "albums_not_allowed";
  if (include.length > 0 && !hasAnyKeyword(msg.text, include))
    return "missing_include_keyword";
  if (exclude.length > 0 && hasAnyKeyword(msg.text, exclude))
    return "excluded_keyword";
  if (filters.block_ads && isQuickAd(msg.text)) return "ad_blocked";
  return null;
}

function transformContent(
  text: string,
  filters: any,
  proPlus: boolean,
  paidUser: boolean,
): string {
  let out = text.trim();

  if (proPlus) {
    const replacements = filters.replacements || {};
    for (const [from, to] of Object.entries(replacements)) {
      if (!from) continue;
      out = out.replace(
        new RegExp(String(from).replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi"),
        String(to),
      );
    }

    if (filters.link_action === "remove" || filters.remove_links) {
      out = out.replace(/https?:\/\/\S+/gi, "").trim();
    } else if (filters.link_action === "replace" && filters.link_replacement) {
      out = out
        .replace(/https?:\/\/\S+/gi, String(filters.link_replacement))
        .trim();
    }

    if (filters.remove_mentions) out = out.replace(/@\w+/g, "").trim();
    if (filters.signature_text)
      out = `${out}\n\n${String(filters.signature_text).trim()}`.trim();
  }

  if (!paidUser) out = `${out}\n\n${FREE_USER_SIGNATURE}`.trim();
  return out;
}

async function recordActivity(supabase: any, payload: Record<string, unknown>) {
  const { error } = await supabase.from("flow_activity").insert(payload);
  if (error) console.error("[Aggregate] flow_activity insert error:", error);
}

async function incrementUsage(supabase: any, userId: string, amount: number) {
  const day = new Date().toISOString().slice(0, 10);
  const { data } = await supabase
    .from("usage_daily")
    .select("id, analyzed_count")
    .eq("user_id", userId)
    .eq("day", day)
    .maybeSingle();

  if (data) {
    await supabase
      .from("usage_daily")
      .update({ analyzed_count: (data.analyzed_count || 0) + amount })
      .eq("id", data.id);
  } else {
    await supabase
      .from("usage_daily")
      .insert({ user_id: userId, day, analyzed_count: amount });
  }
}

// ── Main handler ──────────────────────────────────────────────────────────
Deno.serve(async (_req: Request) => {
  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  try {
    const { data: flows, error: flowError } = await supabase
      .from("flows")
      .select(
        "*, user:app_users(*), source:user_sources(*), target:user_targets(*)",
      )
      .eq("is_active", true)
      .order("created_at", { ascending: true });

    if (flowError) throw flowError;

    if (!flows || flows.length === 0) {
      return new Response(
        JSON.stringify({
          success: true,
          processed: 0,
          new: 0,
          message: "No active user flows",
        }),
        { headers: { "Content-Type": "application/json" } },
      );
    }

    let processedCount = 0;
    let publishedCount = 0;
    let skippedCount = 0;
    const warnedUserIds = new Set<string>();

    for (const flow of flows) {
      const user = flow.user;
      const source = flow.source;
      const target = flow.target;
      const filters = normalizeFilters(flow.filters);

      if (!user?.is_active || !source?.is_active || !target?.is_active)
        continue;
      if (source.type !== "telegram") continue;

      const premium = isPremiumUser(user);
      const proPlus = isProPlusUser(user);

      if (premium && user.plan_expires_at && !warnedUserIds.has(user.id)) {
        const expiresAt = new Date(user.plan_expires_at).getTime();
        const daysLeft = Math.ceil((expiresAt - Date.now()) / 86400000);
        const lastWarn = user.last_subscription_warning_at
          ? new Date(user.last_subscription_warning_at).getTime()
          : 0;
        if (daysLeft > 0 && daysLeft <= 5 && Date.now() - lastWarn > 86400000) {
          await sendTextToTelegram(
            `⏰ Salut ${user.username ? "@" + user.username : "cher utilisateur"},\n\nTon abonnement <b>${user.plan === "pro_plus" ? "Pro Plus" : "Premium"}</b> expire dans <b>${daysLeft} jour(s)</b>. Pense à renouveler pour garder tes fonctionnalités actives.`,
            String(user.telegram_user_id),
          );
          await supabase
            .from("app_users")
            .update({ last_subscription_warning_at: new Date().toISOString() })
            .eq("id", user.id);
          warnedUserIds.add(user.id);
        }
      }

      const channel = source.config?.channel;
      const targetChatId = target.chat_id;
      const lastMsgId = Number(flow.last_message_id || 0);
      if (!channel || !targetChatId) continue;

      console.log(
        `[Aggregate] Flow ${flow.id}: @${channel} → ${targetChatId}, after ${lastMsgId}`,
      );

      try {
        const messages = await scrapeChannel(channel, lastMsgId);
        let messagesToProcess = messages;
        if (
          lastMsgId === 0 &&
          messages.length > Number(flow.initial_last_n || 5)
        ) {
          messagesToProcess = messages.slice(-Number(flow.initial_last_n || 5));
        }

        let maxMsgId = lastMsgId;
        let analyzedForThisFlow = 0;

        for (const msg of messagesToProcess) {
          processedCount++;
          analyzedForThisFlow++;
          maxMsgId = Math.max(maxMsgId, msg.id);

          if (msg.text.length < 20 && !msg.hasMedia) continue;

          const originalUrl = `https://t.me/${channel}/${msg.id}`;
          const { data: alreadyProcessed } = await supabase
            .from("flow_activity")
            .select("id")
            .eq("flow_id", flow.id)
            .eq("source_message_id", msg.id)
            .eq("status", "published")
            .maybeSingle();

          if (alreadyProcessed) continue;

          const reason = proPlus ? skipReasonForFilters(msg, filters) : null;
          if (reason) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "skipped",
              reason,
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          let finalContent = msg.text;
          if (proPlus && filters.use_ai_rewrite && msg.text.length >= 10) {
            const aiRes = await restructureWithAI(msg.text);
            if (aiRes.isAd) {
              skippedCount++;
              await recordActivity(supabase, {
                user_id: user.id,
                flow_id: flow.id,
                source_id: source.id,
                target_id: target.id,
                source_message_id: msg.id,
                status: "skipped",
                reason: "ai_ad_blocked",
                original_url: originalUrl,
                text_preview: msg.text.substring(0, 200),
                media_count:
                  (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
              });
              continue;
            }
            finalContent = aiRes.content.trim();
          }

          if (proPlus && filters.translate_enabled) {
            finalContent = await translateWithAI(
              finalContent,
              filters.target_language || "fr",
            );
          }

          const textToPublish =
            transformContent(finalContent, filters, proPlus, premium) ||
            originalUrl;
          const isShortVideo =
            msg.videoUrl && (!msg.videoDuration || msg.videoDuration <= 60);
          let telegramMsgId: number | null = null;

          if (
            isShortVideo &&
            msg.videoUrl &&
            (proPlus ? filters.allow_videos : true)
          ) {
            telegramMsgId = await sendVideoToTelegram(
              textToPublish,
              msg.videoUrl,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 1 &&
            (proPlus ? filters.allow_albums : true)
          ) {
            const albumUrls = msg.photoUrls.slice(0, 10);
            telegramMsgId = await sendAlbumToTelegram(
              textToPublish,
              albumUrls,
              targetChatId,
            );
          } else if (
            msg.photoUrls &&
            msg.photoUrls.length > 0 &&
            (proPlus ? filters.allow_photos : true)
          ) {
            telegramMsgId = await sendPhotoToTelegram(
              textToPublish,
              msg.photoUrls[0],
              targetChatId,
            );
          } else if (proPlus ? filters.allow_text : true) {
            telegramMsgId = await sendTextToTelegram(
              textToPublish,
              targetChatId,
            );
          }

          if (!telegramMsgId) {
            skippedCount++;
            await recordActivity(supabase, {
              user_id: user.id,
              flow_id: flow.id,
              source_id: source.id,
              target_id: target.id,
              source_message_id: msg.id,
              status: "failed",
              reason: "publish_failed",
              original_url: originalUrl,
              text_preview: msg.text.substring(0, 200),
              media_count:
                (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            });
            continue;
          }

          publishedCount++;
          await recordActivity(supabase, {
            user_id: user.id,
            flow_id: flow.id,
            source_id: source.id,
            target_id: target.id,
            source_message_id: msg.id,
            target_message_ids: [telegramMsgId],
            status: "published",
            original_url: originalUrl,
            text_preview: msg.text.substring(0, 200),
            media_count: (msg.photoUrls?.length || 0) + (msg.videoUrl ? 1 : 0),
            raw_payload: {
              target_chat_id: targetChatId,
              source_channel: channel,
            },
          });

          await new Promise((r) => setTimeout(r, 1500));
        }

        if (analyzedForThisFlow > 0)
          await incrementUsage(supabase, user.id, analyzedForThisFlow);

        await supabase
          .from("flows")
          .update({
            last_message_id: maxMsgId,
            last_run_at: new Date().toISOString(),
            last_error: null,
          })
          .eq("id", flow.id);
      } catch (flowErr) {
        console.error(`[Aggregate] Flow ${flow.id} failed:`, flowErr);
        await supabase
          .from("flows")
          .update({
            last_run_at: new Date().toISOString(),
            last_error: (flowErr as Error).message,
          })
          .eq("id", flow.id);
      }
    }

    return new Response(
      JSON.stringify({
        success: true,
        processed: processedCount,
        new: publishedCount,
        skipped: skippedCount,
      }),
      { headers: { "Content-Type": "application/json" } },
    );
  } catch (err) {
    console.error("[Aggregate] Fatal error:", err);
    return new Response(
      JSON.stringify({ success: false, error: (err as Error).message }),
      { status: 500 },
    );
  }
});

```


## Assistant

Need diagnostics after.

**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/functions/aggregate_news/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


**Tool Call: Check diagnostics for `news/supabase/functions/nelsius_webhook/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


## Assistant

J’ai vérifié et ajusté la synchronisation des plans.

Tu avais raison : le message d’accueil était encore basé sur l’ancienne logique, avec seulement le gratuit et le Premium simple, et il indiquait encore `5 analyses/jour`.

Maintenant c’est corrigé.

## Ce que j’ai synchronisé

### 1. Message d’accueil `/start` mis à jour

Le message d’accueil présente maintenant clairement les 3 plans :

- 🆓 Gratuit
- 💳 Premium — 500 FCFA/mois
- 🚀 Pro Plus — 1000 FCFA/mois

Avec un texte stylisé et plus clair.

Le plan gratuit indique maintenant correctement :

- récupération illimitée ;
- 1 flux actif ;
- signature IzyNews ajoutée en bas.

---

## Nouveau message d’accueil

Il présente maintenant :

### 🆓 Gratuit

- récupération illimitée ;
- 1 flux actif : 1 source → 1 canal cible ;
- signature IzyNews ajoutée en bas des publications.

### 💳 Premium — 500 FCFA/mois

- plusieurs flux actifs ;
- pas de signature automatique.

### 🚀 Pro Plus — 1000 FCFA/mois

- tout Premium ;
- traduction automatique ;
- filtres avancés par flux ;
- remplacement mots/liens ;
- suppression pubs.

---

## 2. `/me` ajusté

Avant, `/me` affichait encore une logique du type :

`Analyses aujourd'hui: used/5`

Maintenant il affiche :

`Messages analysés aujourd'hui: used/∞`

Puisque le gratuit fait bien la récupération illimitée, mais sur un seul flux.

---

## 3. Suppression de la limite gratuite de 5 messages dans l’agrégateur

J’ai retiré cette ancienne limite dans :

`news/supabase/functions/aggregate_news/index.ts`

Avant, il y avait une limite :

- utilisateur gratuit : seulement 5 messages analysés par run.

Maintenant :

- gratuit = récupération illimitée ;
- limitation uniquement sur le nombre de flux actifs ;
- un seul flux actif pour le gratuit.

Donc la logique est synchronisée avec ce que tu veux.

---

## 4. Les filtres média sont maintenant ignorés pour les non-Pro Plus

Pour éviter qu’un ancien filtre bloque un utilisateur gratuit ou Premium, j’ai ajusté l’agrégateur :

- les utilisateurs gratuits et Premium récupèrent tout ;
- les options `allow_text`, `allow_photos`, `allow_videos`, `allow_albums` ne limitent plus les non-Pro Plus ;
- seuls les Pro Plus utilisent les filtres avancés.

Donc :

### Gratuit

Récupère tout sur 1 flux avec signature.

### Premium

Récupère tout sur plusieurs flux sans signature.

### Pro Plus

Peut filtrer, traduire, remplacer les mots/liens, bloquer les pubs, etc.

---

## 5. Vérification des plans côté code

C’est maintenant cohérent :

### Dans `telegram_bot`

- `isPremium()` retourne vrai pour :
  - `premium`
  - `pro_plus`

- `isProPlus()` retourne vrai uniquement pour :
  - `pro_plus`

### Dans `aggregate_news`

- `isPremiumUser()` retourne vrai pour :
  - `premium`
  - `pro_plus`

- `isProPlusUser()` retourne vrai uniquement pour :
  - `pro_plus`

### Dans `nelsius_webhook`

- paiement Premium active :
  - `plan = premium`

- paiement Pro Plus active :
  - `plan = pro_plus`

---

## 6. Diagnostics

Les seuls diagnostics restants sont les mêmes que d’habitude :

`Cannot find module 'https://esm.sh/@supabase/supabase-js@2'`

C’est le diagnostic local TypeScript lié aux imports URL Deno/Supabase.

Pas d’autre erreur logique détectée par les diagnostics disponibles.

---

## Fichiers modifiés

J’ai modifié :

- `news/supabase/functions/telegram_bot/index.ts`
- `news/supabase/functions/aggregate_news/index.ts`

---

## À redéployer

Tu dois redéployer :

`telegram_bot`

et :

`aggregate_news`

Commandes :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

`npx supabase functions deploy aggregate_news --project-ref nxgryaqncfjuazfvekro`

---

## Résumé final synchronisé

### 🆓 Gratuit

- 1 flux actif ;
- récupération illimitée ;
- tous les messages récupérés ;
- médias et albums récupérés ;
- signature IzyNews automatique.

### 💳 Premium — 500 FCFA/mois

- plusieurs flux actifs ;
- récupération illimitée ;
- pas de signature IzyNews automatique.

### 🚀 Pro Plus — 1000 FCFA/mois

- tout Premium ;
- traduction ;
- filtres avancés ;
- suppression pub ;
- remplacement mots-clés ;
- remplacement/suppression liens ;
- signature personnalisée par flux.

## User

voici aussi un exmple de paiement valider . sa donne bien avec notre systeme? {
  "event": "payment.success",
  "data": {
    "transaction_code": "API202605131126064762",
    "reference_id": "REQ2605131026YD2SXDE",
    "status": "completed",
    "amount": "150.00",
    "net_amount": "100.00",
    "currency": "XAF",
    "operator": "orange_money",
    "operator_transaction_id": "REQ2605131026YD2SXDE",
    "description": null,
    "payer_phone": "640114583",
    "payer_email": "nexkynelson@gmail.com",
    "confirmed_at": "2026-05-13T10:26:28.000000Z",
    "created_at": "2026-05-13T10:26:06.000000Z"
  },
  "timestamp": 1778667990,
  "horodatage": 1778667990
}

## Assistant

Oui, avec les corrections qu’on vient de faire, cette structure est maintenant bien supportée.

Ton payload validé :

```/dev/null/nelsius-payment-success.json#L1-22
{
  "event": "payment.success",
  "data": {
    "transaction_code": "API202605131126064762",
    "reference_id": "REQ2605131026YD2SXDE",
    "status": "completed",
    "amount": "150.00",
    "net_amount": "100.00",
    "currency": "XAF",
    "operator": "orange_money",
    "operator_transaction_id": "REQ2605131026YD2SXDE",
    "description": null,
    "payer_phone": "640114583",
    "payer_email": "nexkynelson@gmail.com",
    "confirmed_at": "2026-05-13T10:26:28.000000Z",
    "created_at": "2026-05-13T10:26:06.000000Z"
  },
  "timestamp": 1778667990,
  "horodatage": 1778667990
}
```

## Ce que notre webhook va lire

### Référence

Il va lire :

`data.reference_id`

Donc :

`REQ2605131026YD2SXDE`

Cette valeur sera utilisée pour retrouver le paiement.

Le webhook cherche maintenant dans :

1. `payments.reference = REQ2605131026YD2SXDE`
2. sinon `payments.external_id = REQ2605131026YD2SXDE`

Donc si cette référence a bien été stockée pendant l’initiation du paiement, c’est OK.

---

### Statut

Il va lire :

`data.status`

Donc :

`completed`

`completed` est bien reconnu comme statut payé.

Donc le webhook va considérer le paiement comme validé.

---

### Identifiant externe

Il va lire aussi :

`data.transaction_code`

Donc :

`API202605131126064762`

Et aussi potentiellement :

`data.operator_transaction_id`

Donc :

`REQ2605131026YD2SXDE`

Ces valeurs peuvent servir de fallback si nécessaire.

---

## Résultat attendu selon le type de paiement

### Si c’est un dépôt

Si la ligne dans `payments` a :

`payment_type = deposit`

Alors le webhook va :

1. mettre `payments.status = completed` ;
2. mettre `payments.paid_at` ;
3. enregistrer le payload dans `payments.raw_payload` ;
4. créditer `app_users.wallet_balance` ;
5. créer une ligne dans `wallet_transactions`.

Exemple :

Si le paiement était de `150 XAF`, le wallet augmente de `150`.

---

### Si c’est un abonnement Premium

Si la ligne dans `payments` a :

`payment_type = subscription`

et :

`metadata.plan = premium`

Alors le webhook va :

1. mettre `payments.status = completed` ;
2. passer l’utilisateur en `plan = premium` ;
3. mettre `plan_expires_at` à +1 mois ;
4. créer une ligne dans `subscriptions`.

---

### Si c’est un abonnement Pro Plus

Si la ligne dans `payments` a :

`payment_type = subscription`

et :

`metadata.plan = pro_plus`

Alors le webhook va :

1. mettre `payments.status = completed` ;
2. passer l’utilisateur en `plan = pro_plus` ;
3. mettre `plan_expires_at` à +1 mois ;
4. créer une ligne dans `subscriptions`.

---

## Condition importante

Il faut que `REQ2605131026YD2SXDE` soit connu par notre système.

Autrement dit, à l’initiation du paiement, si Nelsius a retourné :

`reference_id = REQ2605131026YD2SXDE`

notre bot doit l’avoir stocké dans :

`payments.external_id`

On a déjà ajouté cette correction dans `telegram_bot`.

Donc après redéploiement, les nouveaux paiements devraient être bien suivis.

---

## Si tu as initié ce paiement avant la correction

Si ce paiement précis a été initié avant qu’on stocke `reference_id` dans `external_id`, alors il est possible que la table `payments` contienne seulement une référence interne du type :

`DEPOSIT_123456_...`

Dans ce cas, le webhook ne retrouvera pas automatiquement :

`REQ2605131026YD2SXDE`

sauf si Nelsius renvoie aussi notre référence interne ailleurs dans le payload.

Pour vérifier, regarde dans Supabase :

Table :

`payments`

Colonnes à comparer :

- `reference`
- `external_id`

Si tu vois :

`external_id = REQ2605131026YD2SXDE`

alors c’est bon.

---

## Ce que tu dois vérifier maintenant

Dans Supabase, après initiation d’un paiement récent, vérifie dans `payments` :

- `reference` : notre référence interne ;
- `external_id` : référence Nelsius `REQ...` ou `API...` ;
- `status` : `pending` au départ ;
- après webhook : `completed`.

---

## Conclusion

Oui, ce format est maintenant compatible.

Ton webhook sera traité comme paiement réussi car :

- `data.reference_id` est supporté ;
- `data.status = completed` est reconnu comme payé ;
- `data.transaction_code` est aussi supporté ;
- l’idempotence empêche les doubles crédits si le webhook arrive plusieurs fois.

La seule condition : la référence `REQ...` doit correspondre à `payments.reference` ou `payments.external_id`.

## User (checkpoint)

si un client recharge son compte avec 400 par exemple, je peux voir ce solde dans mon interface utilisateur? egalement, si je veux payer un abonement,  si mon solde ateind deja, sa valide directement mais si le solde est insufisant, sa cree un lien de paement pour completer  avec les montant  allant de 100   . pas de 50 ou 75.... aussi,  il ya certains fonctionalites non intuitf pour les utilisateurs  ni certaines cenariot et autres.

## Assistant

<thinking>
**Considering user menu improvements**

I’m thinking about adding a "💼 Wallet" option to the user menu to enhance intuitiveness. Maybe I can replace "🧾 Paiements" with this new button for clearer navigation. I’m considering organizing the menu with options like "💰 Dépôt" and "⚙️ Filtres Pro+" too.

Some features aren’t intuitive, so I’m wondering about proposing guided scenarios for Pro+ filters. However, time is tight. I could add "⚙️ Filtres Pro+" to link to help, but it may not be guided. Overall, it's important to acknowledge and initiate these improvements.
</thinking>

Oui : le solde est déjà visible via `/me`, mais je vais rendre ça plus clair avec un bouton/commande `💼 Wallet`. Je vais aussi modifier le paiement des abonnements : si le wallet suffit, l’abonnement s’active directement ; sinon le bot crée un lien Nelsius seulement pour compléter, avec un minimum de 100 FCFA.

**Tool Call: Search files for regex `createNelsiusCheckout|command === "/subscribe"|command === "/proplus"|userMenuMarkup|wallet_balance|payment.payment_type === "subscription"` (case-sensitive)**
Status: Completed

Showing matches 1-20 (there were more matches found; use offset: 20 to see next page):

## Matches in news\supabase\functions\nelsius_webhook\index.ts

### L170-174
```
  }

  if (payment.payment_type === "subscription") {
    const now = new Date();
    const end = new Date(now);
```

### L199-201
```
    const { data: user } = await supabase
      .from("app_users")
      .select("wallet_balance")
```

### const balanceAfter › L205-206
```
    const balanceAfter =
      (user?.wallet_balance || 0) + Number(payment.amount || 0);
```

### wallet_balance › L208-210
```
    await supabase
      .from("app_users")
      .update({ wallet_balance: balanceAfter })
```

## Matches in news\supabase\functions\telegram_bot\index.ts

### function userMenuMarkup() › L81-91
```
function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "🧾 Paiements"],
        ["⚙️ Filtres Pro+", "❓ Aide"],
```

6 lines remaining in ancestor node. Read the file to see all.

### async function createNelsiusCheckout( ) › L428-438
```
async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
```

85 lines remaining in ancestor node. Read the file to see all.

### L892-896
```
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
```

### L914-923
```
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
```

13 lines remaining in ancestor node. Read the file to see all.

### L968-972
```
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
```

### L1000-1004
```
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
```

### L1006-1010
```
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
```

### const result › L1033-1037
```
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
```

### L1042-1046
```
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
```

### L1064-1068
```
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
```

### L1092-1101
```
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
```

### L1259-1263
```
          `• Remplacement mots/liens et suppression pubs\n\n` +
          `👉 Clique sur <b>➕ Nouveau flux</b> pour commencer.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
```

### L1281-1288
```
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Messages analysés aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
```

### L1290-1305
```
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
```

### L1318-1322
```
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
```

### L1325-1341
```
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
```


## Assistant

Need wallet transaction spending function, activate subscription using wallet, partial payment. Modify createNelsiusCheckout to accept metadata? Since partial payment should create subscription payment for remaining amount but metadata includes total_price? On webhook, subscription sets plan. Good: if remaining paid, okay. But wallet deducted? Need deduct wallet before? If insufficient, we can deduct wallet now and ask to pay remaining. However if payment fails, wallet deducted but subscription not active. Bad. Better for complement with 400 wallet towards 500, create payment amount 100 and metadata.wallet_to_deduct=400. On webhook paid, deduct wallet and activate. But if payment isn't paid, wallet remains. For full wallet, deduct immediately and activate. Need update webhook subscription deduct wallet if metadata.wallet_to_deduct. Need bot helper purchaseSubscription.

Implement activateSubscriptionWithWallet(supabase, appUser, plan, price). If balance>=price: deduct, insert wallet transaction debit/subscription, update app_user plan, insert subscriptions. If 0<balance<price, create NelsiusCheckout amount = price-balance, metadata wallet_to_deduct balance and price plan. If remaining <100? user asks complement amounts going from 100 no 50/75. So if price-balance <100, what? If wallet 450 for premium, remaining 50. It should not create 50 link. Options: either use wallet? "si solde insuffisant, sa cree un lien de paiement pour completer avec les montant allant de 100 pas de 50 ou 75" Means min external complement 100; for 450, charge 100, activate and leave/overpay? Could add extra to wallet? If pays 100 and price is 500, 50 extra remains wallet. We can metadata wallet_to_deduct=400? Wait balance 450, charge 100, total 550. To handle: deduct price - charge = 400 from wallet, leaving 50 in wallet after successful webhook. Good. General: paymentAmount = max(100, price-balance). walletToDeduct = price - paymentAmount. If balance=400, payment=100, walletToDeduct=400. If balance=450, payment=100, walletToDeduct=400; leaves 50. If balance=425, payment=100, walletToDeduct=400; leaves 25. That matches no 75 charge. If balance=50, payment=450 and walletToDeduct=50. But "montants allant de 100" okay 450. If balance 0, payment=500.

Need createNelsiusCheckout accepts metadata extra. Modify metadata.

Webhook subscription after payment paid: deduct wallet_to_deduct before plan. Also if from wallet-only, no webhook.

Implement helper activateSubscription and change /subscribe /proplus.

Also /wallet command. Add to menu maybe "💼 Wallet" instead of just /me. Button. We'll add.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "💼 Wallet"],
        ["🧾 Paiements", "⚙️ Filtres Pro+"],
        ["❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "💼 Wallet": "/wallet",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
  extraMetadata: Record<string, unknown> = {},
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: {
        telegram_user_id: appUser.telegram_user_id,
        plan,
        ...extraMetadata,
      },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.reference_id ||
      data?.reference_id ||
      data?.data?.transaction_code ||
      data?.transaction_code ||
      data?.data?.id ||
      data?.id ||
      data?.data?.session_id ||
      null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus
        ? "🚀 Pro Plus"
        : premium
          ? "💳 Premium"
          : "🆓 Gratuit";
      await reply(
        chatId,
        `👋 <b>Bienvenue sur ton assistant Telegram Auto</b>\n\n` +
          `Ton plan actuel : <b>${planLabel}</b>\n\n` +
          `🆓 <b>Gratuit</b>\n` +
          `• Récupération illimitée\n` +
          `• 1 flux actif : 1 source → 1 canal cible\n` +
          `• Signature IzyNews ajoutée en bas des publications\n\n` +
          `💳 <b>Premium — 500 FCFA/mois</b>\n` +
          `• Plusieurs flux actifs\n` +
          `• Pas de signature automatique\n\n` +
          `🚀 <b>Pro Plus — 1000 FCFA/mois</b>\n` +
          `• Tout Premium\n` +
          `• Traduction automatique\n` +
          `• Filtres avancés par flux\n` +
          `• Remplacement mots/liens et suppression pubs\n\n` +
          `👉 Clique sur <b>➕ Nouveau flux</b> pour commencer.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = "∞";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Messages analysés aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Add helper buySubscription after createNelsius?

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "💼 Wallet"],
        ["🧾 Paiements", "⚙️ Filtres Pro+"],
        ["❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "💼 Wallet": "/wallet",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
  extraMetadata: Record<string, unknown> = {},
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: {
        telegram_user_id: appUser.telegram_user_id,
        plan,
        ...extraMetadata,
      },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.reference_id ||
      data?.reference_id ||
      data?.data?.transaction_code ||
      data?.transaction_code ||
      data?.data?.id ||
      data?.id ||
      data?.data?.session_id ||
      null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function activateSubscriptionFromWallet(
  supabase: any,
  appUser: any,
  plan: "premium" | "pro_plus",
  price: number,
) {
  const balance = Number(appUser.wallet_balance || 0);
  if (balance < price) return { activated: false, error: "Solde insuffisant." };
  const now = new Date();
  const end = new Date(now);
  end.setMonth(end.getMonth() + 1);
  const balanceAfter = balance - price;

  await supabase
    .from("app_users")
    .update({
      wallet_balance: balanceAfter,
      plan,
      plan_expires_at: end.toISOString(),
      is_active: true,
    })
    .eq("id", appUser.id);

  await supabase.from("wallet_transactions").insert({
    user_id: appUser.id,
    type: "subscription",
    amount: -price,
    currency: "XAF",
    balance_after: balanceAfter,
    description: `Paiement abonnement ${plan === "pro_plus" ? "Pro Plus" : "Premium"} par wallet`,
  });

  await supabase.from("subscriptions").insert({
    user_id: appUser.id,
    provider: "wallet",
    status: plan,
    start_at: now.toISOString(),
    end_at: end.toISOString(),
    payment_reference: `WALLET_${plan.toUpperCase()}_${Date.now()}`,
  });

  return { activated: true, balanceAfter, end: end.toISOString(), error: null };
}

async function prepareSubscriptionPayment(
  supabase: any,
  appUser: any,
  plan: "premium" | "pro_plus",
  price: number,
) {
  const balance = Number(appUser.wallet_balance || 0);
  if (balance >= price)
    return await activateSubscriptionFromWallet(supabase, appUser, plan, price);

  const paymentAmount = Math.max(100, price - balance);
  const walletToDeduct = Math.max(0, price - paymentAmount);
  const result = await createNelsiusCheckout(
    supabase,
    appUser,
    "subscription",
    paymentAmount,
    plan,
    {
      subscription_price: price,
      wallet_to_deduct: walletToDeduct,
    },
  );

  return {
    activated: false,
    checkoutUrl: result.checkoutUrl,
    error: result.error,
    paymentAmount,
    walletToDeduct,
  };
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus
        ? "🚀 Pro Plus"
        : premium
          ? "💳 Premium"
          : "🆓 Gratuit";
      await reply(
        chatId,
        `👋 <b>Bienvenue sur ton assistant Telegram Auto</b>\n\n` +
          `Ton plan actuel : <b>${planLabel}</b>\n\n` +
          `🆓 <b>Gratuit</b>\n` +
          `• Récupération illimitée\n` +
          `• 1 flux actif : 1 source → 1 canal cible\n` +
          `• Signature IzyNews ajoutée en bas des publications\n\n` +
          `💳 <b>Premium — 500 FCFA/mois</b>\n` +
          `• Plusieurs flux actifs\n` +
          `• Pas de signature automatique\n\n` +
          `🚀 <b>Pro Plus — 1000 FCFA/mois</b>\n` +
          `• Tout Premium\n` +
          `• Traduction automatique\n` +
          `• Filtres avancés par flux\n` +
          `• Remplacement mots/liens et suppression pubs\n\n` +
          `👉 Clique sur <b>➕ Nouveau flux</b> pour commencer.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = "∞";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Messages analysés aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(chatId, "⏳ Création du lien de paiement Premium...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        500,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton compte deviendra Premium automatiquement après confirmation du paiement.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "💳 Payer 500 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton profil une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(chatId, "⏳ Création du lien de paiement Pro Plus...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "subscription",
        1000,
        "pro_plus",
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\n\nInclus: traduction automatique, filtres avancés par flux, remplacement de mots/liens, suppression publicités, signatures personnalisées.`,
          {
            reply_markup: {
              inline_keyboard: [
                [{ text: "🚀 Payer 1000 FCFA", url: result.checkoutUrl }],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

Modify subscribe/proplus commands and add wallet command.

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "💼 Wallet"],
        ["🧾 Paiements", "⚙️ Filtres Pro+"],
        ["❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "💼 Wallet": "/wallet",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
  extraMetadata: Record<string, unknown> = {},
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: {
        telegram_user_id: appUser.telegram_user_id,
        plan,
        ...extraMetadata,
      },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.reference_id ||
      data?.reference_id ||
      data?.data?.transaction_code ||
      data?.transaction_code ||
      data?.data?.id ||
      data?.id ||
      data?.data?.session_id ||
      null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function activateSubscriptionFromWallet(
  supabase: any,
  appUser: any,
  plan: "premium" | "pro_plus",
  price: number,
) {
  const balance = Number(appUser.wallet_balance || 0);
  if (balance < price) return { activated: false, error: "Solde insuffisant." };
  const now = new Date();
  const end = new Date(now);
  end.setMonth(end.getMonth() + 1);
  const balanceAfter = balance - price;

  await supabase
    .from("app_users")
    .update({
      wallet_balance: balanceAfter,
      plan,
      plan_expires_at: end.toISOString(),
      is_active: true,
    })
    .eq("id", appUser.id);

  await supabase.from("wallet_transactions").insert({
    user_id: appUser.id,
    type: "subscription",
    amount: -price,
    currency: "XAF",
    balance_after: balanceAfter,
    description: `Paiement abonnement ${plan === "pro_plus" ? "Pro Plus" : "Premium"} par wallet`,
  });

  await supabase.from("subscriptions").insert({
    user_id: appUser.id,
    provider: "wallet",
    status: plan,
    start_at: now.toISOString(),
    end_at: end.toISOString(),
    payment_reference: `WALLET_${plan.toUpperCase()}_${Date.now()}`,
  });

  return { activated: true, balanceAfter, end: end.toISOString(), error: null };
}

async function prepareSubscriptionPayment(
  supabase: any,
  appUser: any,
  plan: "premium" | "pro_plus",
  price: number,
) {
  const balance = Number(appUser.wallet_balance || 0);
  if (balance >= price)
    return await activateSubscriptionFromWallet(supabase, appUser, plan, price);

  const paymentAmount = Math.max(100, price - balance);
  const walletToDeduct = Math.max(0, price - paymentAmount);
  const result = await createNelsiusCheckout(
    supabase,
    appUser,
    "subscription",
    paymentAmount,
    plan,
    {
      subscription_price: price,
      wallet_to_deduct: walletToDeduct,
    },
  );

  return {
    activated: false,
    checkoutUrl: result.checkoutUrl,
    error: result.error,
    paymentAmount,
    walletToDeduct,
  };
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus
        ? "🚀 Pro Plus"
        : premium
          ? "💳 Premium"
          : "🆓 Gratuit";
      await reply(
        chatId,
        `👋 <b>Bienvenue sur ton assistant Telegram Auto</b>\n\n` +
          `Ton plan actuel : <b>${planLabel}</b>\n\n` +
          `🆓 <b>Gratuit</b>\n` +
          `• Récupération illimitée\n` +
          `• 1 flux actif : 1 source → 1 canal cible\n` +
          `• Signature IzyNews ajoutée en bas des publications\n\n` +
          `💳 <b>Premium — 500 FCFA/mois</b>\n` +
          `• Plusieurs flux actifs\n` +
          `• Pas de signature automatique\n\n` +
          `🚀 <b>Pro Plus — 1000 FCFA/mois</b>\n` +
          `• Tout Premium\n` +
          `• Traduction automatique\n` +
          `• Filtres avancés par flux\n` +
          `• Remplacement mots/liens et suppression pubs\n\n` +
          `👉 Clique sur <b>➕ Nouveau flux</b> pour commencer.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = "∞";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Messages analysés aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        "⏳ Vérification de ton wallet et préparation Premium...",
      );
      const result = await prepareSubscriptionPayment(
        supabase,
        appUser,
        "premium",
        500,
      );
      if (result.activated) {
        await reply(
          chatId,
          `✅ <b>Premium activé directement avec ton wallet</b>\n\nSolde restant: <b>${result.balanceAfter} XAF</b>`,
          userMenuMarkup(),
        );
      } else if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\nSolde utilisé: <b>${result.walletToDeduct || 0} XAF</b>\nÀ compléter: <b>${result.paymentAmount} XAF</b>\n\nClique sur le bouton ci-dessous pour compléter avec Nelsius Pay.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💳 Payer ${result.paymentAmount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(
        chatId,
        "⏳ Vérification de ton wallet et préparation Pro Plus...",
      );
      const result = await prepareSubscriptionPayment(
        supabase,
        appUser,
        "pro_plus",
        1000,
      );
      if (result.activated) {
        await reply(
          chatId,
          `✅ <b>Pro Plus activé directement avec ton wallet</b>\n\nSolde restant: <b>${result.balanceAfter} XAF</b>`,
          userMenuMarkup(),
        );
      } else if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\nSolde utilisé: <b>${result.walletToDeduct || 0} XAF</b>\nÀ compléter: <b>${result.paymentAmount} XAF</b>\n\nInclus: traduction, filtres avancés, remplacement mots/liens et suppression pubs.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `🚀 Payer ${result.paymentAmount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/wallet") {
      const { data: txs } = await supabase
        .from("wallet_transactions")
        .select(
          "type, amount, currency, balance_after, description, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(8);
      const lines = (txs || []).map((t: any) => {
        const sign = Number(t.amount) >= 0 ? "+" : "";
        return `${sign}${t.amount} ${t.currency} — ${t.description || t.type}\nSolde après: <b>${t.balance_after} ${t.currency}</b>`;
      });
      await reply(
        chatId,
        `💼 <b>Mon wallet</b>\n\nSolde actuel: <b>${appUser.wallet_balance || 0} XAF</b>\n\n${lines.length ? lines.join("\n\n") : "Aucune transaction pour le moment."}`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


## Assistant

<thinking>
**Checking for duplicates**

I need to investigate the profilters to see if the wallet is inserted in a way that could cause duplicates. The diff indicates that there's a userMenuMarkup in the profilters block, which seems a bit unusual. I should look around line 1460 to gather more context about this setup. It's definitely worth taking the time to ensure everything is functioning properly and there aren't any redundant entries. Better to double-check!
</thinking>

**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1440-1498)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
          userMenuMarkup(),
        );
      } else if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\nSolde utilisé: <b>${result.walletToDeduct || 0} XAF</b>\nÀ compléter: <b>${result.paymentAmount} XAF</b>\n\nInclus: traduction, filtres avancés, remplacement mots/liens et suppression pubs.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `🚀 Payer ${result.paymentAmount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/wallet") {
      const { data: txs } = await supabase
        .from("wallet_transactions")
        .select(
          "type, amount, currency, balance_after, description, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(8);
      const lines = (txs || []).map((t: any) => {
        const sign = Number(t.amount) >= 0 ? "+" : "";
        return `${sign}${t.amount} ${t.currency} — ${t.description || t.type}\nSolde après: <b>${t.balance_after} ${t.currency}</b>`;
      });
      await reply(
        chatId,
        `💼 <b>Mon wallet</b>\n\nSolde actuel: <b>${appUser.wallet_balance || 0} XAF</b>\n\n${lines.length ? lines.join("\n\n") : "Aucune transaction pour le moment."}`,

```


**Tool Call: Read file `supabase\functions\telegram_bot\index.ts` (lines 1498-1525)**
Status: Completed

```news/supabase/functions/telegram_bot/index.ts
        `💼 <b>Mon wallet</b>\n\nSolde actuel: <b>${appUser.wallet_balance || 0} XAF</b>\n\n${lines.length ? lines.join("\n\n") : "Aucune transaction pour le moment."}`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",

```


**Tool Call: supabase\\functions\\nelsius\_webhook\\index.ts**
Status: Failed

Diff: supabase\functions\nelsius_webhook\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

function isPaidStatus(status: string): boolean {
  return [
    "paid",
    "success",
    "successful",
    "completed",
    "approved",
    "succeeded",
  ].includes(status.toLowerCase());
}

function extractPayloadValue(payload: any, keys: string[]): string | null {
  for (const key of keys) {
    const value = key
      .split(".")
      .reduce((acc: any, part: string) => acc?.[part], payload);
    if (value !== undefined && value !== null && String(value).trim())
      return String(value);
  }
  return null;
}

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Nelsius webhook OK", { status: 200 });
  }

  const webhookSecret = Deno.env.get("NELSIUS_WEBHOOK_SECRET") || "";
  if (webhookSecret) {
    const incomingSecret =
      req.headers.get("x-nelsius-secret") ||
      req.headers.get("x-webhook-secret") ||
      "";
    if (incomingSecret !== webhookSecret) {
      return new Response(
        JSON.stringify({ success: false, error: "Unauthorized webhook" }),
        { status: 401 },
      );
    }
  }

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  let payload: any;
  try {
    payload = await req.json();
  } catch (_e) {
    return new Response(
      JSON.stringify({ success: false, error: "Invalid JSON" }),
      { status: 400 },
    );
  }

  const reference = extractPayloadValue(payload, [
    "reference",
    "reference_id",
    "data.reference",
    "data.reference_id",
    "payment.reference",
    "payment.reference_id",
    "transaction.reference",
    "transaction.reference_id",
  ]);
  const status =
    extractPayloadValue(payload, [
      "status",
      "data.status",
      "payment.status",
      "transaction.status",
    ]) || "unknown";
  const externalId = extractPayloadValue(payload, [
    "id",
    "data.id",
    "transaction_code",
    "data.transaction_code",
    "charge_id",
    "session_id",
    "data.charge_id",
    "data.session_id",
    "data.operator_transaction_id",
  ]);

  if (!reference) {
    return new Response(
      JSON.stringify({ success: false, error: "Missing reference" }),
      { status: 400 },
    );
  }

  let { data: payment, error: paymentError } = await supabase
    .from("payments")
    .select("*")
    .eq("reference", reference)
    .maybeSingle();

  if (!payment && reference) {
    const fallback = await supabase
      .from("payments")
      .select("*")
      .eq("external_id", reference)
      .maybeSingle();
    payment = fallback.data;
    paymentError = fallback.error;
  }

  if (!payment && externalId) {
    const fallback = await supabase
      .from("payments")
      .select("*")
      .eq("external_id", externalId)
      .maybeSingle();
    payment = fallback.data;
    paymentError = fallback.error;
  }

  if (paymentError) {
    return new Response(
      JSON.stringify({ success: false, error: paymentError.message }),
      { status: 500 },
    );
  }

  if (!payment) {
    return new Response(
      JSON.stringify({ success: false, error: "Payment not found" }),
      { status: 404 },
    );
  }

  const incomingPaid = isPaidStatus(status);
  const wasAlreadyPaid = isPaidStatus(String(payment.status || ""));

  const updatePayload: Record<string, unknown> = {
    status,
    raw_payload: payload,
    external_id: externalId,
  };

  if (incomingPaid && !payment.paid_at)
    updatePayload.paid_at = new Date().toISOString();

  const { error: updateError } = await supabase
    .from("payments")
    .update(updatePayload)
    .eq("id", payment.id);

  if (updateError) {
    return new Response(
      JSON.stringify({ success: false, error: updateError.message }),
      { status: 500 },
    );
  }

  if (!incomingPaid || wasAlreadyPaid) {
    return new Response(
      JSON.stringify({
        success: true,
        message: wasAlreadyPaid
          ? "Payment already processed"
          : "Payment status recorded",
      }),
    );
  }

  if (payment.payment_type === "subscription") {
    const now = new Date();
    const end = new Date(now);
    end.setMonth(end.getMonth() + 1);
    const requestedPlan =
      payment.metadata?.plan === "pro_plus" ? "pro_plus" : "premium";
    const walletToDeduct = Number(payment.metadata?.wallet_to_deduct || 0);

    let userUpdate: Record<string, unknown> = {
      plan: requestedPlan,
      plan_expires_at: end.toISOString(),
      is_active: true,
    };

    if (walletToDeduct > 0) {
      const { data: user } = await supabase
        .from("app_users")
        .select("wallet_balance")
        .eq("id", payment.user_id)
        .maybeSingle();
      const currentBalance = Number(user?.wallet_balance || 0);
      const balanceAfter = Math.max(0, currentBalance - walletToDeduct);
      userUpdate.wallet_balance = balanceAfter;
      await supabase.from("wallet_transactions").insert({
        user_id: payment.user_id,
        payment_id: payment.id,
        type: "subscription",
        amount: -walletToDeduct,
        currency: payment.currency,
        balance_after: balanceAfter,
        description: `Part wallet abonnement ${requestedPlan}`,
      });
    }

    await supabase
      .from("app_users")
      .update(userUpdate)
      .eq("id", payment.user_id);

    await supabase.from("subscriptions").insert({
      user_id: payment.user_id,
      provider: "nelsius",
      status: requestedPlan,
      start_at: now.toISOString(),
      end_at: end.toISOString(),
      payment_reference: reference,
    });
  }

  if (payment.payment_type === "deposit") {
    const { data: user } = await supabase
      .from("app_users")
      .select("wallet_balance")
      .eq("id", payment.user_id)
      .maybeSingle();

    const balanceAfter =
      (user?.wallet_balance || 0) + Number(payment.amount || 0);

    await supabase
      .from("app_users")
      .update({ wallet_balance: balanceAfter })
      .eq("id", payment.user_id);

    await supabase.from("wallet_transactions").insert({
      user_id: payment.user_id,
      payment_id: payment.id,
      type: "deposit",
      amount: payment.amount,
      currency: payment.currency,
      balance_after: balanceAfter,
      description: `Dépôt Nelsius Pay ${reference}`,
    });
  }

  return new Response(JSON.stringify({ success: true }));
});

```


## Assistant

<thinking>
**Improving user experience**

It looks like the tool had a failure, but I made some edits. The user's comment about certain functionalities being non-intuitive stuck out to me. We could enhance our wallet feature and consider explaining the next iteration for guided filters. Maybe introducing guided scenarios for pro filters would help too? There are quick improvements to add, like starting text and diagnostics, plus ensuring the external complement meets the right thresholds.
</thinking>

**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.
error at line 1390: Property 'balanceAfter' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'balanceAfter' does not exist on type '{ activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
error at line 1393: Property 'checkoutUrl' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'checkoutUrl' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1402: Property 'walletToDeduct' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'walletToDeduct' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1402: Property 'paymentAmount' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'paymentAmount' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1408: Property 'paymentAmount' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'paymentAmount' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1409: Property 'checkoutUrl' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'checkoutUrl' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1439: Property 'balanceAfter' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'balanceAfter' does not exist on type '{ activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
error at line 1442: Property 'checkoutUrl' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'checkoutUrl' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1451: Property 'walletToDeduct' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'walletToDeduct' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1451: Property 'paymentAmount' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'paymentAmount' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1457: Property 'paymentAmount' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'paymentAmount' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.
error at line 1458: Property 'checkoutUrl' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; } | { activated: boolean; balanceAfter: number; end: string; error: null; } | { activated: boolean; checkoutUrl: any; error: any; paymentAmount: number; walletToDeduct: number; }'.
  Property 'checkoutUrl' does not exist on type '{ activated: boolean; error: string; balanceAfter?: undefined; end?: undefined; }'.


**Tool Call: Check diagnostics for `news/supabase/functions/nelsius_webhook/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


## Assistant

<thinking>
**Correcting type annotations**

I see that I need to fix the return types for the `prepareSubscriptionPayment` function. Specifically, I should add a type annotation indicating it returns a `Promise<any>`. This will help ensure clarity in what the function is expected to return. It's interesting how small adjustments like this can make the code more robust and easier to understand. I'll remember to keep an eye on return types in the future to avoid any confusion!
</thinking>

**Tool Call: supabase\\functions\\telegram\_bot\\index.ts**
Status: Completed

Diff: supabase\functions\telegram_bot\index.ts
```
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

declare const Deno: any;

const BOT_TOKEN = Deno.env.get("TELEGRAM_BOT_TOKEN") || "";
const OUTPUT_CHAT_ID = Deno.env.get("TELEGRAM_CHAT_ID") || "";
const BASE = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Admin IDs: comma-separated Telegram user/chat IDs allowed to control the bot
const ADMIN_IDS = (Deno.env.get("TELEGRAM_ADMIN_IDS") || OUTPUT_CHAT_ID)
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean);

function isAdmin(id: number | string): boolean {
  return ADMIN_IDS.includes(String(id));
}

async function getOrCreateAppUser(
  supabase: any,
  telegramUserId: number,
  username?: string,
) {
  const { data: existing, error: selErr } = await supabase
    .from("app_users")
    .select("*")
    .eq("telegram_user_id", telegramUserId)
    .maybeSingle();
  if (selErr) throw selErr;
  if (existing) return existing;

  const { data: inserted, error: insErr } = await supabase
    .from("app_users")
    .insert({
      telegram_user_id: telegramUserId,
      username: username || null,
      is_admin: false,
    })
    .select("*")
    .maybeSingle();
  if (insErr) throw insErr;
  return inserted;
}

function isPlanActive(user: any, plans: string[]): boolean {
  if (!user || !plans.includes(user.plan)) return false;
  if (!user.plan_expires_at) return true;
  return new Date(user.plan_expires_at).getTime() > Date.now();
}

function isPremium(user: any): boolean {
  return isPlanActive(user, ["premium", "pro_plus"]);
}

function isProPlus(user: any): boolean {
  return isPlanActive(user, ["pro_plus"]);
}

const DEFAULT_FLOW_FILTERS = {
  include_keywords: [],
  exclude_keywords: [],
  block_ads: false,
  media_only: false,
  allow_text: true,
  allow_photos: true,
  allow_videos: true,
  allow_albums: true,
  use_ai_rewrite: false,
  remove_links: false,
  remove_mentions: false,
  signature_text: "",
  translate_enabled: false,
  target_language: "fr",
  replacements: {},
  link_action: "keep",
  link_replacement: "",
};

const USER_HELP = `🤖 <b>Bot Telegram Auto — Espace utilisateur</b>\n\nUtilise les boutons en bas pour gérer ton automatisation Telegram.\n\n<b>Scénario rapide</b>\n1. Clique sur <b>➕ Nouveau flux</b>\n2. Envoie le canal source\n3. Choisis ou ajoute le canal cible\n4. Le bot crée automatiquement le flux\n\n<b>Commandes disponibles</b>\n/addsource &lt;@canal&gt; — Ajouter une source\n/addtarget &lt;@canal ou -100...&gt; — Ajouter une cible\n/addflow &lt;source_id&gt; &lt;target_id&gt; — Créer un flux\n/flows — Mes flux\n/activity — Activité\n/subscribe — Premium`;

function userMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["➕ Nouveau flux", "🔁 Mes flux"],
        ["⏸ Désactiver flux", "▶️ Réactiver flux"],
        ["📡 Mes sources", "🎯 Mes cibles"],
        ["📊 Activité", "👤 Mon compte"],
        ["💳 Premium", "🚀 Pro Plus"],
        ["💰 Dépôt", "💼 Wallet"],
        ["🧾 Paiements", "⚙️ Filtres Pro+"],
        ["❓ Aide"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function adminMenuMarkup() {
  return {
    reply_markup: {
      keyboard: [
        ["▶️ Lancer agrégation", "📊 Stats"],
        ["➕ Source globale", "📡 Sources globales"],
        ["📰 Articles", "🧹 Nettoyage"],
        ["📣 Notifier utilisateurs"],
        ["❓ Aide admin"],
      ],
      resize_keyboard: true,
      one_time_keyboard: false,
    },
  };
}

function targetChoiceMarkup(targets: any[]) {
  const keyboard = targets
    .slice(0, 20)
    .map((t) => [
      `🎯 ${shortId(t.id)} - ${(t.title || t.chat_id || "Canal").substring(0, 35)}`,
    ]);
  keyboard.push(["➕ Ajouter une cible", "❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function flowChoiceMarkup(flows: any[]) {
  const keyboard = flows
    .slice(0, 20)
    .map((f) => [
      `🔁 ${shortId(f.id)} - @${f.source?.config?.channel || "source"} → ${f.target?.chat_id || "cible"}`.substring(
        0,
        90,
      ),
    ]);
  keyboard.push(["❌ Annuler"]);
  return {
    reply_markup: { keyboard, resize_keyboard: true, one_time_keyboard: true },
  };
}

function commandFromButton(label: string): string | null {
  const map: Record<string, string> = {
    "➕ Nouveau flux": "/newflow",
    "🔁 Mes flux": "/flows",
    "⏸ Désactiver flux": "/choosepauseflow",
    "▶️ Réactiver flux": "/chooseresumeflow",
    "📡 Mes sources": "/sources",
    "🎯 Mes cibles": "/targets",
    "📊 Activité": "/activity",
    "👤 Mon compte": "/me",
    "💳 Premium": "/subscribe",
    "🚀 Pro Plus": "/proplus",
    "💰 Dépôt": "/deposit",
    "🧾 Paiements": "/payments",
    "💼 Wallet": "/wallet",
    "⚙️ Filtres Pro+": "/profilters",
    "❓ Aide": "/help",
    "▶️ Lancer agrégation": "/run",
    "📊 Stats": "/stats",
    "➕ Source globale": "/newglobalsource",
    "📡 Sources globales": "/sources",
    "📰 Articles": "/list",
    "🧹 Nettoyage": "/adminclean",
    "🗑 DB seulement": "/deleteall",
    "🔥 Tout supprimer": "/clearall",
    "📣 Notifier utilisateurs": "/broadcast",
    "❓ Aide admin": "/help",
    "✅ J’ai ajouté le bot": "/verifytargetadmin",
    "❌ Annuler": "/cancel",
  };
  return map[label] || null;
}

function shortId(id: string): string {
  return String(id || "").substring(0, 8);
}

function normalizeTelegramChannel(input: string): string {
  let channel = (input || "").trim();
  if (channel.includes("t.me/")) {
    const parts = channel.split("t.me/");
    channel = parts[parts.length - 1].split("/")[0].split("?")[0];
  }
  return channel.replace(/^@/, "").trim().toLowerCase();
}

function normalizeTargetChat(input: string): string {
  const raw = (input || "").trim();
  if (!raw) return "";
  if (raw.startsWith("-100")) return raw;
  const channel = normalizeTelegramChannel(raw);
  return channel ? `@${channel}` : "";
}

function parseKeywordList(raw: string): string[] {
  return raw
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter(Boolean)
    .slice(0, 30);
}

function parseOnOff(value: string | undefined): boolean | null {
  const v = String(value || "")
    .trim()
    .toLowerCase();
  if (["on", "oui", "yes", "true", "1", "actif"].includes(v)) return true;
  if (["off", "non", "no", "false", "0", "inactif"].includes(v)) return false;
  return null;
}

function mergeFilters(current: any, patch: Record<string, unknown>) {
  return { ...DEFAULT_FLOW_FILTERS, ...(current || {}), ...patch };
}

async function findOwnedRecord(
  supabase: any,
  table: string,
  userId: string,
  idPrefix: string,
) {
  if (!idPrefix) return null;
  const { data } = await supabase
    .from(table)
    .select("*")
    .eq("user_id", userId)
    .ilike("id", `${idPrefix}%`)
    .limit(1)
    .maybeSingle();
  return data || null;
}

async function setBotState(
  supabase: any,
  userId: string,
  state: Record<string, unknown>,
) {
  await supabase
    .from("app_users")
    .update({ bot_state: state })
    .eq("id", userId);
}

async function clearBotState(supabase: any, userId: string) {
  await setBotState(supabase, userId, {});
}

async function getOrCreateUserSource(
  supabase: any,
  userId: string,
  rawChannel: string,
) {
  const channel = normalizeTelegramChannel(rawChannel);
  if (!channel) return { data: null, error: "Canal source invalide." };
  const { data: existing } = await supabase
    .from("user_sources")
    .select("*")
    .eq("user_id", userId)
    .eq("type", "telegram")
    .eq("config->>channel", channel)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_sources")
        .update({ is_active: true })
        .eq("id", existing.id);
    return { data: { ...existing, is_active: true }, error: null };
  }
  const { data, error } = await supabase
    .from("user_sources")
    .insert({
      user_id: userId,
      type: "telegram",
      config: { channel },
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getOrCreateUserTarget(
  supabase: any,
  userId: string,
  rawTarget: string,
  title?: string,
) {
  const chatId = normalizeTargetChat(rawTarget);
  if (!chatId) return { data: null, error: "Canal cible invalide." };
  const targetTitle = title || chatId;
  const { data: existing } = await supabase
    .from("user_targets")
    .select("*")
    .eq("user_id", userId)
    .eq("chat_id", chatId)
    .maybeSingle();
  if (existing) {
    if (!existing.is_active)
      await supabase
        .from("user_targets")
        .update({ is_active: true, title: targetTitle })
        .eq("id", existing.id);
    return {
      data: {
        ...existing,
        is_active: true,
        title: existing.title || targetTitle,
      },
      error: null,
    };
  }
  const { data, error } = await supabase
    .from("user_targets")
    .insert({
      user_id: userId,
      chat_id: chatId,
      title: targetTitle,
      is_active: true,
    })
    .select("*")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function createFlowForUser(
  supabase: any,
  appUser: any,
  sourceId: string,
  targetId: string,
) {
  const premium = isPremium(appUser);
  if (!premium) {
    const { count } = await supabase
      .from("flows")
      .select("*", { count: "exact", head: true })
      .eq("user_id", appUser.id)
      .eq("is_active", true);
    if ((count || 0) >= 1)
      return {
        data: null,
        error:
          "Le plan gratuit autorise 1 seul flux actif. Passe Premium pour en créer plusieurs.",
      };
  }
  const { data, error } = await supabase
    .from("flows")
    .insert({
      user_id: appUser.id,
      source_id: sourceId,
      target_id: targetId,
      mode: "new_only",
      initial_last_n: 5,
      is_active: true,
      filters: DEFAULT_FLOW_FILTERS,
    })
    .select("id")
    .maybeSingle();
  return { data, error: error?.message || null };
}

async function getBotInfo(): Promise<{ id: number; username: string } | null> {
  try {
    const res = await fetch(`${BASE}/getMe`);
    const data = await res.json();
    if (data.ok && data.result?.id && data.result?.username) {
      return { id: data.result.id, username: data.result.username };
    }
  } catch (e) {
    console.error("[Bot] getMe failed:", e);
  }
  return null;
}

function botAdminUrl(username: string): string {
  const allAdminRights = [
    "change_info",
    "post_messages",
    "edit_messages",
    "delete_messages",
    "invite_users",
    "restrict_members",
    "pin_messages",
    "promote_members",
    "manage_video_chats",
    "manage_chat",
    "manage_topics",
    "post_stories",
    "edit_stories",
    "delete_stories",
  ].join("+");
  return `https://t.me/${username}?startchannel=setup&admin=${allAdminRights}`;
}

async function checkBotAdminInChat(
  targetChatId: string,
): Promise<{ ok: boolean; reason?: string }> {
  const bot = await getBotInfo();
  if (!bot)
    return {
      ok: false,
      reason: "Impossible d'identifier le bot via Telegram.",
    };
  try {
    const res = await fetch(`${BASE}/getChatMember`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: targetChatId, user_id: bot.id }),
    });
    const data = await res.json();
    if (!data.ok)
      return {
        ok: false,
        reason:
          data.description || "Le bot n'est pas encore visible dans ce canal.",
      };
    const status = data.result?.status;
    if (status === "administrator" || status === "creator") return { ok: true };
    return {
      ok: false,
      reason: `Statut actuel: ${status || "inconnu"}. Le bot doit être administrateur.`,
    };
  } catch (e) {
    return { ok: false, reason: (e as Error).message };
  }
}

async function createNelsiusCheckout(
  supabase: any,
  appUser: any,
  paymentType: "subscription" | "deposit",
  amount: number,
  plan: "premium" | "pro_plus" = "premium",
  extraMetadata: Record<string, unknown> = {},
) {
  const secretKey = Deno.env.get("NELSIUS_SECRET_KEY") || "";
  if (!secretKey)
    return {
      checkoutUrl: null,
      error: "Clé Nelsius manquante: configure NELSIUS_SECRET_KEY.",
    };

  const bot = await getBotInfo();
  const reference = `${paymentType.toUpperCase()}_${appUser.telegram_user_id}_${Date.now()}`;
  const returnUrl =
    Deno.env.get("NELSIUS_RETURN_URL") ||
    (bot ? `https://t.me/${bot.username}` : "https://t.me/");

  const { data: payment, error: insertError } = await supabase
    .from("payments")
    .insert({
      user_id: appUser.id,
      provider: "nelsius",
      reference,
      amount,
      currency: "XAF",
      status: "created",
      payment_type: paymentType,
      metadata: {
        telegram_user_id: appUser.telegram_user_id,
        plan,
        ...extraMetadata,
      },
    })
    .select("id")
    .maybeSingle();
  if (insertError) return { checkoutUrl: null, error: insertError.message };

  try {
    const res = await fetch(
      `${Deno.env.get("NELSIUS_BASE_URL") || "https://api.nelsius.com"}/api/v1/checkout/sessions`,
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${secretKey}`,
          Accept: "application/json",
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          amount,
          currency: "XAF",
          reference,
          return_url: returnUrl,
          customer: {
            name: appUser.username || `Telegram ${appUser.telegram_user_id}`,
          },
        }),
      },
    );
    const data = await res.json();
    const checkoutUrl = data?.data?.checkout_url || data?.checkout_url || null;
    const externalId =
      data?.data?.reference_id ||
      data?.reference_id ||
      data?.data?.transaction_code ||
      data?.transaction_code ||
      data?.data?.id ||
      data?.id ||
      data?.data?.session_id ||
      null;

    await supabase
      .from("payments")
      .update({
        status: data?.success === false ? "failed" : "pending",
        checkout_url: checkoutUrl,
        external_id: externalId,
        raw_payload: data,
      })
      .eq("id", payment.id);

    if (!checkoutUrl)
      return {
        checkoutUrl: null,
        error: data?.message || "Nelsius n'a pas retourné de lien de paiement.",
      };
    return { checkoutUrl, error: null };
  } catch (e) {
    await supabase
      .from("payments")
      .update({
        status: "failed",
        raw_payload: { error: (e as Error).message },
      })
      .eq("id", payment.id);
    return { checkoutUrl: null, error: (e as Error).message };
  }
}

async function activateSubscriptionFromWallet(
  supabase: any,
  appUser: any,
  plan: "premium" | "pro_plus",
  price: number,
) {
  const balance = Number(appUser.wallet_balance || 0);
  if (balance < price) return { activated: false, error: "Solde insuffisant." };
  const now = new Date();
  const end = new Date(now);
  end.setMonth(end.getMonth() + 1);
  const balanceAfter = balance - price;

  await supabase
    .from("app_users")
    .update({
      wallet_balance: balanceAfter,
      plan,
      plan_expires_at: end.toISOString(),
      is_active: true,
    })
    .eq("id", appUser.id);

  await supabase.from("wallet_transactions").insert({
    user_id: appUser.id,
    type: "subscription",
    amount: -price,
    currency: "XAF",
    balance_after: balanceAfter,
    description: `Paiement abonnement ${plan === "pro_plus" ? "Pro Plus" : "Premium"} par wallet`,
  });

  await supabase.from("subscriptions").insert({
    user_id: appUser.id,
    provider: "wallet",
    status: plan,
    start_at: now.toISOString(),
    end_at: end.toISOString(),
    payment_reference: `WALLET_${plan.toUpperCase()}_${Date.now()}`,
  });

  return { activated: true, balanceAfter, end: end.toISOString(), error: null };
}

async function prepareSubscriptionPayment(
  supabase: any,
  appUser: any,
  plan: "premium" | "pro_plus",
  price: number,
): Promise<any> {
  const balance = Number(appUser.wallet_balance || 0);
  if (balance >= price)
    return await activateSubscriptionFromWallet(supabase, appUser, plan, price);

  const paymentAmount = Math.max(100, price - balance);
  const walletToDeduct = Math.max(0, price - paymentAmount);
  const result = await createNelsiusCheckout(
    supabase,
    appUser,
    "subscription",
    paymentAmount,
    plan,
    {
      subscription_price: price,
      wallet_to_deduct: walletToDeduct,
    },
  );

  return {
    activated: false,
    checkoutUrl: result.checkoutUrl,
    error: result.error,
    paymentAmount,
    walletToDeduct,
  };
}

async function promptBotAdminSetup(
  chatId: number | string,
  targetChatId: string,
) {
  const bot = await getBotInfo();
  if (bot) {
    await reply(
      chatId,
      `🔐 <b>Autorisation nécessaire</b>\n\nPour publier dans <b>${targetChatId}</b>, ajoute le bot comme administrateur du canal.\n\nClique sur le bouton ci-dessous pour ouvrir Telegram et choisir ton canal.`,
      {
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: "➕ Ajouter le bot comme admin",
                url: botAdminUrl(bot.username),
              },
            ],
          ],
        },
      },
    );
  } else {
    await reply(
      chatId,
      `🔐 Ajoute ce bot comme administrateur dans <b>${targetChatId}</b>.`,
    );
  }

  await reply(
    chatId,
    "Quand c'est fait, reviens ici et clique sur <b>✅ J’ai ajouté le bot</b>.",
    {
      reply_markup: {
        keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
        resize_keyboard: true,
        one_time_keyboard: true,
      },
    },
  );
}

async function reply(
  chatId: number | string,
  text: string,
  extra: Record<string, unknown> = {},
) {
  try {
    const res = await fetch(`${BASE}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: text.substring(0, 4096),
        parse_mode: "HTML",
        ...extra,
      }),
    });
    if (!res.ok) console.error("[Bot] sendMessage failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendMessage exception:", e);
  }
}

async function sendPhotoToChat(
  chatId: number | string,
  photoFileId: string,
  caption: string,
) {
  try {
    const res = await fetch(`${BASE}/sendPhoto`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        photo: photoFileId,
        caption: caption.substring(0, 1024),
        parse_mode: "HTML",
      }),
    });
    if (!res.ok) console.error("[Bot] sendPhoto failed:", await res.text());
  } catch (e) {
    console.error("[Bot] sendPhoto exception:", e);
  }
}

async function copyMessageToChat(
  chatId: number | string,
  fromChatId: number | string,
  messageId: number,
  caption?: string,
) {
  const body: any = {
    chat_id: chatId,
    from_chat_id: fromChatId,
    message_id: messageId,
  };
  if (caption !== undefined) {
    body.caption = caption.substring(0, 1024);
    body.parse_mode = "HTML";
  }
  try {
    const res = await fetch(`${BASE}/copyMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const data = await res.json();
    if (!data.ok) console.error("[Bot] copyMessage failed:", data);
    return data;
  } catch (e) {
    console.error("[Bot] copyMessage exception:", e);
    return { ok: false, error: e };
  }
}

async function restructureWithAI(
  rawText: string,
  apiKey: string,
): Promise<{ title: string; content: string; isAd: boolean }> {
  if (!apiKey || !rawText || rawText.length < 5)
    return { title: "", content: rawText, isAd: false };
  try {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: `Tu es un journaliste professionnel et éditeur de nouvelles pour le canal Telegram @izynews.\nVoici une information brute issue d'un canal source :\n\n${rawText}\n\nInstructions :\n1. Analyse très attentivement le texte. S'il s'agit d'une publicité, d'un contenu sponsorisé, d'un appel commercial, de crypto-monnaie promotionnelle ou de spam, mets IMPÉRATIVEMENT "is_ad": true dans le JSON.\n2. Si ce n'est pas une pub, reformule l'information pour qu'elle soit claire, percutante, professionnelle, et attrayante.\n3. Écris un titre court et captivant (max 100 caractères), TOUJOURS bien stylisé et agrémenté de PLUSIEURS émojis (stickers) variés en rapport direct avec la situation pour attirer l'oeil.\n4. Intègre ce titre tout en haut du contenu reformulé, en le mettant en gras avec des balises HTML (<b>Titre</b>).\n5. Ajoute des émojis pertinents tout au long du texte pour aérer la lecture.\n6. Conserve les détails importants (dates, lieux, personnes).\n7. Ne mets PAS "@izynews" à la fin, je le ferai programmatiquement.\n\nRéponds UNIQUEMENT au format JSON strict suivant :\n{\n  "is_ad": false,\n  "title": "Titre stylisé avec emojis 🚀",\n  "content": "<b>Titre stylisé avec emojis 🚀</b>\\n\\nContenu reformulé ici avec des émojis... ✅"\n}`,
                },
              ],
            },
          ],
          generationConfig: { responseMimeType: "application/json" },
        }),
      },
    );
    const data = await res.json();
    const textResp = data.candidates?.[0]?.content?.parts?.[0]?.text;
    if (textResp) {
      let jsonStr = textResp.trim();
      const match = jsonStr.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
      if (match) {
        jsonStr = match[1].trim();
      }
      const parsed = JSON.parse(jsonStr);
      return {
        title: parsed.title || "",
        content: parsed.content || rawText,
        isAd: parsed.is_ad === true,
      };
    }
  } catch (e) {
    console.error("[Gemini] Error:", e);
  }
  return { title: "", content: rawText, isAd: false };
}

// ── HELP TEXT ─────────────────────────────────────────────────────────────────
const HELP = `🤖 <b>Bot Admin — Gestion des infos</b>\n\n<b>📰 Articles</b>\n/list [n] — Derniers articles (défaut: 10, max: 20)\n/article &lt;id&gt; — Voir un article\n/delete &lt;id&gt; — Supprimer un article\n/deleteall — Supprimer TOUS les articles (DB seulement)\n/clearall — Supprimer TOUT (canal Telegram + DB + reset sources)\n/clearall force — Supprimer les 100 derniers messages du canal (nucléaire)\n/publish &lt;texte&gt; — Publier un article (texte)\n  ↳ Envoie une photo avec /publish en légende pour publier avec image\n\n<b>📡 Sources Telegram</b>\n/sources — Liste des sources actives\n/addsource &lt;@canal ou URL&gt; — Ajouter un canal Telegram\n/delsource &lt;id&gt; — Désactiver une source\n\n<b>⚙️ Système</b>\n/run — Lancer l'agrégation maintenant\n/stats — Statistiques de la plateforme\n/help — Afficher ce message`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") {
    return new Response("Telegram Bot Webhook — OK", { status: 200 });
  }

  let body: Record<string, unknown>;
  try {
    body = await req.json();
  } catch {
    return new Response("Bad Request", { status: 400 });
  }

  const message = (body.message || body.edited_message) as
    | Record<string, unknown>
    | undefined;
  if (!message) return new Response("OK", { status: 200 });

  const chatId = (message.chat as any)?.id as number;
  const userId = (message.from as any)?.id as number;
  const username =
    ((message.from as any)?.username as string | undefined) || undefined;
  const messageId = message.message_id as number;
  const text = ((message.text || message.caption || "") as string).trim();
  const photo = (message.photo as any[]) || null;
  const hasMedia = !!(
    photo ||
    message.video ||
    message.document ||
    message.audio ||
    message.animation ||
    message.voice
  );

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL") ?? "",
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "",
  );

  const adminMode = isAdmin(chatId) || isAdmin(userId);
  const buttonCommand = commandFromButton(text);
  const normalizedText = buttonCommand || text;
  const parts = normalizedText.split(/\s+/);
  const command = parts[0]?.toLowerCase() || "";
  const args = parts.slice(1);

  try {
    // ── Ensure app user exists for every Telegram user, including admins ──
    let appUser: any = await getOrCreateAppUser(supabase, userId, username);
    if (adminMode && !appUser.is_admin) {
      await supabase
        .from("app_users")
        .update({ is_admin: true })
        .eq("id", appUser.id);
      appUser = { ...appUser, is_admin: true };
    }

    if (adminMode && command === "/broadcast") {
      await setBotState(supabase, appUser.id, {
        step: "admin_broadcast_input",
        draft: {},
      });
      await reply(
        chatId,
        "📣 <b>Notification utilisateurs</b>\n\nEnvoie maintenant le message à diffuser à tous les utilisateurs actifs du bot.\n\nLe bot ajoutera automatiquement: <code>Salut cher/chère {nom},</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_broadcast_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const messageToSend = text.trim();
      if (!messageToSend) {
        await reply(
          chatId,
          "❌ Message vide. Envoie un message ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { data: users, error } = await supabase
        .from("app_users")
        .select("telegram_user_id, username, is_active")
        .eq("is_active", true);

      if (error) {
        await clearBotState(supabase, appUser.id);
        await reply(
          chatId,
          `❌ Erreur récupération utilisateurs: ${error.message}`,
          adminMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      let sent = 0;
      let failed = 0;
      for (const u of users || []) {
        const name = u.username ? `@${u.username}` : "utilisateur";
        try {
          await reply(
            u.telegram_user_id,
            `Salut cher/chère ${name},\n\n${messageToSend}`,
          );
          sent++;
          await new Promise((r) => setTimeout(r, 80));
        } catch (_e) {
          failed++;
        }
      }
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        `✅ Notification envoyée.\nEnvoyés: <b>${sent}</b>\nÉchecs: <b>${failed}</b>`,
        adminMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/newglobalsource") {
      await setBotState(supabase, appUser.id, {
        step: "admin_global_source_input",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouvelle source globale</b>\n\nEnvoie le canal Telegram source à ajouter au système global.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (
      adminMode &&
      appUser.bot_state?.step === "admin_global_source_input" &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const channel = normalizeTelegramChannel(text);
      if (!channel) {
        await reply(
          chatId,
          "❌ Canal invalide. Envoie un @canal ou une URL t.me, ou clique sur Annuler.",
        );
        return new Response("OK", { status: 200 });
      }

      const { error } = await supabase.from("sources").insert({
        name: `@${channel}`,
        type: "telegram",
        config: { channel },
        is_active: true,
      });
      await clearBotState(supabase, appUser.id);
      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, adminMenuMarkup());
      else
        await reply(
          chatId,
          `✅ Source globale ajoutée: <b>@${channel}</b>`,
          adminMenuMarkup(),
        );
      return new Response("OK", { status: 200 });
    }

    if (adminMode && command === "/adminclean") {
      await reply(
        chatId,
        "🧹 <b>Nettoyage admin</b>\n\nChoisis l'action à exécuter. Attention, ces actions sont sensibles.",
        {
          reply_markup: {
            keyboard: [
              ["🗑 DB seulement", "🔥 Tout supprimer"],
              ["❌ Annuler"],
            ],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (command === "/cancel") {
      await clearBotState(supabase, appUser.id);
      await reply(
        chatId,
        "✅ Action annulée.",
        adminMode ? adminMenuMarkup() : userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/choosepauseflow", "/chooseresumeflow"].includes(command)
    ) {
      const shouldPause = command === "/choosepauseflow";
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .eq("is_active", shouldPause)
        .order("created_at", { ascending: false });

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!flows?.length) {
        await reply(
          chatId,
          shouldPause
            ? "📭 Tu n'as aucun flux actif à désactiver."
            : "📭 Tu n'as aucun flux désactivé à réactiver.",
          userMenuMarkup(),
        );
      } else {
        await setBotState(supabase, appUser.id, {
          step: shouldPause ? "choose_flow_pause" : "choose_flow_resume",
          draft: {},
        });
        await reply(
          chatId,
          shouldPause
            ? "⏸ Choisis le flux à désactiver."
            : "▶️ Choisis le flux à réactiver.",
          flowChoiceMarkup(flows),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/newflow") {
      await setBotState(supabase, appUser.id, {
        step: "new_flow_source",
        draft: {},
      });
      await reply(
        chatId,
        "➕ <b>Nouveau flux</b>\n\nEnvoie maintenant le canal source à surveiller.\n\nExemple: <code>@canal_source</code> ou <code>https://t.me/canal_source</code>",
        {
          reply_markup: {
            keyboard: [["❌ Annuler"]],
            resize_keyboard: true,
            one_time_keyboard: true,
          },
        },
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/verifytargetadmin") {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};
      if (
        state.step !== "new_flow_verify_target" ||
        !draft.source_id ||
        !draft.target_id ||
        !draft.target_chat_id
      ) {
        await reply(
          chatId,
          "ℹ️ Aucun canal cible en attente de vérification.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const check = await checkBotAdminInChat(String(draft.target_chat_id));
      if (!check.ok) {
        await reply(
          chatId,
          `❌ Je ne suis pas encore admin de <b>${draft.target_chat_id}</b>.\n\nDétail: ${check.reason || "vérification impossible"}\n\nAjoute le bot comme admin puis clique encore sur <b>✅ J’ai ajouté le bot</b>.`,
          {
            reply_markup: {
              keyboard: [["✅ J’ai ajouté le bot"], ["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      const result = await createFlowForUser(
        supabase,
        appUser,
        String(draft.source_id),
        String(draft.target_id),
      );
      await clearBotState(supabase, appUser.id);
      if (result.error || !result.data) {
        await reply(
          chatId,
          `❌ ${result.error || "Impossible de créer le flux."}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `✅ <b>Bot confirmé admin</b>\n\nTon flux est créé avec succès.\nID: <code>${shortId(result.data.id)}</code>\nCible: <b>${draft.target_chat_id}</b>`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      appUser.bot_state?.step &&
      !buttonCommand &&
      !text.startsWith("/")
    ) {
      const state = appUser.bot_state || {};
      const draft = state.draft || {};

      if (state.step === "deposit_amount") {
        const amount = Number(text.replace(/\s+/g, ""));
        if (!amount || amount < 100) {
          await reply(
            chatId,
            "❌ Montant invalide. Envoie un montant en FCFA supérieur ou égal à 100, ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        await clearBotState(supabase, appUser.id);
        await reply(chatId, "⏳ Création du lien de dépôt...");
        const result = await createNelsiusCheckout(
          supabase,
          appUser,
          "deposit",
          amount,
        );
        if (result.error || !result.checkoutUrl) {
          await reply(
            chatId,
            `❌ Dépôt indisponible: ${result.error}`,
            userMenuMarkup(),
          );
        } else {
          await reply(
            chatId,
            `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay.`,
            {
              reply_markup: {
                inline_keyboard: [
                  [
                    {
                      text: `💰 Déposer ${amount} FCFA`,
                      url: result.checkoutUrl,
                    },
                  ],
                ],
              },
            },
          );
          await reply(
            chatId,
            "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
            userMenuMarkup(),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (["choose_flow_pause", "choose_flow_resume"].includes(state.step)) {
        const idMatch = text.match(/[0-9a-f]{8}/i);
        const flow = idMatch
          ? await findOwnedRecord(supabase, "flows", appUser.id, idMatch[0])
          : null;
        if (!flow) {
          await reply(
            chatId,
            "❌ Flux non reconnu. Choisis un bouton flux ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }
        const active = state.step === "choose_flow_resume";
        const { error } = await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await clearBotState(supabase, appUser.id);
        if (error)
          await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
        else
          await reply(
            chatId,
            active
              ? `▶️ Flux réactivé: <code>${shortId(flow.id)}</code>`
              : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
            userMenuMarkup(),
          );
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_source") {
        const { data: source, error } = await getOrCreateUserSource(
          supabase,
          appUser.id,
          text,
        );
        if (error || !source) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la source."}\n\nRenvoie un canal source valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        const { data: targets } = await supabase
          .from("user_targets")
          .select("id, chat_id, title, is_active")
          .eq("user_id", appUser.id)
          .eq("is_active", true)
          .order("created_at", { ascending: false });

        if (!targets?.length) {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nMaintenant, envoie le canal cible où republier.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>\n\n⚠️ Ajoute le bot comme admin du canal cible.`,
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
        } else {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_choose_target",
            draft: { source_id: source.id },
          });
          await reply(
            chatId,
            `✅ Source enregistrée: <b>@${source.config?.channel}</b>\n\nChoisis maintenant le canal cible dans les boutons ci-dessous ou ajoute une nouvelle cible.`,
            targetChoiceMarkup(targets),
          );
        }
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_choose_target") {
        if (text === "➕ Ajouter une cible") {
          await setBotState(supabase, appUser.id, {
            step: "new_flow_target_input",
            draft,
          });
          await reply(
            chatId,
            "🎯 Envoie le canal cible à ajouter.\n\nExemple: <code>@mon_canal</code> ou <code>-100...</code>",
            {
              reply_markup: {
                keyboard: [["❌ Annuler"]],
                resize_keyboard: true,
                one_time_keyboard: true,
              },
            },
          );
          return new Response("OK", { status: 200 });
        }

        const idMatch = text.match(/[0-9a-f]{8}/i);
        const target = idMatch
          ? await findOwnedRecord(
              supabase,
              "user_targets",
              appUser.id,
              idMatch[0],
            )
          : null;
        if (!target || !target.is_active) {
          await reply(
            chatId,
            "❌ Cible non reconnue. Choisis un bouton cible ou clique sur Annuler.",
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }

      if (state.step === "new_flow_target_input") {
        const { data: target, error } = await getOrCreateUserTarget(
          supabase,
          appUser.id,
          text,
        );
        if (error || !target) {
          await reply(
            chatId,
            `❌ ${error || "Impossible d'ajouter la cible."}\n\nRenvoie un canal cible valide ou clique sur Annuler.`,
          );
          return new Response("OK", { status: 200 });
        }

        await setBotState(supabase, appUser.id, {
          step: "new_flow_verify_target",
          draft: {
            source_id: draft.source_id,
            target_id: target.id,
            target_chat_id: target.chat_id,
          },
        });
        await promptBotAdminSetup(chatId, target.chat_id);
        return new Response("OK", { status: 200 });
      }
    }

    // ── User commands (non-admin allowed) ─────────────────────────────────
    if (
      !adminMode &&
      (command === "/start" || command === "/help" || !command)
    ) {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus
        ? "🚀 Pro Plus"
        : premium
          ? "💳 Premium"
          : "🆓 Gratuit";
      await reply(
        chatId,
        `👋 <b>Bienvenue sur ton assistant Telegram Auto</b>\n\n` +
          `Ton plan actuel : <b>${planLabel}</b>\n\n` +
          `🆓 <b>Gratuit</b>\n` +
          `• Récupération illimitée\n` +
          `• 1 flux actif : 1 source → 1 canal cible\n` +
          `• Signature IzyNews ajoutée en bas des publications\n\n` +
          `💳 <b>Premium — 500 FCFA/mois</b>\n` +
          `• Plusieurs flux actifs\n` +
          `• Pas de signature automatique\n\n` +
          `🚀 <b>Pro Plus — 1000 FCFA/mois</b>\n` +
          `• Tout Premium\n` +
          `• Traduction automatique\n` +
          `• Filtres avancés par flux\n` +
          `• Remplacement mots/liens et suppression pubs\n\n` +
          `👉 Clique sur <b>➕ Nouveau flux</b> pour commencer.`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/me") {
      const premium = isPremium(appUser);
      const proPlus = isProPlus(appUser);
      const planLabel = proPlus ? "Pro Plus" : premium ? "Premium" : "Gratuit";
      const today = new Date().toISOString().slice(0, 10);
      const { data: usage } = await supabase
        .from("usage_daily")
        .select("analyzed_count")
        .eq("user_id", appUser.id)
        .eq("day", today)
        .maybeSingle();
      const used = usage?.analyzed_count ?? 0;
      const limit = "∞";
      await reply(
        chatId,
        `👤 <b>Mon compte</b>\n\n` +
          `Plan: <b>${planLabel}</b>\n` +
          `Messages analysés aujourd'hui: <b>${used}/${limit}</b>\n` +
          `Solde wallet: <b>${appUser.wallet_balance || 0} XAF</b>\n\n` +
          `${premium && appUser.plan_expires_at ? `Expire: <b>${String(appUser.plan_expires_at).slice(0, 10)}</b>\n\n` : ""}` +
          `Pour t'abonner: <code>/subscribe</code>\nPour déposer: <code>/deposit montant</code>\nPaiements: <code>/payments</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/subscribe") {
      await reply(
        chatId,
        "⏳ Vérification de ton wallet et préparation Premium...",
      );
      const result = await prepareSubscriptionPayment(
        supabase,
        appUser,
        "premium",
        500,
      );
      if (result.activated) {
        await reply(
          chatId,
          `✅ <b>Premium activé directement avec ton wallet</b>\n\nSolde restant: <b>${result.balanceAfter} XAF</b>`,
          userMenuMarkup(),
        );
      } else if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💳 <b>Abonnement Premium</b>\n\nPrix: <b>500 FCFA / mois</b>\nSolde utilisé: <b>${result.walletToDeduct || 0} XAF</b>\nÀ compléter: <b>${result.paymentAmount} XAF</b>\n\nClique sur le bouton ci-dessous pour compléter avec Nelsius Pay.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💳 Payer ${result.paymentAmount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/proplus") {
      await reply(
        chatId,
        "⏳ Vérification de ton wallet et préparation Pro Plus...",
      );
      const result = await prepareSubscriptionPayment(
        supabase,
        appUser,
        "pro_plus",
        1000,
      );
      if (result.activated) {
        await reply(
          chatId,
          `✅ <b>Pro Plus activé directement avec ton wallet</b>\n\nSolde restant: <b>${result.balanceAfter} XAF</b>`,
          userMenuMarkup(),
        );
      } else if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Paiement indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `🚀 <b>Abonnement Pro Plus</b>\n\nPrix: <b>1000 FCFA / mois</b>\nSolde utilisé: <b>${result.walletToDeduct || 0} XAF</b>\nÀ compléter: <b>${result.paymentAmount} XAF</b>\n\nInclus: traduction, filtres avancés, remplacement mots/liens et suppression pubs.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `🚀 Payer ${result.paymentAmount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, clique sur 🧾 Paiements pour suivre le statut.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/profilters") {
      await reply(
        chatId,
        `⚙️ <b>Filtres Pro Plus</b>\n\nCes commandes fonctionnent avec un abonnement Pro Plus actif.\n\nTraduction:\n<code>/translate flow_id fr on</code>\n<code>/translate flow_id en on</code>\n\nRemplacer mots:\n<code>/replace flow_id ancien => nouveau</code>\n\nLiens:\n<code>/links flow_id remove</code>\n<code>/links flow_id replace https://ton-lien.com</code>\n<code>/links flow_id keep</code>\n\nPublicités:\n<code>/blockads flow_id on</code>\n\nMots interdits/obligatoires:\n<code>/include flow_id mot1,mot2</code>\n<code>/exclude flow_id mot1,mot2</code>`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/wallet") {
      const { data: txs } = await supabase
        .from("wallet_transactions")
        .select(
          "type, amount, currency, balance_after, description, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(8);
      const lines = (txs || []).map((t: any) => {
        const sign = Number(t.amount) >= 0 ? "+" : "";
        return `${sign}${t.amount} ${t.currency} — ${t.description || t.type}\nSolde après: <b>${t.balance_after} ${t.currency}</b>`;
      });
      await reply(
        chatId,
        `💼 <b>Mon wallet</b>\n\nSolde actuel: <b>${appUser.wallet_balance || 0} XAF</b>\n\n${lines.length ? lines.join("\n\n") : "Aucune transaction pour le moment."}`,
        userMenuMarkup(),
      );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/payments") {
      const { data: payments, error } = await supabase
        .from("payments")
        .select(
          "reference, amount, currency, status, payment_type, paid_at, created_at",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);

      if (error)
        await reply(chatId, `❌ Erreur: ${error.message}`, userMenuMarkup());
      else if (!payments?.length)
        await reply(
          chatId,
          "📭 Aucun paiement pour le moment.",
          userMenuMarkup(),
        );
      else {
        const statusIcon: Record<string, string> = {
          created: "🕐",
          pending: "⏳",
          paid: "✅",
          success: "✅",
          completed: "✅",
          failed: "❌",
          cancelled: "🚫",
          canceled: "🚫",
        };
        const lines = payments.map((p: any) => {
          const date = new Date(p.created_at).toLocaleString("fr-FR");
          const type = p.payment_type === "deposit" ? "Dépôt" : "Premium";
          const icon = statusIcon[String(p.status || "").toLowerCase()] || "ℹ️";
          return `${icon} <b>${type}</b> — ${p.amount} ${p.currency}\nStatut: <b>${p.status}</b>\nRéf: <code>${p.reference}</code>\n${p.paid_at ? `Payé: ${new Date(p.paid_at).toLocaleString("fr-FR")}` : `Créé: ${date}`}`;
        });
        await reply(
          chatId,
          `🧾 <b>Mes paiements récents</b>\n\n${lines.join("\n\n")}`,
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deposit") {
      const amount = Number(args[0] || 0);
      if (!amount || amount < 100) {
        await setBotState(supabase, appUser.id, {
          step: "deposit_amount",
          draft: {},
        });
        await reply(
          chatId,
          "💰 <b>Dépôt</b>\n\nEntre le montant à déposer en FCFA.\nMinimum: <b>100 FCFA</b>\n\nExemple: <code>1000</code>",
          {
            reply_markup: {
              keyboard: [["❌ Annuler"]],
              resize_keyboard: true,
              one_time_keyboard: true,
            },
          },
        );
        return new Response("OK", { status: 200 });
      }

      await reply(chatId, "⏳ Création du lien de dépôt...");
      const result = await createNelsiusCheckout(
        supabase,
        appUser,
        "deposit",
        amount,
      );
      if (result.error || !result.checkoutUrl) {
        await reply(
          chatId,
          `❌ Dépôt indisponible: ${result.error}`,
          userMenuMarkup(),
        );
      } else {
        await reply(
          chatId,
          `💰 <b>Dépôt wallet</b>\n\nMontant: <b>${amount} FCFA</b>\n\nClique sur le bouton ci-dessous pour payer avec Nelsius Pay. Ton solde sera crédité automatiquement après confirmation.`,
          {
            reply_markup: {
              inline_keyboard: [
                [
                  {
                    text: `💰 Déposer ${amount} FCFA`,
                    url: result.checkoutUrl,
                  },
                ],
              ],
            },
          },
        );
        await reply(
          chatId,
          "Après paiement, reviens ici et clique sur 🧾 Paiements pour suivre le statut. /me affichera ton solde une fois le paiement confirmé.",
          userMenuMarkup(),
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addsource") {
      const channel = normalizeTelegramChannel(args[0] || "");
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource &lt;@canal ou URL t.me&gt;");
      } else {
        const { data: existing } = await supabase
          .from("user_sources")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("type", "telegram")
          .eq("config->>channel", channel)
          .maybeSingle();

        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_sources")
              .update({ is_active: true })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Source déjà enregistrée: <code>${shortId(existing.id)}</code> — @${channel}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_sources")
            .insert({
              user_id: appUser.id,
              type: "telegram",
              config: { channel },
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Source ajoutée\nID: <code>${shortId(inserted.id)}</code>\nCanal: <b>@${channel}</b>`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/sources") {
      const { data: sources, error } = await supabase
        .from("user_sources")
        .select("id, type, config, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!sources?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucune source. Ajoute-en une avec /addsource @canal",
        );
      else {
        const lines = sources.map(
          (s: any) =>
            `${s.is_active ? "🟢" : "🔴"} <code>${shortId(s.id)}</code> — @${s.config?.channel || "?"}`,
        );
        await reply(chatId, `📡 <b>Mes sources</b>\n\n${lines.join("\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/delsource") {
      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      if (!src) await reply(chatId, "❌ Source non trouvée.");
      else {
        await supabase
          .from("user_sources")
          .update({ is_active: false })
          .eq("id", src.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("source_id", src.id);
        await reply(
          chatId,
          `✅ Source désactivée: <b>@${src.config?.channel || "?"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addtarget") {
      const targetChat = normalizeTargetChat(args[0] || "");
      const title = args.slice(1).join(" ").trim() || targetChat;
      if (!targetChat) {
        await reply(
          chatId,
          "⚠️ Usage: /addtarget &lt;@canal ou -100...&gt; [nom]",
        );
      } else {
        let warning = "";
        try {
          const check = await fetch(`${BASE}/getChat`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ chat_id: targetChat }),
          });
          const checkData = await check.json();
          if (!checkData.ok)
            warning =
              "\n\n⚠️ Je n'arrive pas encore à vérifier ce canal. Ajoute le bot comme admin du canal cible.";
        } catch (_e) {
          warning = "\n\n⚠️ Vérification du canal impossible pour le moment.";
        }

        const { data: existing } = await supabase
          .from("user_targets")
          .select("id, is_active")
          .eq("user_id", appUser.id)
          .eq("chat_id", targetChat)
          .maybeSingle();
        if (existing) {
          if (!existing.is_active) {
            await supabase
              .from("user_targets")
              .update({ is_active: true, title })
              .eq("id", existing.id);
          }
          await reply(
            chatId,
            `ℹ️ Cible déjà enregistrée: <code>${shortId(existing.id)}</code> — ${targetChat}${warning}`,
          );
        } else {
          const { data: inserted, error } = await supabase
            .from("user_targets")
            .insert({
              user_id: appUser.id,
              chat_id: targetChat,
              title,
              is_active: true,
            })
            .select("id")
            .maybeSingle();
          if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
          else
            await reply(
              chatId,
              `✅ Canal cible ajouté\nID: <code>${shortId(inserted.id)}</code>\nCible: <b>${targetChat}</b>${warning}`,
            );
        }
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/targets") {
      const { data: targets, error } = await supabase
        .from("user_targets")
        .select("id, chat_id, title, is_active, created_at")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!targets?.length)
        await reply(
          chatId,
          "📭 Tu n'as aucun canal cible. Ajoute-en un avec /addtarget @moncanal",
        );
      else {
        const lines = targets.map(
          (t: any) =>
            `${t.is_active ? "🟢" : "🔴"} <code>${shortId(t.id)}</code> — <b>${t.title || t.chat_id}</b> (${t.chat_id})`,
        );
        await reply(
          chatId,
          `🎯 <b>Mes canaux cibles</b>\n\n${lines.join("\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/deltarget") {
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[0],
      );
      if (!target) await reply(chatId, "❌ Canal cible non trouvé.");
      else {
        await supabase
          .from("user_targets")
          .update({ is_active: false })
          .eq("id", target.id);
        await supabase
          .from("flows")
          .update({ is_active: false })
          .eq("user_id", appUser.id)
          .eq("target_id", target.id);
        await reply(
          chatId,
          `✅ Canal cible désactivé: <b>${target.title || target.chat_id}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/addflow") {
      const premium = isPremium(appUser);
      if (!premium) {
        const { count } = await supabase
          .from("flows")
          .select("*", { count: "exact", head: true })
          .eq("user_id", appUser.id)
          .eq("is_active", true);
        if ((count || 0) >= 1) {
          await reply(
            chatId,
            "🔒 Le plan gratuit autorise 1 seul flux actif. Utilise /subscribe pour passer Premium ou désactive un flux.",
          );
          return new Response("OK", { status: 200 });
        }
      }

      const src = await findOwnedRecord(
        supabase,
        "user_sources",
        appUser.id,
        args[0],
      );
      const target = await findOwnedRecord(
        supabase,
        "user_targets",
        appUser.id,
        args[1],
      );
      if (!src || !src.is_active)
        await reply(
          chatId,
          "❌ Source introuvable ou inactive. Vérifie /sources.",
        );
      else if (!target || !target.is_active)
        await reply(
          chatId,
          "❌ Cible introuvable ou inactive. Vérifie /targets.",
        );
      else {
        const { data: inserted, error } = await supabase
          .from("flows")
          .insert({
            user_id: appUser.id,
            source_id: src.id,
            target_id: target.id,
            mode: "new_only",
            initial_last_n: 5,
            is_active: true,
            filters: DEFAULT_FLOW_FILTERS,
          })
          .select("id")
          .maybeSingle();
        if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
        else
          await reply(
            chatId,
            `✅ Flux créé\nID: <code>${shortId(inserted.id)}</code>\n@${src.config?.channel} → ${target.chat_id}\n\nConfigure les filtres avec /filters ${shortId(inserted.id)}`,
          );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/flows") {
      const { data: flows, error } = await supabase
        .from("flows")
        .select(
          "id, is_active, last_message_id, last_run_at, last_error, filters, source:user_sources(config), target:user_targets(chat_id,title)",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false });
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!flows?.length)
        await reply(
          chatId,
          "📭 Aucun flux. Crée-en un avec /addflow source_id target_id",
        );
      else {
        const lines = flows.map(
          (f: any) =>
            `${f.is_active ? "🟢" : "⏸"} <code>${shortId(f.id)}</code> — @${f.source?.config?.channel || "?"} → ${f.target?.chat_id || "?"}\n   Dernier msg: <b>${f.last_message_id || 0}</b>${f.last_error ? `\n   ⚠️ ${String(f.last_error).substring(0, 80)}` : ""}`,
        );
        await reply(chatId, `🔁 <b>Mes flux</b>\n\n${lines.join("\n\n")}`);
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      ["/pauseflow", "/resumeflow", "/deleteflow"].includes(command)
    ) {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const active = command === "/resumeflow";
        await supabase
          .from("flows")
          .update({ is_active: active })
          .eq("id", flow.id)
          .eq("user_id", appUser.id);
        await reply(
          chatId,
          command === "/resumeflow"
            ? `▶️ Flux repris: <code>${shortId(flow.id)}</code>`
            : `⏸ Flux désactivé: <code>${shortId(flow.id)}</code>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/filters") {
      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) await reply(chatId, "❌ Flux non trouvé.");
      else {
        const filters = mergeFilters(flow.filters, {});
        await reply(
          chatId,
          `🧰 <b>Filtres du flux ${shortId(flow.id)}</b>\n\n` +
            `Mots obligatoires: <b>${filters.include_keywords.length ? filters.include_keywords.join(", ") : "aucun"}</b>\n` +
            `Mots interdits: <b>${filters.exclude_keywords.length ? filters.exclude_keywords.join(", ") : "aucun"}</b>\n` +
            `Bloquer pubs: <b>${filters.block_ads ? "oui" : "non"}</b>\n` +
            `Média seulement: <b>${filters.media_only ? "oui" : "non"}</b>\n` +
            `Albums: <b>${filters.allow_albums ? "oui" : "non"}</b>\n` +
            `Reformulation IA: <b>${filters.use_ai_rewrite ? "oui" : "non"}</b>\n` +
            `Signature: <b>${filters.signature_text || "aucune"}</b>`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    if (
      !adminMode &&
      [
        "/include",
        "/exclude",
        "/blockads",
        "/mediaonly",
        "/allowalbums",
        "/rewriteai",
        "/signature",
        "/translate",
        "/replace",
        "/links",
      ].includes(command)
    ) {
      if (!isProPlus(appUser)) {
        await reply(
          chatId,
          "🔒 Ces filtres avancés sont réservés au plan Pro Plus. Clique sur 🚀 Pro Plus pour l’activer.",
          userMenuMarkup(),
        );
        return new Response("OK", { status: 200 });
      }

      const flow = await findOwnedRecord(
        supabase,
        "flows",
        appUser.id,
        args[0],
      );
      if (!flow) {
        await reply(chatId, "❌ Flux non trouvé.");
        return new Response("OK", { status: 200 });
      }

      let patch: Record<string, unknown> = {};
      if (command === "/include")
        patch = { include_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (command === "/exclude")
        patch = { exclude_keywords: parseKeywordList(args.slice(1).join(" ")) };
      if (
        ["/blockads", "/mediaonly", "/allowalbums", "/rewriteai"].includes(
          command,
        )
      ) {
        const value = parseOnOff(args[1]);
        if (value === null) {
          await reply(chatId, `⚠️ Usage: ${command} &lt;flow_id&gt; on|off`);
          return new Response("OK", { status: 200 });
        }
        const key =
          command === "/blockads"
            ? "block_ads"
            : command === "/mediaonly"
              ? "media_only"
              : command === "/allowalbums"
                ? "allow_albums"
                : "use_ai_rewrite";
        patch = { [key]: value };
      }
      if (command === "/signature")
        patch = {
          signature_text: args.slice(1).join(" ").trim().substring(0, 200),
        };
      if (command === "/translate") {
        const lang = (args[1] || "fr").toLowerCase();
        const enabled = parseOnOff(args[2] || "on");
        patch = { target_language: lang, translate_enabled: enabled !== false };
      }
      if (command === "/replace") {
        const raw = args.slice(1).join(" ");
        const [from, to] = raw.split("=>").map((s) => s.trim());
        if (!from || !to) {
          await reply(chatId, "⚠️ Usage: /replace flow_id ancien => nouveau");
          return new Response("OK", { status: 200 });
        }
        const current = mergeFilters(flow.filters, {});
        patch = {
          replacements: { ...(current.replacements || {}), [from]: to },
        };
      }
      if (command === "/links") {
        const action = (args[1] || "keep").toLowerCase();
        if (!["keep", "remove", "replace"].includes(action)) {
          await reply(
            chatId,
            "⚠️ Usage: /links flow_id keep|remove|replace [url]",
          );
          return new Response("OK", { status: 200 });
        }
        patch = {
          link_action: action,
          link_replacement: action === "replace" ? args[2] || "" : "",
        };
      }

      const filters = mergeFilters(flow.filters, patch);
      const { error } = await supabase
        .from("flows")
        .update({ filters })
        .eq("id", flow.id)
        .eq("user_id", appUser.id);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else
        await reply(
          chatId,
          `✅ Filtres mis à jour pour le flux <code>${shortId(flow.id)}</code>. Voir /filters ${shortId(flow.id)}`,
        );
      return new Response("OK", { status: 200 });
    }

    if (!adminMode && command === "/activity") {
      const { data: rows, error } = await supabase
        .from("flow_activity")
        .select(
          "id, status, reason, original_url, text_preview, media_count, created_at, flow_id",
        )
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(10);
      if (error) await reply(chatId, `❌ Erreur: ${error.message}`);
      else if (!rows?.length)
        await reply(chatId, "📭 Aucune activité pour le moment.");
      else {
        const lines = rows.map((r: any) => {
          const date = new Date(r.created_at).toLocaleString("fr-FR");
          return `${r.status === "published" ? "✅" : "⏭"} <code>${shortId(r.flow_id || r.id)}</code> — ${r.status}${r.reason ? ` (${r.reason})` : ""}\n   ${date} — médias: ${r.media_count}\n   ${(r.text_preview || r.original_url || "").substring(0, 90)}`;
        });
        await reply(
          chatId,
          `📊 <b>Dernières activités</b>\n\n${lines.join("\n\n")}`,
        );
      }
      return new Response("OK", { status: 200 });
    }

    // ── Admin-only security gate ──────────────────────────────────────────
    if (!adminMode) {
      await reply(chatId, `❓ Commande inconnue.\n\n${USER_HELP}`);
      return new Response("OK", { status: 200 });
    }

    // ── /start | /help ────────────────────────────────────────────────────
    if (command === "/start" || command === "/help" || !command) {
      await reply(chatId, HELP, adminMenuMarkup());
    }

    // ── /stats ────────────────────────────────────────────────────────────
    else if (command === "/stats") {
      const [{ count: total }, { count: today }] = await Promise.all([
        supabase.from("articles").select("*", { count: "exact", head: true }),
        supabase
          .from("articles")
          .select("*", { count: "exact", head: true })
          .gte("created_at", new Date(Date.now() - 86400000).toISOString()),
      ]);
      const { count: srcCount } = await supabase
        .from("sources")
        .select("*", { count: "exact", head: true })
        .eq("is_active", true);
      await reply(
        chatId,
        `📊 <b>Statistiques</b>\n\n` +
          `📰 Articles total: <b>${total ?? 0}</b>\n` +
          `🕐 Dernières 24h: <b>${today ?? 0}</b>\n` +
          `📡 Sources actives: <b>${srcCount ?? 0}</b>`,
      );
    }

    // ── /list ─────────────────────────────────────────────────────────────
    else if (command === "/list") {
      const limit = Math.min(parseInt(args[0]) || 10, 20);
      const { data: articles, error } = await supabase
        .from("articles")
        .select("id, title, created_at")
        .order("created_at", { ascending: false })
        .limit(limit);

      if (error) {
        await reply(chatId, `❌ Erreur: ${error.message}`);
      } else if (!articles?.length) {
        await reply(chatId, "📭 Aucun article.");
      } else {
        const lines = articles.map((a: any, i: number) => {
          const date = new Date(a.created_at).toLocaleDateString("fr-FR");
          const shortId = a.id.substring(0, 8);
          return `${i + 1}. <code>${shortId}</code> — ${(a.title || "Sans titre").substring(0, 50)}\n   📅 ${date}`;
        });
        await reply(
          chatId,
          `📰 <b>Derniers ${articles.length} articles:</b>\n\n${lines.join("\n\n")}`,
        );
      }
    }

    // ── /article ──────────────────────────────────────────────────────────
    else if (command === "/article") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /article &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("*")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const msg =
            `📰 <b>${a.title}</b>\n\n${(a.content || a.summary || "").substring(0, 700)}\n\n` +
            `🔗 <a href="${a.original_url}">Source originale</a>\n` +
            `🆔 <code>${a.id}</code>`;
          if (a.image_url) {
            await sendPhotoToChat(chatId, a.image_url, msg);
          } else {
            await reply(chatId, msg);
          }
        }
      }
    }

    // ── /delete ───────────────────────────────────────────────────────────
    else if (command === "/delete") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delete &lt;id&gt;");
      } else {
        const { data: a } = await supabase
          .from("articles")
          .select("id, title")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!a) {
          await reply(chatId, "❌ Article non trouvé.");
        } else {
          const { error } = await supabase
            .from("articles")
            .delete()
            .eq("id", a.id);
          if (error) {
            await reply(chatId, `❌ Erreur: ${error.message}`);
          } else {
            await reply(
              chatId,
              `✅ Article supprimé:\n<i>${a.title}</i>`,
              adminMenuMarkup(),
            );
          }
        }
      }
    }

    // ── /deleteall ────────────────────────────────────────────────────────
    else if (command === "/deleteall") {
      const { error } = await supabase
        .from("articles")
        .delete()
        .neq("id", "00000000-0000-0000-0000-000000000000");
      if (error) {
        await reply(
          chatId,
          `❌ Erreur lors de la suppression: ${error.message}`,
        );
      } else {
        await reply(
          chatId,
          `✅ TOUS les articles ont été supprimés de la base de données.`,
          adminMenuMarkup(),
        );
      }
    }

    // ── /clearall ─────────────────────────────────────────────────────────
    else if (command === "/clearall") {
      const forceMode = args[0] === "force";
      await reply(
        chatId,
        forceMode
          ? "⏳ Nettoyage TOTAL (force) en cours... Cela peut prendre 2-3 minutes."
          : "⏳ Suppression en cours des messages du canal et de la base...",
      );

      try {
        let deletedCount = 0;
        let failedCount = 0;

        // 1. FORCE MODE: Try to delete last 100 messages by message_id range
        if (forceMode && OUTPUT_CHAT_ID) {
          await reply(
            chatId,
            "🔥 Mode FORCE: tentative suppression des 100 derniers messages...",
          );

          // Try to get the latest message first to know where to start
          let startMsgId = 1000; // Default high number
          try {
            const latestRes = await fetch(
              `${BASE}/getUpdates?limit=1&offset=-1`,
            );
            const latestData = await latestRes.json();
            if (latestData.ok && latestData.result?.length > 0) {
              const lastUpdate =
                latestData.result[latestData.result.length - 1];
              if (
                lastUpdate.channel_post?.chat?.id?.toString() ===
                OUTPUT_CHAT_ID.replace("@", "")
              ) {
                startMsgId = lastUpdate.channel_post.message_id;
              }
            }
          } catch (e) {
            console.log(
              "[Bot] Could not get latest update, using default range",
            );
          }

          // Delete from startMsgId down to startMsgId-100
          const endMsgId = Math.max(1, startMsgId - 100);
          for (let msgId = startMsgId; msgId >= endMsgId; msgId--) {
            const delRes = await fetch(`${BASE}/deleteMessage`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                chat_id: OUTPUT_CHAT_ID,
                message_id: msgId,
              }),
            });
            const delData = await delRes.json();
            if (delData.ok) {
              deletedCount++;
            } else if (
              !delData.description?.includes("message to delete not found")
            ) {
              // Only count real errors, not "message not found"
              failedCount++;
            }
            // Small delay to avoid rate limits
            await new Promise((r) => setTimeout(r, 50));
          }
        }

        // 2. Get all articles with telegram_message_id (for tracked messages)
        const { data: articles, error: fetchError } = await supabase
          .from("articles")
          .select("id, telegram_message_id, telegram_chat_id, title")
          .not("telegram_message_id", "is", null)
          .order("created_at", { ascending: false });

        if (fetchError) {
          await reply(
            chatId,
            `❌ Erreur récupération articles: ${fetchError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 3. Delete tracked messages from Telegram channel
        if (articles && articles.length > 0) {
          for (const article of articles) {
            if (article.telegram_message_id && article.telegram_chat_id) {
              const delRes = await fetch(`${BASE}/deleteMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: article.telegram_chat_id,
                  message_id: article.telegram_message_id,
                }),
              });
              const delData = await delRes.json();
              if (delData.ok) {
                deletedCount++;
              } else {
                failedCount++;
                console.warn(
                  `[Bot] Failed to delete message ${article.telegram_message_id}:`,
                  delData.description,
                );
              }
              // Small delay to avoid rate limits
              await new Promise((r) => setTimeout(r, 100));
            }
          }
        }

        // 4. Delete all articles from DB
        const { error: deleteError } = await supabase
          .from("articles")
          .delete()
          .neq("id", "00000000-0000-0000-0000-000000000000");

        if (deleteError) {
          await reply(
            chatId,
            `❌ Erreur suppression DB: ${deleteError.message}`,
          );
          return new Response("OK", { status: 200 });
        }

        // 5. Reset last_message_id in all telegram sources (first run mode)
        const { data: sources } = await supabase
          .from("sources")
          .select("id, config")
          .eq("type", "telegram");

        let resetCount = 0;
        if (sources) {
          for (const source of sources) {
            const newConfig = { ...source.config, last_message_id: "0" };
            const { error: updateError } = await supabase
              .from("sources")
              .update({ config: newConfig })
              .eq("id", source.id);
            if (!updateError) resetCount++;
          }
        }

        await reply(
          chatId,
          `✅ <b>Nettoyage complet terminé</b>\n\n` +
            `${forceMode ? "🔥 Mode FORCE utilisé\n" : ""}` +
            `🗑 Messages Telegram supprimés: <b>${deletedCount}</b>\n` +
            `❌ Échecs: <b>${failedCount}</b>\n` +
            `🗑 Articles DB supprimés: <b>${articles?.length || 0}</b>\n` +
            `🔄 Sources reset: <b>${resetCount}</b>\n\n` +
            `💡 Astuce: Utilise <code>/clearall force</code> pour supprimer les 100 derniers messages du canal (même sans tracking).`,
          adminMenuMarkup(),
        );
      } catch (err) {
        console.error("[Bot] /clearall error:", err);
        await reply(
          chatId,
          `💥 Erreur lors du nettoyage: ${(err as Error).message}`,
        );
      }
    }

    // ── /publish ──────────────────────────────────────────────────────────
    else if (command === "/publish") {
      let content = args.join(" ").trim();
      if (!content && !hasMedia) {
        await reply(
          chatId,
          "⚠️ Usage: /publish <texte>\nOu envoie un média avec /publish en légende.",
        );
      } else {
        let finalTitle = "Publication admin";
        let finalContent = content;
        let isAd = false;

        if (content) {
          const geminiKey = Deno.env.get("GEMINI_API_KEY");
          if (geminiKey) {
            await reply(chatId, "⏳ Structuration avec l'IA...");
            const aiRes = await restructureWithAI(content, geminiKey);
            isAd = aiRes.isAd;
            if (aiRes.title) finalTitle = aiRes.title;
            if (aiRes.content) finalContent = aiRes.content;
          }
        }

        if (isAd) {
          await reply(
            chatId,
            "❌ Ce contenu a été détecté comme une publicité/spam et n'a pas été publié.",
          );
          return new Response("OK", { status: 200 });
        }

        const uniqueSuffix = new Date()
          .toISOString()
          .replace(/[-:.TZ]/g, "")
          .slice(0, 14);
        const uniqueTitle = `${finalTitle} #${uniqueSuffix}`;

        // Automatically append @izynews at the very bottom
        const footer = "\n\n@izynews";
        if (finalContent && !finalContent.includes("@izynews")) {
          finalContent = `${finalContent.trim()}${footer}`;
        } else if (!finalContent && hasMedia) {
          finalContent = footer.trim();
        }

        const imageUrl = photo ? photo[photo.length - 1]?.file_id : null;
        let imagePublicUrl: string | null = null;
        if (imageUrl) {
          const fileRes = await fetch(`${BASE}/getFile?file_id=${imageUrl}`);
          const fileData = await fileRes.json();
          if (fileData.ok) {
            imagePublicUrl = `https://api.telegram.org/file/bot${BOT_TOKEN}/${fileData.result.file_path}`;
          }
        }

        // Insert into database
        const { data: inserted, error } = await supabase
          .from("articles")
          .insert({
            title: uniqueTitle,
            summary: finalContent.substring(0, 500),
            content: finalContent,
            original_url: `https://t.me/admin_publish_${Date.now()}`,
            image_url: imagePublicUrl,
            is_certified: true,
          })
          .select("id")
          .maybeSingle();

        if (error) {
          console.error("[Bot] DB Insert Error:", error);
          await reply(chatId, `❌ Erreur DB: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Article publié!\n🆔 <code>${inserted?.id?.substring(0, 8)}</code>`,
          );

          // Also forward to output channel
          if (OUTPUT_CHAT_ID) {
            if (hasMedia) {
              // Use copyMessage with new caption
              await copyMessageToChat(
                OUTPUT_CHAT_ID,
                chatId,
                messageId,
                finalContent,
              );
            } else {
              await fetch(`${BASE}/sendMessage`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                  chat_id: OUTPUT_CHAT_ID,
                  text: finalContent.substring(0, 4096),
                  parse_mode: "HTML",
                }),
              });
            }
          }
        }
      }
    }

    // ── /sources ──────────────────────────────────────────────────────────
    else if (command === "/sources") {
      const { data: sources } = await supabase
        .from("sources")
        .select("id, name, type, config, is_active")
        .order("created_at", { ascending: false });
      if (!sources?.length) {
        await reply(chatId, "📭 Aucune source.");
      } else {
        const lines = sources.map((s: any) => {
          const icon = s.is_active ? "🟢" : "🔴";
          const channel = s.config?.channel || "";
          return `${icon} <code>${s.id.substring(0, 8)}</code> — <b>${s.name}</b> [${s.type}${channel ? ": @" + channel : ""}]`;
        });
        await reply(
          chatId,
          `📡 <b>Sources (${sources.length}):</b>\n\n${lines.join("\n")}`,
        );
      }
    }

    // ── /addsource ────────────────────────────────────────────────────────
    else if (command === "/addsource") {
      let channel = args[0]?.trim();
      if (!channel) {
        await reply(chatId, "⚠️ Usage: /addsource <@canal ou URL_TME>");
      } else {
        if (channel.includes("t.me/")) {
          const parts = channel.split("t.me/");
          channel = parts[parts.length - 1].split("/")[0].split("?")[0];
        }
        channel = channel.replace(/^@/, "").toLowerCase();

        const { error } = await supabase.from("sources").insert({
          name: `@${channel}`,
          type: "telegram",
          config: { channel },
          is_active: true,
        });
        if (error) {
          await reply(chatId, `❌ Erreur: ${error.message}`);
        } else {
          await reply(
            chatId,
            `✅ Source ajoutée avec succès!\n📡 Canal: <b>@${channel}</b>\n\nL'agrégation automatique récupérera les prochaines nouvelles.`,
          );
        }
      }
    }

    // ── /delsource ────────────────────────────────────────────────────────
    else if (command === "/delsource") {
      if (!args[0]) {
        await reply(chatId, "⚠️ Usage: /delsource &lt;id&gt;");
      } else {
        const { data: src } = await supabase
          .from("sources")
          .select("id, name")
          .ilike("id", `${args[0]}%`)
          .limit(1)
          .maybeSingle();
        if (!src) {
          await reply(chatId, "❌ Source non trouvée.");
        } else {
          await supabase
            .from("sources")
            .update({ is_active: false })
            .eq("id", src.id);
          await reply(chatId, `✅ Source désactivée: <b>${src.name}</b>`);
        }
      }
    }

    // ── /run ──────────────────────────────────────────────────────────────
    else if (command === "/run") {
      await reply(chatId, "⏳ Agrégation en cours...");
      const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
      const serviceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? "";
      const res = await fetch(`${supabaseUrl}/functions/v1/aggregate_news`, {
        method: "POST",
        headers: { Authorization: `Bearer ${serviceKey}` },
      });
      const raw = await res.text();
      let result: any = null;
      try {
        result = raw ? JSON.parse(raw) : null;
      } catch (_e) {
        result = null;
      }

      if (!res.ok) {
        console.error(
          "[Bot] /run aggregate_news HTTP error:",
          res.status,
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation (HTTP ${res.status}). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (!result) {
        console.error(
          "[Bot] /run aggregate_news invalid JSON:",
          raw?.slice(0, 500),
        );
        await reply(
          chatId,
          `❌ Erreur d'agrégation: réponse invalide (non-JSON). Vérifie les logs Supabase.`,
        );
        return new Response("OK", { status: 200 });
      }

      if (result.success) {
        await reply(
          chatId,
          `✅ Agrégation terminée!\n📊 Traités: <b>${result.processed}</b>\n🆕 Nouveaux: <b>${result.new}</b>`,
        );
      } else {
        await reply(
          chatId,
          `❌ Erreur d'agrégation: ${result.error || "inconnue"}`,
        );
      }
    }

    // ── Unknown command ───────────────────────────────────────────────────
    else {
      await reply(
        chatId,
        `❓ Commande inconnue. Tape /help pour voir les commandes disponibles.`,
      );
    }
  } catch (err) {
    console.error("[Bot] Error:", err);
    await reply(chatId, `💥 Erreur serveur: ${(err as Error).message}`);
  }

  return new Response("OK", { status: 200 });
});

```


**Tool Call: Check diagnostics for `news/supabase/functions/telegram_bot/index.ts`**
Status: Completed

error at line 1: Cannot find module 'https://esm.sh/@supabase/supabase-js@2' or its corresponding type declarations.


## Assistant

Oui, tu as raison. J’ai amélioré cette partie.

## 1. Le solde est maintenant plus visible

Avant, le solde était seulement visible dans `/me`.

Maintenant j’ai ajouté un bouton :

`💼 Wallet`

Et la commande :

`/wallet`

L’utilisateur peut voir :

- son solde actuel ;
- ses dernières transactions wallet ;
- les dépôts ;
- les paiements d’abonnement faits avec le wallet.

Donc si un client recharge son compte avec `400 FCFA`, il peut voir ce solde dans :

`💼 Wallet`

ou :

`/wallet`

Et aussi dans :

`/me`

---

## 2. Paiement d’abonnement avec le wallet

J’ai modifié la logique de paiement Premium et Pro Plus.

### Exemple Premium à 500 FCFA

Si l’utilisateur a :

`wallet_balance = 500`

et clique :

`💳 Premium`

Alors le bot active directement Premium, sans créer de lien Nelsius Pay.

Il répond :

> Premium activé directement avec ton wallet.

Et son wallet passe à :

`0 FCFA`

---

## 3. Si le solde est insuffisant

Si le solde ne suffit pas, le bot calcule le complément à payer.

### Exemple 1

Premium coûte :

`500 FCFA`

L’utilisateur a :

`400 FCFA`

Alors il manque :

`100 FCFA`

Le bot crée un lien Nelsius Pay pour :

`100 FCFA`

Après paiement confirmé :

- il utilise les `400 FCFA` du wallet ;
- il ajoute les `100 FCFA` payés ;
- il active Premium.

---

### Exemple 2

Premium coûte :

`500 FCFA`

L’utilisateur a :

`450 FCFA`

Il manque normalement :

`50 FCFA`

Mais tu as demandé :

> pas de 50 ou 75, les paiements externes commencent à 100.

Donc le bot crée un lien de :

`100 FCFA`

Mais il ne déduit du wallet que :

`400 FCFA`

Résultat :

- 400 FCFA wallet utilisés ;
- 100 FCFA payés via Nelsius ;
- Premium activé ;
- il reste 50 FCFA dans le wallet.

Donc on respecte le minimum externe de `100 FCFA`.

---

## 4. Même logique pour Pro Plus

Pro Plus coûte :

`1000 FCFA`

### Si wallet >= 1000

Activation directe.

### Si wallet < 1000

Le bot crée un lien Nelsius Pay pour compléter.

Mais le complément minimum externe est toujours :

`100 FCFA`

---

## 5. Le webhook déduit maintenant la part wallet au bon moment

Avant, si on voulait faire un paiement mixte :

- wallet + Nelsius ;

il fallait éviter de déduire le wallet avant que le paiement externe soit confirmé.

Maintenant c’est propre :

1. l’utilisateur lance l’abonnement ;
2. si complément nécessaire, le bot crée un paiement Nelsius avec metadata :
   - `subscription_price`
   - `wallet_to_deduct`
   - `plan`
3. le wallet n’est pas encore déduit ;
4. quand Nelsius confirme le paiement via webhook :
   - le webhook déduit la part wallet ;
   - active l’abonnement ;
   - crée la transaction wallet.

Donc si le paiement Nelsius échoue, le wallet n’est pas perdu.

---

## 6. Bouton ajouté dans le menu

Le menu utilisateur contient maintenant :

- `💰 Dépôt`
- `💼 Wallet`
- `🧾 Paiements`

Différence :

### `💰 Dépôt`

Permet de recharger son solde.

### `💼 Wallet`

Montre le solde et l’historique wallet.

### `🧾 Paiements`

Montre les paiements Nelsius :

- pending ;
- completed ;
- failed ;
- etc.

---

## 7. Concernant les fonctionnalités pas intuitives

Oui, je suis d’accord. Certaines fonctions comme :

- filtres Pro Plus ;
- traduction ;
- remplacement de liens ;
- remplacement de mots ;
- suppression pub ;

sont encore trop “commande texte”.

On a déjà ajouté :

`⚙️ Filtres Pro+`

Mais pour rendre ça vraiment simple, la prochaine étape devrait être de créer des scénarios guidés avec boutons, comme on l’a fait pour `➕ Nouveau flux`.

Par exemple :

### Scénario guidé Pro Plus

L’utilisateur clique :

`⚙️ Filtres Pro+`

Le bot demande :

1. choisir un flux ;
2. choisir une action :
   - traduire ;
   - bloquer pubs ;
   - remplacer mots ;
   - gérer liens ;
   - signature personnalisée ;
3. le bot pose les questions une par une ;
4. il configure le filtre automatiquement.

Ce serait beaucoup plus intuitif que demander :

`/replace flow_id ancien => nouveau`

ou :

`/translate flow_id fr on`

Je te recommande qu’on fasse ça à l’étape suivante.

---

## 8. Fichiers modifiés

J’ai modifié :

- `news/supabase/functions/telegram_bot/index.ts`
- `news/supabase/functions/nelsius_webhook/index.ts`

---

## 9. Redéploiement nécessaire

Tu dois redéployer :

`telegram_bot`

et :

`nelsius_webhook`

Commandes :

`npx supabase functions deploy telegram_bot --project-ref nxgryaqncfjuazfvekro`

`npx supabase functions deploy nelsius_webhook --project-ref nxgryaqncfjuazfvekro`

---

## 10. Résumé du nouveau comportement

### Recharge wallet

`/deposit 400`

Solde visible dans :

`💼 Wallet`

---

### Paiement Premium 500 FCFA

Si wallet = 500 :

- activation directe.

Si wallet = 400 :

- lien Nelsius de 100.

Si wallet = 450 :

- lien Nelsius de 100 ;
- seulement 400 wallet utilisés ;
- 50 restent dans le wallet.

---

### Paiement Pro Plus 1000 FCFA

Même logique.

---

## 11. Diagnostics

Il reste seulement le diagnostic local habituel :

`Cannot find module 'https://esm.sh/@supabase/supabase-js@2'`

Pas d’autre erreur TypeScript après correction.



